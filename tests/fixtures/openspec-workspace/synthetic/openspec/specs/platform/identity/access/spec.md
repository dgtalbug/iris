# Nested Access Specification

## Requirements

### Requirement: nested behavior
The system MUST preserve nested paths.

#### Scenario: nested spec exists
- **WHEN** the parser reads it
- **THEN** the complete path remains visible
