globalThis.IRIS_SPEC = {"records":{"capability:agent-first-initialization":{"kind":"capability","name":"agent-first-initialization","title":"agent-first-initialization","path":"specs/agent-first-initialization/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>agent-first-initialization\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/agent-first-initialization/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">7\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">22\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-one-command-agent-first-setup\">Requirement: one-command agent-first setup\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-canonical-multi-agent-skill-installation\">Requirement: canonical multi-agent skill installation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-managed-skill-preservation\">Requirement: managed skill preservation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-packaged-offline-initialization\">Requirement: packaged offline initialization\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-safe-retirement-of-adopted-document-records\">Requirement: safe retirement of adopted document records\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-generated-agent-command-surfaces\">Requirement: generated agent command surfaces\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-installed-agent-surface-visibility\">Requirement: installed agent surface visibility\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"agent-first-initialization-specification\">Agent-First Initialization Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Make a single installed Iris command establish and safely upgrade the local visual workspace and its agent-facing instructions without requiring repository-document ingestion or network access.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-one-command-agent-first-setup\">Requirement: one-command agent-first setup\u003c/h3>\n\u003cp>The system MUST make \u003ccode>iris init\u003c/code> the complete project setup and upgrade flow for an installed Iris CLI, MUST report the agent surfaces that operation installed, and MUST NOT record derived tool detection in the user-owned workspace configuration.\u003c/p>\n\u003ch4 id=\"scenario-first-initialization\">Scenario: first initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> in a repository with no Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST create the usable local workspace, install the supported agent-facing Iris skill surfaces, and render the dashboard in that operation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-repeated-initialization\">Scenario: repeated initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> again in an initialized repository\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST refresh Iris-managed outputs idempotently without requiring \u003ccode>iris update\u003c/code>, \u003ccode>iris adopt\u003c/code>, or \u003ccode>iris sync\u003c/code> as a setup step\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-initialized-repository-has-content-commands\">Scenario: initialized repository has content commands\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> initialization completes successfully\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> agents and users MUST still be able to use the existing draft, render, report, archive, publish, export, and open commands\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-initialization-installs-agent-surfaces\">Scenario: initialization installs agent surfaces\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> finishes installing agent surfaces\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST report how many surfaces were created, updated, and left unchanged, and MUST name every surface it could not write, so that the operator can see what was installed without inspecting the filesystem\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-generated-configuration-is-written\">Scenario: generated configuration is written\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the system writes the workspace configuration file\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that file MUST NOT contain detection results derived from the repository's contents, so that a value frozen at creation time can never contradict the detection the dashboard actually reads\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-canonical-multi-agent-skill-installation\">Requirement: canonical multi-agent skill installation\u003c/h3>\n\u003cp>The system MUST generate supported agent skill files from one canonical Iris-owned skill source rather than maintaining independent instruction copies, and that source MUST state when to use Iris in conversational terms and map common user intents to the exact command and generated destination.\u003c/p>\n\u003ch4 id=\"scenario-supported-skill-surfaces-are-installed\">Scenario: supported skill surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs agent guidance\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST provide the \u003ccode>iris-workspace\u003c/code> skill to the generic/Codex, Claude, and GitHub Copilot repository skill locations\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-unrelated-agent-content-exists\">Scenario: unrelated agent content exists\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a supported skill directory already contains sibling skills or unrelated files\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST leave that content unchanged\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-agent-decides-whether-iris-applies\">Scenario: agent decides whether Iris applies\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an agent reads the installed skill while deciding how to record completed work\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the skill MUST name the situations that call for Iris and map each supported intent to its command and generated output location without requiring the agent to read any other file\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-managed-skill-preservation\">Requirement: managed skill preservation\u003c/h3>\n\u003cp>The system MUST identify its generated skill content with verifiable managed ownership metadata and fail safely around user-owned or modified content.\u003c/p>\n\u003ch4 id=\"scenario-managed-content-is-intact\">Scenario: managed content is intact\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Iris skill has valid ownership markers and its managed content matches the recorded digest\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-managed-content-was-edited\">Scenario: managed content was edited\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the managed content no longer matches its recorded digest\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST preserve the file and report an actionable collision instead of overwriting the edit\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-target-is-unmarked-or-malformed\">Scenario: target is unmarked or malformed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the target skill file is user-owned, unmarked, partially marked, misordered, unsafe, or outside the repository boundary\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST not truncate, replace, or follow it and MUST report the affected surface\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-packaged-offline-initialization\">Requirement: packaged offline initialization\u003c/h3>\n\u003cp>The installed Iris package MUST contain every template and runtime file required for initialization after the package itself has been obtained.\u003c/p>\n\u003ch4 id=\"scenario-installed-package-initializes-offline\">Scenario: installed package initializes offline\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> from a packed or globally installed Iris package with no repository checkout or network access\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command MUST generate the workspace and agent skills entirely from packaged local assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-package-payload-is-verified\">Scenario: package payload is verified\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the release package is built\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> release verification MUST fail if the canonical agent template or its installed generator is absent from the package payload\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-safe-retirement-of-adopted-document-records\">Requirement: safe retirement of adopted document records\u003c/h3>\n\u003cp>The system MUST remove a legacy adopted-document page only when Iris can positively prove that the exact record remains an unmodified generated mirror.\u003c/p>\n\u003ch4 id=\"scenario-legacy-generated-mirror-is-proven\">Scenario: legacy generated mirror is proven\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> legacy state provenance, a safe README or docs source path, page identity, generated metadata, and stored and current content digests all agree\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MAY remove that exact generated page and normalize the state before rendering\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-ownership-proof-is-incomplete\">Scenario: ownership proof is incomplete\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> any ownership, path, identity, metadata, or digest check is missing or mismatched\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST preserve the page and report it for manual review\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-arbitrary-user-page-resembles-an-adopted-page\">Scenario: arbitrary user page resembles an adopted page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user-created page has a \u003ccode>doc-\u003c/code> prefix, an \u003ccode>adopted-doc\u003c/code> tag, source-like prose, or only some legacy fields\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST NOT treat those hints alone as permission to delete the page\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-generated-agent-command-surfaces\">Requirement: generated agent command surfaces\u003c/h3>\n\u003cp>The system MUST generate typed command files for the supported agent surfaces from the same canonical Iris source that produces the skill, covering the frequent content actions, and MUST apply its managed-ownership rules to those files.\u003c/p>\n\u003ch4 id=\"scenario-command-surfaces-are-installed\">Scenario: command surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs agent guidance\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST generate one command file per supported content action into the Claude command directory and the GitHub prompt directory, each naming the exact Iris command, the file the agent edits, and the render step\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-generated-command-content-is-refreshed\">Scenario: generated command content is refreshed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a previously generated command file has valid ownership markers and its managed content matches the recorded digest\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-command-file-was-edited-or-is-user-owned\">Scenario: command file was edited or is user-owned\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a target command file is unmarked, partially marked, misordered, edited so its digest no longer matches, symlinked, or outside the repository boundary\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST preserve the file, MUST NOT truncate or follow it, and MUST report the affected surface\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-unrelated-command-content-exists\">Scenario: unrelated command content exists\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a supported command directory already contains unrelated commands or prompts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST leave that content unchanged\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-installed-agent-surface-visibility\">Requirement: installed agent surface visibility\u003c/h3>\n\u003cp>The generated workspace MUST show which agent surfaces Iris installed and where each one lives, so that the setup Iris performs is inspectable from the dashboard rather than only from the filesystem.\u003c/p>\n\u003ch4 id=\"scenario-surfaces-are-installed\">Scenario: surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the generated command reference after initialization\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST list each installed agent surface with its destination path and the agent tooling that reads it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-a-surface-could-not-be-written\">Scenario: a surface could not be written\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a surface was preserved as a conflict instead of being written\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the listing MUST mark that surface as not installed and give the reason, rather than presenting it as available\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Agent-First Initialization Specification\n\n## Purpose\n\nMake a single installed Iris command establish and safely upgrade the local visual workspace and its agent-facing instructions without requiring repository-document ingestion or network access.\n\n## Requirements\n\n### Requirement: one-command agent-first setup\nThe system MUST make `iris init` the complete project setup and upgrade flow for an installed Iris CLI, MUST report the agent surfaces that operation installed, and MUST NOT record derived tool detection in the user-owned workspace configuration.\n\n#### Scenario: first initialization\n- **WHEN** a user runs `iris init` in a repository with no Iris workspace\n- **THEN** the system MUST create the usable local workspace, install the supported agent-facing Iris skill surfaces, and render the dashboard in that operation\n\n#### Scenario: repeated initialization\n- **WHEN** a user runs `iris init` again in an initialized repository\n- **THEN** the system MUST refresh Iris-managed outputs idempotently without requiring `iris update`, `iris adopt`, or `iris sync` as a setup step\n\n#### Scenario: initialized repository has content commands\n- **WHEN** initialization completes successfully\n- **THEN** agents and users MUST still be able to use the existing draft, render, report, archive, publish, export, and open commands\n\n#### Scenario: initialization installs agent surfaces\n- **WHEN** `iris init` finishes installing agent surfaces\n- **THEN** it MUST report how many surfaces were created, updated, and left unchanged, and MUST name every surface it could not write, so that the operator can see what was installed without inspecting the filesystem\n\n#### Scenario: generated configuration is written\n- **WHEN** the system writes the workspace configuration file\n- **THEN** that file MUST NOT contain detection results derived from the repository&#039;s contents, so that a value frozen at creation time can never contradict the detection the dashboard actually reads\n\n### Requirement: canonical multi-agent skill installation\n\nThe system MUST generate supported agent skill files from one canonical Iris-owned skill source rather than maintaining independent instruction copies, and that source MUST state when to use Iris in conversational terms and map common user intents to the exact command and generated destination.\n\n#### Scenario: supported skill surfaces are installed\n\n- **WHEN** `iris init` installs agent guidance\n- **THEN** it MUST provide the `iris-workspace` skill to the generic/Codex, Claude, and GitHub Copilot repository skill locations\n\n#### Scenario: unrelated agent content exists\n\n- **WHEN** a supported skill directory already contains sibling skills or unrelated files\n- **THEN** initialization MUST leave that content unchanged\n\n#### Scenario: agent decides whether Iris applies\n\n- **WHEN** an agent reads the installed skill while deciding how to record completed work\n- **THEN** the skill MUST name the situations that call for Iris and map each supported intent to its command and generated output location without requiring the agent to read any other file\n\n### Requirement: managed skill preservation\nThe system MUST identify its generated skill content with verifiable managed ownership metadata and fail safely around user-owned or modified content.\n\n#### Scenario: managed content is intact\n- **WHEN** a generated Iris skill has valid ownership markers and its managed content matches the recorded digest\n- **THEN** initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\n\n#### Scenario: managed content was edited\n- **WHEN** the managed content no longer matches its recorded digest\n- **THEN** initialization MUST preserve the file and report an actionable collision instead of overwriting the edit\n\n#### Scenario: target is unmarked or malformed\n- **WHEN** the target skill file is user-owned, unmarked, partially marked, misordered, unsafe, or outside the repository boundary\n- **THEN** initialization MUST not truncate, replace, or follow it and MUST report the affected surface\n\n### Requirement: packaged offline initialization\nThe installed Iris package MUST contain every template and runtime file required for initialization after the package itself has been obtained.\n\n#### Scenario: installed package initializes offline\n- **WHEN** a user runs `iris init` from a packed or globally installed Iris package with no repository checkout or network access\n- **THEN** the command MUST generate the workspace and agent skills entirely from packaged local assets\n\n#### Scenario: package payload is verified\n- **WHEN** the release package is built\n- **THEN** release verification MUST fail if the canonical agent template or its installed generator is absent from the package payload\n\n### Requirement: safe retirement of adopted document records\nThe system MUST remove a legacy adopted-document page only when Iris can positively prove that the exact record remains an unmodified generated mirror.\n\n#### Scenario: legacy generated mirror is proven\n- **WHEN** legacy state provenance, a safe README or docs source path, page identity, generated metadata, and stored and current content digests all agree\n- **THEN** initialization MAY remove that exact generated page and normalize the state before rendering\n\n#### Scenario: ownership proof is incomplete\n- **WHEN** any ownership, path, identity, metadata, or digest check is missing or mismatched\n- **THEN** initialization MUST preserve the page and report it for manual review\n\n#### Scenario: arbitrary user page resembles an adopted page\n- **WHEN** a user-created page has a `doc-` prefix, an `adopted-doc` tag, source-like prose, or only some legacy fields\n- **THEN** initialization MUST NOT treat those hints alone as permission to delete the page\n\n### Requirement: generated agent command surfaces\n\nThe system MUST generate typed command files for the supported agent surfaces from the same canonical Iris source that produces the skill, covering the frequent content actions, and MUST apply its managed-ownership rules to those files.\n\n#### Scenario: command surfaces are installed\n\n- **WHEN** `iris init` installs agent guidance\n- **THEN** it MUST generate one command file per supported content action into the Claude command directory and the GitHub prompt directory, each naming the exact Iris command, the file the agent edits, and the render step\n\n#### Scenario: generated command content is refreshed\n\n- **WHEN** a previously generated command file has valid ownership markers and its managed content matches the recorded digest\n- **THEN** initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\n\n#### Scenario: command file was edited or is user-owned\n\n- **WHEN** a target command file is unmarked, partially marked, misordered, edited so its digest no longer matches, symlinked, or outside the repository boundary\n- **THEN** initialization MUST preserve the file, MUST NOT truncate or follow it, and MUST report the affected surface\n\n#### Scenario: unrelated command content exists\n\n- **WHEN** a supported command directory already contains unrelated commands or prompts\n- **THEN** initialization MUST leave that content unchanged\n\n### Requirement: installed agent surface visibility\n\nThe generated workspace MUST show which agent surfaces Iris installed and where each one lives, so that the setup Iris performs is inspectable from the dashboard rather than only from the filesystem.\n\n#### Scenario: surfaces are installed\n\n- **WHEN** a user opens the generated command reference after initialization\n- **THEN** it MUST list each installed agent surface with its destination path and the agent tooling that reads it\n\n#### Scenario: a surface could not be written\n\n- **WHEN** a surface was preserved as a conflict instead of being written\n- **THEN** the listing MUST mark that surface as not installed and give the reason, rather than presenting it as available\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:aperture-design-system":{"kind":"capability","name":"aperture-design-system","title":"aperture-design-system","path":"specs/aperture-design-system/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>aperture-design-system\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/aperture-design-system/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">9\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">33\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-aperture-component-language\">Requirement: Aperture component language\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-dense-context-preserving-work-browser\">Requirement: dense context-preserving Work browser\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-accessible-work-item-detail-drawer\">Requirement: accessible work-item detail drawer\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-workspace-navigation-shell\">Requirement: workspace navigation shell\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-generated-command-reference-page\">Requirement: generated command reference page\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-section-summary-strips\">Requirement: section summary strips\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"aperture-design-system-specification\">Aperture Design System Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Define the deterministic visual language and dashboard hierarchy that lets a newcomer understand an iris-enabled repository from a single offline HTML entry point.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/h3>\n\u003cp>The system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, MUST validate text contrast at 4.5:1 or better, MUST validate non-text contrast for control boundaries and border visibility, and MUST define navigation-shell surface, text, and selection tokens alongside the surface, text, accent, type, status, and priority tokens so that every generated page and the shell render from one token set.\u003c/p>\n\u003ch4 id=\"scenario-generated-styles-are-validated\">Scenario: generated styles are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a contributor runs the token validation command\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme, including navigation-shell pairs\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-theme-is-switched\">Scenario: theme is switched\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user toggles the theme on any generated page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that page and its navigation shell MUST render in the selected theme from the same tokens, and the preference MUST be restored on other generated pages in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-non-text-surfaces-are-validated\">Scenario: non-text surfaces are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the token validation command checks a configured non-text pair\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST reject a control-boundary pair below 3:1 and a border pair below the declared visibility floor in either theme, and the failure message MUST state which threshold applied so a border floor is never read as a text contrast result\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-aperture-component-language\">Requirement: Aperture component language\u003c/h3>\n\u003cp>The system MUST provide generated component styles and semantic markup for aperture marks, the navigation shell, page headers, summary strip tiles, progress bars, command cards, restrained status/type/priority indicators, compact work rows, structured tables, Kanban columns and cards, detail drawers, callouts, timelines, keyboard hints, and command-specific empty states.\u003c/p>\n\u003ch4 id=\"scenario-meaning-is-communicated-accessibly\">Scenario: meaning is communicated accessibly\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page uses type, status, or priority color, interactive controls, selection, current-section marking, or motion\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide a non-color signal, visible keyboard focus, semantic state, and a static reduced-motion presentation\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated workspace MUST consist of an Overview page at \u003ccode>iris/index.html\u003c/code>, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page presenting an index of canonical specs, active changes, and the archive with each record addressable in place, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\u003c/p>\n\u003ch4 id=\"scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Overview from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-repository-has-generated-work-pages\">Scenario: repository has generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Work page with one or more page contracts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-a-spec-detail-page-is-opened\">Scenario: a spec detail page is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens a canonical spec, change, or legacy record from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST keep the navigation shell and the Spec section current, present that record's content, and provide a way back to the index\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/h3>\n\u003cp>The generated workspace MUST retain classic deferred-script interactions for navigation-shell collapse, Work layout selection, shared filtering, theme switching, detail-drawer behavior, and keyboard navigation, MUST keep every page usable without JavaScript, and MUST remain operable at a 360 px viewport.\u003c/p>\n\u003ch4 id=\"scenario-keyboard-navigation-is-used\">Scenario: keyboard navigation is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses \u003ccode>/\u003c/code>, \u003ccode>t\u003c/code>, or \u003ccode>b\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> \u003ccode>/\u003c/code> MUST focus the relevant visible filter, \u003ccode>t\u003c/code> MUST toggle the theme, and \u003ccode>b\u003c/code> MUST toggle the navigation shell, none of which loads external assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-top-level-tabs-are-keyboard-operated\">Scenario: top-level tabs are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on a navigation-shell entry and the user presses Tab or Enter, or focus is on the List/Table/Kanban tablist and the user presses supported arrow, Home, or End keys\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> section navigation MUST follow standard link behavior with visible focus, and the layout tablist MUST move focus and selection according to the tab pattern with correct tab/panel semantics\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-work-items-are-keyboard-operated\">Scenario: work items are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on a work-item link and the user presses Enter or Space\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the corresponding detail drawer MUST open without navigating away\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-javascript-is-unavailable\">Scenario: JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page opens without executing JavaScript\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the navigation shell MUST remain expanded and fully linked, the default Work List MUST remain readable, and every work item MUST keep its full-page link\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-narrow-viewport-is-used\">Scenario: narrow viewport is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> any generated page viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell menu, Work List rows, prioritized Table columns, stacked Kanban columns, full-screen drawer, Spec content, Commands cards, warnings, and source fallbacks MUST remain readable without horizontal page overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-reduced-motion-is-requested\">Scenario: reduced motion is requested\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user enables reduced-motion preferences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> collapsing the shell, switching layouts, opening the drawer, and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-dense-context-preserving-work-browser\">Requirement: dense context-preserving Work browser\u003c/h3>\n\u003cp>The generated Work surface MUST provide peer List, Table, and Kanban views over the same Iris page records, MUST share one text filter across all views, and MUST use only metadata available from validated contracts or explicit unavailable labels.\u003c/p>\n\u003ch4 id=\"scenario-user-changes-work-layout\">Scenario: user changes Work layout\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user selects List, Table, or Kanban\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the corresponding view MUST present the same matching records with type, ID, title, status, and every supported optional field without changing source data\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-filters-work\">Scenario: user filters work\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user enters text matching a title, ID, type, status, priority, agent, or tag\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every Work view MUST hide nonmatching records and report the matching unique-record count\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-optional-metadata-is-unavailable\">Scenario: optional metadata is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a page contract does not define priority or another requested field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> List, Table, Kanban, and detail views MUST use a neutral unavailable value and MUST NOT infer or fabricate data\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-javascript-is-unavailable-2\">Scenario: JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the generated dashboard opens without executing JavaScript\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every work item MUST retain a navigable full-page link and the default List view MUST remain readable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-accessible-work-item-detail-drawer\">Requirement: accessible work-item detail drawer\u003c/h3>\n\u003cp>Selecting a work item MUST open one right-side detail drawer that preserves the current Work view, displays available contract metadata, description, evidence, and a full-page link, and provides deterministic close and focus behavior.\u003c/p>\n\u003ch4 id=\"scenario-item-is-activated\">Scenario: item is activated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user clicks a work item or activates its link with Enter or Space\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the drawer MUST open with dialog semantics, identify the selected ID and title, move focus to its close control, and update a local hash that can reopen the same item\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-drawer-is-closed\">Scenario: drawer is closed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses Escape, activates the close control, activates the backdrop, or navigates back from the item hash\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the drawer MUST close, remove its modal state, and return focus to the item that opened it when that item remains available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-focus-moves-within-the-drawer\">Scenario: focus moves within the drawer\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the drawer is open and the user presses Tab or Shift+Tab at its focus boundaries\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> focus MUST remain within the drawer until it is closed\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-narrow-viewport-opens-a-drawer\">Scenario: narrow viewport opens a drawer\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the viewport is 360 px wide and a work item is activated\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the drawer MUST occupy the viewport without horizontal page overflow and MUST keep its close and full-page actions reachable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-workspace-navigation-shell\">Requirement: workspace navigation shell\u003c/h3>\n\u003cp>Every generated Iris HTML page MUST share one navigation shell that lists the workspace sections (Overview, Work, Spec, Commands, and the project docs group), marks the current section, links each section with a relative \u003ccode>file://\u003c/code>-safe path, and can be collapsed and restored without any network request. Every section count shown in the shell MUST be a total for that section, measured the same way across sections.\u003c/p>\n\u003ch4 id=\"scenario-user-moves-between-sections\">Scenario: user moves between sections\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates a navigation entry on any generated page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the browser MUST open that section's own page and the shell on that page MUST mark it as current\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-sidebar-is-collapsed\">Scenario: sidebar is collapsed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user activates the collapse control or presses \u003ccode>b\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell MUST collapse to a compact rail in which every section remains reachable with an accessible label, and the same collapsed or expanded state MUST be restored on the next generated page opened in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-narrow-viewport\">Scenario: narrow viewport\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell MUST present the sections behind a menu control in the top bar and the page MUST have no horizontal overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-artifact-is-published\">Scenario: artifact is published\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a page is published or exported as a standalone artifact\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the navigation shell MUST be omitted and the page content MUST remain readable and self-contained\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-a-section-holds-records-but-has-no-active-ones\">Scenario: a section holds records but has no active ones\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the shell shows the count for a section whose records exist but none are active\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that count MUST reflect the records the section holds rather than the active subset, so that a populated section is never labelled zero\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-generated-command-reference-page\">Requirement: generated command reference page\u003c/h3>\n\u003cp>The system MUST generate a Commands page that lists every CLI command from one command catalog, grouped by purpose, with synopsis, usage, and an explicit implementation status, and \u003ccode>iris --help\u003c/code> MUST present the same groups and commands from that catalog.\u003c/p>\n\u003ch4 id=\"scenario-command-surface-changes\">Scenario: command surface changes\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a command is added, removed, regrouped, or its status changes in the catalog\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated Commands page and \u003ccode>iris --help\u003c/code> MUST both reflect the change without a separate edit\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-command-is-not-fully-implemented\">Scenario: command is not fully implemented\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a catalog entry is partial or stubbed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Commands page MUST label it with a textual status and MUST NOT present it as fully available\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-section-summary-strips\">Requirement: section summary strips\u003c/h3>\n\u003cp>Each section page MUST open with a summary strip of counts derived only from generated data for that section, and the Overview page MUST summarize every section with links to the owning section page rather than embedding that section's full content. A summary MUST report the totals it holds even when the section's most active measure is zero.\u003c/p>\n\u003ch4 id=\"scenario-overview-is-opened\">Scenario: overview is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens \u003ccode>iris/index.html\u003c/code> from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST show the repository briefing, per-section counts, the most recently updated work records, active spec changes with task progress, and quick-start commands, each linking to the owning section page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-section-has-no-data\">Scenario: section has no data\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a section page has no records\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST name the exact command that populates it instead of showing an empty area\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-a-detected-source-has-totals-but-no-active-items\">Scenario: a detected source has totals but no active items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Overview summarizes a source that is present and non-empty but has no active items\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST report the totals it holds and describe the absence of active items as one part of that summary, and it MUST NOT present the summary in a form a reader could mistake for the source being absent or unread\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Aperture Design System Specification\n\n## Purpose\n\nDefine the deterministic visual language and dashboard hierarchy that lets a newcomer understand an iris-enabled repository from a single offline HTML entry point.\n\n## Requirements\n\n### Requirement: Aperture token contract\n\nThe system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, MUST validate text contrast at 4.5:1 or better, MUST validate non-text contrast for control boundaries and border visibility, and MUST define navigation-shell surface, text, and selection tokens alongside the surface, text, accent, type, status, and priority tokens so that every generated page and the shell render from one token set.\n\n#### Scenario: generated styles are validated\n\n- **WHEN** a contributor runs the token validation command\n- **THEN** the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme, including navigation-shell pairs\n\n#### Scenario: theme is switched\n\n- **WHEN** the user toggles the theme on any generated page\n- **THEN** that page and its navigation shell MUST render in the selected theme from the same tokens, and the preference MUST be restored on other generated pages in that browser when local storage is available\n\n#### Scenario: non-text surfaces are validated\n\n- **WHEN** the token validation command checks a configured non-text pair\n- **THEN** it MUST reject a control-boundary pair below 3:1 and a border pair below the declared visibility floor in either theme, and the failure message MUST state which threshold applied so a border floor is never read as a text contrast result\n\n### Requirement: Aperture component language\n\nThe system MUST provide generated component styles and semantic markup for aperture marks, the navigation shell, page headers, summary strip tiles, progress bars, command cards, restrained status/type/priority indicators, compact work rows, structured tables, Kanban columns and cards, detail drawers, callouts, timelines, keyboard hints, and command-specific empty states.\n\n#### Scenario: meaning is communicated accessibly\n\n- **WHEN** a generated page uses type, status, or priority color, interactive controls, selection, current-section marking, or motion\n- **THEN** the page MUST provide a non-color signal, visible keyboard focus, semantic state, and a static reduced-motion presentation\n\n### Requirement: newcomer-first dashboard hierarchy\n\nThe generated workspace MUST consist of an Overview page at `iris/index.html`, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page presenting an index of canonical specs, active changes, and the archive with each record addressable in place, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\n\n#### Scenario: repository has no generated work pages\n\n- **WHEN** a newcomer opens the Overview from `file://`\n- **THEN** it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n#### Scenario: repository has generated work pages\n\n- **WHEN** a newcomer opens the Work page with one or more page contracts\n- **THEN** the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\n\n#### Scenario: user opens the Spec view\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\n\n#### Scenario: state is communicated in the Spec view\n\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n#### Scenario: a spec detail page is opened\n\n- **WHEN** a user opens a canonical spec, change, or legacy record from the index\n- **THEN** the page MUST keep the navigation shell and the Spec section current, present that record&#039;s content, and provide a way back to the index\n\n### Requirement: deterministic responsive interaction\n\nThe generated workspace MUST retain classic deferred-script interactions for navigation-shell collapse, Work layout selection, shared filtering, theme switching, detail-drawer behavior, and keyboard navigation, MUST keep every page usable without JavaScript, and MUST remain operable at a 360 px viewport.\n\n#### Scenario: keyboard navigation is used\n\n- **WHEN** the user presses `/`, `t`, or `b` outside an editable field\n- **THEN** `/` MUST focus the relevant visible filter, `t` MUST toggle the theme, and `b` MUST toggle the navigation shell, none of which loads external assets\n\n#### Scenario: top-level tabs are keyboard operated\n\n- **WHEN** focus is on a navigation-shell entry and the user presses Tab or Enter, or focus is on the List/Table/Kanban tablist and the user presses supported arrow, Home, or End keys\n- **THEN** section navigation MUST follow standard link behavior with visible focus, and the layout tablist MUST move focus and selection according to the tab pattern with correct tab/panel semantics\n\n#### Scenario: work items are keyboard operated\n\n- **WHEN** focus is on a work-item link and the user presses Enter or Space\n- **THEN** the corresponding detail drawer MUST open without navigating away\n\n#### Scenario: JavaScript is unavailable\n\n- **WHEN** a generated page opens without executing JavaScript\n- **THEN** the navigation shell MUST remain expanded and fully linked, the default Work List MUST remain readable, and every work item MUST keep its full-page link\n\n#### Scenario: narrow viewport is used\n\n- **WHEN** any generated page viewport is 360 px wide\n- **THEN** the shell menu, Work List rows, prioritized Table columns, stacked Kanban columns, full-screen drawer, Spec content, Commands cards, warnings, and source fallbacks MUST remain readable without horizontal page overflow\n\n#### Scenario: reduced motion is requested\n\n- **WHEN** the user enables reduced-motion preferences\n- **THEN** collapsing the shell, switching layouts, opening the drawer, and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\n\n### Requirement: dense context-preserving Work browser\n\nThe generated Work surface MUST provide peer List, Table, and Kanban views over the same Iris page records, MUST share one text filter across all views, and MUST use only metadata available from validated contracts or explicit unavailable labels.\n\n#### Scenario: user changes Work layout\n\n- **WHEN** a user selects List, Table, or Kanban\n- **THEN** the corresponding view MUST present the same matching records with type, ID, title, status, and every supported optional field without changing source data\n\n#### Scenario: user filters work\n\n- **WHEN** a user enters text matching a title, ID, type, status, priority, agent, or tag\n- **THEN** every Work view MUST hide nonmatching records and report the matching unique-record count\n\n#### Scenario: optional metadata is unavailable\n\n- **WHEN** a page contract does not define priority or another requested field\n- **THEN** List, Table, Kanban, and detail views MUST use a neutral unavailable value and MUST NOT infer or fabricate data\n\n#### Scenario: JavaScript is unavailable\n\n- **WHEN** the generated dashboard opens without executing JavaScript\n- **THEN** every work item MUST retain a navigable full-page link and the default List view MUST remain readable\n\n### Requirement: accessible work-item detail drawer\n\nSelecting a work item MUST open one right-side detail drawer that preserves the current Work view, displays available contract metadata, description, evidence, and a full-page link, and provides deterministic close and focus behavior.\n\n#### Scenario: item is activated\n\n- **WHEN** a user clicks a work item or activates its link with Enter or Space\n- **THEN** the drawer MUST open with dialog semantics, identify the selected ID and title, move focus to its close control, and update a local hash that can reopen the same item\n\n#### Scenario: drawer is closed\n\n- **WHEN** the user presses Escape, activates the close control, activates the backdrop, or navigates back from the item hash\n- **THEN** the drawer MUST close, remove its modal state, and return focus to the item that opened it when that item remains available\n\n#### Scenario: focus moves within the drawer\n\n- **WHEN** the drawer is open and the user presses Tab or Shift+Tab at its focus boundaries\n- **THEN** focus MUST remain within the drawer until it is closed\n\n#### Scenario: narrow viewport opens a drawer\n\n- **WHEN** the viewport is 360 px wide and a work item is activated\n- **THEN** the drawer MUST occupy the viewport without horizontal page overflow and MUST keep its close and full-page actions reachable\n\n### Requirement: workspace navigation shell\n\nEvery generated Iris HTML page MUST share one navigation shell that lists the workspace sections (Overview, Work, Spec, Commands, and the project docs group), marks the current section, links each section with a relative `file://`-safe path, and can be collapsed and restored without any network request. Every section count shown in the shell MUST be a total for that section, measured the same way across sections.\n\n#### Scenario: user moves between sections\n\n- **WHEN** a user activates a navigation entry on any generated page\n- **THEN** the browser MUST open that section&#039;s own page and the shell on that page MUST mark it as current\n\n#### Scenario: sidebar is collapsed\n\n- **WHEN** the user activates the collapse control or presses `b` outside an editable field\n- **THEN** the shell MUST collapse to a compact rail in which every section remains reachable with an accessible label, and the same collapsed or expanded state MUST be restored on the next generated page opened in that browser when local storage is available\n\n#### Scenario: narrow viewport\n\n- **WHEN** the viewport is 360 px wide\n- **THEN** the shell MUST present the sections behind a menu control in the top bar and the page MUST have no horizontal overflow\n\n#### Scenario: artifact is published\n\n- **WHEN** a page is published or exported as a standalone artifact\n- **THEN** the navigation shell MUST be omitted and the page content MUST remain readable and self-contained\n\n#### Scenario: a section holds records but has no active ones\n\n- **WHEN** the shell shows the count for a section whose records exist but none are active\n- **THEN** that count MUST reflect the records the section holds rather than the active subset, so that a populated section is never labelled zero\n\n### Requirement: generated command reference page\n\nThe system MUST generate a Commands page that lists every CLI command from one command catalog, grouped by purpose, with synopsis, usage, and an explicit implementation status, and `iris --help` MUST present the same groups and commands from that catalog.\n\n#### Scenario: command surface changes\n\n- **WHEN** a command is added, removed, regrouped, or its status changes in the catalog\n- **THEN** the generated Commands page and `iris --help` MUST both reflect the change without a separate edit\n\n#### Scenario: command is not fully implemented\n\n- **WHEN** a catalog entry is partial or stubbed\n- **THEN** the Commands page MUST label it with a textual status and MUST NOT present it as fully available\n\n### Requirement: section summary strips\n\nEach section page MUST open with a summary strip of counts derived only from generated data for that section, and the Overview page MUST summarize every section with links to the owning section page rather than embedding that section&#039;s full content. A summary MUST report the totals it holds even when the section&#039;s most active measure is zero.\n\n#### Scenario: overview is opened\n\n- **WHEN** a newcomer opens `iris/index.html` from `file://`\n- **THEN** it MUST show the repository briefing, per-section counts, the most recently updated work records, active spec changes with task progress, and quick-start commands, each linking to the owning section page\n\n#### Scenario: section has no data\n\n- **WHEN** a section page has no records\n- **THEN** it MUST name the exact command that populates it instead of showing an empty area\n\n#### Scenario: a detected source has totals but no active items\n\n- **WHEN** the Overview summarizes a source that is present and non-empty but has no active items\n- **THEN** it MUST report the totals it holds and describe the absence of active items as one part of that summary, and it MUST NOT present the summary in a form a reader could mistake for the source being absent or unread\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:brew-formula-and-installability":{"kind":"capability","name":"brew-formula-and-installability","title":"brew-formula-and-installability","path":"specs/brew-formula-and-installability/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>brew-formula-and-installability\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/brew-formula-and-installability/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">4\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">9\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-supported-cli-installation-path\">Requirement: supported CLI installation path\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-versioned-package-distribution\">Requirement: versioned package distribution\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-minimal-install-smoke-validation\">Requirement: minimal install smoke validation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-documentation-and-upgrade-guidance\">Requirement: documentation and upgrade guidance\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"brew-formula-and-installability-specification\">brew-formula-and-installability Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Provide a reliable, low-friction installation and upgrade path for the \u003ccode>iris\u003c/code> CLI so the project remains easy to adopt without depending on a repo checkout or manual build steps.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-supported-cli-installation-path\">Requirement: supported CLI installation path\u003c/h3>\n\u003cp>The system MUST provide a clear installation path for the \u003ccode>iris\u003c/code> command that works across supported environments.\u003c/p>\n\u003ch4 id=\"scenario-user-installs-the-cli\">Scenario: user installs the CLI\u003c/h4>\n\u003cp>WHEN a user follows the documented install flow\nTHEN the CLI MUST be available as \u003ccode>iris\u003c/code> and MUST expose the expected command surface.\u003c/p>\n\u003ch4 id=\"scenario-install-fails-with-clear-guidance\">Scenario: install fails with clear guidance\u003c/h4>\n\u003cp>WHEN installation prerequisites are missing or unsupported\nTHEN the system MUST fail with a simple, actionable message instead of a vague runtime error.\u003c/p>\n\u003ch3 id=\"requirement-versioned-package-distribution\">Requirement: versioned package distribution\u003c/h3>\n\u003cp>The project MUST distribute the CLI through a package entrypoint that installs the same binary and command surface as the repository build on macOS, Linux, and Windows, and MUST NOT publish package-manager metadata for a distribution channel whose release inputs do not yet exist.\u003c/p>\n\u003ch4 id=\"scenario-package-install-path-is-available\">Scenario: package install path is available\u003c/h4>\n\u003cp>WHEN a user installs the published package with the documented package manager\nTHEN the command MUST resolve to the same project binary and versioned CLI behavior as the repository build\u003c/p>\n\u003ch4 id=\"scenario-upgrade-path-is-clear\">Scenario: upgrade path is clear\u003c/h4>\n\u003cp>WHEN a user upgrades the installed CLI\nTHEN the system MUST expose a predictable version and install flow that keeps the CLI current\u003c/p>\n\u003ch4 id=\"scenario-a-distribution-channel-lacks-release-inputs\">Scenario: a distribution channel lacks release inputs\u003c/h4>\n\u003cp>WHEN a channel such as a Homebrew formula would require a published release URL and checksum that do not exist yet\nTHEN the project MUST document that channel as unavailable and MUST NOT ship a formula or manifest that claims otherwise\u003c/p>\n\u003ch3 id=\"requirement-minimal-install-smoke-validation\">Requirement: minimal install smoke validation\u003c/h3>\n\u003cp>The release workflow MUST include a minimal verification that the installed CLI works for a basic command path.\u003c/p>\n\u003ch4 id=\"scenario-install-smoke-test-passes\">Scenario: install smoke test passes\u003c/h4>\n\u003cp>WHEN the package or formula is installed\nTHEN a fast smoke check such as \u003ccode>iris --help\u003c/code> or a minimal render run MUST succeed without additional repo setup.\u003c/p>\n\u003ch4 id=\"scenario-invalid-installation-is-surfaced-early\">Scenario: invalid installation is surfaced early\u003c/h4>\n\u003cp>WHEN the install path is broken or incompatible\nTHEN the validation step MUST fail early and identify the missing or unsupported prerequisite.\u003c/p>\n\u003ch3 id=\"requirement-documentation-and-upgrade-guidance\">Requirement: documentation and upgrade guidance\u003c/h3>\n\u003cp>The project MUST provide install instructions that align with the local-first design and current project conventions.\u003c/p>\n\u003ch4 id=\"scenario-user-can-self-serve-setup\">Scenario: user can self-serve setup\u003c/h4>\n\u003cp>WHEN a new user reads the docs\nTHEN they MUST be able to install and verify the CLI with a single, documented path.\u003c/p>\n\u003ch4 id=\"scenario-command-reference-stays-current\">Scenario: command reference stays current\u003c/h4>\n\u003cp>WHEN package distribution changes\nTHEN the install docs and command reference MUST stay aligned with the actual CLI behavior.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># brew-formula-and-installability Specification\n\n## Purpose\nProvide a reliable, low-friction installation and upgrade path for the `iris` CLI so the project remains easy to adopt without depending on a repo checkout or manual build steps.\n\n## Requirements\n\n### Requirement: supported CLI installation path\nThe system MUST provide a clear installation path for the `iris` command that works across supported environments.\n\n#### Scenario: user installs the CLI\nWHEN a user follows the documented install flow\nTHEN the CLI MUST be available as `iris` and MUST expose the expected command surface.\n\n#### Scenario: install fails with clear guidance\nWHEN installation prerequisites are missing or unsupported\nTHEN the system MUST fail with a simple, actionable message instead of a vague runtime error.\n\n### Requirement: versioned package distribution\nThe project MUST distribute the CLI through a package entrypoint that installs the same binary and command surface as the repository build on macOS, Linux, and Windows, and MUST NOT publish package-manager metadata for a distribution channel whose release inputs do not yet exist.\n\n#### Scenario: package install path is available\nWHEN a user installs the published package with the documented package manager\nTHEN the command MUST resolve to the same project binary and versioned CLI behavior as the repository build\n\n#### Scenario: upgrade path is clear\nWHEN a user upgrades the installed CLI\nTHEN the system MUST expose a predictable version and install flow that keeps the CLI current\n\n#### Scenario: a distribution channel lacks release inputs\nWHEN a channel such as a Homebrew formula would require a published release URL and checksum that do not exist yet\nTHEN the project MUST document that channel as unavailable and MUST NOT ship a formula or manifest that claims otherwise\n\n### Requirement: minimal install smoke validation\nThe release workflow MUST include a minimal verification that the installed CLI works for a basic command path.\n\n#### Scenario: install smoke test passes\nWHEN the package or formula is installed\nTHEN a fast smoke check such as `iris --help` or a minimal render run MUST succeed without additional repo setup.\n\n#### Scenario: invalid installation is surfaced early\nWHEN the install path is broken or incompatible\nTHEN the validation step MUST fail early and identify the missing or unsupported prerequisite.\n\n### Requirement: documentation and upgrade guidance\nThe project MUST provide install instructions that align with the local-first design and current project conventions.\n\n#### Scenario: user can self-serve setup\nWHEN a new user reads the docs\nTHEN they MUST be able to install and verify the CLI with a single, documented path.\n\n#### Scenario: command reference stays current\nWHEN package distribution changes\nTHEN the install docs and command reference MUST stay aligned with the actual CLI behavior.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:markdown-diagram-rendering":{"kind":"capability","name":"markdown-diagram-rendering","title":"markdown-diagram-rendering","path":"specs/markdown-diagram-rendering/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>markdown-diagram-rendering\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/markdown-diagram-rendering/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">5\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">10\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-explicit-mermaid-fence-rendering\">Requirement: explicit Mermaid fence rendering\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-offline-progressive-enhancement\">Requirement: offline progressive enhancement\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-isolated-safe-failure\">Requirement: isolated safe failure\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-bounded-and-accessible-diagram-presentation\">Requirement: bounded and accessible diagram presentation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-explicit-local-asset-installation\">Requirement: explicit local asset installation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"markdown-diagram-rendering-specification\">markdown-diagram-rendering Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Provide safe, offline visual rendering for Mermaid fences in Iris Markdown while retaining readable source evidence whenever rendering is unavailable or invalid.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-explicit-mermaid-fence-rendering\">Requirement: explicit Mermaid fence rendering\u003c/h3>\n\u003cp>The system SHALL recognize only fenced Markdown code blocks whose normalized language identifier is exactly \u003ccode>mermaid\u003c/code> as diagrams and SHALL leave every other fenced block as literal code.\u003c/p>\n\u003ch4 id=\"scenario-markdown-contains-a-mermaid-fence\">Scenario: Markdown contains a Mermaid fence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> rendered Markdown contains a closed fenced block labeled \u003ccode>mermaid\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated HTML SHALL contain a diagram host and the exact escaped diagram source as its fallback\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-another-code-fence-resembles-a-diagram\">Scenario: another code fence resembles a diagram\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> rendered Markdown contains an unlabeled fence or a fence with another language identifier\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated HTML SHALL preserve it as literal code and SHALL NOT submit it to the diagram runtime\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-offline-progressive-enhancement\">Requirement: offline progressive enhancement\u003c/h3>\n\u003cp>The system MUST render Mermaid diagrams using only a pinned local classic-script asset and MUST keep the Markdown readable through \u003ccode>file://\u003c/code> with no server, runtime module, or network request.\u003c/p>\n\u003ch4 id=\"scenario-local-mermaid-asset-is-installed\">Scenario: local Mermaid asset is installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Iris page containing a valid Mermaid fence opens from \u003ccode>file://\u003c/code> and the vendored runtime exists\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page SHALL replace the diagram host with an accessible inline SVG without requesting a remote resource\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-local-mermaid-asset-is-absent-or-javascript-is-unav\">Scenario: local Mermaid asset is absent or JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page cannot load or execute the local diagram runtime\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each diagram SHALL retain its readable escaped-source fallback and the rest of the Markdown SHALL remain usable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-isolated-safe-failure\">Requirement: isolated safe failure\u003c/h3>\n\u003cp>The system MUST process each Mermaid fence independently under strict untrusted-content settings, MUST disable diagram click behavior and active HTML, and MUST preserve a diagram's escaped source when parsing or rendering that diagram fails.\u003c/p>\n\u003ch4 id=\"scenario-one-document-contains-valid-and-invalid-diagrams\">Scenario: one document contains valid and invalid diagrams\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Markdown document contains a valid Mermaid fence and a separate invalid Mermaid fence\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the valid diagram SHALL render, the invalid diagram SHALL show an actionable local error and its escaped source, and neither outcome SHALL hide the surrounding document\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-diagram-source-requests-active-behavior\">Scenario: diagram source requests active behavior\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Mermaid source includes HTML labels, click callbacks, links, scripts, or configuration intended to weaken the renderer\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the rendered page MUST keep active HTML and click behavior disabled and MUST NOT execute repository-authored JavaScript\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-bounded-and-accessible-diagram-presentation\">Requirement: bounded and accessible diagram presentation\u003c/h3>\n\u003cp>The system MUST give each diagram an accessible name, MUST bound accepted source and graph complexity, and MUST keep both rendered output and fallback source usable in light, dark, print, reduced-motion, and 360-pixel layouts.\u003c/p>\n\u003ch4 id=\"scenario-diagram-renders-successfully\">Scenario: diagram renders successfully\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Mermaid fence becomes an inline SVG\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the diagram SHALL expose a non-empty accessible label and fit its content region without forcing page-level horizontal overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-diagram-input-exceeds-a-configured-bound\">Scenario: diagram input exceeds a configured bound\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Mermaid source or graph complexity exceeds the renderer's configured limit\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that diagram SHALL fail locally with its escaped source and an actionable error while other content remains available\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-explicit-local-asset-installation\">Requirement: explicit local asset installation\u003c/h3>\n\u003cp>The system SHALL provide an idempotent \u003ccode>iris vendor\u003c/code> operation that copies the pinned Mermaid browser bundle and upstream license from the installed Iris package into the workspace without fetching from the network.\u003c/p>\n\u003ch4 id=\"scenario-user-vendors-diagram-assets\">Scenario: user vendors diagram assets\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris vendor\u003c/code> in an initialized Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command SHALL install or refresh the exact pinned Mermaid runtime and license under \u003ccode>iris/design/vendor/\u003c/code> and report the installed version\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-vendors-before-initialization\">Scenario: user vendors before initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris vendor\u003c/code> without an initialized Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command SHALL fail with an actionable instruction to run \u003ccode>iris init\u003c/code> and SHALL NOT create a partial workspace\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># markdown-diagram-rendering Specification\n\n## Purpose\nProvide safe, offline visual rendering for Mermaid fences in Iris Markdown while retaining readable source evidence whenever rendering is unavailable or invalid.\n\n## Requirements\n\n### Requirement: explicit Mermaid fence rendering\n\nThe system SHALL recognize only fenced Markdown code blocks whose normalized language identifier is exactly `mermaid` as diagrams and SHALL leave every other fenced block as literal code.\n\n#### Scenario: Markdown contains a Mermaid fence\n\n- **WHEN** rendered Markdown contains a closed fenced block labeled `mermaid`\n- **THEN** the generated HTML SHALL contain a diagram host and the exact escaped diagram source as its fallback\n\n#### Scenario: another code fence resembles a diagram\n\n- **WHEN** rendered Markdown contains an unlabeled fence or a fence with another language identifier\n- **THEN** the generated HTML SHALL preserve it as literal code and SHALL NOT submit it to the diagram runtime\n\n### Requirement: offline progressive enhancement\n\nThe system MUST render Mermaid diagrams using only a pinned local classic-script asset and MUST keep the Markdown readable through `file://` with no server, runtime module, or network request.\n\n#### Scenario: local Mermaid asset is installed\n\n- **WHEN** a generated Iris page containing a valid Mermaid fence opens from `file://` and the vendored runtime exists\n- **THEN** the page SHALL replace the diagram host with an accessible inline SVG without requesting a remote resource\n\n#### Scenario: local Mermaid asset is absent or JavaScript is unavailable\n\n- **WHEN** a generated page cannot load or execute the local diagram runtime\n- **THEN** each diagram SHALL retain its readable escaped-source fallback and the rest of the Markdown SHALL remain usable\n\n### Requirement: isolated safe failure\n\nThe system MUST process each Mermaid fence independently under strict untrusted-content settings, MUST disable diagram click behavior and active HTML, and MUST preserve a diagram&#039;s escaped source when parsing or rendering that diagram fails.\n\n#### Scenario: one document contains valid and invalid diagrams\n\n- **WHEN** a Markdown document contains a valid Mermaid fence and a separate invalid Mermaid fence\n- **THEN** the valid diagram SHALL render, the invalid diagram SHALL show an actionable local error and its escaped source, and neither outcome SHALL hide the surrounding document\n\n#### Scenario: diagram source requests active behavior\n\n- **WHEN** Mermaid source includes HTML labels, click callbacks, links, scripts, or configuration intended to weaken the renderer\n- **THEN** the rendered page MUST keep active HTML and click behavior disabled and MUST NOT execute repository-authored JavaScript\n\n### Requirement: bounded and accessible diagram presentation\n\nThe system MUST give each diagram an accessible name, MUST bound accepted source and graph complexity, and MUST keep both rendered output and fallback source usable in light, dark, print, reduced-motion, and 360-pixel layouts.\n\n#### Scenario: diagram renders successfully\n\n- **WHEN** a Mermaid fence becomes an inline SVG\n- **THEN** the diagram SHALL expose a non-empty accessible label and fit its content region without forcing page-level horizontal overflow\n\n#### Scenario: diagram input exceeds a configured bound\n\n- **WHEN** Mermaid source or graph complexity exceeds the renderer&#039;s configured limit\n- **THEN** that diagram SHALL fail locally with its escaped source and an actionable error while other content remains available\n\n### Requirement: explicit local asset installation\n\nThe system SHALL provide an idempotent `iris vendor` operation that copies the pinned Mermaid browser bundle and upstream license from the installed Iris package into the workspace without fetching from the network.\n\n#### Scenario: user vendors diagram assets\n\n- **WHEN** a user runs `iris vendor` in an initialized Iris workspace\n- **THEN** the command SHALL install or refresh the exact pinned Mermaid runtime and license under `iris/design/vendor/` and report the installed version\n\n#### Scenario: user vendors before initialization\n\n- **WHEN** a user runs `iris vendor` without an initialized Iris workspace\n- **THEN** the command SHALL fail with an actionable instruction to run `iris init` and SHALL NOT create a partial workspace\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:openspec-spec-browser":{"kind":"capability","name":"openspec-spec-browser","title":"openspec-spec-browser","path":"specs/openspec-spec-browser/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>openspec-spec-browser\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/openspec-spec-browser/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">11\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">35\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-explicit-openspec-workspace-snapshots\">Requirement: explicit OpenSpec workspace snapshots\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-complete-normalized-workspace-view\">Requirement: complete normalized workspace view\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-evidence-based-task-and-artifact-status\">Requirement: evidence-based task and artifact status\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-safe-bounded-filesystem-parsing\">Requirement: safe bounded filesystem parsing\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-graceful-unknown-and-empty-states\">Requirement: graceful unknown and empty states\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-offline-runtime-independence\">Requirement: offline runtime independence\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-inert-markdown-generation\">Requirement: inert Markdown generation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-spec-index-and-detail-pages\">Requirement: spec index and detail pages\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-deep-linkable-specification-headings\">Requirement: deep-linkable specification headings\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-safe-generated-data-bundle\">Requirement: safe generated data bundle\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"openspec-spec-browser-specification\">OpenSpec Spec Browser Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Provide a deterministic, offline dashboard view of the repository's complete OpenSpec filesystem so users and agents can inspect specifications, change artifacts, and real task progress without running OpenSpec.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-explicit-openspec-workspace-snapshots\">Requirement: explicit OpenSpec workspace snapshots\u003c/h3>\n\u003cp>The system MUST detect and read the repository's \u003ccode>openspec/\u003c/code> workspace during \u003ccode>iris init\u003c/code> and \u003ccode>iris render --all\u003c/code>, and MUST NOT introduce implicit synchronization, background monitoring, or a separate refresh command for the same operation.\u003c/p>\n\u003ch4 id=\"scenario-repository-is-initialized-with-openspec\">Scenario: repository is initialized with OpenSpec\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> in a repository containing \u003ccode>openspec/\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated dashboard MUST contain a snapshot of that workspace from the files read during initialization\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-explicitly-refreshes-all-output\">Scenario: user explicitly refreshes all output\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris render --all\u003c/code> after OpenSpec files change\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST re-read the current OpenSpec filesystem and replace the previous generated snapshot\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-renders-one-work-page\">Scenario: user renders one work page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris render &lt;id&gt;\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST render that page without treating the operation as an OpenSpec workspace refresh\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-complete-normalized-workspace-view\">Requirement: complete normalized workspace view\u003c/h3>\n\u003cp>The system MUST represent canonical specs, active changes, structured archived changes, legacy archived Markdown, project/config context, proposal, design, tasks, and change-local delta specs while keeping canonical, active, archived, legacy, invalid, and incomplete states distinct.\u003c/p>\n\u003ch4 id=\"scenario-repository-contains-current-structured-layouts\">Scenario: repository contains current structured layouts\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical specs and structured active or archived changes are present\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST preserve their repository-relative identities, lifecycle state, artifact availability, requirements, scenarios, and delta-operation labels\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-capability-path-is-nested\">Scenario: capability path is nested\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a canonical or change-local spec exists below a nested capability path\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST preserve and display the complete capability path without flattening ambiguous names\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-archive-contains-a-legacy-markdown-record\">Scenario: archive contains a legacy Markdown record\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>openspec/changes/archive/\u003c/code> contains an archived single Markdown file rather than a structured change directory\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST identify it as legacy archive content and keep it separate from structured archived changes\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-evidence-based-task-and-artifact-status\">Requirement: evidence-based task and artifact status\u003c/h3>\n\u003cp>The system MUST derive task totals only from actual Markdown task checkboxes and MUST keep task progress, artifact completeness, lifecycle state, and parser health as independent dimensions.\u003c/p>\n\u003ch4 id=\"scenario-tasks-contain-checked-and-unchecked-items\">Scenario: tasks contain checked and unchecked items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a structured change has task list items using supported Markdown checkbox syntax outside fenced code\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST report completed, open, and total task counts from those items without inferring completion from the directory name or archive location\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-change-is-partially-complete\">Scenario: change is partially complete\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a change is missing an expected artifact or contains open tasks\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST label the available evidence and incompleteness without presenting the change as invalid or complete by assumption\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-checkbox-like-text-appears-in-a-code-fence\">Scenario: checkbox-like text appears in a code fence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact contains checkbox syntax inside fenced source examples\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that text MUST NOT contribute to task progress\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-safe-bounded-filesystem-parsing\">Requirement: safe bounded filesystem parsing\u003c/h3>\n\u003cp>The system MUST parse OpenSpec as untrusted local content using deterministic sorted traversal, repository confinement, bounded input, and symlink refusal, and MUST never execute file content.\u003c/p>\n\u003ch4 id=\"scenario-content-contains-active-markup-or-script-text\">Scenario: content contains active markup or script text\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an OpenSpec file contains HTML, script text, or other executable-looking content\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every rendered representation MUST escape the content so opening the dashboard cannot execute it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-path-is-unsafe-or-input-exceeds-a-bound\">Scenario: path is unsafe or input exceeds a bound\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> traversal encounters a symlink, path escape, unsupported file location, excessive depth, excessive file count, or oversized file\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the parser MUST skip the unsafe input, retain the rest of the workspace, and expose a path-specific actionable warning\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-filesystem-entry-cannot-be-read\">Scenario: filesystem entry cannot be read\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> one OpenSpec file or directory produces a filesystem error\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the parser MUST preserve successfully read entries and report the isolated failure rather than crashing the entire dashboard refresh\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-graceful-unknown-and-empty-states\">Requirement: graceful unknown and empty states\u003c/h3>\n\u003cp>The system MUST render readable fallback states for absent, empty, malformed, unsupported, or partially complete OpenSpec workspaces, and MUST keep a detected workspace visually distinguishable from an absent one wherever it is summarized.\u003c/p>\n\u003ch4 id=\"scenario-repository-has-no-openspec-directory\">Scenario: repository has no OpenSpec directory\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the dashboard snapshot is generated without an \u003ccode>openspec/\u003c/code> directory\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST explain that no OpenSpec workspace was detected without suggesting general-document ingestion\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-openspec-workspace-is-empty\">Scenario: OpenSpec workspace is empty\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>openspec/\u003c/code> exists but contains no supported project, spec, or change records\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST show a distinct empty-workspace state\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-artifact-is-malformed-or-unknown\">Scenario: artifact is malformed or unknown\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a recognized path contains malformed Markdown or an unsupported layout\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST render escaped readable source when available, identify the affected path, and show an actionable warning without hiding other valid records\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-a-detected-workspace-has-no-active-changes\">Scenario: a detected workspace has no active changes\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the snapshot records a detected OpenSpec workspace that holds canonical specs or archived changes but no active change\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every summary of it MUST report the canonical and archived totals it holds, and MUST NOT reduce to a message whose only content is the absence of active changes\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-offline-runtime-independence\">Requirement: offline runtime independence\u003c/h3>\n\u003cp>The system MUST generate and operate the Spec view without requiring the OpenSpec CLI, a server, network access, runtime ES modules, telemetry, or general project-document ingestion.\u003c/p>\n\u003ch4 id=\"scenario-dashboard-opens-directly-from-disk\">Scenario: dashboard opens directly from disk\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens \u003ccode>iris/index.html\u003c/code> through \u003ccode>file://\u003c/code> after generation\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST remain navigable and readable using only local generated assets and classic deferred scripts with zero network requests\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-openspec-executable-is-unavailable\">Scenario: OpenSpec executable is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Iris initializes or renders in a repository whose \u003ccode>openspec/\u003c/code> files exist but no OpenSpec CLI is installed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the filesystem snapshot and Spec view MUST still be generated successfully\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/h3>\n\u003cp>The system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST progressively render fenced blocks labeled exactly \u003ccode>mermaid\u003c/code> as safe offline diagrams with escaped-source fallback, MUST keep the exact escaped document source available as a secondary disclosure, MUST give each rendered heading on a detail page an identifier derived from its text, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\u003c/p>\n\u003ch4 id=\"scenario-supported-markdown-structures-are-present\">Scenario: supported Markdown structures are present\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, fenced code, or Mermaid fences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec section MUST render those structures with readable semantic markup, progressively enhance Mermaid fences when the local runtime is available, and preserve escaped fallback source rather than displaying Markdown punctuation as the primary presentation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-needs-exact-source-evidence\">Scenario: user needs exact source evidence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a rendered Markdown artifact is displayed on its detail page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the user MUST be able to reveal the exact escaped source without leaving that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-configuration-artifact-is-displayed\">Scenario: configuration artifact is displayed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Spec section presents an OpenSpec YAML manifest or workspace configuration\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown or submitted to the diagram runtime\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-rendered-markdown-is-navigable\">Scenario: rendered Markdown is navigable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a rendered artifact contains section headings on a detail page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> those headings MUST be individually addressable so a reader can link to a requirement or section rather than only to the whole document\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-inert-markdown-generation\">Requirement: inert Markdown generation\u003c/h3>\n\u003cp>The system MUST generate Markdown presentation without allowing repository content or Mermaid diagram source to inject active HTML, unsafe URL schemes, executable click behavior, runtime modules, or implicit network requests.\u003c/p>\n\u003ch4 id=\"scenario-markdown-contains-embedded-html\">Scenario: Markdown contains embedded HTML\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact contains an HTML element, event handler, script, iframe, style, or other executable-looking markup outside or inside a Mermaid fence\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated document MUST keep that input inert and MUST NOT add it as executable DOM content\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-markdown-contains-an-unsafe-link\">Scenario: Markdown contains an unsafe link\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Markdown link or Mermaid interaction uses an unsafe scheme such as \u003ccode>javascript:\u003c/code> or \u003ccode>data:\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated document MUST not emit a navigable link or executable interaction for that destination\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-dashboard-opens-offline\">Scenario: dashboard opens offline\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the generated dashboard through \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> rendered Markdown and diagram fallbacks MUST remain readable without loading a Markdown library, module, stylesheet, font, script, or other asset from the network\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-spec-index-and-detail-pages\">Requirement: spec index and detail pages\u003c/h3>\n\u003cp>The generated Spec section MUST provide one index page listing every canonical spec, active change, and archived change with its real counts, health, and task progress, and MUST make each record's artifact bodies available from that page through a local hash address backed by the generated data bundle. The index listing MUST remain complete and MUST name each record's on-disk source path whether or not scripts run.\u003c/p>\n\u003ch4 id=\"scenario-user-opens-the-spec-index\">Scenario: user opens the Spec index\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index MUST show overview counts and compact canonical, active-change, and archive listings, each row naming its source path and addressing that record\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-opens-a-canonical-spec-detail-page\">Scenario: user opens a canonical spec detail page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows a canonical spec row from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST show the capability name, source path, requirement and scenario counts, health, the rendered specification, and its exact escaped source without a full page load\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-user-opens-a-change-detail-page\">Scenario: user opens a change detail page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows a change row from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST show the change lifecycle, completeness, task progress, and every available artifact including delta specs, each with its exact escaped source\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-a-record-has-no-readable-artifacts\">Scenario: a record has no readable artifacts\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a listed change or capability has missing or unreadable artifacts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> its detail view MUST state which artifacts are missing and MUST render the readable ones\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-scripts-are-unavailable\">Scenario: scripts are unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Spec index is opened without executing JavaScript\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index listings, counts, source paths, and warnings MUST remain readable and the page MUST state that opening a record requires scripts rather than presenting an empty region\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-an-unknown-record-is-addressed\">Scenario: an unknown record is addressed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the page is opened with a hash that names no record in the bundle\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index MUST remain displayed unchanged rather than showing an empty or partial record\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-deep-linkable-specification-headings\">Requirement: deep-linkable specification headings\u003c/h3>\n\u003cp>A rendered record MUST give every heading in a rendered document a stable identifier derived from its text, MUST provide a table of contents for a document with two or more section headings, and MUST keep identifiers unique when one record renders several documents.\u003c/p>\n\u003ch4 id=\"scenario-a-specification-with-several-requirements-is-render\">Scenario: a specification with several requirements is rendered\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a canonical spec containing multiple requirements is displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each requirement heading MUST carry an identifier derived from its text and the table of contents MUST link to it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-one-page-renders-several-documents\">Scenario: one page renders several documents\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a change record on one page renders a proposal, a design, and a delta spec that share a heading name\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each rendered heading MUST receive a distinct identifier and each table-of-contents entry MUST resolve to its own document's heading\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-safe-generated-data-bundle\">Requirement: safe generated data bundle\u003c/h3>\n\u003cp>The Spec section MUST carry its record detail in one generated classic-script data file that assigns a single global, and that file MUST be encoded so no record content can terminate the script element, close an HTML comment, or introduce executable or network-loading markup.\u003c/p>\n\u003ch4 id=\"scenario-a-record-contains-a-script-like-sequence\">Scenario: a record contains a script-like sequence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact legitimately contains the characters that would close a script element or an HTML comment\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated bundle MUST encode them so the browser still parses one complete script and the characters appear as inert text when the record is displayed\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-the-bundle-is-loaded\">Scenario: the bundle is loaded\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Spec page loads from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the bundle MUST load as a classic script without a network request and MUST expose every listed record\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># OpenSpec Spec Browser Specification\n\n## Purpose\n\nProvide a deterministic, offline dashboard view of the repository&#039;s complete OpenSpec filesystem so users and agents can inspect specifications, change artifacts, and real task progress without running OpenSpec.\n\n## Requirements\n\n### Requirement: explicit OpenSpec workspace snapshots\nThe system MUST detect and read the repository&#039;s `openspec/` workspace during `iris init` and `iris render --all`, and MUST NOT introduce implicit synchronization, background monitoring, or a separate refresh command for the same operation.\n\n#### Scenario: repository is initialized with OpenSpec\n- **WHEN** a user runs `iris init` in a repository containing `openspec/`\n- **THEN** the generated dashboard MUST contain a snapshot of that workspace from the files read during initialization\n\n#### Scenario: user explicitly refreshes all output\n- **WHEN** a user runs `iris render --all` after OpenSpec files change\n- **THEN** the dashboard MUST re-read the current OpenSpec filesystem and replace the previous generated snapshot\n\n#### Scenario: user renders one work page\n- **WHEN** a user runs `iris render &lt;id&gt;`\n- **THEN** the system MUST render that page without treating the operation as an OpenSpec workspace refresh\n\n### Requirement: complete normalized workspace view\nThe system MUST represent canonical specs, active changes, structured archived changes, legacy archived Markdown, project/config context, proposal, design, tasks, and change-local delta specs while keeping canonical, active, archived, legacy, invalid, and incomplete states distinct.\n\n#### Scenario: repository contains current structured layouts\n- **WHEN** canonical specs and structured active or archived changes are present\n- **THEN** the Spec view MUST preserve their repository-relative identities, lifecycle state, artifact availability, requirements, scenarios, and delta-operation labels\n\n#### Scenario: capability path is nested\n- **WHEN** a canonical or change-local spec exists below a nested capability path\n- **THEN** the Spec view MUST preserve and display the complete capability path without flattening ambiguous names\n\n#### Scenario: archive contains a legacy Markdown record\n- **WHEN** `openspec/changes/archive/` contains an archived single Markdown file rather than a structured change directory\n- **THEN** the Spec view MUST identify it as legacy archive content and keep it separate from structured archived changes\n\n### Requirement: evidence-based task and artifact status\nThe system MUST derive task totals only from actual Markdown task checkboxes and MUST keep task progress, artifact completeness, lifecycle state, and parser health as independent dimensions.\n\n#### Scenario: tasks contain checked and unchecked items\n- **WHEN** a structured change has task list items using supported Markdown checkbox syntax outside fenced code\n- **THEN** the Spec view MUST report completed, open, and total task counts from those items without inferring completion from the directory name or archive location\n\n#### Scenario: change is partially complete\n- **WHEN** a change is missing an expected artifact or contains open tasks\n- **THEN** the Spec view MUST label the available evidence and incompleteness without presenting the change as invalid or complete by assumption\n\n#### Scenario: checkbox-like text appears in a code fence\n- **WHEN** an artifact contains checkbox syntax inside fenced source examples\n- **THEN** that text MUST NOT contribute to task progress\n\n### Requirement: safe bounded filesystem parsing\nThe system MUST parse OpenSpec as untrusted local content using deterministic sorted traversal, repository confinement, bounded input, and symlink refusal, and MUST never execute file content.\n\n#### Scenario: content contains active markup or script text\n- **WHEN** an OpenSpec file contains HTML, script text, or other executable-looking content\n- **THEN** every rendered representation MUST escape the content so opening the dashboard cannot execute it\n\n#### Scenario: path is unsafe or input exceeds a bound\n- **WHEN** traversal encounters a symlink, path escape, unsupported file location, excessive depth, excessive file count, or oversized file\n- **THEN** the parser MUST skip the unsafe input, retain the rest of the workspace, and expose a path-specific actionable warning\n\n#### Scenario: filesystem entry cannot be read\n- **WHEN** one OpenSpec file or directory produces a filesystem error\n- **THEN** the parser MUST preserve successfully read entries and report the isolated failure rather than crashing the entire dashboard refresh\n\n### Requirement: graceful unknown and empty states\nThe system MUST render readable fallback states for absent, empty, malformed, unsupported, or partially complete OpenSpec workspaces, and MUST keep a detected workspace visually distinguishable from an absent one wherever it is summarized.\n\n#### Scenario: repository has no OpenSpec directory\n- **WHEN** the dashboard snapshot is generated without an `openspec/` directory\n- **THEN** the Spec tab MUST explain that no OpenSpec workspace was detected without suggesting general-document ingestion\n\n#### Scenario: OpenSpec workspace is empty\n- **WHEN** `openspec/` exists but contains no supported project, spec, or change records\n- **THEN** the Spec tab MUST show a distinct empty-workspace state\n\n#### Scenario: artifact is malformed or unknown\n- **WHEN** a recognized path contains malformed Markdown or an unsupported layout\n- **THEN** the Spec tab MUST render escaped readable source when available, identify the affected path, and show an actionable warning without hiding other valid records\n\n#### Scenario: a detected workspace has no active changes\n- **WHEN** the snapshot records a detected OpenSpec workspace that holds canonical specs or archived changes but no active change\n- **THEN** every summary of it MUST report the canonical and archived totals it holds, and MUST NOT reduce to a message whose only content is the absence of active changes\n\n### Requirement: offline runtime independence\nThe system MUST generate and operate the Spec view without requiring the OpenSpec CLI, a server, network access, runtime ES modules, telemetry, or general project-document ingestion.\n\n#### Scenario: dashboard opens directly from disk\n- **WHEN** a user opens `iris/index.html` through `file://` after generation\n- **THEN** the Spec view MUST remain navigable and readable using only local generated assets and classic deferred scripts with zero network requests\n\n#### Scenario: OpenSpec executable is unavailable\n- **WHEN** Iris initializes or renders in a repository whose `openspec/` files exist but no OpenSpec CLI is installed\n- **THEN** the filesystem snapshot and Spec view MUST still be generated successfully\n\n### Requirement: safe semantic Markdown presentation\n\nThe system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST progressively render fenced blocks labeled exactly `mermaid` as safe offline diagrams with escaped-source fallback, MUST keep the exact escaped document source available as a secondary disclosure, MUST give each rendered heading on a detail page an identifier derived from its text, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\n\n#### Scenario: supported Markdown structures are present\n\n- **WHEN** an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, fenced code, or Mermaid fences\n- **THEN** the Spec section MUST render those structures with readable semantic markup, progressively enhance Mermaid fences when the local runtime is available, and preserve escaped fallback source rather than displaying Markdown punctuation as the primary presentation\n\n#### Scenario: user needs exact source evidence\n\n- **WHEN** a rendered Markdown artifact is displayed on its detail page\n- **THEN** the user MUST be able to reveal the exact escaped source without leaving that page\n\n#### Scenario: configuration artifact is displayed\n\n- **WHEN** the Spec section presents an OpenSpec YAML manifest or workspace configuration\n- **THEN** the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown or submitted to the diagram runtime\n\n#### Scenario: rendered Markdown is navigable\n\n- **WHEN** a rendered artifact contains section headings on a detail page\n- **THEN** those headings MUST be individually addressable so a reader can link to a requirement or section rather than only to the whole document\n\n### Requirement: inert Markdown generation\n\nThe system MUST generate Markdown presentation without allowing repository content or Mermaid diagram source to inject active HTML, unsafe URL schemes, executable click behavior, runtime modules, or implicit network requests.\n\n#### Scenario: Markdown contains embedded HTML\n\n- **WHEN** an artifact contains an HTML element, event handler, script, iframe, style, or other executable-looking markup outside or inside a Mermaid fence\n- **THEN** the generated document MUST keep that input inert and MUST NOT add it as executable DOM content\n\n#### Scenario: Markdown contains an unsafe link\n\n- **WHEN** a Markdown link or Mermaid interaction uses an unsafe scheme such as `javascript:` or `data:`\n- **THEN** the generated document MUST not emit a navigable link or executable interaction for that destination\n\n#### Scenario: dashboard opens offline\n\n- **WHEN** a user opens the generated dashboard through `file://`\n- **THEN** rendered Markdown and diagram fallbacks MUST remain readable without loading a Markdown library, module, stylesheet, font, script, or other asset from the network\n\n### Requirement: spec index and detail pages\n\nThe generated Spec section MUST provide one index page listing every canonical spec, active change, and archived change with its real counts, health, and task progress, and MUST make each record&#039;s artifact bodies available from that page through a local hash address backed by the generated data bundle. The index listing MUST remain complete and MUST name each record&#039;s on-disk source path whether or not scripts run.\n\n#### Scenario: user opens the Spec index\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the index MUST show overview counts and compact canonical, active-change, and archive listings, each row naming its source path and addressing that record\n\n#### Scenario: user opens a canonical spec detail page\n\n- **WHEN** a user follows a canonical spec row from the index\n- **THEN** the page MUST show the capability name, source path, requirement and scenario counts, health, the rendered specification, and its exact escaped source without a full page load\n\n#### Scenario: user opens a change detail page\n\n- **WHEN** a user follows a change row from the index\n- **THEN** the page MUST show the change lifecycle, completeness, task progress, and every available artifact including delta specs, each with its exact escaped source\n\n#### Scenario: a record has no readable artifacts\n\n- **WHEN** a listed change or capability has missing or unreadable artifacts\n- **THEN** its detail view MUST state which artifacts are missing and MUST render the readable ones\n\n#### Scenario: scripts are unavailable\n\n- **WHEN** the Spec index is opened without executing JavaScript\n- **THEN** the index listings, counts, source paths, and warnings MUST remain readable and the page MUST state that opening a record requires scripts rather than presenting an empty region\n\n#### Scenario: an unknown record is addressed\n\n- **WHEN** the page is opened with a hash that names no record in the bundle\n- **THEN** the index MUST remain displayed unchanged rather than showing an empty or partial record\n\n### Requirement: deep-linkable specification headings\n\nA rendered record MUST give every heading in a rendered document a stable identifier derived from its text, MUST provide a table of contents for a document with two or more section headings, and MUST keep identifiers unique when one record renders several documents.\n\n#### Scenario: a specification with several requirements is rendered\n\n- **WHEN** a canonical spec containing multiple requirements is displayed\n- **THEN** each requirement heading MUST carry an identifier derived from its text and the table of contents MUST link to it\n\n#### Scenario: one page renders several documents\n\n- **WHEN** a change record on one page renders a proposal, a design, and a delta spec that share a heading name\n- **THEN** each rendered heading MUST receive a distinct identifier and each table-of-contents entry MUST resolve to its own document&#039;s heading\n\n### Requirement: safe generated data bundle\n\nThe Spec section MUST carry its record detail in one generated classic-script data file that assigns a single global, and that file MUST be encoded so no record content can terminate the script element, close an HTML comment, or introduce executable or network-loading markup.\n\n#### Scenario: a record contains a script-like sequence\n\n- **WHEN** an artifact legitimately contains the characters that would close a script element or an HTML comment\n- **THEN** the generated bundle MUST encode them so the browser still parses one complete script and the characters appear as inert text when the record is displayed\n\n#### Scenario: the bundle is loaded\n\n- **WHEN** a generated Spec page loads from `file://`\n- **THEN** the bundle MUST load as a classic script without a network request and MUST expose every listed record\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:project-lifecycle-automation":{"kind":"capability","name":"project-lifecycle-automation","title":"project-lifecycle-automation","path":"specs/project-lifecycle-automation/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>project-lifecycle-automation\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/project-lifecycle-automation/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">3\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">4\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-lifecycle-aware-project-bootstrap\">Requirement: lifecycle-aware project bootstrap\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-archival-and-cleanup\">Requirement: archival and cleanup\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-managed-updates-without-user-content-loss\">Requirement: managed updates without user-content loss\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"project-lifecycle-automation-specification\">Project Lifecycle Automation Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Make the \u003ccode>iris\u003c/code> project lifecycle explicit and reproducible by aligning the generated dashboard and page registry with the repository state and user workflow. The system must provide a safe path for boosting, syncing, archiving, and updating local project artifacts while preserving user-owned content outside managed blocks.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-lifecycle-aware-project-bootstrap\">Requirement: lifecycle-aware project bootstrap\u003c/h3>\n\u003cp>The system MUST provide one project bootstrap flow that creates or refreshes the local structure needed for page generation, dashboard rendering, managed updates, and supported agent skill use without ingesting general repository documentation.\u003c/p>\n\u003ch4 id=\"scenario-user-initializes-the-project\">Scenario: user initializes the project\u003c/h4>\n\u003cp>WHEN a user runs \u003ccode>iris init\u003c/code> in a repository\nTHEN the system MUST create or safely refresh the required \u003ccode>iris/\u003c/code> structure, scaffold managed task metadata, install supported agent skills, and render the dashboard.\u003c/p>\n\u003ch4 id=\"scenario-repository-contains-readme-or-docs-markdown\">Scenario: repository contains README or docs markdown\u003c/h4>\n\u003cp>WHEN a user runs \u003ccode>iris init\u003c/code> in a repository containing \u003ccode>README.md\u003c/code> or \u003ccode>docs/**/*.md\u003c/code>\nTHEN the system MUST NOT automatically copy, mirror, hash, monitor, or create Iris page records from those files.\u003c/p>\n\u003ch3 id=\"requirement-archival-and-cleanup\">Requirement: archival and cleanup\u003c/h3>\n\u003cp>The system MUST provide a clear archiving path that removes a user-selected page from the active feed while preserving its historical generated artifact and registry navigation.\u003c/p>\n\u003ch4 id=\"scenario-a-page-becomes-outdated\">Scenario: a page becomes outdated\u003c/h4>\n\u003cp>WHEN a user runs the archive command for an existing active page\nTHEN the archive command MUST move that exact page into the archive state and update the page registry and dashboard consistently.\u003c/p>\n\u003ch3 id=\"requirement-managed-updates-without-user-content-loss\">Requirement: managed updates without user-content loss\u003c/h3>\n\u003cp>The system MUST support update actions that refresh generated shims or managed file blocks without clobbering user-authored content outside those managed boundaries.\u003c/p>\n\u003ch4 id=\"scenario-a-project-is-updated-after-scaffold-generation\">Scenario: a project is updated after scaffold generation\u003c/h4>\n\u003cp>WHEN a user runs the update flow\nTHEN the system MUST preserve user edits outside managed sections and refresh only the managed integration points.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Project Lifecycle Automation Specification\n\n## Purpose\n\nMake the `iris` project lifecycle explicit and reproducible by aligning the generated dashboard and page registry with the repository state and user workflow. The system must provide a safe path for boosting, syncing, archiving, and updating local project artifacts while preserving user-owned content outside managed blocks.\n\n## Requirements\n\n### Requirement: lifecycle-aware project bootstrap\nThe system MUST provide one project bootstrap flow that creates or refreshes the local structure needed for page generation, dashboard rendering, managed updates, and supported agent skill use without ingesting general repository documentation.\n\n#### Scenario: user initializes the project\nWHEN a user runs `iris init` in a repository\nTHEN the system MUST create or safely refresh the required `iris/` structure, scaffold managed task metadata, install supported agent skills, and render the dashboard.\n\n#### Scenario: repository contains README or docs markdown\nWHEN a user runs `iris init` in a repository containing `README.md` or `docs/**/*.md`\nTHEN the system MUST NOT automatically copy, mirror, hash, monitor, or create Iris page records from those files.\n\n### Requirement: archival and cleanup\nThe system MUST provide a clear archiving path that removes a user-selected page from the active feed while preserving its historical generated artifact and registry navigation.\n\n#### Scenario: a page becomes outdated\nWHEN a user runs the archive command for an existing active page\nTHEN the archive command MUST move that exact page into the archive state and update the page registry and dashboard consistently.\n\n### Requirement: managed updates without user-content loss\nThe system MUST support update actions that refresh generated shims or managed file blocks without clobbering user-authored content outside those managed boundaries.\n\n#### Scenario: a project is updated after scaffold generation\nWHEN a user runs the update flow\nTHEN the system MUST preserve user edits outside managed sections and refresh only the managed integration points.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:research-markdown-pages":{"kind":"capability","name":"research-markdown-pages","title":"research-markdown-pages","path":"specs/research-markdown-pages/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>research-markdown-pages\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/research-markdown-pages/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">5\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">12\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-markdown-research-source-contract\">Requirement: Markdown research source contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-research-document-rendering\">Requirement: research document rendering\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-research-section-page-and-navigation\">Requirement: research section page and navigation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-research-records-in-the-work-browser-and-lifecyc\">Requirement: research records in the work browser and lifecycle\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-agent-guidance-for-research-pages\">Requirement: agent guidance for research pages\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"research-markdown-pages-specification\">research-markdown-pages Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Let agents record research and investigation output as plain Markdown files that Iris reads directly and renders into organized, navigable, offline HTML pages with their own section, so research stops living as loose Markdown.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-markdown-research-source-contract\">Requirement: Markdown research source contract\u003c/h3>\n\u003cp>The system MUST treat \u003ccode>iris/research/&lt;id&gt;/index.md\u003c/code> as the editable source of a research page, where \u003ccode>&lt;id&gt;\u003c/code> is lowercase kebab-case, the file MAY begin with front matter providing \u003ccode>title\u003c/code>, \u003ccode>status\u003c/code> (\u003ccode>draft\u003c/code>, \u003ccode>active\u003c/code>, \u003ccode>done\u003c/code>, \u003ccode>archived\u003c/code>), \u003ccode>tags\u003c/code> (list of strings), \u003ccode>agent\u003c/code>, and \u003ccode>updated\u003c/code> (ISO date), and the remainder is the Markdown body. Missing front matter values MUST fall back to the first level-one heading or the id for \u003ccode>title\u003c/code>, \u003ccode>draft\u003c/code> for \u003ccode>status\u003c/code>, and an explicit unavailable value for the rest; malformed front matter MUST produce a path-specific warning and defaults, never a failed render of other pages. Reading MUST stay inside \u003ccode>iris/research/\u003c/code>, MUST refuse symlinks, MUST bound file size, and MUST NOT execute or fetch anything referenced by the file.\u003c/p>\n\u003ch4 id=\"scenario-agent-creates-a-research-skeleton\">Scenario: agent creates a research skeleton\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user or agent runs \u003ccode>iris research &lt;id&gt;\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST create \u003ccode>iris/research/&lt;id&gt;/index.md\u003c/code> with front matter for the supported keys and Question, Findings, Evidence, and Next steps headings, and MUST refuse an id that already exists or is not kebab-case\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-front-matter-is-absent\">Scenario: front matter is absent\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a research file has no front matter\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST render with the first level-one heading or the id as its title, \u003ccode>draft\u003c/code> status, and unavailable tags, agent, and updated values\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-front-matter-is-malformed\">Scenario: front matter is malformed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> front matter cannot be parsed or contains an unsupported status value\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST render the page with defaults, report a warning naming the file, and continue rendering every other page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-file-exceeds-bounds-or-is-unsafe\">Scenario: file exceeds bounds or is unsafe\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>index.md\u003c/code> is larger than the configured bound, is a symlink, or escapes the research root\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST skip that item with a path-specific warning and MUST NOT read the target\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-research-document-rendering\">Requirement: research document rendering\u003c/h3>\n\u003cp>Each research source MUST render to \u003ccode>iris/research/&lt;id&gt;/page.html\u003c/code> as a deterministic document page inside the shared navigation shell with a header showing title, type \u003ccode>research\u003c/code>, status, tags, agent, and updated, a generated table of contents when the body has two or more level-two or level-three headings, the body rendered by the safe Markdown renderer with embedded HTML, unsafe destinations, and active images disabled, Mermaid fences as source-first diagram hosts, and a print presentation.\u003c/p>\n\u003ch4 id=\"scenario-body-contains-headings-and-a-mermaid-fence\">Scenario: body contains headings and a Mermaid fence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a research body has several level-two headings and an exact \u003ccode>mermaid\u003c/code> fence\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the rendered page MUST contain a table of contents linking to each heading and a diagram host that renders after \u003ccode>iris vendor\u003c/code> while keeping escaped source as fallback\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-body-contains-hostile-content\">Scenario: body contains hostile content\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the body contains raw HTML, script, remote image, or \u003ccode>javascript:\u003c/code> link Markdown\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the rendered page MUST show it as inert text or omit the active part and MUST NOT emit executable or network-loading markup\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-page-is-published-standalone\">Scenario: page is published standalone\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris publish &lt;id&gt;\u003c/code> or \u003ccode>iris export &lt;id&gt; --single\u003c/code> targets a research page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the artifact MUST be self-contained, omit the navigation shell, and keep the document readable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-research-section-page-and-navigation\">Requirement: research section page and navigation\u003c/h3>\n\u003cp>The system MUST generate \u003ccode>iris/research.html\u003c/code> listing every research item with a summary strip (total and per-status counts), a filterable list showing title, id, status, tags, agent, and updated with unavailable values labelled, and MUST add a Research entry to the navigation shell on every generated page.\u003c/p>\n\u003ch4 id=\"scenario-repository-has-research-items\">Scenario: repository has research items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the Research section\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST list every rendered research item with a link to its page and honest metadata, and the shell MUST mark Research as current\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-repository-has-no-research-items\">Scenario: repository has no research items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Research section has no items\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST name \u003ccode>iris research &lt;id&gt;\u003c/code> and \u003ccode>iris render --all\u003c/code> as the commands that populate it\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-research-records-in-the-work-browser-and-lifecyc\">Requirement: research records in the work browser and lifecycle\u003c/h3>\n\u003cp>Research items MUST appear in the Work browser projection as type \u003ccode>research\u003c/code> with status from front matter, priority unavailable, a bounded description from the first paragraph, and a full-page link, so List, Table, Kanban, filter, and the detail drawer cover them; \u003ccode>iris archive &lt;id&gt;\u003c/code> MUST move a research item into the archive while keeping its page reachable, and the Overview summary MUST count research items.\u003c/p>\n\u003ch4 id=\"scenario-research-item-is-filtered-or-opened-from-work\">Scenario: research item is filtered or opened from Work\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user filters by a research tag or activates a research row\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the matching research record MUST be shown with type \u003ccode>research\u003c/code> and its drawer MUST link to the research page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"scenario-research-item-is-archived\">Scenario: research item is archived\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris archive &lt;id&gt;\u003c/code> for a research item\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the source and page MUST move under \u003ccode>iris/archive/&lt;id&gt;/\u003c/code>, the registry MUST mark it archived, and the dashboard MUST keep a working link\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"requirement-agent-guidance-for-research-pages\">Requirement: agent guidance for research pages\u003c/h3>\n\u003cp>The canonical \u003ccode>iris-workspace\u003c/code> skill MUST describe creating, writing, and rendering research pages as Markdown, naming the source path and the render command, so agents write Markdown rather than JSON for research responses.\u003c/p>\n\u003ch4 id=\"scenario-skill-is-installed\">Scenario: skill is installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs or refreshes the agent skill\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every generated skill surface MUST include the research workflow alongside the existing content commands\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># research-markdown-pages Specification\n\n## Purpose\nLet agents record research and investigation output as plain Markdown files that Iris reads directly and renders into organized, navigable, offline HTML pages with their own section, so research stops living as loose Markdown.\n\n## Requirements\n\n### Requirement: Markdown research source contract\n\nThe system MUST treat `iris/research/&lt;id&gt;/index.md` as the editable source of a research page, where `&lt;id&gt;` is lowercase kebab-case, the file MAY begin with front matter providing `title`, `status` (`draft`, `active`, `done`, `archived`), `tags` (list of strings), `agent`, and `updated` (ISO date), and the remainder is the Markdown body. Missing front matter values MUST fall back to the first level-one heading or the id for `title`, `draft` for `status`, and an explicit unavailable value for the rest; malformed front matter MUST produce a path-specific warning and defaults, never a failed render of other pages. Reading MUST stay inside `iris/research/`, MUST refuse symlinks, MUST bound file size, and MUST NOT execute or fetch anything referenced by the file.\n\n#### Scenario: agent creates a research skeleton\n\n- **WHEN** a user or agent runs `iris research &lt;id&gt;`\n- **THEN** the system MUST create `iris/research/&lt;id&gt;/index.md` with front matter for the supported keys and Question, Findings, Evidence, and Next steps headings, and MUST refuse an id that already exists or is not kebab-case\n\n#### Scenario: front matter is absent\n\n- **WHEN** a research file has no front matter\n- **THEN** the page MUST render with the first level-one heading or the id as its title, `draft` status, and unavailable tags, agent, and updated values\n\n#### Scenario: front matter is malformed\n\n- **WHEN** front matter cannot be parsed or contains an unsupported status value\n- **THEN** the system MUST render the page with defaults, report a warning naming the file, and continue rendering every other page\n\n#### Scenario: file exceeds bounds or is unsafe\n\n- **WHEN** `index.md` is larger than the configured bound, is a symlink, or escapes the research root\n- **THEN** the system MUST skip that item with a path-specific warning and MUST NOT read the target\n\n### Requirement: research document rendering\n\nEach research source MUST render to `iris/research/&lt;id&gt;/page.html` as a deterministic document page inside the shared navigation shell with a header showing title, type `research`, status, tags, agent, and updated, a generated table of contents when the body has two or more level-two or level-three headings, the body rendered by the safe Markdown renderer with embedded HTML, unsafe destinations, and active images disabled, Mermaid fences as source-first diagram hosts, and a print presentation.\n\n#### Scenario: body contains headings and a Mermaid fence\n\n- **WHEN** a research body has several level-two headings and an exact `mermaid` fence\n- **THEN** the rendered page MUST contain a table of contents linking to each heading and a diagram host that renders after `iris vendor` while keeping escaped source as fallback\n\n#### Scenario: body contains hostile content\n\n- **WHEN** the body contains raw HTML, script, remote image, or `javascript:` link Markdown\n- **THEN** the rendered page MUST show it as inert text or omit the active part and MUST NOT emit executable or network-loading markup\n\n#### Scenario: page is published standalone\n\n- **WHEN** `iris publish &lt;id&gt;` or `iris export &lt;id&gt; --single` targets a research page\n- **THEN** the artifact MUST be self-contained, omit the navigation shell, and keep the document readable\n\n### Requirement: research section page and navigation\n\nThe system MUST generate `iris/research.html` listing every research item with a summary strip (total and per-status counts), a filterable list showing title, id, status, tags, agent, and updated with unavailable values labelled, and MUST add a Research entry to the navigation shell on every generated page.\n\n#### Scenario: repository has research items\n\n- **WHEN** a user opens the Research section\n- **THEN** it MUST list every rendered research item with a link to its page and honest metadata, and the shell MUST mark Research as current\n\n#### Scenario: repository has no research items\n\n- **WHEN** the Research section has no items\n- **THEN** it MUST name `iris research &lt;id&gt;` and `iris render --all` as the commands that populate it\n\n### Requirement: research records in the work browser and lifecycle\n\nResearch items MUST appear in the Work browser projection as type `research` with status from front matter, priority unavailable, a bounded description from the first paragraph, and a full-page link, so List, Table, Kanban, filter, and the detail drawer cover them; `iris archive &lt;id&gt;` MUST move a research item into the archive while keeping its page reachable, and the Overview summary MUST count research items.\n\n#### Scenario: research item is filtered or opened from Work\n\n- **WHEN** a user filters by a research tag or activates a research row\n- **THEN** the matching research record MUST be shown with type `research` and its drawer MUST link to the research page\n\n#### Scenario: research item is archived\n\n- **WHEN** a user runs `iris archive &lt;id&gt;` for a research item\n- **THEN** the source and page MUST move under `iris/archive/&lt;id&gt;/`, the registry MUST mark it archived, and the dashboard MUST keep a working link\n\n### Requirement: agent guidance for research pages\n\nThe canonical `iris-workspace` skill MUST describe creating, writing, and rendering research pages as Markdown, naming the source path and the render command, so agents write Markdown rather than JSON for research responses.\n\n#### Scenario: skill is installed\n\n- **WHEN** `iris init` installs or refreshes the agent skill\n- **THEN** every generated skill surface MUST include the research workflow alongside the existing content commands\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:session-ingestion-and-reporting":{"kind":"capability","name":"session-ingestion-and-reporting","title":"session-ingestion-and-reporting","path":"specs/session-ingestion-and-reporting/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>session-ingestion-and-reporting\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/session-ingestion-and-reporting/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">4\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">7\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-session-discovery-and-normalization\">Requirement: session discovery and normalization\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-report-content-derived-from-session-evidence\">Requirement: report content derived from session evidence\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-rendered-page-output\">Requirement: rendered page output\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-local-first-offline-safe-operation\">Requirement: local-first, offline-safe operation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"session-ingestion-and-reporting-specification\">Session Ingestion and Reporting Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Provide a reliable local-first path for converting session history into a visual, versioned report page. The system must preserve evidence from the user session, translate it into the project report schema, and render it in a way that remains useful for later review and sharing.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-session-discovery-and-normalization\">Requirement: session discovery and normalization\u003c/h3>\n\u003cp>The system MUST support ingesting session data from a local directory or exported session bundle and MUST normalize the relevant files into a single report dataset.\u003c/p>\n\u003ch4 id=\"scenario-user-points-the-cli-at-a-session-directory\">Scenario: user points the CLI at a session directory\u003c/h4>\n\u003cp>WHEN a user runs the report workflow with a session source\nTHEN the CLI MUST discover session metadata, turns, checkpoints, references, and file activity and MUST ignore irrelevant files unless they are part of the session dataset.\u003c/p>\n\u003ch4 id=\"scenario-session-data-is-partial-or-noisy\">Scenario: session data is partial or noisy\u003c/h4>\n\u003cp>WHEN session data is incomplete or contains extra non-session artifacts\nTHEN the system MUST still build a usable report while preserving only the evidence that can be mapped to the report contract.\u003c/p>\n\u003ch3 id=\"requirement-report-content-derived-from-session-evidence\">Requirement: report content derived from session evidence\u003c/h3>\n\u003cp>The system MUST turn session metadata into a report summary that includes the workstream, branch, status, changed files, and notable references.\u003c/p>\n\u003ch4 id=\"scenario-session-shows-completed-work\">Scenario: session shows completed work\u003c/h4>\n\u003cp>WHEN a session contains concrete work, notes, or checkpoints\nTHEN the generated report MUST summarize the outcome in a concise, reviewable form and MUST keep the underlying evidence attached to the page.\u003c/p>\n\u003ch4 id=\"scenario-a-session-includes-pr-or-issue-references\">Scenario: a session includes PR or issue references\u003c/h4>\n\u003cp>WHEN evidence contains a linked PR, issue, or commit reference\nTHEN the report MUST surface the reference in a way that is searchable and visible in the generated contract.\u003c/p>\n\u003ch3 id=\"requirement-rendered-page-output\">Requirement: rendered page output\u003c/h3>\n\u003cp>The system MUST create or update a page record and render the resulting report so it appears in the local dashboard or page registry.\u003c/p>\n\u003ch4 id=\"scenario-report-is-generated-from-session-input\">Scenario: report is generated from session input\u003c/h4>\n\u003cp>WHEN the report pipeline succeeds\nTHEN the CLI MUST emit a rendered page artifact and the page SHOULD be discoverable through the same dashboard workflow as other iris pages.\u003c/p>\n\u003ch4 id=\"scenario-generation-fails-for-invalid-input\">Scenario: generation fails for invalid input\u003c/h4>\n\u003cp>WHEN the source input does not contain valid session evidence\nTHEN the system MUST stop with a clear validation error and MUST describe the missing or unsupported input format.\u003c/p>\n\u003ch3 id=\"requirement-local-first-offline-safe-operation\">Requirement: local-first, offline-safe operation\u003c/h3>\n\u003cp>The ingestion and reporting flow MUST require no external service call to generate the page and MUST operate purely from local files or a local session export.\u003c/p>\n\u003ch4 id=\"scenario-offline-usage\">Scenario: offline usage\u003c/h4>\n\u003cp>WHEN a user runs the tool without network access\nTHEN the session ingestion workflow MUST still create a report contract and rendered output from local data.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Session Ingestion and Reporting Specification\n\n## Purpose\n\nProvide a reliable local-first path for converting session history into a visual, versioned report page. The system must preserve evidence from the user session, translate it into the project report schema, and render it in a way that remains useful for later review and sharing.\n\n## Requirements\n\n### Requirement: session discovery and normalization\nThe system MUST support ingesting session data from a local directory or exported session bundle and MUST normalize the relevant files into a single report dataset.\n\n#### Scenario: user points the CLI at a session directory\nWHEN a user runs the report workflow with a session source\nTHEN the CLI MUST discover session metadata, turns, checkpoints, references, and file activity and MUST ignore irrelevant files unless they are part of the session dataset.\n\n#### Scenario: session data is partial or noisy\nWHEN session data is incomplete or contains extra non-session artifacts\nTHEN the system MUST still build a usable report while preserving only the evidence that can be mapped to the report contract.\n\n### Requirement: report content derived from session evidence\nThe system MUST turn session metadata into a report summary that includes the workstream, branch, status, changed files, and notable references.\n\n#### Scenario: session shows completed work\nWHEN a session contains concrete work, notes, or checkpoints\nTHEN the generated report MUST summarize the outcome in a concise, reviewable form and MUST keep the underlying evidence attached to the page.\n\n#### Scenario: a session includes PR or issue references\nWHEN evidence contains a linked PR, issue, or commit reference\nTHEN the report MUST surface the reference in a way that is searchable and visible in the generated contract.\n\n### Requirement: rendered page output\nThe system MUST create or update a page record and render the resulting report so it appears in the local dashboard or page registry.\n\n#### Scenario: report is generated from session input\nWHEN the report pipeline succeeds\nTHEN the CLI MUST emit a rendered page artifact and the page SHOULD be discoverable through the same dashboard workflow as other iris pages.\n\n#### Scenario: generation fails for invalid input\nWHEN the source input does not contain valid session evidence\nTHEN the system MUST stop with a clear validation error and MUST describe the missing or unsupported input format.\n\n### Requirement: local-first, offline-safe operation\nThe ingestion and reporting flow MUST require no external service call to generate the page and MUST operate purely from local files or a local session export.\n\n#### Scenario: offline usage\nWHEN a user runs the tool without network access\nTHEN the session ingestion workflow MUST still create a report contract and rendered output from local data.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"capability:standalone-publish-and-export":{"kind":"capability","name":"standalone-publish-and-export","title":"standalone-publish-and-export","path":"specs/standalone-publish-and-export/spec.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">canonical spec\u003c/span>\n        \u003ch1>standalone-publish-and-export\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">specs/standalone-publish-and-export/spec.md\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"capability summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">4\u003c/span>\u003cspan class=\"label\">requirements\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">6\u003c/span>\u003cspan class=\"label\">scenarios\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">warnings\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#requirements\">Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-publishing-of-portable-html\">Requirement: publishing of portable HTML\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-export-variants\">Requirement: export variants\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-offline-safe-asset-handling\">Requirement: offline-safe asset handling\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#requirement-output-path-control\">Requirement: output path control\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"standalone-publish-and-export-specification\">standalone-publish-and-export Specification\u003c/h1>\n\u003ch2 id=\"purpose\">Purpose\u003c/h2>\n\u003cp>Provide a reliable way to turn a generated page into a standalone artifact that can be shared, inspected, or archived without requiring a live \u003ccode>iris\u003c/code> project to be present. The system must preserve the rendered output, embed or vendor any required assets, and work in an offline-safe local workflow.\u003c/p>\n\u003ch2 id=\"requirements\">Requirements\u003c/h2>\n\u003ch3 id=\"requirement-publishing-of-portable-html\">Requirement: publishing of portable HTML\u003c/h3>\n\u003cp>The system MUST support generating a single-file static HTML artifact from a rendered page while preserving the project’s visual structure and data.\u003c/p>\n\u003ch4 id=\"scenario-user-publishes-a-page\">Scenario: user publishes a page\u003c/h4>\n\u003cp>WHEN a user runs the publish command for a valid page\nTHEN the system MUST create a generated artifact that can be opened directly in a browser without needing the rest of the project tree.\u003c/p>\n\u003ch4 id=\"scenario-page-is-missing-or-invalid\">Scenario: page is missing or invalid\u003c/h4>\n\u003cp>WHEN the selected page does not exist or cannot be rendered\nTHEN the system MUST fail clearly and MUST explain the missing or invalid page identifier.\u003c/p>\n\u003ch3 id=\"requirement-export-variants\">Requirement: export variants\u003c/h3>\n\u003cp>The system MUST support the documented single-file HTML export, and for any export mode the project has defined but not made available it MUST fail with an actionable message rather than emitting an artifact of the wrong type.\u003c/p>\n\u003ch4 id=\"scenario-user-requests-the-single-file-export\">Scenario: user requests the single-file export\u003c/h4>\n\u003cp>WHEN a user selects the single-file HTML export mode\nTHEN the system MUST produce a standalone HTML artifact and MUST use the correct output path and naming conventions\u003c/p>\n\u003ch4 id=\"scenario-user-requests-an-unavailable-export-mode\">Scenario: user requests an unavailable export mode\u003c/h4>\n\u003cp>WHEN a user selects an export mode that the project has deferred, such as an image or PDF mode awaiting a deterministic renderer decision\nTHEN the system MUST refuse the request and name the reason, and MUST NOT write a file of a different type under the requested name\u003c/p>\n\u003ch3 id=\"requirement-offline-safe-asset-handling\">Requirement: offline-safe asset handling\u003c/h3>\n\u003cp>The system MUST ensure generated artifacts do not depend on a network call to render or display correctly.\u003c/p>\n\u003ch4 id=\"scenario-offline-export\">Scenario: offline export\u003c/h4>\n\u003cp>WHEN a page is generated in an offline environment\nTHEN the export MUST rely on vendored or bundled assets instead of remote CDN content.\u003c/p>\n\u003ch3 id=\"requirement-output-path-control\">Requirement: output path control\u003c/h3>\n\u003cp>The system MUST allow the user to choose the destination path for published and exported artifacts and MUST handle output directories consistently.\u003c/p>\n\u003ch4 id=\"scenario-user-specifies-a-target-path\">Scenario: user specifies a target path\u003c/h4>\n\u003cp>WHEN the output directory is valid\nTHEN the CLI MUST write the artifact there and MUST create missing parent directories as needed.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># standalone-publish-and-export Specification\n\n## Purpose\nProvide a reliable way to turn a generated page into a standalone artifact that can be shared, inspected, or archived without requiring a live `iris` project to be present. The system must preserve the rendered output, embed or vendor any required assets, and work in an offline-safe local workflow.\n\n## Requirements\n\n### Requirement: publishing of portable HTML\nThe system MUST support generating a single-file static HTML artifact from a rendered page while preserving the project’s visual structure and data.\n\n#### Scenario: user publishes a page\nWHEN a user runs the publish command for a valid page\nTHEN the system MUST create a generated artifact that can be opened directly in a browser without needing the rest of the project tree.\n\n#### Scenario: page is missing or invalid\nWHEN the selected page does not exist or cannot be rendered\nTHEN the system MUST fail clearly and MUST explain the missing or invalid page identifier.\n\n### Requirement: export variants\nThe system MUST support the documented single-file HTML export, and for any export mode the project has defined but not made available it MUST fail with an actionable message rather than emitting an artifact of the wrong type.\n\n#### Scenario: user requests the single-file export\nWHEN a user selects the single-file HTML export mode\nTHEN the system MUST produce a standalone HTML artifact and MUST use the correct output path and naming conventions\n\n#### Scenario: user requests an unavailable export mode\nWHEN a user selects an export mode that the project has deferred, such as an image or PDF mode awaiting a deterministic renderer decision\nTHEN the system MUST refuse the request and name the reason, and MUST NOT write a file of a different type under the requested name\n\n### Requirement: offline-safe asset handling\nThe system MUST ensure generated artifacts do not depend on a network call to render or display correctly.\n\n#### Scenario: offline export\nWHEN a page is generated in an offline environment\nTHEN the export MUST rely on vendored or bundled assets instead of remote CDN content.\n\n### Requirement: output path control\nThe system MUST allow the user to choose the destination path for published and exported artifacts and MUST handle output directories consistently.\n\n#### Scenario: user specifies a target path\nWHEN the output directory is valid\nTHEN the CLI MUST write the artifact there and MUST create missing parent directories as needed.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"change:electric-design-system":{"kind":"change","name":"electric-design-system","title":"electric-design-system","path":"changes/electric-design-system","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">active change\u003c/span>\n        \u003ch1>electric-design-system\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/electric-design-system\u003c/span>\n          \u003cspan class=\"badge b-warning\">incomplete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">30\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">2\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter\" role=\"img\" aria-label=\"electric-design-system: 30 of 31 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 97%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">30/31 tasks complete · 1 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/electric-design-system/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The workspace's current look — &quot;Aperture / Electra&quot; — was an interpretation made when no real design system existed: the 2026-08-21 redesign guessed at &quot;Electra&quot; as neutrals plus one electric accent. The actual system has now been supplied — Vision &quot;Electric&quot; v2.0, a token-and-component contract for standalone HTML research reports — and the owner wants iris to adopt it as the one visual language for every generated page, with Vision's radar identity replacing the aperture ring. Adopting it now, before more pages and agent-authored research accumulate against the interim look, keeps the redesign a single coordinated move rather than a series of partial restyles.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>The token stylesheet becomes Vision §2 — its names (\u003ccode>--background\u003c/code>, \u003ccode>--card\u003c/code>, \u003ccode>--card-2\u003c/code>, \u003ccode>--code-bg\u003c/code>, \u003ccode>--border\u003c/code>, \u003ccode>--muted\u003c/code>, \u003ccode>--primary\u003c/code>, \u003ccode>--primary-fg\u003c/code>, \u003ccode>--accent-1..4\u003c/code>, \u003ccode>--success\u003c/code>, \u003ccode>--warning\u003c/code>, \u003ccode>--danger\u003c/code>, \u003ccode>--info\u003c/code>, \u003ccode>--glow\u003c/code>, \u003ccode>--shadow-card\u003c/code>, \u003ccode>--radius\u003c/code>, \u003ccode>--radius-sm\u003c/code>, \u003ccode>--radius-pill\u003c/code>, \u003ccode>--font-sans\u003c/code>, \u003ccode>--font-mono\u003c/code>) and its oklch values verbatim — plus an iris extension block for what Vision does not define: navigation-shell surfaces, the six page-type colors as aliases onto Vision accents, priority colors, code-block foreground tokens, the sRGB fallback tokens Mermaid needs, and the existing size/space/motion/layout ramps. The only value edits to Vision's block are the measured lightness nudges needed to keep iris's 4.5:1 text, 3:1 control-boundary, and 1.45:1 border-visibility floors in both themes, and each is recorded.\u003c/li>\n\u003cli>The component stylesheet becomes Vision §3 verbatim (cards, badges, callouts, stats, tabs, code, file tree, tables, flow, steps, timeline, meters, evidence, footnotes, collapsibles, buttons, kbd, mode toggle, diagram/chart containers, print) with its three inline color literals tokenised, plus an iris layer — written only against Vision tokens — for the app shell, Work browser, Kanban, detail drawer, Spec browser, Commands, research documents, and contract pages.\u003c/li>\n\u003cli>Generated markup migrates to Vision's vocabulary: \u003ccode>.pill\u003c/code>/\u003ccode>.type-chip\u003c/code>/\u003ccode>.status-chip\u003c/code>/\u003ccode>.priority-chip\u003c/code> → \u003ccode>.badge.b-*\u003c/code>; \u003ccode>.stat-tile\u003c/code>/\u003ccode>.metric-card\u003c/code> → \u003ccode>.card.stat\u003c/code>; \u003ccode>.callout.warn\u003c/code> → \u003ccode>.callout.c-warn\u003c/code>; \u003ccode>.tab-button\u003c/code> → \u003ccode>.tab\u003c/code>; \u003ccode>.button\u003c/code> → \u003ccode>.btn\u003c/code>; \u003ccode>.progress\u003c/code> → \u003ccode>.meter\u003c/code>; \u003ccode>.surface\u003c/code> → \u003ccode>.card\u003c/code>; research and contract headers use \u003ccode>h1.page\u003c/code>, \u003ccode>p.subtitle\u003c/code>, \u003ccode>.meta-row\u003c/code>; documents use Vision's \u003ccode>.layout\u003c/code> with a sticky left \u003ccode>.toc\u003c/code>.\u003c/li>\n\u003cli>Icons come from Lucide 0.469.0 (the version Vision pins), rendered at generation time as inline SVG from the npm package — zero runtime, no script, no network — replacing the hand-drawn nav paths. The brand mark is the \u003ccode>radar\u003c/code> icon in \u003ccode>--primary\u003c/code>; the Overview hero is Vision's \u003ccode>card.hero\u003c/code> with the violet glow.\u003c/li>\n\u003cli>\u003cstrong>BREAKING\u003c/strong> (generated output only): the aperture ring, per-page aperture glyphs, \u003ccode>.tp-*\u003c/code> classes, and the \u003ccode>aperture-open\u003c/code> animation are removed; the ring's information survives as a &quot;pages by type&quot; badge row in the hero. Old token names (\u003ccode>--bg\u003c/code>, \u003ccode>--surface-*\u003c/code>, \u003ccode>--text-*\u003c/code>, \u003ccode>--accent*\u003c/code>, \u003ccode>--line-1\u003c/code>, \u003ccode>--*-soft\u003c/code>, \u003ccode>--elevation-1\u003c/code>, \u003ccode>--font-display\u003c/code>, \u003ccode>--radius-1/2/3/full\u003c/code>) and old component class names stop existing in \u003ccode>iris/design/\u003c/code>. Contracts, research Markdown, archives, and CLI behavior are unaffected.\u003c/li>\n\u003cli>The theme toggle becomes Vision's two-button mode toggle; \u003ccode>data-theme\u003c/code>, \u003ccode>iris/config.yaml\u003c/code>'s \u003ccode>theme:\u003c/code>, the \u003ccode>t\u003c/code> shortcut, and the stored preference keep working. Theme changes dispatch an \u003ccode>iris:theme\u003c/code> event.\u003c/li>\n\u003cli>Mermaid keeps the vendored classic runtime but is themed from the token set (\u003ccode>theme: &quot;base&quot;\u003c/code> with \u003ccode>themeVariables\u003c/code> read from the \u003ccode>--mmd-*\u003c/code> tokens) and re-renders on theme change; the agent workspace template gains Vision's semantic \u003ccode>classDef\u003c/code> snippet for authored flowcharts.\u003c/li>\n\u003cli>The token contract validator learns oklch (with sRGB gamut mapping) and \u003ccode>var()\u003c/code> aliases, and its required-token and contrast-pair lists move to the new names; token-lint additionally bans \u003ccode>oklch()\u003c/code>, \u003ccode>oklab()\u003c/code>, \u003ccode>lab()\u003c/code>, \u003ccode>lch()\u003c/code>, \u003ccode>hwb()\u003c/code>, and \u003ccode>color()\u003c/code> literals outside the token block.\u003c/li>\n\u003cli>\u003ccode>docs/design-system.md\u003c/code> is rewritten as &quot;iris design system 4.0 — Electric&quot; with an explicit &quot;relationship to Vision Electric v2.0&quot; section (what is verbatim, what deviates and why); the supplied Vision document is stored at \u003ccode>docs/vision-electric-v2.md\u003c/code> as the upstream contract. The unused \u003ccode>src/cdn.ts\u003c/code> is deleted.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cp>None — icons, tokens, and components are all facets of the existing design-system capability.\u003c/p>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: the token contract requirement changes from the Aperture token set to Vision's token names and oklch values plus an iris extension block, with the validator parsing oklch and \u003ccode>var()\u003c/code> aliases and the contrast pairs re-stated against the new names; the component language requirement changes to Vision's component vocabulary with generation-time Lucide icons and no aperture marks; the dashboard-hierarchy requirement's Overview scenario changes from the aperture ring to the hero badge row for pages-by-type.\u003c/li>\n\u003cli>\u003ccode>markdown-diagram-rendering\u003c/code>: the offline-progressive-enhancement and presentation requirements gain that diagrams are themed from the design tokens and re-render when the theme changes, still without any remote resource.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>\u003ccode>src/templates/tokens.ts\u003c/code>, \u003ccode>styles.ts\u003c/code>, \u003ccode>script.ts\u003c/code>, \u003ccode>shell.ts\u003c/code>, \u003ccode>common.ts\u003c/code>, \u003ccode>workspace.ts\u003c/code>, and every \u003ccode>pages/*.ts\u003c/code>; new \u003ccode>src/templates/icons.ts\u003c/code>; \u003ccode>src/cdn.ts\u003c/code> removed.\u003c/li>\n\u003cli>\u003ccode>scripts/token-contract.mjs\u003c/code> and \u003ccode>scripts/token-lint.mjs\u003c/code>; \u003ccode>tests/token-contract.test.mjs\u003c/code>, \u003ccode>html-navigation\u003c/code>, \u003ccode>workspace-shell\u003c/code>, \u003ccode>publish-export\u003c/code>, \u003ccode>openspec-browser\u003c/code>, \u003ccode>work-board\u003c/code>, \u003ccode>research-pages\u003c/code> assertions that name old classes or tokens; a new icons test.\u003c/li>\n\u003cli>One new generation-time dependency, \u003ccode>lucide@0.469.0\u003c/code> (ISC); no new runtime asset, CLI command, schema, or network access. \u003ccode>iris publish\u003c/code> and \u003ccode>iris export --single\u003c/code> artifacts remain self-contained — inline SVG adds no resource reference.\u003c/li>\n\u003cli>\u003ccode>docs/design-system.md\u003c/code>, \u003ccode>docs/tech.md\u003c/code> (decision row), \u003ccode>README.md\u003c/code> (one line), \u003ccode>templates/agents/iris-workspace.md\u003c/code> (classDef snippet), new \u003ccode>docs/vision-electric-v2.md\u003c/code>.\u003c/li>\n\u003cli>The checked-in \u003ccode>iris/\u003c/code> dogfood tree is regenerated; \u003ccode>iris/design/tokens.css\u003c/code>, \u003ccode>components/base.css\u003c/code>, and \u003ccode>components/base.js\u003c/code> change wholesale.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe workspace&#039;s current look — &quot;Aperture / Electra&quot; — was an interpretation made when no real design system existed: the 2026-08-21 redesign guessed at &quot;Electra&quot; as neutrals plus one electric accent. The actual system has now been supplied — Vision &quot;Electric&quot; v2.0, a token-and-component contract for standalone HTML research reports — and the owner wants iris to adopt it as the one visual language for every generated page, with Vision&#039;s radar identity replacing the aperture ring. Adopting it now, before more pages and agent-authored research accumulate against the interim look, keeps the redesign a single coordinated move rather than a series of partial restyles.\n\n## What Changes\n\n- The token stylesheet becomes Vision §2 — its names (`--background`, `--card`, `--card-2`, `--code-bg`, `--border`, `--muted`, `--primary`, `--primary-fg`, `--accent-1..4`, `--success`, `--warning`, `--danger`, `--info`, `--glow`, `--shadow-card`, `--radius`, `--radius-sm`, `--radius-pill`, `--font-sans`, `--font-mono`) and its oklch values verbatim — plus an iris extension block for what Vision does not define: navigation-shell surfaces, the six page-type colors as aliases onto Vision accents, priority colors, code-block foreground tokens, the sRGB fallback tokens Mermaid needs, and the existing size/space/motion/layout ramps. The only value edits to Vision&#039;s block are the measured lightness nudges needed to keep iris&#039;s 4.5:1 text, 3:1 control-boundary, and 1.45:1 border-visibility floors in both themes, and each is recorded.\n- The component stylesheet becomes Vision §3 verbatim (cards, badges, callouts, stats, tabs, code, file tree, tables, flow, steps, timeline, meters, evidence, footnotes, collapsibles, buttons, kbd, mode toggle, diagram/chart containers, print) with its three inline color literals tokenised, plus an iris layer — written only against Vision tokens — for the app shell, Work browser, Kanban, detail drawer, Spec browser, Commands, research documents, and contract pages.\n- Generated markup migrates to Vision&#039;s vocabulary: `.pill`/`.type-chip`/`.status-chip`/`.priority-chip` → `.badge.b-*`; `.stat-tile`/`.metric-card` → `.card.stat`; `.callout.warn` → `.callout.c-warn`; `.tab-button` → `.tab`; `.button` → `.btn`; `.progress` → `.meter`; `.surface` → `.card`; research and contract headers use `h1.page`, `p.subtitle`, `.meta-row`; documents use Vision&#039;s `.layout` with a sticky left `.toc`.\n- Icons come from Lucide 0.469.0 (the version Vision pins), rendered at generation time as inline SVG from the npm package — zero runtime, no script, no network — replacing the hand-drawn nav paths. The brand mark is the `radar` icon in `--primary`; the Overview hero is Vision&#039;s `card.hero` with the violet glow.\n- **BREAKING** (generated output only): the aperture ring, per-page aperture glyphs, `.tp-*` classes, and the `aperture-open` animation are removed; the ring&#039;s information survives as a &quot;pages by type&quot; badge row in the hero. Old token names (`--bg`, `--surface-*`, `--text-*`, `--accent*`, `--line-1`, `--*-soft`, `--elevation-1`, `--font-display`, `--radius-1/2/3/full`) and old component class names stop existing in `iris/design/`. Contracts, research Markdown, archives, and CLI behavior are unaffected.\n- The theme toggle becomes Vision&#039;s two-button mode toggle; `data-theme`, `iris/config.yaml`&#039;s `theme:`, the `t` shortcut, and the stored preference keep working. Theme changes dispatch an `iris:theme` event.\n- Mermaid keeps the vendored classic runtime but is themed from the token set (`theme: &quot;base&quot;` with `themeVariables` read from the `--mmd-*` tokens) and re-renders on theme change; the agent workspace template gains Vision&#039;s semantic `classDef` snippet for authored flowcharts.\n- The token contract validator learns oklch (with sRGB gamut mapping) and `var()` aliases, and its required-token and contrast-pair lists move to the new names; token-lint additionally bans `oklch()`, `oklab()`, `lab()`, `lch()`, `hwb()`, and `color()` literals outside the token block.\n- `docs/design-system.md` is rewritten as &quot;iris design system 4.0 — Electric&quot; with an explicit &quot;relationship to Vision Electric v2.0&quot; section (what is verbatim, what deviates and why); the supplied Vision document is stored at `docs/vision-electric-v2.md` as the upstream contract. The unused `src/cdn.ts` is deleted.\n\n## Capabilities\n\n### New Capabilities\n\nNone — icons, tokens, and components are all facets of the existing design-system capability.\n\n### Modified Capabilities\n\n- `aperture-design-system`: the token contract requirement changes from the Aperture token set to Vision&#039;s token names and oklch values plus an iris extension block, with the validator parsing oklch and `var()` aliases and the contrast pairs re-stated against the new names; the component language requirement changes to Vision&#039;s component vocabulary with generation-time Lucide icons and no aperture marks; the dashboard-hierarchy requirement&#039;s Overview scenario changes from the aperture ring to the hero badge row for pages-by-type.\n- `markdown-diagram-rendering`: the offline-progressive-enhancement and presentation requirements gain that diagrams are themed from the design tokens and re-render when the theme changes, still without any remote resource.\n\n## Impact\n\n- `src/templates/tokens.ts`, `styles.ts`, `script.ts`, `shell.ts`, `common.ts`, `workspace.ts`, and every `pages/*.ts`; new `src/templates/icons.ts`; `src/cdn.ts` removed.\n- `scripts/token-contract.mjs` and `scripts/token-lint.mjs`; `tests/token-contract.test.mjs`, `html-navigation`, `workspace-shell`, `publish-export`, `openspec-browser`, `work-board`, `research-pages` assertions that name old classes or tokens; a new icons test.\n- One new generation-time dependency, `lucide@0.469.0` (ISC); no new runtime asset, CLI command, schema, or network access. `iris publish` and `iris export --single` artifacts remain self-contained — inline SVG adds no resource reference.\n- `docs/design-system.md`, `docs/tech.md` (decision row), `README.md` (one line), `templates/agents/iris-workspace.md` (classDef snippet), new `docs/vision-electric-v2.md`.\n- The checked-in `iris/` dogfood tree is regenerated; `iris/design/tokens.css`, `components/base.css`, and `components/base.js` change wholesale.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/electric-design-system/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See proposal.md — Why. The constraints that shape the approach are all already enforced in the repo:\u003c/p>\n\u003cul>\n\u003cli>Generated pages open from \u003ccode>file://\u003c/code> and \u003ccode>iris publish\u003c/code> / \u003ccode>iris export --single\u003c/code> must stand alone: \u003ccode>publish.ts\u003c/code> throws on any \u003ccode>&lt;link&gt;\u003c/code>/\u003ccode>&lt;script&gt;\u003c/code>/\u003ccode>&lt;img&gt;\u003c/code> that references a resource, module scripts are CORS-blocked on \u003ccode>file://\u003c/code>, and \u003ccode>docs/design-system.md\u003c/code> §7 has rejected React Flow. Vision's pinned CDN manifest (Lucide UMD, Mermaid ESM, Chart.js, React Flow via esm.sh) therefore cannot be used as written.\u003c/li>\n\u003cli>\u003ccode>scripts/token-lint.mjs\u003c/code> bans hex/rgb/hsl literals outside \u003ccode>src/templates/tokens.ts\u003c/code>; \u003ccode>scripts/token-contract.mjs\u003c/code> requires a fixed token-name list and measures WCAG ratios — but only parses opaque 6-digit hex. Vision's tokens are oklch.\u003c/li>\n\u003cli>Measured against iris's floors (throwaway script, 2026-08-21): every Vision dark value passes except the border-visibility floor (1.24–1.44:1 vs 1.45) and the inline comment color (4.13:1); in light mode \u003ccode>--accent-1/-2/-3/-4\u003c/code>, \u003ccode>--success\u003c/code>, \u003ccode>--warning\u003c/code>, \u003ccode>--info\u003c/code> land at 3.3–4.5:1 on white, \u003ccode>--muted\u003c/code> on the dark code block is 2.7:1, and borders are 1.20–1.35:1. Dark \u003ccode>--primary\u003c/code> and \u003ccode>--accent-1\u003c/code> are outside the sRGB gamut and browsers render them desaturated.\u003c/li>\n\u003cli>Mermaid 11.17.0 is vendored as a classic UMD and initialised with \u003ccode>theme: 'neutral'\u003c/code>; it cannot read CSS variables or oklch strings.\u003c/li>\n\u003cli>Theme state is \u003ccode>data-theme\u003c/code> on \u003ccode>&lt;html&gt;\u003c/code>, seeded from \u003ccode>iris/config.yaml\u003c/code> and overridden per browser in \u003ccode>localStorage\u003c/code>; tests and the validator's theme parser key on that attribute.\u003c/li>\n\u003cli>The owner chose, in order: whole-workspace adoption (not research pages only), Vision's radar identity over the aperture ring, and the Vision-native approach (Vision token and class names become iris's vocabulary) over a skin-only restyle or an alias layer.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Make the supplied Vision document a truthful reference for what iris generates: same token names, same class names, same semantic color mapping, same section/header anatomy on document pages.\u003c/li>\n\u003cli>Keep every existing guarantee: offline \u003ccode>file://\u003c/code>, self-contained publish, no-JS readability, 4.5:1 / 3:1 / 1.45:1 floors, reduced motion, 360 px, deterministic output.\u003c/li>\n\u003cli>Leave a single swap surface (\u003ccode>tokens.ts\u003c/code>) so a future Vision v3 is a token-block replacement plus re-validation.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Vision's §6 report blueprint (TL;DR, question, map, findings…) for research Markdown — a later change; this one gives research pages the component styling and header anatomy only.\u003c/li>\n\u003cli>Chart.js, React Flow, or any runtime chart; webfonts; PNG/PDF export; renaming the \u003ccode>aperture-design-system\u003c/code> capability id or its requirement titles (history links stay valid; prose inside them changes).\u003c/li>\n\u003cli>Changing how agents author research (still plain Markdown; only a Mermaid \u003ccode>classDef\u003c/code> snippet is added to the skill).\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-vision-native-vocabulary-rather-than-a-skin-or-an-alias-laye\">Vision-native vocabulary rather than a skin or an alias layer\u003c/h3>\n\u003cp>Token names and class names come from Vision; iris keeps only what Vision lacks, as an extension block (tokens) and an iris layer (CSS). A skin-only restyle would keep \u003ccode>--surface-1\u003c/code>/\u003ccode>.pill\u003c/code> and make the document a lie the moment a generated page is inspected; an alias layer (\u003ccode>--bg: var(--background)\u003c/code>, \u003ccode>.pill\u003c/code> re-pointed at \u003ccode>.badge\u003c/code>) would carry two vocabularies forever and still needs a \u003ccode>var()\u003c/code>-resolving validator. The wide diff is mechanical and lands once.\u003c/p>\n\u003ch3 id=\"design-oklch-stays-verbatim-the-validator-learns-it\">oklch stays verbatim; the validator learns it\u003c/h3>\n\u003cp>Alternative: convert Vision's oklch to hex once and keep the validator untouched. Rejected because the point of adopting the system is that \u003ccode>tokens.ts\u003c/code> can be swapped for the next Vision block without translation. The validator gains an oklch → linear-sRGB conversion (oklab matrices) with CSS-style gamut mapping — reduce chroma until the color is inside sRGB — before computing relative luminance; channel clamping would misreport the luminance of dark \u003ccode>--primary\u003c/code> and \u003ccode>--accent-1\u003c/code>, which are out of gamut. It also resolves \u003ccode>var(--x)\u003c/code> chains so \u003ccode>--type-report: var(--accent-1)\u003c/code> and \u003ccode>--info: var(--accent-1)\u003c/code> validate. Alpha on a background token still fails as &quot;not opaque&quot;. \u003ccode>color-mix()\u003c/code> in component CSS is not a literal and stays legal; token-lint additionally bans \u003ccode>oklch( oklab( lab( lch( hwb( color(\u003c/code> outside the token block so Vision's two inline oklch literals (\u003ccode>pre.code\u003c/code> color, \u003ccode>.tok-cm\u003c/code>) become tokens.\u003c/p>\n\u003ch3 id=\"design-measured-reconciliation-not-relaxed-floors\">Measured reconciliation, not relaxed floors\u003c/h3>\n\u003cp>The only edits to Vision's values are lightness nudges to the first value that clears a floor: dark \u003ccode>--border\u003c/code> L 0.30 → 0.345 and light \u003ccode>--border\u003c/code> 0.90 → 0.84 (border floor); light \u003ccode>--accent-1/-2/-3/-4\u003c/code>, \u003ccode>--success\u003c/code>, \u003ccode>--warning\u003c/code>, \u003ccode>--info\u003c/code> lowered to ≈0.54–0.59 (4.5:1 on \u003ccode>--card\u003c/code>); \u003ccode>--code-comment\u003c/code> (ex \u003ccode>.tok-cm\u003c/code>) set to L 0.62 and \u003ccode>--code-muted\u003c/code> (filename line) to 0.65 in both themes, because the code block is dark in both. Relaxing the floors was rejected: 11 px badge text is normal text under WCAG AA, and the 1.45 border floor is a deliberate iris decision with its own test. Exact nudged values are set by running the validator during implementation and recorded in \u003ccode>docs/design-system.md\u003c/code>.\u003c/p>\n\u003ch3 id=\"design-data-theme-is-kept-data-mode-is-not-adopted\">\u003ccode>data-theme\u003c/code> is kept; \u003ccode>data-mode\u003c/code> is not adopted\u003c/h3>\n\u003cp>Vision switches on \u003ccode>data-mode=&quot;light&quot;\u003c/code>; iris on \u003ccode>data-theme='light'\u003c/code>. The attribute is internal — config, stored preference, tests, and the validator's theme parser all use \u003ccode>data-theme\u003c/code> — so the token block's light selector is written as \u003ccode>[data-theme='light']\u003c/code>. This is the single selector-level deviation from §2 and is documented.\u003c/p>\n\u003ch3 id=\"design-extension-token-mapping\">Extension token mapping\u003c/h3>\n\u003ctable>\n\u003cthead>\n\u003ctr>\n\u003cth>Token\u003c/th>\n\u003cth>Value\u003c/th>\n\u003cth>Why\u003c/th>\n\u003c/tr>\n\u003c/thead>\n\u003ctbody>\n\u003ctr>\n\u003ctd>\u003ccode>--nav-bg\u003c/code>\u003c/td>\n\u003ctd>dark \u003ccode>oklch(0.13 0.02 285)\u003c/code>, light \u003ccode>var(--card)\u003c/code>\u003c/td>\n\u003ctd>sidebar sits one step below the page in dark, reads as a white panel in light — same intent as before\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--nav-text\u003c/code>, \u003ccode>--nav-active-text\u003c/code>, \u003ccode>--nav-active-bg\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>var(--muted)\u003c/code>, \u003ccode>var(--primary)\u003c/code>, \u003ccode>color-mix(in oklch, var(--primary) 18%, transparent)\u003c/code>\u003c/td>\n\u003ctd>Vision's badge-fill recipe applied to the active entry\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--type-report\u003c/code> / \u003ccode>-feature\u003c/code> / \u003ccode>-bug\u003c/code> / \u003ccode>-idea\u003c/code> / \u003ccode>-plan\u003c/code> / \u003ccode>-research\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>--accent-1\u003c/code> cyan / \u003ccode>--accent-3\u003c/code> lime / \u003ccode>--danger\u003c/code> / \u003ccode>--accent-4\u003c/code> pink / \u003ccode>--accent-2\u003c/code> amber / \u003ccode>--primary\u003c/code> violet\u003c/td>\n\u003ctd>six types onto Vision's wheel; research = primary follows Vision's own meta-row convention (\u003ccode>badge b-primary\u003c/code> is the doc-type badge); \u003ccode>--success\u003c/code> stays reserved for done/verified\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>status draft / active / done / archived\u003c/td>\n\u003ctd>\u003ccode>--muted\u003c/code> / \u003ccode>--primary\u003c/code> / \u003ccode>--success\u003c/code> / \u003ccode>--muted\u003c/code> with dashed border\u003c/td>\n\u003ctd>Vision §5: success = accepted, muted = legacy; &quot;6th category uses muted with a dashed border&quot;\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--priority-urgent\u003c/code> / \u003ccode>-high\u003c/code> / \u003ccode>-medium\u003c/code> / \u003ccode>-low\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>--danger\u003c/code> / \u003ccode>--warning\u003c/code> / \u003ccode>--info\u003c/code> / \u003ccode>--muted\u003c/code>\u003c/td>\n\u003ctd>Vision status semantics\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--code-fg\u003c/code>, \u003ccode>--code-muted\u003c/code>, \u003ccode>--code-comment\u003c/code>\u003c/td>\n\u003ctd>constant across themes\u003c/td>\n\u003ctd>the code block is dark in both themes\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--mmd-*\u003c/code>\u003c/td>\n\u003ctd>§8.1 themeVariables and §8.2 classDef hexes per theme\u003c/td>\n\u003ctd>Vision's &quot;only permitted literals&quot;, relocated into the one file token-lint exempts\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--size-*\u003c/code>, \u003ccode>--space-*\u003c/code>, \u003ccode>--leading-*\u003c/code>, \u003ccode>--weight-*\u003c/code>, \u003ccode>--duration-*\u003c/code>, \u003ccode>--easing\u003c/code>, \u003ccode>--nav-width\u003c/code>, \u003ccode>--nav-rail\u003c/code>, \u003ccode>--backdrop\u003c/code>, \u003ccode>--border-1\u003c/code>\u003c/td>\n\u003ctd>unchanged\u003c/td>\n\u003ctd>Vision has no ramps; iris's stay\u003c/td>\n\u003c/tr>\n\u003c/tbody>\n\u003c/table>\n\u003cp>Type/status collisions inside one row (a violet research badge next to a violet active badge) are accepted: the contract already requires the type name as text, and iris's previous palette overloaded bug/danger and feature/ok the same way. Dedicated extra hues were rejected — the wheel is full and Vision's semantics are the point.\u003c/p>\n\u003ch3 id=\"design-icons-at-generation-time-from-the-pinned-package\">Icons at generation time from the pinned package\u003c/h3>\n\u003cp>\u003ccode>lucide@0.469.0\u003c/code> (ISC; the version Vision §7 pins; ESM entry \u003ccode>dist/esm/lucide.js\u003c/code>) is added as a dependency and imported only by the CLI at render time. \u003ccode>src/templates/icons.ts\u003c/code> serialises an icon's node array into \u003ccode>&lt;svg class=&quot;lucide lucide-&lt;name&gt; …&quot; viewBox=&quot;0 0 24 24&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; stroke-width=&quot;2&quot; stroke-linecap=&quot;round&quot; stroke-linejoin=&quot;round&quot;&gt;…&lt;/svg&gt;\u003c/code>; decorative use gets \u003ccode>aria-hidden\u003c/code>, labelled use gets \u003ccode>role=&quot;img&quot;\u003c/code> + \u003ccode>aria-label\u003c/code>. Unknown names throw at build so output never silently degrades. Alternatives: vendoring Lucide's UMD and calling \u003ccode>createIcons()\u003c/code> at view time (adds a 300 KB runtime, a \u003ccode>&lt;script&gt;\u003c/code> publish would have to strip, and a no-JS gap); hand-copying paths as today (drifts, unlicensed-looking). If the package's export shape differs from expectation at install time, \u003ccode>lucide-static\u003c/code> SVG files are the fallback with the same serialiser; either way the CLI, not the browser, does the work. Icon names are resolved against the installed package and aliases in the icons test.\u003c/p>\n\u003ch3 id=\"design-vision-3-verbatim-plus-an-iris-layer-vision-wins-collisions\">Vision §3 verbatim plus an iris layer; Vision wins collisions\u003c/h3>\n\u003cp>\u003ccode>BASE_COMPONENTS_CSS\u003c/code> is Vision §3 (with the three literals tokenised) followed by an iris layer written only in Vision tokens. Where both define a class (\u003ccode>.card\u003c/code>, \u003ccode>.topbar\u003c/code>, \u003ccode>.tabs\u003c/code>, \u003ccode>.grid-2\u003c/code>, \u003ccode>.timeline\u003c/code>, \u003ccode>.callout\u003c/code>, \u003ccode>kbd\u003c/code>), Vision's rule is the one that ships and iris markup migrates. Markdown output (\u003ccode>markdown-it\u003c/code>, \u003ccode>html: false\u003c/code>) cannot carry Vision classes, so \u003ccode>.doc-body table\u003c/code>, \u003ccode>.doc-body pre\u003c/code>, and \u003ccode>.doc-body code\u003c/code> mirror \u003ccode>table.ds\u003c/code> / \u003ccode>pre.code\u003c/code> / inline-code rules by descendant selector rather than by renderer rules — reversible later if the blueprint change wants per-element classes. Research documents and Spec detail use Vision's \u003ccode>.layout\u003c/code> with the sticky left \u003ccode>.toc\u003c/code> (replacing iris's right-hand \u003ccode>doc-toc\u003c/code>). Breakpoints: Vision's 900 px rules govern grids and the TOC; iris's 48 rem rule governs the sidebar overlay — two breakpoints for two different concerns.\u003c/p>\n\u003ch3 id=\"design-theme-toggle-and-event\">Theme toggle and event\u003c/h3>\n\u003cp>The toggle becomes Vision's two-button \u003ccode>.mode-toggle\u003c/code> (\u003ccode>[data-theme-set=&quot;dark|light&quot;]\u003c/code>); \u003ccode>t\u003c/code> still flips. After any change the script syncs the active button and dispatches \u003ccode>iris:theme\u003c/code> on \u003ccode>document\u003c/code> with the mode — Vision's \u003ccode>vision:mode\u003c/code> under iris's event namespace — so Mermaid (and any later chart code) re-themes from one signal.\u003c/p>\n\u003ch3 id=\"design-mermaid-themed-from-tokens-re-rendered-on-toggle\">Mermaid themed from tokens, re-rendered on toggle\u003c/h3>\n\u003cp>\u003ccode>setupMermaid\u003c/code> initialises with \u003ccode>theme: 'base'\u003c/code> and \u003ccode>themeVariables\u003c/code> built by reading the \u003ccode>--mmd-*\u003c/code> tokens through \u003ccode>getComputedStyle\u003c/code>, keeping strict security, disabled HTML labels, and the size/edge bounds. Each figure keeps its source (already present in the escaped fallback); on \u003ccode>iris:theme\u003c/code> the runtime is re-initialised for the new theme and every rendered figure is reset to its source and run again, with the existing error path. Mermaid's color parser accepts hex, not oklch or CSS variables, which is why the fallbacks are hex tokens; normalising oklch through a canvas \u003ccode>fillStyle\u003c/code> round-trip was rejected as browser-dependent. The figure wrapper takes Vision's \u003ccode>.diagram\u003c/code> styling; the status line and no-JS fallback stay. The agent workspace template gains the §8.2 \u003ccode>classDef\u003c/code> snippet so authored flowcharts use the semantic colors.\u003c/p>\n\u003ch3 id=\"design-hero-replaces-the-ring-with-a-badge-row\">Hero replaces the ring with a badge row\u003c/h3>\n\u003cp>\u003ccode>overview.ts\u003c/code> renders \u003ccode>card.hero\u003c/code>: radar mark, project name as \u003ccode>h1.page\u003c/code>, the briefing as \u003ccode>p.subtitle\u003c/code>, quick-start commands, and a pages-by-type row — one \u003ccode>.badge\u003c/code> per type with the typed icon, label, and count, \u003ccode>aria-label=&quot;N pages by type&quot;\u003c/code>, and an empty state naming \u003ccode>iris research &lt;id&gt;\u003c/code>. \u003ccode>apertureRing\u003c/code>/\u003ccode>apertureGlyph\u003c/code>, the \u003ccode>.aperture*\u003c/code> CSS, \u003ccode>.tp-*\u003c/code>, and the \u003ccode>aperture-open\u003c/code> keyframes are deleted; the per-row glyph becomes the typed Lucide icon.\u003c/p>\n\u003ch3 id=\"design-spec-bookkeeping\">Spec bookkeeping\u003c/h3>\n\u003cp>The capability id \u003ccode>aperture-design-system\u003c/code> and its requirement titles are kept so archive history and links stay valid; the prose inside the requirements changes. \u003ccode>docs/design-system.md\u003c/code> is retitled &quot;iris design system 4.0 — Electric&quot; and gains a section stating what is verbatim from Vision and what deviates, and the supplied Vision document is stored at \u003ccode>docs/vision-electric-v2.md\u003c/code> as the upstream contract. \u003ccode>src/cdn.ts\u003c/code>, unused since the offline policy, is deleted so the codebase does not advertise CDNs it never loads.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[oklch and \u003ccode>color-mix()\u003c/code> need a 2023+ browser] → All evergreen browsers support both; iris already assumes a modern browser for \u003ccode>file://\u003c/code> module-free scripts. No fallback colors are generated; accepted.\u003c/li>\n\u003cli>[Light-mode accents are slightly darker than Vision's] → Recorded in \u003ccode>docs/design-system.md\u003c/code> with the measured ratios; a future Vision block is re-validated the same way.\u003c/li>\n\u003cli>[Token rename breaks \u003ccode>styles.ts\u003c/code> until markup migrates] → The branch is the CI unit; commits are sequenced but only the whole branch must be green.\u003c/li>\n\u003cli>[Package export shape of \u003ccode>lucide\u003c/code> at generation time] → Verified at install; \u003ccode>lucide-static\u003c/code> fallback with the same serialiser; the icons test fails fast on any missing name.\u003c/li>\n\u003cli>[Re-rendering Mermaid on toggle costs time on diagram-heavy pages] → Only figures already rendered are re-run, sequentially, under the same bounds; failure falls back to source per figure.\u003c/li>\n\u003cli>[Large regenerated-dogfood diff] → Expected and one-time; \u003ccode>html-check\u003c/code> and the publish test guard correctness.\u003c/li>\n\u003cli>[Same hue for a research type badge and an active status badge] → Text label is always present; accepted per the contract's non-color-signal rule.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Branch \u003ccode>feat/electric-design-system\u003c/code> from \u003ccode>main\u003c/code> (the uncommitted dogfood regeneration and the untracked \u003ccode>iris/pages/report-from-agent-sessions/\u003c/code> on \u003ccode>main\u003c/code> are left in place; regeneration rewrites the HTML and includes that page).\u003c/li>\n\u003cli>Tokens + validator + lint; icons module + dependency; component CSS; template/shell/script migration — green together at step 4.\u003c/li>\n\u003cli>Tests, docs, delta specs; \u003ccode>pnpm build &amp;&amp; node dist/src/index.js update &amp;&amp; node dist/src/index.js render --all\u003c/code> to regenerate \u003ccode>iris/\u003c/code>; \u003ccode>pnpm release:check\u003c/code>.\u003c/li>\n\u003cli>PR; \u003ccode>/opsx:archive electric-design-system\u003c/code> after merge.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback: revert the branch; user workspaces regenerate \u003ccode>iris/design/*\u003c/code> from whichever CLI version runs \u003ccode>iris init\u003c/code>/\u003ccode>render --all\u003c/code> next, and the vendored Mermaid bundle is untouched either way.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee proposal.md — Why. The constraints that shape the approach are all already enforced in the repo:\n\n- Generated pages open from `file://` and `iris publish` / `iris export --single` must stand alone: `publish.ts` throws on any `&lt;link&gt;`/`&lt;script&gt;`/`&lt;img&gt;` that references a resource, module scripts are CORS-blocked on `file://`, and `docs/design-system.md` §7 has rejected React Flow. Vision&#039;s pinned CDN manifest (Lucide UMD, Mermaid ESM, Chart.js, React Flow via esm.sh) therefore cannot be used as written.\n- `scripts/token-lint.mjs` bans hex/rgb/hsl literals outside `src/templates/tokens.ts`; `scripts/token-contract.mjs` requires a fixed token-name list and measures WCAG ratios — but only parses opaque 6-digit hex. Vision&#039;s tokens are oklch.\n- Measured against iris&#039;s floors (throwaway script, 2026-08-21): every Vision dark value passes except the border-visibility floor (1.24–1.44:1 vs 1.45) and the inline comment color (4.13:1); in light mode `--accent-1/-2/-3/-4`, `--success`, `--warning`, `--info` land at 3.3–4.5:1 on white, `--muted` on the dark code block is 2.7:1, and borders are 1.20–1.35:1. Dark `--primary` and `--accent-1` are outside the sRGB gamut and browsers render them desaturated.\n- Mermaid 11.17.0 is vendored as a classic UMD and initialised with `theme: &#039;neutral&#039;`; it cannot read CSS variables or oklch strings.\n- Theme state is `data-theme` on `&lt;html&gt;`, seeded from `iris/config.yaml` and overridden per browser in `localStorage`; tests and the validator&#039;s theme parser key on that attribute.\n- The owner chose, in order: whole-workspace adoption (not research pages only), Vision&#039;s radar identity over the aperture ring, and the Vision-native approach (Vision token and class names become iris&#039;s vocabulary) over a skin-only restyle or an alias layer.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Make the supplied Vision document a truthful reference for what iris generates: same token names, same class names, same semantic color mapping, same section/header anatomy on document pages.\n- Keep every existing guarantee: offline `file://`, self-contained publish, no-JS readability, 4.5:1 / 3:1 / 1.45:1 floors, reduced motion, 360 px, deterministic output.\n- Leave a single swap surface (`tokens.ts`) so a future Vision v3 is a token-block replacement plus re-validation.\n\n**Non-Goals:**\n\n- Vision&#039;s §6 report blueprint (TL;DR, question, map, findings…) for research Markdown — a later change; this one gives research pages the component styling and header anatomy only.\n- Chart.js, React Flow, or any runtime chart; webfonts; PNG/PDF export; renaming the `aperture-design-system` capability id or its requirement titles (history links stay valid; prose inside them changes).\n- Changing how agents author research (still plain Markdown; only a Mermaid `classDef` snippet is added to the skill).\n\n## Decisions\n\n### Vision-native vocabulary rather than a skin or an alias layer\n\nToken names and class names come from Vision; iris keeps only what Vision lacks, as an extension block (tokens) and an iris layer (CSS). A skin-only restyle would keep `--surface-1`/`.pill` and make the document a lie the moment a generated page is inspected; an alias layer (`--bg: var(--background)`, `.pill` re-pointed at `.badge`) would carry two vocabularies forever and still needs a `var()`-resolving validator. The wide diff is mechanical and lands once.\n\n### oklch stays verbatim; the validator learns it\n\nAlternative: convert Vision&#039;s oklch to hex once and keep the validator untouched. Rejected because the point of adopting the system is that `tokens.ts` can be swapped for the next Vision block without translation. The validator gains an oklch → linear-sRGB conversion (oklab matrices) with CSS-style gamut mapping — reduce chroma until the color is inside sRGB — before computing relative luminance; channel clamping would misreport the luminance of dark `--primary` and `--accent-1`, which are out of gamut. It also resolves `var(--x)` chains so `--type-report: var(--accent-1)` and `--info: var(--accent-1)` validate. Alpha on a background token still fails as &quot;not opaque&quot;. `color-mix()` in component CSS is not a literal and stays legal; token-lint additionally bans `oklch( oklab( lab( lch( hwb( color(` outside the token block so Vision&#039;s two inline oklch literals (`pre.code` color, `.tok-cm`) become tokens.\n\n### Measured reconciliation, not relaxed floors\n\nThe only edits to Vision&#039;s values are lightness nudges to the first value that clears a floor: dark `--border` L 0.30 → 0.345 and light `--border` 0.90 → 0.84 (border floor); light `--accent-1/-2/-3/-4`, `--success`, `--warning`, `--info` lowered to ≈0.54–0.59 (4.5:1 on `--card`); `--code-comment` (ex `.tok-cm`) set to L 0.62 and `--code-muted` (filename line) to 0.65 in both themes, because the code block is dark in both. Relaxing the floors was rejected: 11 px badge text is normal text under WCAG AA, and the 1.45 border floor is a deliberate iris decision with its own test. Exact nudged values are set by running the validator during implementation and recorded in `docs/design-system.md`.\n\n### `data-theme` is kept; `data-mode` is not adopted\n\nVision switches on `data-mode=&quot;light&quot;`; iris on `data-theme=&#039;light&#039;`. The attribute is internal — config, stored preference, tests, and the validator&#039;s theme parser all use `data-theme` — so the token block&#039;s light selector is written as `[data-theme=&#039;light&#039;]`. This is the single selector-level deviation from §2 and is documented.\n\n### Extension token mapping\n\n| Token | Value | Why |\n| --- | --- | --- |\n| `--nav-bg` | dark `oklch(0.13 0.02 285)`, light `var(--card)` | sidebar sits one step below the page in dark, reads as a white panel in light — same intent as before |\n| `--nav-text`, `--nav-active-text`, `--nav-active-bg` | `var(--muted)`, `var(--primary)`, `color-mix(in oklch, var(--primary) 18%, transparent)` | Vision&#039;s badge-fill recipe applied to the active entry |\n| `--type-report` / `-feature` / `-bug` / `-idea` / `-plan` / `-research` | `--accent-1` cyan / `--accent-3` lime / `--danger` / `--accent-4` pink / `--accent-2` amber / `--primary` violet | six types onto Vision&#039;s wheel; research = primary follows Vision&#039;s own meta-row convention (`badge b-primary` is the doc-type badge); `--success` stays reserved for done/verified |\n| status draft / active / done / archived | `--muted` / `--primary` / `--success` / `--muted` with dashed border | Vision §5: success = accepted, muted = legacy; &quot;6th category uses muted with a dashed border&quot; |\n| `--priority-urgent` / `-high` / `-medium` / `-low` | `--danger` / `--warning` / `--info` / `--muted` | Vision status semantics |\n| `--code-fg`, `--code-muted`, `--code-comment` | constant across themes | the code block is dark in both themes |\n| `--mmd-*` | §8.1 themeVariables and §8.2 classDef hexes per theme | Vision&#039;s &quot;only permitted literals&quot;, relocated into the one file token-lint exempts |\n| `--size-*`, `--space-*`, `--leading-*`, `--weight-*`, `--duration-*`, `--easing`, `--nav-width`, `--nav-rail`, `--backdrop`, `--border-1` | unchanged | Vision has no ramps; iris&#039;s stay |\n\nType/status collisions inside one row (a violet research badge next to a violet active badge) are accepted: the contract already requires the type name as text, and iris&#039;s previous palette overloaded bug/danger and feature/ok the same way. Dedicated extra hues were rejected — the wheel is full and Vision&#039;s semantics are the point.\n\n### Icons at generation time from the pinned package\n\n`lucide@0.469.0` (ISC; the version Vision §7 pins; ESM entry `dist/esm/lucide.js`) is added as a dependency and imported only by the CLI at render time. `src/templates/icons.ts` serialises an icon&#039;s node array into `&lt;svg class=&quot;lucide lucide-&lt;name&gt; …&quot; viewBox=&quot;0 0 24 24&quot; fill=&quot;none&quot; stroke=&quot;currentColor&quot; stroke-width=&quot;2&quot; stroke-linecap=&quot;round&quot; stroke-linejoin=&quot;round&quot;&gt;…&lt;/svg&gt;`; decorative use gets `aria-hidden`, labelled use gets `role=&quot;img&quot;` + `aria-label`. Unknown names throw at build so output never silently degrades. Alternatives: vendoring Lucide&#039;s UMD and calling `createIcons()` at view time (adds a 300 KB runtime, a `&lt;script&gt;` publish would have to strip, and a no-JS gap); hand-copying paths as today (drifts, unlicensed-looking). If the package&#039;s export shape differs from expectation at install time, `lucide-static` SVG files are the fallback with the same serialiser; either way the CLI, not the browser, does the work. Icon names are resolved against the installed package and aliases in the icons test.\n\n### Vision §3 verbatim plus an iris layer; Vision wins collisions\n\n`BASE_COMPONENTS_CSS` is Vision §3 (with the three literals tokenised) followed by an iris layer written only in Vision tokens. Where both define a class (`.card`, `.topbar`, `.tabs`, `.grid-2`, `.timeline`, `.callout`, `kbd`), Vision&#039;s rule is the one that ships and iris markup migrates. Markdown output (`markdown-it`, `html: false`) cannot carry Vision classes, so `.doc-body table`, `.doc-body pre`, and `.doc-body code` mirror `table.ds` / `pre.code` / inline-code rules by descendant selector rather than by renderer rules — reversible later if the blueprint change wants per-element classes. Research documents and Spec detail use Vision&#039;s `.layout` with the sticky left `.toc` (replacing iris&#039;s right-hand `doc-toc`). Breakpoints: Vision&#039;s 900 px rules govern grids and the TOC; iris&#039;s 48 rem rule governs the sidebar overlay — two breakpoints for two different concerns.\n\n### Theme toggle and event\n\nThe toggle becomes Vision&#039;s two-button `.mode-toggle` (`[data-theme-set=&quot;dark|light&quot;]`); `t` still flips. After any change the script syncs the active button and dispatches `iris:theme` on `document` with the mode — Vision&#039;s `vision:mode` under iris&#039;s event namespace — so Mermaid (and any later chart code) re-themes from one signal.\n\n### Mermaid themed from tokens, re-rendered on toggle\n\n`setupMermaid` initialises with `theme: &#039;base&#039;` and `themeVariables` built by reading the `--mmd-*` tokens through `getComputedStyle`, keeping strict security, disabled HTML labels, and the size/edge bounds. Each figure keeps its source (already present in the escaped fallback); on `iris:theme` the runtime is re-initialised for the new theme and every rendered figure is reset to its source and run again, with the existing error path. Mermaid&#039;s color parser accepts hex, not oklch or CSS variables, which is why the fallbacks are hex tokens; normalising oklch through a canvas `fillStyle` round-trip was rejected as browser-dependent. The figure wrapper takes Vision&#039;s `.diagram` styling; the status line and no-JS fallback stay. The agent workspace template gains the §8.2 `classDef` snippet so authored flowcharts use the semantic colors.\n\n### Hero replaces the ring with a badge row\n\n`overview.ts` renders `card.hero`: radar mark, project name as `h1.page`, the briefing as `p.subtitle`, quick-start commands, and a pages-by-type row — one `.badge` per type with the typed icon, label, and count, `aria-label=&quot;N pages by type&quot;`, and an empty state naming `iris research &lt;id&gt;`. `apertureRing`/`apertureGlyph`, the `.aperture*` CSS, `.tp-*`, and the `aperture-open` keyframes are deleted; the per-row glyph becomes the typed Lucide icon.\n\n### Spec bookkeeping\n\nThe capability id `aperture-design-system` and its requirement titles are kept so archive history and links stay valid; the prose inside the requirements changes. `docs/design-system.md` is retitled &quot;iris design system 4.0 — Electric&quot; and gains a section stating what is verbatim from Vision and what deviates, and the supplied Vision document is stored at `docs/vision-electric-v2.md` as the upstream contract. `src/cdn.ts`, unused since the offline policy, is deleted so the codebase does not advertise CDNs it never loads.\n\n## Risks / Trade-offs\n\n- [oklch and `color-mix()` need a 2023+ browser] → All evergreen browsers support both; iris already assumes a modern browser for `file://` module-free scripts. No fallback colors are generated; accepted.\n- [Light-mode accents are slightly darker than Vision&#039;s] → Recorded in `docs/design-system.md` with the measured ratios; a future Vision block is re-validated the same way.\n- [Token rename breaks `styles.ts` until markup migrates] → The branch is the CI unit; commits are sequenced but only the whole branch must be green.\n- [Package export shape of `lucide` at generation time] → Verified at install; `lucide-static` fallback with the same serialiser; the icons test fails fast on any missing name.\n- [Re-rendering Mermaid on toggle costs time on diagram-heavy pages] → Only figures already rendered are re-run, sequentially, under the same bounds; failure falls back to source per figure.\n- [Large regenerated-dogfood diff] → Expected and one-time; `html-check` and the publish test guard correctness.\n- [Same hue for a research type badge and an active status badge] → Text label is always present; accepted per the contract&#039;s non-color-signal rule.\n\n## Migration Plan\n\n1. Branch `feat/electric-design-system` from `main` (the uncommitted dogfood regeneration and the untracked `iris/pages/report-from-agent-sessions/` on `main` are left in place; regeneration rewrites the HTML and includes that page).\n2. Tokens + validator + lint; icons module + dependency; component CSS; template/shell/script migration — green together at step 4.\n3. Tests, docs, delta specs; `pnpm build &amp;&amp; node dist/src/index.js update &amp;&amp; node dist/src/index.js render --all` to regenerate `iris/`; `pnpm release:check`.\n4. PR; `/opsx:archive electric-design-system` after merge.\n\nRollback: revert the branch; user workspaces regenerate `iris/design/*` from whichever CLI version runs `iris init`/`render --all` next, and the vendored Mermaid bundle is untouched either way.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/electric-design-system/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-tokens-and-validation\">1. Tokens and validation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Replace \u003ccode>TOKENS_CSS\u003c/code> in \u003ccode>src/templates/tokens.ts\u003c/code> with Vision §2 verbatim (oklch, dark under \u003ccode>:root\u003c/code>, light under \u003ccode>[data-theme='light']\u003c/code>) followed by the iris extension block: \u003ccode>--nav-bg/--nav-text/--nav-active-text/--nav-active-bg\u003c/code>, the six \u003ccode>--type-*\u003c/code> aliases, \u003ccode>--priority-*\u003c/code>, \u003ccode>--code-fg/--code-muted/--code-comment\u003c/code>, the per-theme \u003ccode>--mmd-*\u003c/code> sRGB fallbacks (§8.1 themeVariables and §8.2 classDef hexes), \u003ccode>--backdrop\u003c/code>, \u003ccode>--border-1\u003c/code>, and the existing size/space/leading/weight/duration/easing/nav ramps; keep the \u003ccode>body\u003c/code> and \u003ccode>:focus-visible\u003c/code> rules.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Teach \u003ccode>scripts/token-contract.mjs\u003c/code> to parse oklch (chroma-reduction gamut mapping before luminance), hex, and \u003ccode>var()\u003c/code> chains; move \u003ccode>REQUIRED_TOKENS\u003c/code>, \u003ccode>THEME_REQUIRED_TOKENS\u003c/code>, contrast, control-boundary, and border pairs to the new names (foreground/muted on background/card/card-2; primary on background/card; type-*, success, warning, danger, info on card; nav pairs on nav-bg; code-fg/code-muted/code-comment on code-bg; primary control on the three surfaces; border floor on the three surfaces); keep the threshold-naming messages.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Run the validator and apply the smallest lightness nudges that clear each floor (expected: dark/light \u003ccode>--border\u003c/code>; light \u003ccode>--accent-1..4\u003c/code>, \u003ccode>--success\u003c/code>, \u003ccode>--warning\u003c/code>, \u003ccode>--info\u003c/code>; \u003ccode>--code-comment\u003c/code>/\u003ccode>--code-muted\u003c/code>); note every nudged token and ratio for the docs task.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.4 Extend \u003ccode>scripts/token-lint.mjs\u003c/code> to ban \u003ccode>oklch( oklab( lab( lch( hwb( color(\u003c/code> literals outside the token block while leaving \u003ccode>color-mix(\u003c/code> legal, and keep the generated \u003ccode>iris/design/tokens.css\u003c/code> sync check.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.5 Rewrite \u003ccode>tests/token-contract.test.mjs\u003c/code> for the new names and oklch values, adding cases for oklch parsing, out-of-gamut mapping (dark \u003ccode>--primary\u003c/code>), \u003ccode>var()\u003c/code> resolution, an alpha background rejection, and the three threshold messages.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-icons\">2. Icons\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add \u003ccode>lucide@0.469.0\u003c/code> as a dependency with a \u003ccode>runtimeDependencyRationale\u003c/code> entry stating it is used only at generation time; confirm the package's icon-node export shape (fall back to \u003ccode>lucide-static\u003c/code> with the same serialiser if it differs).\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Create \u003ccode>src/templates/icons.ts\u003c/code>: a serialiser from icon nodes to inline \u003ccode>&lt;svg class=&quot;lucide lucide-&lt;name&gt; …&quot;&gt;\u003c/code> (decorative → \u003ccode>aria-hidden\u003c/code>, labelled → \u003ccode>role=&quot;img&quot;\u003c/code> + \u003ccode>aria-label\u003c/code>), a named map for brand (\u003ccode>radar\u003c/code>), sections, project docs, page types, meta-row, callouts, and chrome, and a build-time throw on unknown names.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add \u003ccode>tests/icons.test.ts\u003c/code>: every mapped icon renders an SVG with the expected class and \u003ccode>viewBox\u003c/code>, unknown names throw, output contains no \u003ccode>&lt;script&gt;\u003c/code> or external reference.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Delete the unused \u003ccode>src/cdn.ts\u003c/code>.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-component-css\">3. Component CSS\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Replace the head of \u003ccode>BASE_COMPONENTS_CSS\u003c/code> in \u003ccode>src/templates/styles.ts\u003c/code> with Vision §3 verbatim, tokenising its three literals (\u003ccode>pre.code\u003c/code> color → \u003ccode>--code-fg\u003c/code>, \u003ccode>.filename\u003c/code> → \u003ccode>--code-muted\u003c/code>, \u003ccode>.tok-cm\u003c/code> → \u003ccode>--code-comment\u003c/code>), and keep \u003ccode>[hidden]\u003c/code> and \u003ccode>.visually-hidden\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Write the iris layer against Vision tokens only: app grid, sidebar/nav (active fill + inset rule), topbar extension, crumbs, content, footer, menu button, page head/section heading, \u003ccode>.strip\u003c/code> of \u003ccode>.card.stat\u003c/code>, Work rows/table/Kanban/drawer, Spec browser, command cards, research/document \u003ccode>.layout\u003c/code> + left \u003ccode>.toc\u003c/code> + \u003ccode>.doc-body\u003c/code> descendant rules mirroring \u003ccode>table.ds\u003c/code>/\u003ccode>pre.code\u003c/code>/inline code, empty state, filter input, \u003ccode>.b-muted\u003c/code> and the dashed archived badge.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Merge the media blocks: reduced-motion (no aperture rule; cover \u003ccode>.btn\u003c/code>, \u003ccode>.tab\u003c/code>, \u003ccode>.nav-item\u003c/code>, drawer, rows), the 48 rem shell overlay block, and print (Vision palette plus hide sidebar/topbar/drawer/footer/toc and show the Mermaid fallback).\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-markup-migration\">4. Markup migration\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 \u003ccode>shell.ts\u003c/code>: radar brand mark, Lucide section and project-doc icons, mono badge counts, Vision topbar with the two-button \u003ccode>.mode-toggle\u003c/code> (\u003ccode>data-theme-set\u003c/code>) and \u003ccode>t\u003c/code> hint, \u003ccode>data-iris-nav\u003c/code> retained; remove the hand-drawn \u003ccode>ICONS\u003c/code> map.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 \u003ccode>common.ts\u003c/code>: remove \u003ccode>apertureGlyph\u003c/code>/\u003ccode>apertureRing\u003c/code>; add \u003ccode>typeIcon\u003c/code> and \u003ccode>typeBadge\u003c/code>/\u003ccode>statusBadge\u003c/code>/\u003ccode>priorityBadge\u003c/code> emitting \u003ccode>.badge.b-*\u003c/code> with the type icon; \u003ccode>statTile\u003c/code> → \u003ccode>.card.stat\u003c/code>; \u003ccode>progressBar\u003c/code> → \u003ccode>.meter\u003c/code>; update \u003ccode>design.ts\u003c/code> exports.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 \u003ccode>pages/overview.ts\u003c/code>: \u003ccode>card.hero\u003c/code> with radar, \u003ccode>h1.page\u003c/code>, \u003ccode>p.subtitle\u003c/code>, quick start, and the pages-by-type badge row with \u003ccode>aria-label\u003c/code> and empty state; \u003ccode>grid-4\u003c/code> stats; \u003ccode>grid-2\u003c/code> Recent work / Spec movement with \u003ccode>.meter\u003c/code>; Architecture card; project docs as a \u003ccode>grid-3\u003c/code> of icon cards.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 \u003ccode>pages/work.ts\u003c/code>: stat cards, Vision \u003ccode>.tabs .tab\u003c/code>, rows/table/Kanban/drawer class migration, typed icons at row start, badges.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.5 \u003ccode>pages/research.ts\u003c/code>: index rows; document header as \u003ccode>h1.page\u003c/code> + \u003ccode>.meta-row\u003c/code> (iconed id/agent/updated/tags, type and status badges); body in \u003ccode>.layout\u003c/code> with the sticky left \u003ccode>.toc\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.6 \u003ccode>pages/contract-page.ts\u003c/code> and the project-doc placeholder: the same header anatomy; sections as \u003ccode>.card\u003c/code>; timeline → Vision \u003ccode>.timeline\u003c/code>; tasks → \u003ccode>table.ds\u003c/code>; steps → \u003ccode>.steps\u003c/code>; metrics → \u003ccode>.card.stat\u003c/code>; callouts with icons.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.7 \u003ccode>pages/spec.ts\u003c/code> and \u003ccode>pages/spec-detail.ts\u003c/code>: \u003ccode>table.ds\u003c/code>, \u003ccode>details.ds\u003c/code>, \u003ccode>pre.code\u003c/code>, callouts, \u003ccode>.layout\u003c/code>/\u003ccode>.toc\u003c/code> in detail records.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.8 \u003ccode>pages/commands.ts\u003c/code>: \u003ccode>.card\u003c/code> grid with status badges.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.9 \u003ccode>lib/markdown.ts\u003c/code>: the Mermaid figure takes the \u003ccode>diagram\u003c/code> class; status line and escaped fallback unchanged.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.10 \u003ccode>script.ts\u003c/code>: theme set/flip with active-button sync and an \u003ccode>iris:theme\u003c/code> dispatch; Mermaid \u003ccode>theme: 'base'\u003c/code> with \u003ccode>themeVariables\u003c/code> read from \u003ccode>--mmd-*\u003c/code>, source retained per figure, re-initialise and re-render on \u003ccode>iris:theme\u003c/code>, security and bounds unchanged.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-5-tests\">5. Tests\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.1 Update \u003ccode>html-navigation\u003c/code> (hero anatomy, badge row, reduced-motion block), \u003ccode>workspace-shell\u003c/code> (selectors, mode toggle, \u003ccode>iris:theme\u003c/code>), \u003ccode>work-board\u003c/code>, \u003ccode>research-pages\u003c/code>, \u003ccode>openspec-browser\u003c/code>, \u003ccode>lifecycle\u003c/code>, and \u003ccode>publish-export\u003c/code> (inline SVG present, still zero resource references) for the new markup.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.2 Add a \u003ccode>markdown-renderer\u003c/code>/script assertion that Mermaid setup reads \u003ccode>--mmd-\u003c/code> tokens and listens for \u003ccode>iris:theme\u003c/code>.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-6-documentation-agent-surface-and-spec\">6. Documentation, agent surface, and spec\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.1 Store the supplied Vision &quot;Electric&quot; v2.0 document verbatim at \u003ccode>docs/vision-electric-v2.md\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.2 Rewrite \u003ccode>docs/design-system.md\u003c/code> as &quot;iris design system 4.0 — Electric&quot;: keep the goal and market research; replace identity, tokens (Vision §2 + reconciliation table with measured ratios + extension table), component inventory (Vision vocabulary), library policy (generation-time icons; Mermaid themed; no Chart.js/React Flow), quality floor (oklch validator), migration step 10, and add &quot;Relationship to Vision Electric v2.0&quot;.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.3 Add a \u003ccode>docs/tech.md\u003c/code> decision row and dependency line, one README line naming the design system, and the §8.2 \u003ccode>classDef\u003c/code> snippet to \u003ccode>templates/agents/iris-workspace.md\u003c/code>; regenerate the managed agent surfaces.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.4 Check \u003ccode>docs/status.md\u003c/code> for design-version statements and update them.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-7-regenerate-and-verify\">7. Regenerate and verify\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 7.1 \u003ccode>pnpm build &amp;&amp; node dist/src/index.js update &amp;&amp; node dist/src/index.js render --all\u003c/code>; confirm \u003ccode>iris/design/tokens.css\u003c/code>, \u003ccode>components/base.css\u003c/code>, and \u003ccode>components/base.js\u003c/code> regenerate and \u003ccode>iris/design/vendor/\u003c/code> is untouched.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 7.2 \u003ccode>pnpm release:check\u003c/code> (lint, token-lint, typecheck, vitest, html-check, smoke) and \u003ccode>openspec validate electric-design-system --strict\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled aria-label=\"open task\" /> 7.3 Open Overview, Work, Research (index and a document with a Mermaid fence), Spec, and a contract page from \u003ccode>file://\u003c/code> in both themes and at 360 px; publish one page and open the standalone artifact; record anything that needs a follow-up.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Tokens and validation\n\n- [x] 1.1 Replace `TOKENS_CSS` in `src/templates/tokens.ts` with Vision §2 verbatim (oklch, dark under `:root`, light under `[data-theme=&#039;light&#039;]`) followed by the iris extension block: `--nav-bg/--nav-text/--nav-active-text/--nav-active-bg`, the six `--type-*` aliases, `--priority-*`, `--code-fg/--code-muted/--code-comment`, the per-theme `--mmd-*` sRGB fallbacks (§8.1 themeVariables and §8.2 classDef hexes), `--backdrop`, `--border-1`, and the existing size/space/leading/weight/duration/easing/nav ramps; keep the `body` and `:focus-visible` rules.\n- [x] 1.2 Teach `scripts/token-contract.mjs` to parse oklch (chroma-reduction gamut mapping before luminance), hex, and `var()` chains; move `REQUIRED_TOKENS`, `THEME_REQUIRED_TOKENS`, contrast, control-boundary, and border pairs to the new names (foreground/muted on background/card/card-2; primary on background/card; type-*, success, warning, danger, info on card; nav pairs on nav-bg; code-fg/code-muted/code-comment on code-bg; primary control on the three surfaces; border floor on the three surfaces); keep the threshold-naming messages.\n- [x] 1.3 Run the validator and apply the smallest lightness nudges that clear each floor (expected: dark/light `--border`; light `--accent-1..4`, `--success`, `--warning`, `--info`; `--code-comment`/`--code-muted`); note every nudged token and ratio for the docs task.\n- [x] 1.4 Extend `scripts/token-lint.mjs` to ban `oklch( oklab( lab( lch( hwb( color(` literals outside the token block while leaving `color-mix(` legal, and keep the generated `iris/design/tokens.css` sync check.\n- [x] 1.5 Rewrite `tests/token-contract.test.mjs` for the new names and oklch values, adding cases for oklch parsing, out-of-gamut mapping (dark `--primary`), `var()` resolution, an alpha background rejection, and the three threshold messages.\n\n## 2. Icons\n\n- [x] 2.1 Add `lucide@0.469.0` as a dependency with a `runtimeDependencyRationale` entry stating it is used only at generation time; confirm the package&#039;s icon-node export shape (fall back to `lucide-static` with the same serialiser if it differs).\n- [x] 2.2 Create `src/templates/icons.ts`: a serialiser from icon nodes to inline `&lt;svg class=&quot;lucide lucide-&lt;name&gt; …&quot;&gt;` (decorative → `aria-hidden`, labelled → `role=&quot;img&quot;` + `aria-label`), a named map for brand (`radar`), sections, project docs, page types, meta-row, callouts, and chrome, and a build-time throw on unknown names.\n- [x] 2.3 Add `tests/icons.test.ts`: every mapped icon renders an SVG with the expected class and `viewBox`, unknown names throw, output contains no `&lt;script&gt;` or external reference.\n- [x] 2.4 Delete the unused `src/cdn.ts`.\n\n## 3. Component CSS\n\n- [x] 3.1 Replace the head of `BASE_COMPONENTS_CSS` in `src/templates/styles.ts` with Vision §3 verbatim, tokenising its three literals (`pre.code` color → `--code-fg`, `.filename` → `--code-muted`, `.tok-cm` → `--code-comment`), and keep `[hidden]` and `.visually-hidden`.\n- [x] 3.2 Write the iris layer against Vision tokens only: app grid, sidebar/nav (active fill + inset rule), topbar extension, crumbs, content, footer, menu button, page head/section heading, `.strip` of `.card.stat`, Work rows/table/Kanban/drawer, Spec browser, command cards, research/document `.layout` + left `.toc` + `.doc-body` descendant rules mirroring `table.ds`/`pre.code`/inline code, empty state, filter input, `.b-muted` and the dashed archived badge.\n- [x] 3.3 Merge the media blocks: reduced-motion (no aperture rule; cover `.btn`, `.tab`, `.nav-item`, drawer, rows), the 48 rem shell overlay block, and print (Vision palette plus hide sidebar/topbar/drawer/footer/toc and show the Mermaid fallback).\n\n## 4. Markup migration\n\n- [x] 4.1 `shell.ts`: radar brand mark, Lucide section and project-doc icons, mono badge counts, Vision topbar with the two-button `.mode-toggle` (`data-theme-set`) and `t` hint, `data-iris-nav` retained; remove the hand-drawn `ICONS` map.\n- [x] 4.2 `common.ts`: remove `apertureGlyph`/`apertureRing`; add `typeIcon` and `typeBadge`/`statusBadge`/`priorityBadge` emitting `.badge.b-*` with the type icon; `statTile` → `.card.stat`; `progressBar` → `.meter`; update `design.ts` exports.\n- [x] 4.3 `pages/overview.ts`: `card.hero` with radar, `h1.page`, `p.subtitle`, quick start, and the pages-by-type badge row with `aria-label` and empty state; `grid-4` stats; `grid-2` Recent work / Spec movement with `.meter`; Architecture card; project docs as a `grid-3` of icon cards.\n- [x] 4.4 `pages/work.ts`: stat cards, Vision `.tabs .tab`, rows/table/Kanban/drawer class migration, typed icons at row start, badges.\n- [x] 4.5 `pages/research.ts`: index rows; document header as `h1.page` + `.meta-row` (iconed id/agent/updated/tags, type and status badges); body in `.layout` with the sticky left `.toc`.\n- [x] 4.6 `pages/contract-page.ts` and the project-doc placeholder: the same header anatomy; sections as `.card`; timeline → Vision `.timeline`; tasks → `table.ds`; steps → `.steps`; metrics → `.card.stat`; callouts with icons.\n- [x] 4.7 `pages/spec.ts` and `pages/spec-detail.ts`: `table.ds`, `details.ds`, `pre.code`, callouts, `.layout`/`.toc` in detail records.\n- [x] 4.8 `pages/commands.ts`: `.card` grid with status badges.\n- [x] 4.9 `lib/markdown.ts`: the Mermaid figure takes the `diagram` class; status line and escaped fallback unchanged.\n- [x] 4.10 `script.ts`: theme set/flip with active-button sync and an `iris:theme` dispatch; Mermaid `theme: &#039;base&#039;` with `themeVariables` read from `--mmd-*`, source retained per figure, re-initialise and re-render on `iris:theme`, security and bounds unchanged.\n\n## 5. Tests\n\n- [x] 5.1 Update `html-navigation` (hero anatomy, badge row, reduced-motion block), `workspace-shell` (selectors, mode toggle, `iris:theme`), `work-board`, `research-pages`, `openspec-browser`, `lifecycle`, and `publish-export` (inline SVG present, still zero resource references) for the new markup.\n- [x] 5.2 Add a `markdown-renderer`/script assertion that Mermaid setup reads `--mmd-` tokens and listens for `iris:theme`.\n\n## 6. Documentation, agent surface, and spec\n\n- [x] 6.1 Store the supplied Vision &quot;Electric&quot; v2.0 document verbatim at `docs/vision-electric-v2.md`.\n- [x] 6.2 Rewrite `docs/design-system.md` as &quot;iris design system 4.0 — Electric&quot;: keep the goal and market research; replace identity, tokens (Vision §2 + reconciliation table with measured ratios + extension table), component inventory (Vision vocabulary), library policy (generation-time icons; Mermaid themed; no Chart.js/React Flow), quality floor (oklch validator), migration step 10, and add &quot;Relationship to Vision Electric v2.0&quot;.\n- [x] 6.3 Add a `docs/tech.md` decision row and dependency line, one README line naming the design system, and the §8.2 `classDef` snippet to `templates/agents/iris-workspace.md`; regenerate the managed agent surfaces.\n- [x] 6.4 Check `docs/status.md` for design-version statements and update them.\n\n## 7. Regenerate and verify\n\n- [x] 7.1 `pnpm build &amp;&amp; node dist/src/index.js update &amp;&amp; node dist/src/index.js render --all`; confirm `iris/design/tokens.css`, `components/base.css`, and `components/base.js` regenerate and `iris/design/vendor/` is untouched.\n- [x] 7.2 `pnpm release:check` (lint, token-lint, typecheck, vitest, html-check, smoke) and `openspec validate electric-design-system --strict`.\n- [ ] 7.3 Open Overview, Work, Research (index and a document with a Mermaid fence), Spec, and a contract page from `file://` in both themes and at 360 px; publish one page and open the standalone artifact; record anything that needs a follow-up.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/electric-design-system/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/electric-design-system/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/h3>\n\u003cp>The system MUST generate dark and light themes from one token stylesheet whose color, shape, and type tokens are the Vision &quot;Electric&quot; v2.0 token set — its token names and its oklch values — followed by an iris extension block that MUST define navigation-shell surface, text, and selection tokens, the six page-type colors as aliases onto Vision accent and status tokens, priority colors, code-block foreground tokens, the sRGB fallback tokens diagram rendering needs, and the size, space, motion, and layout ramps. The system MUST keep style literals confined to that token stylesheet, MUST validate text contrast at 4.5:1 or better, MUST validate non-text contrast for control boundaries and border visibility, and MUST record every value that deviates from the upstream Vision block together with the floor that required it, so that every generated page and the shell render from one token set and a future restyle is a token-block swap rather than a rewrite.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-generated-styles-are-validated\">Scenario: generated styles are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a contributor runs the token validation command\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the validator MUST accept oklch, hex, and token-alias values (mapping out-of-gamut oklch into sRGB the way browsers do before measuring), MUST reject undeclared token names, MUST reject style literals outside the token stylesheet — hex, rgb, hsl, oklch, oklab, lab, lch, hwb, and color() alike — and MUST reject configured foreground/background pairs below 4.5:1 in either theme, including page-type, status, navigation-shell, and code-block pairs\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-theme-is-switched\">Scenario: theme is switched\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user toggles the theme on any generated page, by the mode toggle or the keyboard shortcut\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that page and its navigation shell MUST render in the selected theme from the same tokens, the mode toggle MUST show which theme is active, the page MUST notify its own diagram code so diagrams re-theme, and the preference MUST be restored on other generated pages in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-non-text-surfaces-are-validated\">Scenario: non-text surfaces are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the token validation command checks a configured non-text pair\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST reject a control-boundary pair below 3:1 and a border pair below the declared visibility floor in either theme, and the failure message MUST state which threshold applied so a border floor is never read as a text contrast result\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-an-upstream-value-cannot-meet-a-floor\">Scenario: an upstream value cannot meet a floor\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Vision token value fails a validated text, control-boundary, or border floor in one theme\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the token stylesheet MUST carry the smallest lightness adjustment that meets the floor, that adjustment MUST be recorded with the floor that required it, and every other Vision value MUST remain verbatim\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/h3>\n\u003cp>The system MUST provide generated component styles and semantic markup in the Vision &quot;Electric&quot; component vocabulary — cards and hero cards, badges, callouts, stat cards, tabs, code blocks, file trees, tables, flow strips, steps, timelines, meters, evidence blocks, footnotes, collapsibles, buttons, keyboard hints, the mode toggle, and diagram containers — plus an iris layer written only against the same tokens for the navigation shell, page headers, summary strips, command cards, compact work rows, Kanban columns and cards, detail drawers, document layouts with a table of contents, and command-specific empty states. Every icon MUST be an inline SVG emitted at generation time that inherits its color from the semantic icon classes, with no icon script, icon font, or network request. The workspace brand mark is the radar icon in the primary color; no aperture ring or aperture glyph is generated.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-meaning-is-communicated-accessibly\">Scenario: meaning is communicated accessibly\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page uses type, status, or priority color, interactive controls, selection, current-section marking, or motion\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide a non-color signal such as label text or a typed icon, visible keyboard focus, semantic state, and a static reduced-motion presentation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-page-type-is-shown\">Scenario: a page type is shown\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a work record's type appears in a row, card, table, drawer, or page header\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST be rendered as a badge and/or typed icon in that type's token color with the type name as text, and the six type colors MUST be the documented aliases onto Vision accent and status tokens\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-published-artifact-is-inspected\">Scenario: a published artifact is inspected\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a page is published or exported as a standalone artifact\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every icon MUST remain visible as inline SVG and the artifact MUST contain no external icon, font, or script reference\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated workspace MUST consist of an Overview page at \u003ccode>iris/index.html\u003c/code>, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page presenting an index of canonical specs, active changes, and the archive with each record addressable in place, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip, and the hero MUST present pages-by-type counts as labelled badges with typed icons rather than as a ring graphic.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Overview from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request, and the pages-by-type row MUST state that no pages exist and name the command that creates one\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-generated-work-pages\">Scenario: repository has generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Work page with one or more page contracts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-spec-detail-page-is-opened\">Scenario: a spec detail page is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens a canonical spec, change, or legacy record from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST keep the navigation shell and the Spec section current, present that record's content, and provide a way back to the index\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: Aperture token contract\n\nThe system MUST generate dark and light themes from one token stylesheet whose color, shape, and type tokens are the Vision &quot;Electric&quot; v2.0 token set — its token names and its oklch values — followed by an iris extension block that MUST define navigation-shell surface, text, and selection tokens, the six page-type colors as aliases onto Vision accent and status tokens, priority colors, code-block foreground tokens, the sRGB fallback tokens diagram rendering needs, and the size, space, motion, and layout ramps. The system MUST keep style literals confined to that token stylesheet, MUST validate text contrast at 4.5:1 or better, MUST validate non-text contrast for control boundaries and border visibility, and MUST record every value that deviates from the upstream Vision block together with the floor that required it, so that every generated page and the shell render from one token set and a future restyle is a token-block swap rather than a rewrite.\n\n#### Scenario: generated styles are validated\n\n- **WHEN** a contributor runs the token validation command\n- **THEN** the validator MUST accept oklch, hex, and token-alias values (mapping out-of-gamut oklch into sRGB the way browsers do before measuring), MUST reject undeclared token names, MUST reject style literals outside the token stylesheet — hex, rgb, hsl, oklch, oklab, lab, lch, hwb, and color() alike — and MUST reject configured foreground/background pairs below 4.5:1 in either theme, including page-type, status, navigation-shell, and code-block pairs\n\n#### Scenario: theme is switched\n\n- **WHEN** the user toggles the theme on any generated page, by the mode toggle or the keyboard shortcut\n- **THEN** that page and its navigation shell MUST render in the selected theme from the same tokens, the mode toggle MUST show which theme is active, the page MUST notify its own diagram code so diagrams re-theme, and the preference MUST be restored on other generated pages in that browser when local storage is available\n\n#### Scenario: non-text surfaces are validated\n\n- **WHEN** the token validation command checks a configured non-text pair\n- **THEN** it MUST reject a control-boundary pair below 3:1 and a border pair below the declared visibility floor in either theme, and the failure message MUST state which threshold applied so a border floor is never read as a text contrast result\n\n#### Scenario: an upstream value cannot meet a floor\n\n- **WHEN** a Vision token value fails a validated text, control-boundary, or border floor in one theme\n- **THEN** the token stylesheet MUST carry the smallest lightness adjustment that meets the floor, that adjustment MUST be recorded with the floor that required it, and every other Vision value MUST remain verbatim\n\n### Requirement: Aperture component language\n\nThe system MUST provide generated component styles and semantic markup in the Vision &quot;Electric&quot; component vocabulary — cards and hero cards, badges, callouts, stat cards, tabs, code blocks, file trees, tables, flow strips, steps, timelines, meters, evidence blocks, footnotes, collapsibles, buttons, keyboard hints, the mode toggle, and diagram containers — plus an iris layer written only against the same tokens for the navigation shell, page headers, summary strips, command cards, compact work rows, Kanban columns and cards, detail drawers, document layouts with a table of contents, and command-specific empty states. Every icon MUST be an inline SVG emitted at generation time that inherits its color from the semantic icon classes, with no icon script, icon font, or network request. The workspace brand mark is the radar icon in the primary color; no aperture ring or aperture glyph is generated.\n\n#### Scenario: meaning is communicated accessibly\n\n- **WHEN** a generated page uses type, status, or priority color, interactive controls, selection, current-section marking, or motion\n- **THEN** the page MUST provide a non-color signal such as label text or a typed icon, visible keyboard focus, semantic state, and a static reduced-motion presentation\n\n#### Scenario: a page type is shown\n\n- **WHEN** a work record&#039;s type appears in a row, card, table, drawer, or page header\n- **THEN** it MUST be rendered as a badge and/or typed icon in that type&#039;s token color with the type name as text, and the six type colors MUST be the documented aliases onto Vision accent and status tokens\n\n#### Scenario: a published artifact is inspected\n\n- **WHEN** a page is published or exported as a standalone artifact\n- **THEN** every icon MUST remain visible as inline SVG and the artifact MUST contain no external icon, font, or script reference\n\n### Requirement: newcomer-first dashboard hierarchy\n\nThe generated workspace MUST consist of an Overview page at `iris/index.html`, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page presenting an index of canonical specs, active changes, and the archive with each record addressable in place, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip, and the hero MUST present pages-by-type counts as labelled badges with typed icons rather than as a ring graphic.\n\n#### Scenario: repository has no generated work pages\n\n- **WHEN** a newcomer opens the Overview from `file://`\n- **THEN** it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request, and the pages-by-type row MUST state that no pages exist and name the command that creates one\n\n#### Scenario: repository has generated work pages\n\n- **WHEN** a newcomer opens the Work page with one or more page contracts\n- **THEN** the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\n\n#### Scenario: user opens the Spec view\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\n\n#### Scenario: state is communicated in the Spec view\n\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n#### Scenario: a spec detail page is opened\n\n- **WHEN** a user opens a canonical spec, change, or legacy record from the index\n- **THEN** the page MUST keep the navigation shell and the Spec section current, present that record&#039;s content, and provide a way back to the index\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-markdown-diagram-rendering-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · markdown-diagram-rendering\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/electric-design-system/specs/markdown-diagram-rendering/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-markdown-diagram-rendering-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-offline-progressive-enhancement\">Requirement: offline progressive enhancement\u003c/h3>\n\u003cp>The system MUST render Mermaid diagrams using only a pinned local classic-script asset, MUST theme every rendered diagram from the design-token set so it matches the page's active theme, MUST re-render diagrams when the theme changes, and MUST keep the Markdown readable through \u003ccode>file://\u003c/code> with no server, runtime module, or network request.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-local-mermaid-asset-is-installed\">Scenario: local Mermaid asset is installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Iris page containing a valid Mermaid fence opens from \u003ccode>file://\u003c/code> and the vendored runtime exists\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page SHALL replace the diagram host with an accessible inline SVG colored from the active theme's diagram tokens without requesting a remote resource\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-local-mermaid-asset-is-absent-or-javascript-is-unav\">Scenario: local Mermaid asset is absent or JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page cannot load or execute the local diagram runtime\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each diagram SHALL retain its readable escaped-source fallback and the rest of the Markdown SHALL remain usable\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-theme-is-changed-while-diagrams-are-rendered\">Scenario: theme is changed while diagrams are rendered\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user switches the page theme after diagrams have rendered\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each rendered diagram SHALL be re-rendered from its retained source in the colors of the new theme without a page reload or remote request, and a diagram that fails to re-render SHALL fall back to its escaped source exactly as on first render\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-bounded-and-accessible-diagram-presentation\">Requirement: bounded and accessible diagram presentation\u003c/h3>\n\u003cp>The system MUST give each diagram an accessible name, MUST bound accepted source and graph complexity, MUST present each diagram inside the design system's diagram container, and MUST keep both rendered output and fallback source usable in light, dark, print, reduced-motion, and 360-pixel layouts.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-diagram-renders-successfully\">Scenario: diagram renders successfully\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Mermaid fence becomes an inline SVG\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the diagram SHALL expose a non-empty accessible label, SHALL sit inside the diagram container with its status line, and SHALL fit its content region without forcing page-level horizontal overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-diagram-input-exceeds-a-configured-bound\">Scenario: diagram input exceeds a configured bound\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Mermaid source or graph complexity exceeds the renderer's configured limit\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that diagram SHALL fail locally with its escaped source and an actionable error while other content remains available\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: offline progressive enhancement\n\nThe system MUST render Mermaid diagrams using only a pinned local classic-script asset, MUST theme every rendered diagram from the design-token set so it matches the page&#039;s active theme, MUST re-render diagrams when the theme changes, and MUST keep the Markdown readable through `file://` with no server, runtime module, or network request.\n\n#### Scenario: local Mermaid asset is installed\n\n- **WHEN** a generated Iris page containing a valid Mermaid fence opens from `file://` and the vendored runtime exists\n- **THEN** the page SHALL replace the diagram host with an accessible inline SVG colored from the active theme&#039;s diagram tokens without requesting a remote resource\n\n#### Scenario: local Mermaid asset is absent or JavaScript is unavailable\n\n- **WHEN** a generated page cannot load or execute the local diagram runtime\n- **THEN** each diagram SHALL retain its readable escaped-source fallback and the rest of the Markdown SHALL remain usable\n\n#### Scenario: theme is changed while diagrams are rendered\n\n- **WHEN** the user switches the page theme after diagrams have rendered\n- **THEN** each rendered diagram SHALL be re-rendered from its retained source in the colors of the new theme without a page reload or remote request, and a diagram that fails to re-render SHALL fall back to its escaped source exactly as on first render\n\n### Requirement: bounded and accessible diagram presentation\n\nThe system MUST give each diagram an accessible name, MUST bound accepted source and graph complexity, MUST present each diagram inside the design system&#039;s diagram container, and MUST keep both rendered output and fallback source usable in light, dark, print, reduced-motion, and 360-pixel layouts.\n\n#### Scenario: diagram renders successfully\n\n- **WHEN** a Mermaid fence becomes an inline SVG\n- **THEN** the diagram SHALL expose a non-empty accessible label, SHALL sit inside the diagram container with its status line, and SHALL fit its content region without forcing page-level horizontal overflow\n\n#### Scenario: diagram input exceeds a configured bound\n\n- **WHEN** Mermaid source or graph complexity exceeds the renderer&#039;s configured limit\n- **THEN** that diagram SHALL fail locally with its escaped source and an actionable error while other content remains available\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-vision-native-vocabulary-rather-than-a-skin-or-an-alias-laye\">Vision-native vocabulary rather than a skin or an alias layer\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-oklch-stays-verbatim-the-validator-learns-it\">oklch stays verbatim; the validator learns it\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-measured-reconciliation-not-relaxed-floors\">Measured reconciliation, not relaxed floors\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-data-theme-is-kept-data-mode-is-not-adopted\">`data-theme` is kept; `data-mode` is not adopted\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-extension-token-mapping\">Extension token mapping\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-icons-at-generation-time-from-the-pinned-package\">Icons at generation time from the pinned package\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-vision-3-verbatim-plus-an-iris-layer-vision-wins-collisions\">Vision §3 verbatim plus an iris layer; Vision wins collisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-theme-toggle-and-event\">Theme toggle and event\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-mermaid-themed-from-tokens-re-rendered-on-toggle\">Mermaid themed from tokens, re-rendered on toggle\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-hero-replaces-the-ring-with-a-badge-row\">Hero replaces the ring with a badge row\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-spec-bookkeeping\">Spec bookkeeping\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-tokens-and-validation\">1. Tokens and validation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-icons\">2. Icons\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-component-css\">3. Component CSS\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-markup-migration\">4. Markup migration\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-5-tests\">5. Tests\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-6-documentation-agent-surface-and-spec\">6. Documentation, agent surface, and spec\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-7-regenerate-and-verify\">7. Regenerate and verify\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-markdown-diagram-rendering-artifact\">Delta spec · markdown-diagram-rendering\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-markdown-diagram-rendering-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-offline-progressive-enhancement\">Requirement: offline progressive enhancement\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-bounded-and-accessible-diagram-presentation\">Requirement: bounded and accessible diagram presentation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-18-release-automation-and-install-smoke":{"kind":"change","name":"2026-08-18-release-automation-and-install-smoke","title":"2026-08-18-release-automation-and-install-smoke","path":"changes/archive/2026-08-18-release-automation-and-install-smoke","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-18-release-automation-and-install-smoke\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-18-release-automation-and-install-smoke\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">9\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-18-release-automation-and-install-smoke: 9 of 9 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">9/9 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-release-automation-and-install-smoke/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The project now has a working local render pipeline, but it still lacks a repeatable install and release story. A tool that must be run from a repo checkout or shell-specific setup path is harder to trust, harder to validate, and harder to keep consistent across environments. This change establishes a predictable install and smoke-validation flow so the \u003ccode>iris\u003c/code> CLI can be installed, verified, and upgraded with confidence.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Define the supported install path for the \u003ccode>iris\u003c/code> CLI and the package or distribution surface it relies on.\u003c/li>\n\u003cli>Add release metadata and automation needed to ship versioned artifacts cleanly.\u003c/li>\n\u003cli>Add a smoke-check path that validates the installed command in a clean environment before release.\u003c/li>\n\u003cli>Keep the install flow aligned with the project’s offline-safe, local-first design.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>release-automation-and-install-smoke\u003c/code>: establish a repeatable package, install, and validation path for the CLI.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>None\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>CLI adoption: users can install the tool without repo-specific setup or shell tricks.\u003c/li>\n\u003cli>Release quality: packaging and versioning become explicit and testable.\u003c/li>\n\u003cli>Validation: the project can verify the installed command surface before shipping or documenting it.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe project now has a working local render pipeline, but it still lacks a repeatable install and release story. A tool that must be run from a repo checkout or shell-specific setup path is harder to trust, harder to validate, and harder to keep consistent across environments. This change establishes a predictable install and smoke-validation flow so the `iris` CLI can be installed, verified, and upgraded with confidence.\n\n## What Changes\n\n- Define the supported install path for the `iris` CLI and the package or distribution surface it relies on.\n- Add release metadata and automation needed to ship versioned artifacts cleanly.\n- Add a smoke-check path that validates the installed command in a clean environment before release.\n- Keep the install flow aligned with the project’s offline-safe, local-first design.\n\n## Capabilities\n\n### New Capabilities\n- `release-automation-and-install-smoke`: establish a repeatable package, install, and validation path for the CLI.\n\n### Modified Capabilities\n- None\n\n## Impact\n\n- CLI adoption: users can install the tool without repo-specific setup or shell tricks.\n- Release quality: packaging and versioning become explicit and testable.\n- Validation: the project can verify the installed command surface before shipping or documenting it.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-release-automation-and-install-smoke/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The project already has a working local-first CLI and deterministic HTML render flow. The gap is productization: users need a stable install story, release metadata, and a validation path that confirms the binary works after installation.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Define the primary install path for the CLI.\u003c/li>\n\u003cli>Ensure the package or release artifact exposes consistent version information.\u003c/li>\n\u003cli>Add a repeatable smoke check for post-install verification.\u003c/li>\n\u003cli>Keep the install story aligned with the project’s offline-safe approach.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Remote hosted publishing or SaaS delivery.\u003c/li>\n\u003cli>Broad platform or framework changes outside the installability/release slice.\u003c/li>\n\u003cli>Reworking the render pipeline itself.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Install path first: prefer a single clear install story over multiple overlapping options. This reduces confusion and keeps documentation honest.\u003c/li>\n\u003cli>Versioned package metadata: every install path should expose a release and upgrade model, not just a local repo checkout.\u003c/li>\n\u003cli>Smoke-check as a release gate: validate the installed binary in a clean environment before it is treated as a supported path.\u003c/li>\n\u003cli>Keep the workflow local-first: installation should not require the user to hand-hold a remote service or rely on hosted infrastructure for the core workflow.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Ambiguous supported environments] → Mitigation: document the minimum supported Node/runtime environment and validate it explicitly.\u003c/li>\n\u003cli>[Packaging drift] → Mitigation: use a single package metadata source and verify install checks against the produced artifact.\u003c/li>\n\u003cli>[Installer complexity] → Mitigation: keep the release surface narrow and avoid adding hidden remote dependencies for basic use.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Choose the primary install story and versioning surface for the CLI.\u003c/li>\n\u003cli>Add or formalize the release metadata needed for packaging and upgrades.\u003c/li>\n\u003cli>Add the smoke-check command or script that validates a clean install.\u003c/li>\n\u003cli>Document the install, upgrade, and validation path for users and release maintainers.\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"design-open-questions\">Open Questions\u003c/h2>\n\u003cp>None at this stage; the problem is narrow and the scope is kept to installation and release validation.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe project already has a working local-first CLI and deterministic HTML render flow. The gap is productization: users need a stable install story, release metadata, and a validation path that confirms the binary works after installation.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Define the primary install path for the CLI.\n- Ensure the package or release artifact exposes consistent version information.\n- Add a repeatable smoke check for post-install verification.\n- Keep the install story aligned with the project’s offline-safe approach.\n\n**Non-Goals:**\n- Remote hosted publishing or SaaS delivery.\n- Broad platform or framework changes outside the installability/release slice.\n- Reworking the render pipeline itself.\n\n## Decisions\n\n- Install path first: prefer a single clear install story over multiple overlapping options. This reduces confusion and keeps documentation honest.\n- Versioned package metadata: every install path should expose a release and upgrade model, not just a local repo checkout.\n- Smoke-check as a release gate: validate the installed binary in a clean environment before it is treated as a supported path.\n- Keep the workflow local-first: installation should not require the user to hand-hold a remote service or rely on hosted infrastructure for the core workflow.\n\n## Risks / Trade-offs\n\n- [Ambiguous supported environments] → Mitigation: document the minimum supported Node/runtime environment and validate it explicitly.\n- [Packaging drift] → Mitigation: use a single package metadata source and verify install checks against the produced artifact.\n- [Installer complexity] → Mitigation: keep the release surface narrow and avoid adding hidden remote dependencies for basic use.\n\n## Migration Plan\n\n1. Choose the primary install story and versioning surface for the CLI.\n2. Add or formalize the release metadata needed for packaging and upgrades.\n3. Add the smoke-check command or script that validates a clean install.\n4. Document the install, upgrade, and validation path for users and release maintainers.\n\n## Open Questions\n\nNone at this stage; the problem is narrow and the scope is kept to installation and release validation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-release-automation-and-install-smoke/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-install-path-and-release-definition\">1. Install path and release definition\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Choose the primary install surface for the \u003ccode>iris\u003c/code> CLI and confirm the supported target environments.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Add or formalize the package metadata needed for a repeatable install and upgrade flow.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Document the supported installation and upgrade steps for end users.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-smoke-validation-and-release-automation\">2. Smoke validation and release automation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add a smoke-check command or script that validates the installed CLI in a clean environment.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Wire release automation or packaging metadata to produce versioned install artifacts consistently.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Confirm the release path remains compatible with the project’s offline-safe, local-first model.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-review-and-rollout\">3. Review and rollout\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Verify the install path works against the minimum supported Node version.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Review the documentation and release outputs for clarity and version consistency.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Keep the change scoped to installability and distribution, not broader product changes.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Install path and release definition\n\n- [x] 1.1 Choose the primary install surface for the `iris` CLI and confirm the supported target environments.\n- [x] 1.2 Add or formalize the package metadata needed for a repeatable install and upgrade flow.\n- [x] 1.3 Document the supported installation and upgrade steps for end users.\n\n## 2. Smoke validation and release automation\n\n- [x] 2.1 Add a smoke-check command or script that validates the installed CLI in a clean environment.\n- [x] 2.2 Wire release automation or packaging metadata to produce versioned install artifacts consistently.\n- [x] 2.3 Confirm the release path remains compatible with the project’s offline-safe, local-first model.\n\n## 3. Review and rollout\n\n- [x] 3.1 Verify the install path works against the minimum supported Node version.\n- [x] 3.2 Review the documentation and release outputs for clarity and version consistency.\n- [x] 3.3 Keep the change scoped to installability and distribution, not broader product changes.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-release-automation-and-install-smoke/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-18\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-release-automation-and-install-smoke-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · release-automation-and-install-smoke\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-release-automation-and-install-smoke/specs/release-automation-and-install-smoke/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-release-automation-and-install-smoke-purpose\">Purpose\u003c/h2>\n\u003cp>Define a repeatable install and release path for the \u003ccode>iris\u003c/code> CLI so it can be installed, verified, and upgraded predictably without repo-specific workarounds.\u003c/p>\n\u003ch2 id=\"delta-release-automation-and-install-smoke-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-release-automation-and-install-smoke-requirement-reliable-install-path\">Requirement: Reliable install path\u003c/h3>\n\u003cp>The system SHALL provide a documented install path for the \u003ccode>iris\u003c/code> CLI that works in a clean environment and clearly identifies the supported package or distribution method.\u003c/p>\n\u003ch4 id=\"delta-release-automation-and-install-smoke-scenario-fresh-install-in-a-supported-environment\">Scenario: Fresh install in a supported environment\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user installs \u003ccode>iris\u003c/code> in a clean terminal environment\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the CLI SHALL be available as a command with the expected help output and core commands\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-release-automation-and-install-smoke-requirement-versioned-release-artifact\">Requirement: Versioned release artifact\u003c/h3>\n\u003cp>The project SHALL expose a versioned distribution artifact or package metadata so users can install a specific release and upgrade intentionally.\u003c/p>\n\u003ch4 id=\"delta-release-automation-and-install-smoke-scenario-release-version-lookup\">Scenario: Release version lookup\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user inspects the package or release metadata\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system SHALL expose a version identifier and a repeatable installation route\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-release-automation-and-install-smoke-requirement-install-smoke-validation\">Requirement: Install smoke validation\u003c/h3>\n\u003cp>The project SHALL provide a smoke-check validation flow that proves the installed CLI can initialize a project and run a minimal render command.\u003c/p>\n\u003ch4 id=\"delta-release-automation-and-install-smoke-scenario-post-install-validation\">Scenario: Post-install validation\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user or release process runs the install smoke check\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command SHALL fail clearly on genuine issues and pass only when the CLI can start and execute core functionality\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-release-automation-and-install-smoke-requirement-offline-safe-documentation\">Requirement: Offline-safe documentation\u003c/h3>\n\u003cp>The install and release documentation SHALL describe a local-first workflow and avoid requiring remote services for basic use.\u003c/p>\n\u003ch4 id=\"delta-release-automation-and-install-smoke-scenario-local-use-after-install\">Scenario: Local use after install\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows the install instructions in a network-restricted environment\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the CLI SHALL still support the local project initialization and rendering flow described by the main product\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nDefine a repeatable install and release path for the `iris` CLI so it can be installed, verified, and upgraded predictably without repo-specific workarounds.\n\n## ADDED Requirements\n\n### Requirement: Reliable install path\nThe system SHALL provide a documented install path for the `iris` CLI that works in a clean environment and clearly identifies the supported package or distribution method.\n\n#### Scenario: Fresh install in a supported environment\n- **WHEN** a user installs `iris` in a clean terminal environment\n- **THEN** the CLI SHALL be available as a command with the expected help output and core commands\n\n### Requirement: Versioned release artifact\nThe project SHALL expose a versioned distribution artifact or package metadata so users can install a specific release and upgrade intentionally.\n\n#### Scenario: Release version lookup\n- **WHEN** a user inspects the package or release metadata\n- **THEN** the system SHALL expose a version identifier and a repeatable installation route\n\n### Requirement: Install smoke validation\nThe project SHALL provide a smoke-check validation flow that proves the installed CLI can initialize a project and run a minimal render command.\n\n#### Scenario: Post-install validation\n- **WHEN** a user or release process runs the install smoke check\n- **THEN** the command SHALL fail clearly on genuine issues and pass only when the CLI can start and execute core functionality\n\n### Requirement: Offline-safe documentation\nThe install and release documentation SHALL describe a local-first workflow and avoid requiring remote services for basic use.\n\n#### Scenario: Local use after install\n- **WHEN** a user follows the install instructions in a network-restricted environment\n- **THEN** the CLI SHALL still support the local project initialization and rendering flow described by the main product\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-open-questions\">Open Questions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-install-path-and-release-definition\">1. Install path and release definition\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-smoke-validation-and-release-automation\">2. Smoke validation and release automation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-review-and-rollout\">3. Review and rollout\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-release-automation-and-install-smoke-artifact\">Delta spec · release-automation-and-install-smoke\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-release-automation-and-install-smoke-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-release-automation-and-install-smoke-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-release-automation-and-install-smoke-requirement-reliable-install-path\">Requirement: Reliable install path\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-release-automation-and-install-smoke-requirement-versioned-release-artifact\">Requirement: Versioned release artifact\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-release-automation-and-install-smoke-requirement-install-smoke-validation\">Requirement: Install smoke validation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-release-automation-and-install-smoke-requirement-offline-safe-documentation\">Requirement: Offline-safe documentation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-18-typed-templates-and-distribution":{"kind":"change","name":"2026-08-18-typed-templates-and-distribution","title":"2026-08-18-typed-templates-and-distribution","path":"changes/archive/2026-08-18-typed-templates-and-distribution","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-18-typed-templates-and-distribution\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-18-typed-templates-and-distribution\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">11\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-18-typed-templates-and-distribution: 11 of 11 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">11/11 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-typed-templates-and-distribution/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The project already has a deterministic CLI, strict JSON schema validation, and a tokenized offline dashboard, but it still lacks the type-aware rendering and distribution layer that turns those contracts into shareable, reliable outputs. Without a typed template registry and publish/report path, the tool remains a local scaffold instead of a usable documentation workflow.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Add a typed template registry for the core report, feature, bug, idea, and plan contract types.\u003c/li>\n\u003cli>Make rendering schema-driven and deterministic, with validation failures preventing partial page output.\u003c/li>\n\u003cli>Add a local publish/export flow for standalone HTML artifacts that work without a server.\u003c/li>\n\u003cli>Add supporting report extraction from local agent session data.\u003c/li>\n\u003cli>Keep the install and publish story local-first and offline-safe.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>typed-templates-and-distribution: define the contract-driven template mapping, deterministic rendering flow, and local distribution/reporting workflow for iris pages.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>None\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>CLI surface: render, publish, and report flows under \u003ccode>iris\u003c/code>\u003c/li>\n\u003cli>Runtime behavior: validation gates before page generation and offline-safe artifact export\u003c/li>\n\u003cli>Repo structure: template registry, page rendering rules, and docs for command usage\u003c/li>\n\u003cli>Distribution model: static, portable outputs without remote render or server dependencies\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe project already has a deterministic CLI, strict JSON schema validation, and a tokenized offline dashboard, but it still lacks the type-aware rendering and distribution layer that turns those contracts into shareable, reliable outputs. Without a typed template registry and publish/report path, the tool remains a local scaffold instead of a usable documentation workflow.\n\n## What Changes\n\n- Add a typed template registry for the core report, feature, bug, idea, and plan contract types.\n- Make rendering schema-driven and deterministic, with validation failures preventing partial page output.\n- Add a local publish/export flow for standalone HTML artifacts that work without a server.\n- Add supporting report extraction from local agent session data.\n- Keep the install and publish story local-first and offline-safe.\n\n## Capabilities\n\n### New Capabilities\n- typed-templates-and-distribution: define the contract-driven template mapping, deterministic rendering flow, and local distribution/reporting workflow for iris pages.\n\n### Modified Capabilities\n- None\n\n## Impact\n\n- CLI surface: render, publish, and report flows under `iris`\n- Runtime behavior: validation gates before page generation and offline-safe artifact export\n- Repo structure: template registry, page rendering rules, and docs for command usage\n- Distribution model: static, portable outputs without remote render or server dependencies\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-typed-templates-and-distribution/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The repo already enforces strict contract envelopes and a tokenized dashboard model. The missing layer is a typed registry that maps each page contract to a fixed template and a distribution path that turns rendered output into shareable local artifacts. This change keeps the existing local-first model while adding predictable render logic and portable export flows.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Define a contract-to-template registry for the supported page types.\u003c/li>\n\u003cli>Validate all contract inputs before render output is written.\u003c/li>\n\u003cli>Keep generated pages deterministic and visually consistent with the existing design tokens.\u003c/li>\n\u003cli>Add local publish/export and session-based report workflows.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Remote rendering or hosted publishing.\u003c/li>\n\u003cli>Authenticated user accounts or SaaS features.\u003c/li>\n\u003cli>A broad platform rewrite beyond the render/distribution slice.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Registry-first rendering: each supported contract type resolves through a single canonical template entry. This keeps the render path explicit and easy to test.\u003c/li>\n\u003cli>Validation as a hard gate: rendering starts only after a contract passes schema validation. This prevents partial HTML and keeps error handling deterministic.\u003c/li>\n\u003cli>Static artifact output: publish/export generates a portable local artifact rather than a server-backed asset. This preserves the repo's offline-first design philosophy.\u003c/li>\n\u003cli>Session extraction stays local and structured: report generation should operate on structured local session data and then re-enter the normal validation/render pipeline instead of bypassing it.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Template drift] → Mitigation: keep each template tied to a fixed contract shape and add render tests for every supported type.\u003c/li>\n\u003cli>[Publish complexity] → Mitigation: prefer a narrow single-file static artifact path over a broader remote distribution platform.\u003c/li>\n\u003cli>[Session ingest ambiguity] → Mitigation: normalize to the report contract schema early and reject malformed input before generation.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add the template registry and map contract types to their rendering templates.\u003c/li>\n\u003cli>Wire the render CLI to validate contracts before writing HTML output.\u003c/li>\n\u003cli>Add local publish/export and session report generation behind the same validation model.\u003c/li>\n\u003cli>Update docs and validation fixtures to cover the new contract-to-template flow.\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"design-open-questions\">Open Questions\u003c/h2>\n\u003cp>None at this stage; the chosen approach is narrow and grounded in the existing CLI and schema contracts.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe repo already enforces strict contract envelopes and a tokenized dashboard model. The missing layer is a typed registry that maps each page contract to a fixed template and a distribution path that turns rendered output into shareable local artifacts. This change keeps the existing local-first model while adding predictable render logic and portable export flows.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Define a contract-to-template registry for the supported page types.\n- Validate all contract inputs before render output is written.\n- Keep generated pages deterministic and visually consistent with the existing design tokens.\n- Add local publish/export and session-based report workflows.\n\n**Non-Goals:**\n- Remote rendering or hosted publishing.\n- Authenticated user accounts or SaaS features.\n- A broad platform rewrite beyond the render/distribution slice.\n\n## Decisions\n\n- Registry-first rendering: each supported contract type resolves through a single canonical template entry. This keeps the render path explicit and easy to test.\n- Validation as a hard gate: rendering starts only after a contract passes schema validation. This prevents partial HTML and keeps error handling deterministic.\n- Static artifact output: publish/export generates a portable local artifact rather than a server-backed asset. This preserves the repo&#039;s offline-first design philosophy.\n- Session extraction stays local and structured: report generation should operate on structured local session data and then re-enter the normal validation/render pipeline instead of bypassing it.\n\n## Risks / Trade-offs\n\n- [Template drift] → Mitigation: keep each template tied to a fixed contract shape and add render tests for every supported type.\n- [Publish complexity] → Mitigation: prefer a narrow single-file static artifact path over a broader remote distribution platform.\n- [Session ingest ambiguity] → Mitigation: normalize to the report contract schema early and reject malformed input before generation.\n\n## Migration Plan\n\n1. Add the template registry and map contract types to their rendering templates.\n2. Wire the render CLI to validate contracts before writing HTML output.\n3. Add local publish/export and session report generation behind the same validation model.\n4. Update docs and validation fixtures to cover the new contract-to-template flow.\n\n## Open Questions\n\nNone at this stage; the chosen approach is narrow and grounded in the existing CLI and schema contracts.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-typed-templates-and-distribution/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-template-and-registry-foundation\">1. Template and registry foundation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Define the template registry and contract-to-template mapping for report, feature, bug, idea, and plan types.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Add validation gating so invalid contracts fail before any page output is written.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Implement the first core templates and ensure they respect the existing tokenized design system.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-render-pipeline-and-tests\">2. Render pipeline and tests\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Update the render command to read the registry and produce deterministic page HTML.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Refresh the dashboard and page-index output when pages are rendered.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add render tests for template selection, validation failures, and stable output snapshots.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-distribution-and-report-features\">3. Distribution and report features\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Implement local publish/export artifact generation for a portable static page.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Add session-derived report extraction for local agent artifacts.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Document the publish/report command surface and install story.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-verification-and-review\">4. Verification and review\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Run lint, typecheck, and relevant tests for the new rendering flow.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Review the change against the acceptance criteria and keep scope limited to the M2 slice.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Template and registry foundation\n\n- [x] 1.1 Define the template registry and contract-to-template mapping for report, feature, bug, idea, and plan types.\n- [x] 1.2 Add validation gating so invalid contracts fail before any page output is written.\n- [x] 1.3 Implement the first core templates and ensure they respect the existing tokenized design system.\n\n## 2. Render pipeline and tests\n\n- [x] 2.1 Update the render command to read the registry and produce deterministic page HTML.\n- [x] 2.2 Refresh the dashboard and page-index output when pages are rendered.\n- [x] 2.3 Add render tests for template selection, validation failures, and stable output snapshots.\n\n## 3. Distribution and report features\n\n- [x] 3.1 Implement local publish/export artifact generation for a portable static page.\n- [x] 3.2 Add session-derived report extraction for local agent artifacts.\n- [x] 3.3 Document the publish/report command surface and install story.\n\n## 4. Verification and review\n\n- [x] 4.1 Run lint, typecheck, and relevant tests for the new rendering flow.\n- [x] 4.2 Review the change against the acceptance criteria and keep scope limited to the M2 slice.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-typed-templates-and-distribution/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-18\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-typed-templates-and-distribution-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · typed-templates-and-distribution\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-18-typed-templates-and-distribution/specs/typed-templates-and-distribution/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-typed-templates-and-distribution-purpose\">Purpose\u003c/h2>\n\u003cp>Provide a consistent, contract-driven rendering and distribution workflow for iris pages so agent output is deterministic, inspectable, and shareable without requiring a remote service.\u003c/p>\n\u003ch2 id=\"delta-typed-templates-and-distribution-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-typed-templates-and-distribution-requirement-typed-template-registry\">Requirement: typed template registry\u003c/h3>\n\u003cp>The system MUST resolve each supported contract type to a single canonical template definition.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-contract-renders-via-the-correct-template\">Scenario: contract renders via the correct template\u003c/h4>\n\u003cp>WHEN a valid report, feature, bug, idea, or plan contract is passed to the render pipeline\nTHEN the system MUST select the matching template and render the page using the contract's declared sections.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-unsupported-contract-type-rejects-cleanly\">Scenario: unsupported contract type rejects cleanly\u003c/h4>\n\u003cp>WHEN a contract type is not recognized by the registry\nTHEN the system MUST stop with a clear validation or render error and MUST NOT emit a partial page.\u003c/p>\n\u003ch3 id=\"delta-typed-templates-and-distribution-requirement-schema-first-render-failures\">Requirement: schema-first render failures\u003c/h3>\n\u003cp>The system MUST validate each contract before writing any page output.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-invalid-contract-blocks-render\">Scenario: invalid contract blocks render\u003c/h4>\n\u003cp>WHEN required fields or section structure are missing\nTHEN the system MUST fail before writing output and MUST surface schema errors.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-valid-contract-renders-deterministically\">Scenario: valid contract renders deterministically\u003c/h4>\n\u003cp>WHEN the same valid contract is rendered twice\nTHEN the output MUST be stable and use the same template structure and tokenized styling.\u003c/p>\n\u003ch3 id=\"delta-typed-templates-and-distribution-requirement-local-distribution-artifact-output\">Requirement: local distribution artifact output\u003c/h3>\n\u003cp>The system MUST support producing a portable local artifact that can be shared without a live server.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-publish-creates-a-single-artifact\">Scenario: publish creates a single artifact\u003c/h4>\n\u003cp>WHEN a user runs the publish flow for a page\nTHEN the system MUST generate a standalone artifact from the rendered HTML or packaged output.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-publish-remains-offline-safe\">Scenario: publish remains offline-safe\u003c/h4>\n\u003cp>WHEN the user runs a publish/export flow\nTHEN the system MUST not depend on remote services or live network access to generate the artifact.\u003c/p>\n\u003ch3 id=\"delta-typed-templates-and-distribution-requirement-session-derived-report-extraction\">Requirement: session-derived report extraction\u003c/h3>\n\u003cp>The system MUST support turning local agent session content into a contract-ready report page.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-report-extraction-uses-structured-inputs\">Scenario: report extraction uses structured inputs\u003c/h4>\n\u003cp>WHEN a user imports session data for a report\nTHEN the system MUST normalize it into the report contract and pass it through the existing validation/render pipeline.\u003c/p>\n\u003ch4 id=\"delta-typed-templates-and-distribution-scenario-report-extraction-without-valid-session-data-reject\">Scenario: report extraction without valid session data rejects\u003c/h4>\n\u003cp>WHEN session content is missing or malformed\nTHEN the system MUST fail clearly and MUST NOT create a page with incomplete metadata.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nProvide a consistent, contract-driven rendering and distribution workflow for iris pages so agent output is deterministic, inspectable, and shareable without requiring a remote service.\n\n## ADDED Requirements\n\n### Requirement: typed template registry\nThe system MUST resolve each supported contract type to a single canonical template definition.\n\n#### Scenario: contract renders via the correct template\nWHEN a valid report, feature, bug, idea, or plan contract is passed to the render pipeline\nTHEN the system MUST select the matching template and render the page using the contract&#039;s declared sections.\n\n#### Scenario: unsupported contract type rejects cleanly\nWHEN a contract type is not recognized by the registry\nTHEN the system MUST stop with a clear validation or render error and MUST NOT emit a partial page.\n\n### Requirement: schema-first render failures\nThe system MUST validate each contract before writing any page output.\n\n#### Scenario: invalid contract blocks render\nWHEN required fields or section structure are missing\nTHEN the system MUST fail before writing output and MUST surface schema errors.\n\n#### Scenario: valid contract renders deterministically\nWHEN the same valid contract is rendered twice\nTHEN the output MUST be stable and use the same template structure and tokenized styling.\n\n### Requirement: local distribution artifact output\nThe system MUST support producing a portable local artifact that can be shared without a live server.\n\n#### Scenario: publish creates a single artifact\nWHEN a user runs the publish flow for a page\nTHEN the system MUST generate a standalone artifact from the rendered HTML or packaged output.\n\n#### Scenario: publish remains offline-safe\nWHEN the user runs a publish/export flow\nTHEN the system MUST not depend on remote services or live network access to generate the artifact.\n\n### Requirement: session-derived report extraction\nThe system MUST support turning local agent session content into a contract-ready report page.\n\n#### Scenario: report extraction uses structured inputs\nWHEN a user imports session data for a report\nTHEN the system MUST normalize it into the report contract and pass it through the existing validation/render pipeline.\n\n#### Scenario: report extraction without valid session data rejects\nWHEN session content is missing or malformed\nTHEN the system MUST fail clearly and MUST NOT create a page with incomplete metadata.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-open-questions\">Open Questions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-template-and-registry-foundation\">1. Template and registry foundation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-render-pipeline-and-tests\">2. Render pipeline and tests\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-distribution-and-report-features\">3. Distribution and report features\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-verification-and-review\">4. Verification and review\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-typed-templates-and-distribution-artifact\">Delta spec · typed-templates-and-distribution\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-typed-templates-and-distribution-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-typed-templates-and-distribution-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-typed-templates-and-distribution-requirement-typed-template-registry\">Requirement: typed template registry\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-typed-templates-and-distribution-requirement-schema-first-render-failures\">Requirement: schema-first render failures\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-typed-templates-and-distribution-requirement-local-distribution-artifact-output\">Requirement: local distribution artifact output\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-typed-templates-and-distribution-requirement-session-derived-report-extraction\">Requirement: session-derived report extraction\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-agent-first-initialization":{"kind":"change","name":"2026-08-21-agent-first-initialization","title":"2026-08-21-agent-first-initialization","path":"changes/archive/2026-08-21-agent-first-initialization","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-agent-first-initialization\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">21\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">2\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-agent-first-initialization: 21 of 21 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">21/21 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Iris currently requires repository-document adoption and synchronization to make its dashboard useful, while its advertised agent surfaces are not shipped by the installed package. Project setup should instead be one local, deterministic \u003ccode>iris init\u003c/code> operation that installs agent guidance and preserves user-owned work without copying or monitoring general documentation.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>\u003cstrong>BREAKING\u003c/strong> remove the Iris CLI commands \u003ccode>iris adopt\u003c/code> and \u003ccode>iris sync\u003c/code>, including their help entries, document-mirroring behavior, adopted-source hashes, stale-source states, tests, documentation, and generated dashboard copy.\u003c/li>\n\u003cli>Make \u003ccode>iris init\u003c/code> the complete first-run and upgrade flow: scaffold or refresh the Iris workspace, safely migrate proven legacy adopted pages, install supported agent skills, and render the usable dashboard.\u003c/li>\n\u003cli>Ship one canonical \u003ccode>iris-workspace\u003c/code> skill template in the npm package and generate managed skill files for generic/Codex agents, Claude, and GitHub Copilot without a network request after package installation.\u003c/li>\n\u003cli>Preserve user configuration, pages, archives, unrelated editor tasks, sibling skills, unmarked files, and user-edited managed skill content; refresh only positively identified intact Iris-managed regions.\u003c/li>\n\u003cli>Preserve all existing content commands and lifecycle behavior for \u003ccode>archive\u003c/code> and \u003ccode>update\u003c/code>; do not change OpenSpec \u003ccode>/opsx:sync\u003c/code> or \u003ccode>openspec-sync-specs\u003c/code> tooling.\u003c/li>\n\u003cli>Remove this repository's proven generated \u003ccode>adopted-doc\u003c/code> dogfood records through the CLI migration/generation path and regenerate all CLI-owned HTML and state.\u003c/li>\n\u003cli>Update the canonical requirements and product documentation to describe agent-first initialization and retire product document adoption/synchronization.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>agent-first-initialization\u003c/code>: One-command local workspace and agent-skill installation, including managed ownership, offline packaging, idempotence, upgrade, and preservation behavior.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>project-lifecycle-automation\u003c/code>: Retire document adoption, incremental product synchronization, adopted-source metadata, and stale-source states while preserving bootstrap, archive, managed updates, and explicit rendering.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>CLI routing and help in \u003ccode>src/cli.ts\u003c/code>; initialization, lifecycle, rendering, project-state, filesystem, and design-template code under \u003ccode>src/\u003c/code>.\u003c/li>\n\u003cli>Npm package payload and installed smoke verification in \u003ccode>package.json\u003c/code>, release scripts, and tests.\u003c/li>\n\u003cli>Generated agent surfaces under \u003ccode>.agents/skills\u003c/code>, \u003ccode>.claude/skills\u003c/code>, and \u003ccode>.github/skills\u003c/code> from one packaged canonical template.\u003c/li>\n\u003cli>Tracked dogfood under \u003ccode>iris/\u003c/code>, regenerated only through the built CLI.\u003c/li>\n\u003cli>Lifecycle, navigation, packaging, migration-safety, and documentation tests plus \u003ccode>README.md\u003c/code> and \u003ccode>docs/\u003c/code> command/technical/design/status references.\u003c/li>\n\u003cli>Canonical OpenSpec lifecycle requirements; OpenSpec's own sync workflow and historical archives remain unchanged.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nIris currently requires repository-document adoption and synchronization to make its dashboard useful, while its advertised agent surfaces are not shipped by the installed package. Project setup should instead be one local, deterministic `iris init` operation that installs agent guidance and preserves user-owned work without copying or monitoring general documentation.\n\n## What Changes\n\n- **BREAKING** remove the Iris CLI commands `iris adopt` and `iris sync`, including their help entries, document-mirroring behavior, adopted-source hashes, stale-source states, tests, documentation, and generated dashboard copy.\n- Make `iris init` the complete first-run and upgrade flow: scaffold or refresh the Iris workspace, safely migrate proven legacy adopted pages, install supported agent skills, and render the usable dashboard.\n- Ship one canonical `iris-workspace` skill template in the npm package and generate managed skill files for generic/Codex agents, Claude, and GitHub Copilot without a network request after package installation.\n- Preserve user configuration, pages, archives, unrelated editor tasks, sibling skills, unmarked files, and user-edited managed skill content; refresh only positively identified intact Iris-managed regions.\n- Preserve all existing content commands and lifecycle behavior for `archive` and `update`; do not change OpenSpec `/opsx:sync` or `openspec-sync-specs` tooling.\n- Remove this repository&#039;s proven generated `adopted-doc` dogfood records through the CLI migration/generation path and regenerate all CLI-owned HTML and state.\n- Update the canonical requirements and product documentation to describe agent-first initialization and retire product document adoption/synchronization.\n\n## Capabilities\n\n### New Capabilities\n\n- `agent-first-initialization`: One-command local workspace and agent-skill installation, including managed ownership, offline packaging, idempotence, upgrade, and preservation behavior.\n\n### Modified Capabilities\n\n- `project-lifecycle-automation`: Retire document adoption, incremental product synchronization, adopted-source metadata, and stale-source states while preserving bootstrap, archive, managed updates, and explicit rendering.\n\n## Impact\n\n- CLI routing and help in `src/cli.ts`; initialization, lifecycle, rendering, project-state, filesystem, and design-template code under `src/`.\n- Npm package payload and installed smoke verification in `package.json`, release scripts, and tests.\n- Generated agent surfaces under `.agents/skills`, `.claude/skills`, and `.github/skills` from one packaged canonical template.\n- Tracked dogfood under `iris/`, regenerated only through the built CLI.\n- Lifecycle, navigation, packaging, migration-safety, and documentation tests plus `README.md` and `docs/` command/technical/design/status references.\n- Canonical OpenSpec lifecycle requirements; OpenSpec&#039;s own sync workflow and historical archives remain unchanged.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See \u003ccode>proposal.md\u003c/code> for motivation and the two delta specs for observable behavior. Today \u003ccode>init\u003c/code> scaffolds the workspace and managed design/editor files, while \u003ccode>adopt\u003c/code> creates README/docs report mirrors and \u003ccode>sync\u003c/code> owns page/source hashes and stale states. The npm allowlist omits all agent templates.\u003c/p>\n\u003cp>The locally installed OpenSpec 1.9.0 implementation is the reference for agent setup: its initializer uses a canonical workflow template registry, resolves tool skill directories, transforms tool-specific references, and writes packaged generated skills. Its skill writer replaces complete generated files, so Iris needs a stricter preservation boundary to satisfy this change's user-edit requirements.\u003c/p>\n\u003cp>The installed Iris CLI must remain framework-free and locally deterministic. Initialization cannot depend on the OpenSpec package, a server, runtime network access, background monitoring, or repository-only ignored files.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Make first run and upgrades converge through \u003ccode>iris init\u003c/code>.\u003c/li>\n\u003cli>Keep one auditable agent instruction source and small generated surfaces.\u003c/li>\n\u003cli>Prove ownership before updating skills or deleting legacy generated pages.\u003c/li>\n\u003cli>Reduce project state to data still used by render/archive navigation.\u003c/li>\n\u003cli>Keep every generated dogfood file reproducible from source and the CLI.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Adding new content commands, agent runtimes, prompt/slash-command adapters, network discovery, telemetry, watchers, or OpenSpec runtime integration.\u003c/li>\n\u003cli>Removing or renaming \u003ccode>iris update\u003c/code>; it remains a compatible explicit managed-surface refresh, but it is no longer required for setup.\u003c/li>\n\u003cli>Changing OpenSpec's \u003ccode>/opsx:sync\u003c/code>, \u003ccode>openspec-sync-specs\u003c/code>, active changes, or historical archive artifacts.\u003c/li>\n\u003cli>Deleting archived adopted pages automatically; an explicit archive is treated as user-selected history.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-1-one-packaged-canonical-skill-with-three-repository-targets\">1. One packaged canonical skill with three repository targets\u003c/h3>\n\u003cp>Store the canonical skill template at \u003ccode>templates/agents/iris-workspace.md\u003c/code>, include \u003ccode>templates/agents\u003c/code> in the npm \u003ccode>files\u003c/code> allowlist, and resolve it relative to the installed package at runtime. Generate:\u003c/p>\n\u003cul>\n\u003cli>\u003ccode>.agents/skills/iris-workspace/SKILL.md\u003c/code>\u003c/li>\n\u003cli>\u003ccode>.claude/skills/iris-workspace/SKILL.md\u003c/code>\u003c/li>\n\u003cli>\u003ccode>.github/skills/iris-workspace/SKILL.md\u003c/code>\u003c/li>\n\u003c/ul>\n\u003cp>The template describes the existing Iris workflow and content commands. Surface adapters may transform invocation wording or frontmatter, but they cannot carry separately authored workflow instructions.\u003c/p>\n\u003cp>This follows OpenSpec's canonical-template pipeline while keeping Iris's supported set deliberately small. Generating \u003ccode>.claude/commands\u003c/code> or \u003ccode>.github/prompts\u003c/code> was rejected because these are optional command-adapter surfaces, duplicate the skill contract, and are unnecessary for agent-first setup.\u003c/p>\n\u003ch3 id=\"design-2-hash-verified-managed-regions-preserve-user-ownership\">2. Hash-verified managed regions preserve user ownership\u003c/h3>\n\u003cp>Each generated skill contains one start marker with template id, schema version, package version, and SHA-256 of the normalized managed body, plus one end marker. The installer parses markers without executing content.\u003c/p>\n\u003cul>\n\u003cli>Missing target: atomically create the complete generated skill.\u003c/li>\n\u003cli>Valid markers and matching body digest: replace only the managed region, preserving all prefix/suffix bytes.\u003c/li>\n\u003cli>Identical desired output: do not rewrite the file.\u003c/li>\n\u003cli>Unmarked target, malformed/nested/misordered markers, digest mismatch, symlink, or confinement failure: preserve the file, accumulate an actionable conflict, and continue inspecting other targets.\u003c/li>\n\u003cli>After workspace generation, any conflict makes \u003ccode>init\u003c/code> return a user-error status so incomplete skill setup is observable; successfully updated surfaces and user data remain intact for a safe rerun.\u003c/li>\n\u003c/ul>\n\u003cp>Atomic sibling-temporary-file plus rename writes prevent partial truncation. This is stricter than OpenSpec's whole-file replacement and is required because Iris explicitly promises edit preservation.\u003c/p>\n\u003ch3 id=\"design-3-init-orchestrates-migration-managed-surfaces-skills-and-re\">3. \u003ccode>init\u003c/code> orchestrates migration, managed surfaces, skills, and rendering\u003c/h3>\n\u003cp>Use this order:\u003c/p>\n\u003col>\n\u003cli>Create missing workspace directories and user-owned baseline files.\u003c/li>\n\u003cli>Load either legacy state version 1 or normalized version 2.\u003c/li>\n\u003cli>Classify and remove only proven active generated adopted pages.\u003c/li>\n\u003cli>Save normalized state version 2 containing only \u003ccode>page_index\u003c/code> identity/type/title and active/archived status.\u003c/li>\n\u003cli>Refresh managed design assets, safe project placeholders, and the single VS Code task.\u003c/li>\n\u003cli>Install or refresh the three agent skills.\u003c/li>\n\u003cli>Render the dashboard and validate required readable outputs.\u003c/li>\n\u003cli>Report preserved legacy pages and skill collisions; return failure if agent setup is incomplete.\u003c/li>\n\u003c/ol>\n\u003cp>\u003ccode>update\u003c/code> reuses managed-surface and skill installation functions for compatibility, while documentation tells users to rerun \u003ccode>init\u003c/code> for setup and upgrades. A separate hidden update phase was rejected because it recreates the multi-command setup this change removes.\u003c/p>\n\u003ch3 id=\"design-4-legacy-cleanup-requires-a-complete-positive-proof\">4. Legacy cleanup requires a complete positive proof\u003c/h3>\n\u003cp>Only an active \u003ccode>iris/pages/&lt;id&gt;\u003c/code> directory is eligible. The migration requires all of:\u003c/p>\n\u003cul>\n\u003cli>a legacy state entry whose source kind is Markdown;\u003c/li>\n\u003cli>a normalized, repository-relative source exactly \u003ccode>README.md\u003c/code> or below \u003ccode>docs/\u003c/code> with a Markdown extension and no traversal;\u003c/li>\n\u003cli>a non-symlink page directory confined below \u003ccode>iris/pages\u003c/code>;\u003c/li>\n\u003cli>parseable \u003ccode>data.json\u003c/code> whose \u003ccode>id\u003c/code> equals the registry id and whose tags contain \u003ccode>adopted-doc\u003c/code>;\u003c/li>\n\u003cli>a valid page id;\u003c/li>\n\u003cli>registry \u003ccode>data_hash\u003c/code> and \u003ccode>content_hashes[&quot;pages/&lt;id&gt;/data.json&quot;]\u003c/code> both matching SHA-256 of the exact current data bytes.\u003c/li>\n\u003c/ul>\n\u003cp>The migration removes only that page directory, registry entry, and corresponding legacy hashes. Missing fields, malformed data, unsafe paths, or any mismatch preserves the page and emits a warning. Prefixes, titles, prose, or tags alone never prove ownership. Archived records are normalized but preserved.\u003c/p>\n\u003cp>This narrow conjunction trades aggressive cleanup for data safety. Users can manually archive or remove a preserved ambiguous page after review.\u003c/p>\n\u003ch3 id=\"design-5-remove-product-sync-state-without-weakening-ordinary-rende\">5. Remove product sync state without weakening ordinary rendering\u003c/h3>\n\u003cp>Version 2 state keeps \u003ccode>page_index\u003c/code> because rendering registers active pages and archive navigation needs historical entries. Remove \u003ccode>source\u003c/code>, \u003ccode>data_hash\u003c/code>, \u003ccode>content_hashes\u003c/code>, \u003ccode>last_synced_sha\u003c/code>, and the \u003ccode>stale\u003c/code> status after migration. Rendering writes registry identity/status directly and \u003ccode>archive\u003c/code> retains only ordinary registry metadata.\u003c/p>\n\u003cp>Remove README briefing extraction and stale display/count logic from the dashboard template. Empty-state and project-placeholder copy points to \u003ccode>iris init\u003c/code>, agent skills, and explicit content/render commands. Generic report Markdown fields and session-ingestion source evidence remain unrelated and unchanged.\u003c/p>\n\u003ch3 id=\"design-6-package-and-generated-output-verification-are-release-cont\">6. Package and generated-output verification are release contracts\u003c/h3>\n\u003cp>Extend package verification and the installed smoke test to inspect \u003ccode>npm pack --json\u003c/code>, install the tarball into an unrelated temporary directory, run \u003ccode>iris init\u003c/code> without the source checkout as an asset provider, and assert the canonical template plus all three generated skills exist. The smoke test also reruns init and uses an existing content command to prove the installed workflow remains usable.\u003c/p>\n\u003cp>Tracked \u003ccode>iris/\u003c/code> dogfood is regenerated by building the CLI and running its migration/init/render path. No generated HTML, CSS, JavaScript, state, or adopted page is hand-edited.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Ambiguous legacy record survives] → Preserve it with an actionable warning; data safety outranks cosmetic cleanup.\u003c/li>\n\u003cli>[State migration fails after partial filesystem work] → Classify before deletion, use exact confined targets, save normalized state after successful removals, and cover rerun idempotence.\u003c/li>\n\u003cli>[Skill collision leaves setup incomplete] → Preserve the file, finish safe independent work, return a user error, and name the exact resolution path.\u003c/li>\n\u003cli>[Three generated files drift] → Generate all from one packaged template and test normalized surface parity.\u003c/li>\n\u003cli>[Ignored repository agent files hide package omissions] → Treat the packed tarball file list and installed smoke as the release evidence.\u003c/li>\n\u003cli>[Broad sync removal damages OpenSpec tooling] → Scope code/docs searches to the \u003ccode>iris sync\u003c/code> product surface and assert OpenSpec skill/command files remain present.\u003c/li>\n\u003cli>[Generated dogfood deletions resemble hand editing] → Perform them only through the built CLI migration and verify the final tree with tests and HTML integrity checks.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add version-tolerant state loading and conservative adopted-page classification tests before removing legacy metadata consumers.\u003c/li>\n\u003cli>Add the packaged canonical skill and managed-region installer with collision, atomicity, confinement, rerun, and upgrade tests.\u003c/li>\n\u003cli>Reorder \u003ccode>init\u003c/code> around migration, skill installation, and final render; keep \u003ccode>update\u003c/code> delegating to the shared safe refresh path.\u003c/li>\n\u003cli>Remove CLI routes, adoption/sync implementation, stale/source/hash state, template copy, and coupled tests while retaining archive/update behavior.\u003c/li>\n\u003cli>Update documentation and package verification.\u003c/li>\n\u003cli>Build the CLI, run \u003ccode>iris init\u003c/code> against this repository to migrate the six proven active dogfood mirrors, and run \u003ccode>iris render --all\u003c/code>; inspect the resulting diff instead of editing generated files.\u003c/li>\n\u003cli>Run focused tests, strict OpenSpec verification, spec sync/archive, and the complete repository gate before the single commit and PR.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback before merge is the branch diff. After release, a preserved legacy page can be recovered from version control; user-owned or ambiguous pages are never automatically deleted. Version 2 state intentionally cannot recreate retired adopted-source monitoring, so rollback to an older CLI requires restoring the previous state file from version control or backup.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee `proposal.md` for motivation and the two delta specs for observable behavior. Today `init` scaffolds the workspace and managed design/editor files, while `adopt` creates README/docs report mirrors and `sync` owns page/source hashes and stale states. The npm allowlist omits all agent templates.\n\nThe locally installed OpenSpec 1.9.0 implementation is the reference for agent setup: its initializer uses a canonical workflow template registry, resolves tool skill directories, transforms tool-specific references, and writes packaged generated skills. Its skill writer replaces complete generated files, so Iris needs a stricter preservation boundary to satisfy this change&#039;s user-edit requirements.\n\nThe installed Iris CLI must remain framework-free and locally deterministic. Initialization cannot depend on the OpenSpec package, a server, runtime network access, background monitoring, or repository-only ignored files.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Make first run and upgrades converge through `iris init`.\n- Keep one auditable agent instruction source and small generated surfaces.\n- Prove ownership before updating skills or deleting legacy generated pages.\n- Reduce project state to data still used by render/archive navigation.\n- Keep every generated dogfood file reproducible from source and the CLI.\n\n**Non-Goals:**\n\n- Adding new content commands, agent runtimes, prompt/slash-command adapters, network discovery, telemetry, watchers, or OpenSpec runtime integration.\n- Removing or renaming `iris update`; it remains a compatible explicit managed-surface refresh, but it is no longer required for setup.\n- Changing OpenSpec&#039;s `/opsx:sync`, `openspec-sync-specs`, active changes, or historical archive artifacts.\n- Deleting archived adopted pages automatically; an explicit archive is treated as user-selected history.\n\n## Decisions\n\n### 1. One packaged canonical skill with three repository targets\n\nStore the canonical skill template at `templates/agents/iris-workspace.md`, include `templates/agents` in the npm `files` allowlist, and resolve it relative to the installed package at runtime. Generate:\n\n- `.agents/skills/iris-workspace/SKILL.md`\n- `.claude/skills/iris-workspace/SKILL.md`\n- `.github/skills/iris-workspace/SKILL.md`\n\nThe template describes the existing Iris workflow and content commands. Surface adapters may transform invocation wording or frontmatter, but they cannot carry separately authored workflow instructions.\n\nThis follows OpenSpec&#039;s canonical-template pipeline while keeping Iris&#039;s supported set deliberately small. Generating `.claude/commands` or `.github/prompts` was rejected because these are optional command-adapter surfaces, duplicate the skill contract, and are unnecessary for agent-first setup.\n\n### 2. Hash-verified managed regions preserve user ownership\n\nEach generated skill contains one start marker with template id, schema version, package version, and SHA-256 of the normalized managed body, plus one end marker. The installer parses markers without executing content.\n\n- Missing target: atomically create the complete generated skill.\n- Valid markers and matching body digest: replace only the managed region, preserving all prefix/suffix bytes.\n- Identical desired output: do not rewrite the file.\n- Unmarked target, malformed/nested/misordered markers, digest mismatch, symlink, or confinement failure: preserve the file, accumulate an actionable conflict, and continue inspecting other targets.\n- After workspace generation, any conflict makes `init` return a user-error status so incomplete skill setup is observable; successfully updated surfaces and user data remain intact for a safe rerun.\n\nAtomic sibling-temporary-file plus rename writes prevent partial truncation. This is stricter than OpenSpec&#039;s whole-file replacement and is required because Iris explicitly promises edit preservation.\n\n### 3. `init` orchestrates migration, managed surfaces, skills, and rendering\n\nUse this order:\n\n1. Create missing workspace directories and user-owned baseline files.\n2. Load either legacy state version 1 or normalized version 2.\n3. Classify and remove only proven active generated adopted pages.\n4. Save normalized state version 2 containing only `page_index` identity/type/title and active/archived status.\n5. Refresh managed design assets, safe project placeholders, and the single VS Code task.\n6. Install or refresh the three agent skills.\n7. Render the dashboard and validate required readable outputs.\n8. Report preserved legacy pages and skill collisions; return failure if agent setup is incomplete.\n\n`update` reuses managed-surface and skill installation functions for compatibility, while documentation tells users to rerun `init` for setup and upgrades. A separate hidden update phase was rejected because it recreates the multi-command setup this change removes.\n\n### 4. Legacy cleanup requires a complete positive proof\n\nOnly an active `iris/pages/&lt;id&gt;` directory is eligible. The migration requires all of:\n\n- a legacy state entry whose source kind is Markdown;\n- a normalized, repository-relative source exactly `README.md` or below `docs/` with a Markdown extension and no traversal;\n- a non-symlink page directory confined below `iris/pages`;\n- parseable `data.json` whose `id` equals the registry id and whose tags contain `adopted-doc`;\n- a valid page id;\n- registry `data_hash` and `content_hashes[&quot;pages/&lt;id&gt;/data.json&quot;]` both matching SHA-256 of the exact current data bytes.\n\nThe migration removes only that page directory, registry entry, and corresponding legacy hashes. Missing fields, malformed data, unsafe paths, or any mismatch preserves the page and emits a warning. Prefixes, titles, prose, or tags alone never prove ownership. Archived records are normalized but preserved.\n\nThis narrow conjunction trades aggressive cleanup for data safety. Users can manually archive or remove a preserved ambiguous page after review.\n\n### 5. Remove product sync state without weakening ordinary rendering\n\nVersion 2 state keeps `page_index` because rendering registers active pages and archive navigation needs historical entries. Remove `source`, `data_hash`, `content_hashes`, `last_synced_sha`, and the `stale` status after migration. Rendering writes registry identity/status directly and `archive` retains only ordinary registry metadata.\n\nRemove README briefing extraction and stale display/count logic from the dashboard template. Empty-state and project-placeholder copy points to `iris init`, agent skills, and explicit content/render commands. Generic report Markdown fields and session-ingestion source evidence remain unrelated and unchanged.\n\n### 6. Package and generated-output verification are release contracts\n\nExtend package verification and the installed smoke test to inspect `npm pack --json`, install the tarball into an unrelated temporary directory, run `iris init` without the source checkout as an asset provider, and assert the canonical template plus all three generated skills exist. The smoke test also reruns init and uses an existing content command to prove the installed workflow remains usable.\n\nTracked `iris/` dogfood is regenerated by building the CLI and running its migration/init/render path. No generated HTML, CSS, JavaScript, state, or adopted page is hand-edited.\n\n## Risks / Trade-offs\n\n- [Ambiguous legacy record survives] → Preserve it with an actionable warning; data safety outranks cosmetic cleanup.\n- [State migration fails after partial filesystem work] → Classify before deletion, use exact confined targets, save normalized state after successful removals, and cover rerun idempotence.\n- [Skill collision leaves setup incomplete] → Preserve the file, finish safe independent work, return a user error, and name the exact resolution path.\n- [Three generated files drift] → Generate all from one packaged template and test normalized surface parity.\n- [Ignored repository agent files hide package omissions] → Treat the packed tarball file list and installed smoke as the release evidence.\n- [Broad sync removal damages OpenSpec tooling] → Scope code/docs searches to the `iris sync` product surface and assert OpenSpec skill/command files remain present.\n- [Generated dogfood deletions resemble hand editing] → Perform them only through the built CLI migration and verify the final tree with tests and HTML integrity checks.\n\n## Migration Plan\n\n1. Add version-tolerant state loading and conservative adopted-page classification tests before removing legacy metadata consumers.\n2. Add the packaged canonical skill and managed-region installer with collision, atomicity, confinement, rerun, and upgrade tests.\n3. Reorder `init` around migration, skill installation, and final render; keep `update` delegating to the shared safe refresh path.\n4. Remove CLI routes, adoption/sync implementation, stale/source/hash state, template copy, and coupled tests while retaining archive/update behavior.\n5. Update documentation and package verification.\n6. Build the CLI, run `iris init` against this repository to migrate the six proven active dogfood mirrors, and run `iris render --all`; inspect the resulting diff instead of editing generated files.\n7. Run focused tests, strict OpenSpec verification, spec sync/archive, and the complete repository gate before the single commit and PR.\n\nRollback before merge is the branch diff. After release, a preserved legacy page can be recovered from version control; user-owned or ambiguous pages are never automatically deleted. Version 2 state intentionally cannot recreate retired adopted-source monitoring, so rollback to an older CLI requires restoring the previous state file from version control or backup.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-lock-migration-and-state-safety\">1. Lock migration and state safety\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Add version-tolerant state parsing and tests for normalized version 2 registry data while retaining the legacy fields needed during migration.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Implement confined positive-proof classification for active adopted-document records and cover proven removal, hash mismatch, partial metadata, malformed data, unsafe paths, symlinks, user-created lookalikes, and archived-record preservation.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Normalize migrated state to the minimal page registry and verify repeated migration is idempotent.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-package-and-install-the-canonical-agent-skill\">2. Package and install the canonical agent skill\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add one canonical packaged \u003ccode>iris-workspace\u003c/code> skill template describing the supported content workflow without product adopt/sync instructions.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Implement the three-surface skill generator with ownership/version/digest markers, confinement and symlink guards, atomic writes, and canonical-body parity.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add tests for first install, unchanged rerun, managed upgrade, outside-marker preservation, edited-body collision, unmarked/malformed targets, sibling preservation, and isolated write failure.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-make-init-the-complete-setup-flow\">3. Make init the complete setup flow\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Reorder \u003ccode>iris init\u003c/code> to scaffold, migrate, normalize state, refresh managed workspace surfaces, install skills, render, validate outputs, and report incomplete setup conflicts.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Keep \u003ccode>iris update\u003c/code> compatible by reusing the safe managed-surface and skill installer while making \u003ccode>iris init\u003c/code> the documented setup and upgrade command.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Extend lifecycle/init tests for first run, rerun, user configuration/pages/archive preservation, unrelated VS Code tasks, and continued use of ordinary content commands.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-retire-product-adoption-and-synchronization\">4. Retire product adoption and synchronization\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Remove \u003ccode>adopt\u003c/code> and \u003ccode>sync\u003c/code> from CLI routing and help and assert both names are rejected as unknown Iris commands.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Remove adoption/sync implementation and adopted-source/hash/stale contracts while retaining archive, update, render registration, session-source evidence, and generic report Markdown support.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Remove stale/adopted README assumptions from dashboard data and Aperture templates and update navigation/accessibility assertions for the agent-first empty state.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 Assert OpenSpec \u003ccode>/opsx:sync\u003c/code> and \u003ccode>openspec-sync-specs\u003c/code> instruction surfaces remain present and unchanged in purpose.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-5-verify-installed-packaging-and-offline-behavior\">5. Verify installed packaging and offline behavior\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.1 Add the canonical template path to the npm allowlist and release-package required-file checks.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.2 Extend the packed-install smoke test to inspect the tarball payload, initialize in an unrelated temporary repository, verify all three skills, rerun idempotently, and create/render an ordinary page without source-checkout assets or runtime network access.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.3 Run focused packaging and installed smoke checks and resolve any path differences across the supported Node platforms.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-6-update-documentation-and-regenerate-dogfood\">6. Update documentation and regenerate dogfood\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.1 Update \u003ccode>README.md\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/tech.md\u003c/code>, \u003ccode>docs/design-system.md\u003c/code>, and \u003ccode>docs/status.md\u003c/code> for the one-command flow, installed surfaces, preservation policy, explicit rendering, and removal of product adoption/synchronization.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.2 Build the CLI and run its migration/init/render path against this repository to remove only the six proven active adopted dogfood pages and regenerate state, design assets, project placeholders, and dashboard HTML.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.3 Inspect the generated diff and run HTML/link integrity checks to prove no generated file was hand-edited and no user-owned page or archive was lost.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-7-complete-change-verification\">7. Complete change verification\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 7.1 Run lint, token lint, typecheck, the full Vitest suite, HTML integrity, and packed-install smoke as the complete repository gate.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 7.2 Run strict OpenSpec validation and the verification workflow, reconcile every requirement and scenario with code/test evidence, and leave no incomplete implementation task before spec sync and archive.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Lock migration and state safety\n\n- [x] 1.1 Add version-tolerant state parsing and tests for normalized version 2 registry data while retaining the legacy fields needed during migration.\n- [x] 1.2 Implement confined positive-proof classification for active adopted-document records and cover proven removal, hash mismatch, partial metadata, malformed data, unsafe paths, symlinks, user-created lookalikes, and archived-record preservation.\n- [x] 1.3 Normalize migrated state to the minimal page registry and verify repeated migration is idempotent.\n\n## 2. Package and install the canonical agent skill\n\n- [x] 2.1 Add one canonical packaged `iris-workspace` skill template describing the supported content workflow without product adopt/sync instructions.\n- [x] 2.2 Implement the three-surface skill generator with ownership/version/digest markers, confinement and symlink guards, atomic writes, and canonical-body parity.\n- [x] 2.3 Add tests for first install, unchanged rerun, managed upgrade, outside-marker preservation, edited-body collision, unmarked/malformed targets, sibling preservation, and isolated write failure.\n\n## 3. Make init the complete setup flow\n\n- [x] 3.1 Reorder `iris init` to scaffold, migrate, normalize state, refresh managed workspace surfaces, install skills, render, validate outputs, and report incomplete setup conflicts.\n- [x] 3.2 Keep `iris update` compatible by reusing the safe managed-surface and skill installer while making `iris init` the documented setup and upgrade command.\n- [x] 3.3 Extend lifecycle/init tests for first run, rerun, user configuration/pages/archive preservation, unrelated VS Code tasks, and continued use of ordinary content commands.\n\n## 4. Retire product adoption and synchronization\n\n- [x] 4.1 Remove `adopt` and `sync` from CLI routing and help and assert both names are rejected as unknown Iris commands.\n- [x] 4.2 Remove adoption/sync implementation and adopted-source/hash/stale contracts while retaining archive, update, render registration, session-source evidence, and generic report Markdown support.\n- [x] 4.3 Remove stale/adopted README assumptions from dashboard data and Aperture templates and update navigation/accessibility assertions for the agent-first empty state.\n- [x] 4.4 Assert OpenSpec `/opsx:sync` and `openspec-sync-specs` instruction surfaces remain present and unchanged in purpose.\n\n## 5. Verify installed packaging and offline behavior\n\n- [x] 5.1 Add the canonical template path to the npm allowlist and release-package required-file checks.\n- [x] 5.2 Extend the packed-install smoke test to inspect the tarball payload, initialize in an unrelated temporary repository, verify all three skills, rerun idempotently, and create/render an ordinary page without source-checkout assets or runtime network access.\n- [x] 5.3 Run focused packaging and installed smoke checks and resolve any path differences across the supported Node platforms.\n\n## 6. Update documentation and regenerate dogfood\n\n- [x] 6.1 Update `README.md`, `docs/cmds.md`, `docs/tech.md`, `docs/design-system.md`, and `docs/status.md` for the one-command flow, installed surfaces, preservation policy, explicit rendering, and removal of product adoption/synchronization.\n- [x] 6.2 Build the CLI and run its migration/init/render path against this repository to remove only the six proven active adopted dogfood pages and regenerate state, design assets, project placeholders, and dashboard HTML.\n- [x] 6.3 Inspect the generated diff and run HTML/link integrity checks to prove no generated file was hand-edited and no user-owned page or archive was lost.\n\n## 7. Complete change verification\n\n- [x] 7.1 Run lint, token lint, typecheck, the full Vitest suite, HTML integrity, and packed-install smoke as the complete repository gate.\n- [x] 7.2 Run strict OpenSpec validation and the verification workflow, reconcile every requirement and scenario with code/test evidence, and leave no incomplete implementation task before spec sync and archive.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-agent-first-initialization-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · agent-first-initialization\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization/specs/agent-first-initialization/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-agent-first-initialization-purpose\">Purpose\u003c/h2>\n\u003cp>Make a single installed Iris command establish and safely upgrade the local visual workspace and its agent-facing instructions without requiring repository-document ingestion or network access.\u003c/p>\n\u003ch2 id=\"delta-agent-first-initialization-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-one-command-agent-first-setup\">Requirement: one-command agent-first setup\u003c/h3>\n\u003cp>The system MUST make \u003ccode>iris init\u003c/code> the complete project setup and upgrade flow for an installed Iris CLI.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-first-initialization\">Scenario: first initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> in a repository with no Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST create the usable local workspace, install the supported agent-facing Iris skill surfaces, and render the dashboard in that operation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-repeated-initialization\">Scenario: repeated initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> again in an initialized repository\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST refresh Iris-managed outputs idempotently without requiring \u003ccode>iris update\u003c/code>, \u003ccode>iris adopt\u003c/code>, or \u003ccode>iris sync\u003c/code> as a setup step\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-initialized-repository-has-content-commands\">Scenario: initialized repository has content commands\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> initialization completes successfully\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> agents and users MUST still be able to use the existing draft, render, report, archive, publish, export, and open commands\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-canonical-multi-agent-skill-installation\">Requirement: canonical multi-agent skill installation\u003c/h3>\n\u003cp>The system MUST generate supported agent skill files from one canonical Iris-owned skill source rather than maintaining independent instruction copies.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-supported-skill-surfaces-are-installed\">Scenario: supported skill surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs agent guidance\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST provide the \u003ccode>iris-workspace\u003c/code> skill to the generic/Codex, Claude, and GitHub Copilot repository skill locations\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-unrelated-agent-content-exists\">Scenario: unrelated agent content exists\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a supported skill directory already contains sibling skills or unrelated files\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST leave that content unchanged\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-managed-skill-preservation\">Requirement: managed skill preservation\u003c/h3>\n\u003cp>The system MUST identify its generated skill content with verifiable managed ownership metadata and fail safely around user-owned or modified content.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-managed-content-is-intact\">Scenario: managed content is intact\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Iris skill has valid ownership markers and its managed content matches the recorded digest\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-managed-content-was-edited\">Scenario: managed content was edited\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the managed content no longer matches its recorded digest\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST preserve the file and report an actionable collision instead of overwriting the edit\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-target-is-unmarked-or-malformed\">Scenario: target is unmarked or malformed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the target skill file is user-owned, unmarked, partially marked, misordered, unsafe, or outside the repository boundary\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST not truncate, replace, or follow it and MUST report the affected surface\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-packaged-offline-initialization\">Requirement: packaged offline initialization\u003c/h3>\n\u003cp>The installed Iris package MUST contain every template and runtime file required for initialization after the package itself has been obtained.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-installed-package-initializes-offline\">Scenario: installed package initializes offline\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> from a packed or globally installed Iris package with no repository checkout or network access\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command MUST generate the workspace and agent skills entirely from packaged local assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-package-payload-is-verified\">Scenario: package payload is verified\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the release package is built\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> release verification MUST fail if the canonical agent template or its installed generator is absent from the package payload\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-safe-retirement-of-adopted-document-records\">Requirement: safe retirement of adopted document records\u003c/h3>\n\u003cp>The system MUST remove a legacy adopted-document page only when Iris can positively prove that the exact record remains an unmodified generated mirror.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-legacy-generated-mirror-is-proven\">Scenario: legacy generated mirror is proven\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> legacy state provenance, a safe README or docs source path, page identity, generated metadata, and stored and current content digests all agree\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MAY remove that exact generated page and normalize the state before rendering\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-ownership-proof-is-incomplete\">Scenario: ownership proof is incomplete\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> any ownership, path, identity, metadata, or digest check is missing or mismatched\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST preserve the page and report it for manual review\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-arbitrary-user-page-resembles-an-adopted-page\">Scenario: arbitrary user page resembles an adopted page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user-created page has a \u003ccode>doc-\u003c/code> prefix, an \u003ccode>adopted-doc\u003c/code> tag, source-like prose, or only some legacy fields\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST NOT treat those hints alone as permission to delete the page\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nMake a single installed Iris command establish and safely upgrade the local visual workspace and its agent-facing instructions without requiring repository-document ingestion or network access.\n\n## ADDED Requirements\n\n### Requirement: one-command agent-first setup\nThe system MUST make `iris init` the complete project setup and upgrade flow for an installed Iris CLI.\n\n#### Scenario: first initialization\n- **WHEN** a user runs `iris init` in a repository with no Iris workspace\n- **THEN** the system MUST create the usable local workspace, install the supported agent-facing Iris skill surfaces, and render the dashboard in that operation\n\n#### Scenario: repeated initialization\n- **WHEN** a user runs `iris init` again in an initialized repository\n- **THEN** the system MUST refresh Iris-managed outputs idempotently without requiring `iris update`, `iris adopt`, or `iris sync` as a setup step\n\n#### Scenario: initialized repository has content commands\n- **WHEN** initialization completes successfully\n- **THEN** agents and users MUST still be able to use the existing draft, render, report, archive, publish, export, and open commands\n\n### Requirement: canonical multi-agent skill installation\nThe system MUST generate supported agent skill files from one canonical Iris-owned skill source rather than maintaining independent instruction copies.\n\n#### Scenario: supported skill surfaces are installed\n- **WHEN** `iris init` installs agent guidance\n- **THEN** it MUST provide the `iris-workspace` skill to the generic/Codex, Claude, and GitHub Copilot repository skill locations\n\n#### Scenario: unrelated agent content exists\n- **WHEN** a supported skill directory already contains sibling skills or unrelated files\n- **THEN** initialization MUST leave that content unchanged\n\n### Requirement: managed skill preservation\nThe system MUST identify its generated skill content with verifiable managed ownership metadata and fail safely around user-owned or modified content.\n\n#### Scenario: managed content is intact\n- **WHEN** a generated Iris skill has valid ownership markers and its managed content matches the recorded digest\n- **THEN** initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\n\n#### Scenario: managed content was edited\n- **WHEN** the managed content no longer matches its recorded digest\n- **THEN** initialization MUST preserve the file and report an actionable collision instead of overwriting the edit\n\n#### Scenario: target is unmarked or malformed\n- **WHEN** the target skill file is user-owned, unmarked, partially marked, misordered, unsafe, or outside the repository boundary\n- **THEN** initialization MUST not truncate, replace, or follow it and MUST report the affected surface\n\n### Requirement: packaged offline initialization\nThe installed Iris package MUST contain every template and runtime file required for initialization after the package itself has been obtained.\n\n#### Scenario: installed package initializes offline\n- **WHEN** a user runs `iris init` from a packed or globally installed Iris package with no repository checkout or network access\n- **THEN** the command MUST generate the workspace and agent skills entirely from packaged local assets\n\n#### Scenario: package payload is verified\n- **WHEN** the release package is built\n- **THEN** release verification MUST fail if the canonical agent template or its installed generator is absent from the package payload\n\n### Requirement: safe retirement of adopted document records\nThe system MUST remove a legacy adopted-document page only when Iris can positively prove that the exact record remains an unmodified generated mirror.\n\n#### Scenario: legacy generated mirror is proven\n- **WHEN** legacy state provenance, a safe README or docs source path, page identity, generated metadata, and stored and current content digests all agree\n- **THEN** initialization MAY remove that exact generated page and normalize the state before rendering\n\n#### Scenario: ownership proof is incomplete\n- **WHEN** any ownership, path, identity, metadata, or digest check is missing or mismatched\n- **THEN** initialization MUST preserve the page and report it for manual review\n\n#### Scenario: arbitrary user page resembles an adopted page\n- **WHEN** a user-created page has a `doc-` prefix, an `adopted-doc` tag, source-like prose, or only some legacy fields\n- **THEN** initialization MUST NOT treat those hints alone as permission to delete the page\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-project-lifecycle-automation-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · project-lifecycle-automation\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003cspan class=\"badge b-muted\">REMOVED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-first-initialization/specs/project-lifecycle-automation/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-project-lifecycle-automation-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-lifecycle-aware-project-bootstrap\">Requirement: lifecycle-aware project bootstrap\u003c/h3>\n\u003cp>The system MUST provide one project bootstrap flow that creates or refreshes the local structure needed for page generation, dashboard rendering, managed updates, and supported agent skill use without ingesting general repository documentation.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-user-initializes-the-project\">Scenario: user initializes the project\u003c/h4>\n\u003cp>WHEN a user runs \u003ccode>iris init\u003c/code> in a repository\nTHEN the system MUST create or safely refresh the required \u003ccode>iris/\u003c/code> structure, scaffold managed task metadata, install supported agent skills, and render the dashboard.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-repository-contains-readme-or-docs-markdown\">Scenario: repository contains README or docs markdown\u003c/h4>\n\u003cp>WHEN a user runs \u003ccode>iris init\u003c/code> in a repository containing \u003ccode>README.md\u003c/code> or \u003ccode>docs/**/*.md\u003c/code>\nTHEN the system MUST NOT automatically copy, mirror, hash, monitor, or create Iris page records from those files.\u003c/p>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-archival-and-cleanup\">Requirement: archival and cleanup\u003c/h3>\n\u003cp>The system MUST provide a clear archiving path that removes a user-selected page from the active feed while preserving its historical generated artifact and registry navigation.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-a-page-becomes-outdated\">Scenario: a page becomes outdated\u003c/h4>\n\u003cp>WHEN a user runs the archive command for an existing active page\nTHEN the archive command MUST move that exact page into the archive state and update the page registry and dashboard consistently.\u003c/p>\n\u003ch2 id=\"delta-project-lifecycle-automation-removed-requirements\">REMOVED Requirements\u003c/h2>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-incremental-sync-and-stale-state-awareness\">Requirement: incremental sync and stale-state awareness\u003c/h3>\n\u003cp>\u003cstrong>Reason\u003c/strong>: Explicit \u003ccode>iris init\u003c/code> and \u003ccode>iris render --all\u003c/code> operations replace product synchronization; adopted-source monitoring and stale-source state no longer belong in the agent-first workspace.\u003c/p>\n\u003cp>\u003cstrong>Migration\u003c/strong>: Run \u003ccode>iris init\u003c/code> once to migrate legacy state and then use \u003ccode>iris render --all\u003c/code> when page contracts or repository-backed explicit views need regeneration.\u003c/p>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-doc-adoption-and-mirroring\">Requirement: doc adoption and mirroring\u003c/h3>\n\u003cp>\u003cstrong>Reason\u003c/strong>: Iris no longer ingests general project documentation, and agents use installed skills plus explicit content commands instead of README/docs mirrors.\u003c/p>\n\u003cp>\u003cstrong>Migration\u003c/strong>: Preserve user-owned pages, allow \u003ccode>iris init\u003c/code> to remove only positively proven unmodified generated mirrors, and create intentional Iris content with the existing report, feature, bug, idea, or plan commands.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: lifecycle-aware project bootstrap\nThe system MUST provide one project bootstrap flow that creates or refreshes the local structure needed for page generation, dashboard rendering, managed updates, and supported agent skill use without ingesting general repository documentation.\n\n#### Scenario: user initializes the project\nWHEN a user runs `iris init` in a repository\nTHEN the system MUST create or safely refresh the required `iris/` structure, scaffold managed task metadata, install supported agent skills, and render the dashboard.\n\n#### Scenario: repository contains README or docs markdown\nWHEN a user runs `iris init` in a repository containing `README.md` or `docs/**/*.md`\nTHEN the system MUST NOT automatically copy, mirror, hash, monitor, or create Iris page records from those files.\n\n### Requirement: archival and cleanup\nThe system MUST provide a clear archiving path that removes a user-selected page from the active feed while preserving its historical generated artifact and registry navigation.\n\n#### Scenario: a page becomes outdated\nWHEN a user runs the archive command for an existing active page\nTHEN the archive command MUST move that exact page into the archive state and update the page registry and dashboard consistently.\n\n## REMOVED Requirements\n\n### Requirement: incremental sync and stale-state awareness\n**Reason**: Explicit `iris init` and `iris render --all` operations replace product synchronization; adopted-source monitoring and stale-source state no longer belong in the agent-first workspace.\n\n**Migration**: Run `iris init` once to migrate legacy state and then use `iris render --all` when page contracts or repository-backed explicit views need regeneration.\n\n### Requirement: doc adoption and mirroring\n**Reason**: Iris no longer ingests general project documentation, and agents use installed skills plus explicit content commands instead of README/docs mirrors.\n\n**Migration**: Preserve user-owned pages, allow `iris init` to remove only positively proven unmodified generated mirrors, and create intentional Iris content with the existing report, feature, bug, idea, or plan commands.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-1-one-packaged-canonical-skill-with-three-repository-targets\">1. One packaged canonical skill with three repository targets\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-2-hash-verified-managed-regions-preserve-user-ownership\">2. Hash-verified managed regions preserve user ownership\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-3-init-orchestrates-migration-managed-surfaces-skills-and-re\">3. `init` orchestrates migration, managed surfaces, skills, and rendering\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-4-legacy-cleanup-requires-a-complete-positive-proof\">4. Legacy cleanup requires a complete positive proof\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-5-remove-product-sync-state-without-weakening-ordinary-rende\">5. Remove product sync state without weakening ordinary rendering\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-6-package-and-generated-output-verification-are-release-cont\">6. Package and generated-output verification are release contracts\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-lock-migration-and-state-safety\">1. Lock migration and state safety\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-package-and-install-the-canonical-agent-skill\">2. Package and install the canonical agent skill\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-make-init-the-complete-setup-flow\">3. Make init the complete setup flow\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-retire-product-adoption-and-synchronization\">4. Retire product adoption and synchronization\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-5-verify-installed-packaging-and-offline-behavior\">5. Verify installed packaging and offline behavior\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-6-update-documentation-and-regenerate-dogfood\">6. Update documentation and regenerate dogfood\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-7-complete-change-verification\">7. Complete change verification\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-artifact\">Delta spec · agent-first-initialization\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-one-command-agent-first-setup\">Requirement: one-command agent-first setup\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-canonical-multi-agent-skill-installation\">Requirement: canonical multi-agent skill installation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-managed-skill-preservation\">Requirement: managed skill preservation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-packaged-offline-initialization\">Requirement: packaged offline initialization\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-safe-retirement-of-adopted-document-records\">Requirement: safe retirement of adopted document records\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-project-lifecycle-automation-artifact\">Delta spec · project-lifecycle-automation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-project-lifecycle-automation-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-lifecycle-aware-project-bootstrap\">Requirement: lifecycle-aware project bootstrap\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-archival-and-cleanup\">Requirement: archival and cleanup\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-project-lifecycle-automation-removed-requirements\">REMOVED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-incremental-sync-and-stale-state-awareness\">Requirement: incremental sync and stale-state awareness\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-doc-adoption-and-mirroring\">Requirement: doc adoption and mirroring\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-agent-surface-triggers":{"kind":"change","name":"2026-08-21-agent-surface-triggers","title":"2026-08-21-agent-surface-triggers","path":"changes/archive/2026-08-21-agent-surface-triggers","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-agent-surface-triggers\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-agent-surface-triggers\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">10\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-agent-surface-triggers: 10 of 10 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">10/10 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-surface-triggers/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Iris installs one \u003ccode>iris-workspace\u003c/code> skill whose description — &quot;Use Iris to create and render intentional local visual workspace content&quot; — says what Iris is but never says \u003cem>when\u003c/em> to reach for it, so in ordinary conversation an agent finishes a bug fix, a plan, or a piece of research and the result stays in chat instead of landing on the dashboard. Claude Code and Copilot users also expect typed slash commands for frequent actions, and today Iris exposes none.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Rewrite the canonical skill so its trigger is conversational: a description naming the moments that call for Iris (finished a fix, a feature, a plan, an idea, research, or a session) and a compact intent-to-command table mapping what a user says to the exact command and where it lands.\u003c/li>\n\u003cli>Generate typed slash commands from the same canonical source into \u003ccode>.claude/commands/iris/\u003c/code> and \u003ccode>.github/prompts/\u003c/code>, one per frequent content action (\u003ccode>report\u003c/code>, \u003ccode>research\u003c/code>, \u003ccode>bug\u003c/code>, \u003ccode>feature\u003c/code>, \u003ccode>idea\u003c/code>, \u003ccode>plan\u003c/code>, \u003ccode>render\u003c/code>), each a short create → fill → render → report-path instruction.\u003c/li>\n\u003cli>Apply the existing managed-region ownership rules to the generated command files: verifiable ownership markers with a body digest, atomic confined writes, refresh only an intact managed region, preserve and report anything unmarked, edited, malformed, symlinked, or escaping.\u003c/li>\n\u003cli>Keep the skill the single source of agent instructions; command files are generated projections of it, never independently maintained copies.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cp>None.\u003c/p>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>agent-first-initialization\u003c/code>: the canonical skill installation requirement gains conversational trigger and intent-mapping content obligations; a new requirement covers generated slash-command surfaces installed and preserved under the same managed-ownership rules.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>\u003ccode>templates/agents/iris-workspace.md\u003c/code> is rewritten; a new packaged command template source and \u003ccode>src/lib/agent-skills.ts\u003c/code> gain command-file generation; \u003ccode>package.json\u003c/code> \u003ccode>files\u003c/code> must ship the new template assets and release verification must check them.\u003c/li>\n\u003cli>\u003ccode>src/commands/init.ts\u003c/code> and \u003ccode>src/commands/lifecycle.ts\u003c/code> report command-surface results alongside skill results.\u003c/li>\n\u003cli>Tests for skill content, command generation, collision preservation, and packaged payload; docs (\u003ccode>README.md\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/status.md\u003c/code>) updated.\u003c/li>\n\u003cli>Depends on \u003ccode>dashboard-shell-redesign\u003c/code> (command catalog and Commands page) and \u003ccode>research-markdown-pages\u003c/code> (the \u003ccode>research\u003c/code> command the skill and slash commands reference). No new dependency, network access, or runtime.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nIris installs one `iris-workspace` skill whose description — &quot;Use Iris to create and render intentional local visual workspace content&quot; — says what Iris is but never says _when_ to reach for it, so in ordinary conversation an agent finishes a bug fix, a plan, or a piece of research and the result stays in chat instead of landing on the dashboard. Claude Code and Copilot users also expect typed slash commands for frequent actions, and today Iris exposes none.\n\n## What Changes\n\n- Rewrite the canonical skill so its trigger is conversational: a description naming the moments that call for Iris (finished a fix, a feature, a plan, an idea, research, or a session) and a compact intent-to-command table mapping what a user says to the exact command and where it lands.\n- Generate typed slash commands from the same canonical source into `.claude/commands/iris/` and `.github/prompts/`, one per frequent content action (`report`, `research`, `bug`, `feature`, `idea`, `plan`, `render`), each a short create → fill → render → report-path instruction.\n- Apply the existing managed-region ownership rules to the generated command files: verifiable ownership markers with a body digest, atomic confined writes, refresh only an intact managed region, preserve and report anything unmarked, edited, malformed, symlinked, or escaping.\n- Keep the skill the single source of agent instructions; command files are generated projections of it, never independently maintained copies.\n\n## Capabilities\n\n### New Capabilities\n\nNone.\n\n### Modified Capabilities\n\n- `agent-first-initialization`: the canonical skill installation requirement gains conversational trigger and intent-mapping content obligations; a new requirement covers generated slash-command surfaces installed and preserved under the same managed-ownership rules.\n\n## Impact\n\n- `templates/agents/iris-workspace.md` is rewritten; a new packaged command template source and `src/lib/agent-skills.ts` gain command-file generation; `package.json` `files` must ship the new template assets and release verification must check them.\n- `src/commands/init.ts` and `src/commands/lifecycle.ts` report command-surface results alongside skill results.\n- Tests for skill content, command generation, collision preservation, and packaged payload; docs (`README.md`, `docs/cmds.md`, `docs/status.md`) updated.\n- Depends on `dashboard-shell-redesign` (command catalog and Commands page) and `research-markdown-pages` (the `research` command the skill and slash commands reference). No new dependency, network access, or runtime.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-surface-triggers/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See proposal.md. \u003ccode>src/lib/agent-skills.ts\u003c/code> already implements the managed-surface machinery this change reuses: a packaged template read from the installed package root, front matter plus \u003ccode>IRIS:MANAGED:START/END\u003c/code> markers carrying template id, schema, version, and a SHA-256 body digest, symlink and confinement checks, and atomic temp-file writes. It installs three skill files today.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>An agent reading only the skill can decide whether Iris applies and which command to run.\u003c/li>\n\u003cli>Slash commands exist where users type them (Claude Code, Copilot) without a second source of truth.\u003c/li>\n\u003cli>Reuse the existing ownership, digest, confinement, and atomic-write rules verbatim.\u003c/li>\n\u003cli>Keep the skill small; the intent table is the payload, not prose.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>A Codex command surface (Codex consumes the skill directly).\u003c/li>\n\u003cli>Commands for setup or share actions (\u003ccode>init\u003c/code>, \u003ccode>update\u003c/code>, \u003ccode>vendor\u003c/code>, \u003ccode>open\u003c/code>, \u003ccode>publish\u003c/code>, \u003ccode>export\u003c/code>); the Commands page and \u003ccode>--help\u003c/code> cover those.\u003c/li>\n\u003cli>Auto-invocation, hooks, or watching. Iris still runs when an agent or user runs it.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-generalize-the-installer-over-a-surface-descriptor\">Generalize the installer over a surface descriptor\u003c/h3>\n\u003cp>\u003ccode>installAgentSkills\u003c/code> becomes \u003ccode>installAgentSurfaces\u003c/code>, iterating a list of \u003ccode>{ templateId, target, frontMatter, body }\u003c/code> descriptors instead of one hard-coded template. Marker parsing keys off the descriptor's template id rather than the literal \u003ccode>iris-workspace\u003c/code>, so skill and command files share one code path and one conflict-reporting shape. The public \u003ccode>SkillInstallResult\u003c/code> shape is unchanged so \u003ccode>assertSkillInstallComplete\u003c/code> and its callers keep working.\u003c/p>\n\u003cp>Alternative: a second parallel installer for commands. Rejected — it duplicates the security-relevant marker and confinement logic, which is exactly the drift the agent-first change removed.\u003c/p>\n\u003ch3 id=\"design-one-packaged-command-template-projected-per-action\">One packaged command template, projected per action\u003c/h3>\n\u003cp>\u003ccode>templates/agents/iris-commands.md\u003c/code> holds a table of actions (\u003ccode>report\u003c/code>, \u003ccode>research\u003c/code>, \u003ccode>bug\u003c/code>, \u003ccode>feature\u003c/code>, \u003ccode>idea\u003c/code>, \u003ccode>plan\u003c/code>, \u003ccode>render\u003c/code>) with the one-line body each generated command carries. The generator emits \u003ccode>.claude/commands/iris/&lt;action&gt;.md\u003c/code> and \u003ccode>.github/prompts/iris-&lt;action&gt;.prompt.md\u003c/code> from that single table, differing only in the front matter each host expects. Bodies stay ~8 lines: what it creates, what to edit, what to run, what to report back.\u003c/p>\n\u003ch3 id=\"design-conversational-skill-trigger\">Conversational skill trigger\u003c/h3>\n\u003cp>The skill's \u003ccode>description\u003c/code> changes to name the moments rather than the product, and the body opens with a &quot;When to use this&quot; intent table (user says → command → lands as). Everything already in the skill (setup, contract workflow, render semantics, preservation rules) stays but is compressed so the file does not grow materially; the intent table earns its ~150 tokens by being the part that fires in conversation.\u003c/p>\n\u003ch3 id=\"design-packaging-and-verification\">Packaging and verification\u003c/h3>\n\u003cp>\u003ccode>package.json\u003c/code> \u003ccode>files\u003c/code> already ships \u003ccode>templates/agents\u003c/code>, so the new template is included by directory. \u003ccode>scripts/verify-release.mjs\u003c/code> gains an assertion for the command template so a missing asset fails the release rather than silently installing fewer surfaces.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[More generated files to preserve safely] → Same marker, digest, confinement, and atomic-write path as skills; conflicts are reported, never overwritten.\u003c/li>\n\u003cli>[Slash commands could drift from the CLI] → Actions are derived from the command catalog introduced by \u003ccode>dashboard-shell-redesign\u003c/code>; a test asserts every generated command names a catalog entry that exists.\u003c/li>\n\u003cli>[A larger skill costs tokens on every agent turn] → The rewrite compresses existing prose to offset the intent table; a test asserts the rendered skill stays under a fixed byte budget.\u003c/li>\n\u003cli>[Copilot prompt front matter differs from Claude's] → Encoded per surface in the descriptor; unknown hosts are simply not targeted.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Generalize the installer and add descriptor-driven generation with tests, still installing only skills.\u003c/li>\n\u003cli>Add the command template and the two command surfaces; extend init/update reporting.\u003c/li>\n\u003cli>Rewrite the skill template; update packaging verification and docs.\u003c/li>\n\u003cli>Run the release gate and regenerate local surfaces.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback: revert; generated command files are inert Markdown that a later \u003ccode>iris init\u003c/code> no longer refreshes, and users may delete them.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee proposal.md. `src/lib/agent-skills.ts` already implements the managed-surface machinery this change reuses: a packaged template read from the installed package root, front matter plus `IRIS:MANAGED:START/END` markers carrying template id, schema, version, and a SHA-256 body digest, symlink and confinement checks, and atomic temp-file writes. It installs three skill files today.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- An agent reading only the skill can decide whether Iris applies and which command to run.\n- Slash commands exist where users type them (Claude Code, Copilot) without a second source of truth.\n- Reuse the existing ownership, digest, confinement, and atomic-write rules verbatim.\n- Keep the skill small; the intent table is the payload, not prose.\n\n**Non-Goals:**\n\n- A Codex command surface (Codex consumes the skill directly).\n- Commands for setup or share actions (`init`, `update`, `vendor`, `open`, `publish`, `export`); the Commands page and `--help` cover those.\n- Auto-invocation, hooks, or watching. Iris still runs when an agent or user runs it.\n\n## Decisions\n\n### Generalize the installer over a surface descriptor\n\n`installAgentSkills` becomes `installAgentSurfaces`, iterating a list of `{ templateId, target, frontMatter, body }` descriptors instead of one hard-coded template. Marker parsing keys off the descriptor&#039;s template id rather than the literal `iris-workspace`, so skill and command files share one code path and one conflict-reporting shape. The public `SkillInstallResult` shape is unchanged so `assertSkillInstallComplete` and its callers keep working.\n\nAlternative: a second parallel installer for commands. Rejected — it duplicates the security-relevant marker and confinement logic, which is exactly the drift the agent-first change removed.\n\n### One packaged command template, projected per action\n\n`templates/agents/iris-commands.md` holds a table of actions (`report`, `research`, `bug`, `feature`, `idea`, `plan`, `render`) with the one-line body each generated command carries. The generator emits `.claude/commands/iris/&lt;action&gt;.md` and `.github/prompts/iris-&lt;action&gt;.prompt.md` from that single table, differing only in the front matter each host expects. Bodies stay ~8 lines: what it creates, what to edit, what to run, what to report back.\n\n### Conversational skill trigger\n\nThe skill&#039;s `description` changes to name the moments rather than the product, and the body opens with a &quot;When to use this&quot; intent table (user says → command → lands as). Everything already in the skill (setup, contract workflow, render semantics, preservation rules) stays but is compressed so the file does not grow materially; the intent table earns its ~150 tokens by being the part that fires in conversation.\n\n### Packaging and verification\n\n`package.json` `files` already ships `templates/agents`, so the new template is included by directory. `scripts/verify-release.mjs` gains an assertion for the command template so a missing asset fails the release rather than silently installing fewer surfaces.\n\n## Risks / Trade-offs\n\n- [More generated files to preserve safely] → Same marker, digest, confinement, and atomic-write path as skills; conflicts are reported, never overwritten.\n- [Slash commands could drift from the CLI] → Actions are derived from the command catalog introduced by `dashboard-shell-redesign`; a test asserts every generated command names a catalog entry that exists.\n- [A larger skill costs tokens on every agent turn] → The rewrite compresses existing prose to offset the intent table; a test asserts the rendered skill stays under a fixed byte budget.\n- [Copilot prompt front matter differs from Claude&#039;s] → Encoded per surface in the descriptor; unknown hosts are simply not targeted.\n\n## Migration Plan\n\n1. Generalize the installer and add descriptor-driven generation with tests, still installing only skills.\n2. Add the command template and the two command surfaces; extend init/update reporting.\n3. Rewrite the skill template; update packaging verification and docs.\n4. Run the release gate and regenerate local surfaces.\n\nRollback: revert; generated command files are inert Markdown that a later `iris init` no longer refreshes, and users may delete them.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-surface-triggers/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-surface-installer\">1. Surface installer\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Generalize the managed-surface installer over a surface descriptor (template id, target, front matter, body) while preserving marker, digest, confinement, symlink, and atomic-write behavior and the existing result shape.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Add focused tests for descriptor-driven install, refresh, unchanged, edited-digest, unmarked, and escaping-target cases.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-command-surfaces\">2. Command surfaces\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add the packaged \u003ccode>iris-commands\u003c/code> template describing the generated content actions and their one-line bodies.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Generate \u003ccode>.claude/commands/iris/&lt;action&gt;.md\u003c/code> and \u003ccode>.github/prompts/iris-&lt;action&gt;.prompt.md\u003c/code> for each action with host-appropriate front matter; report results from init and update.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add tests that every generated command names an existing catalog command, that unrelated sibling commands are preserved, and that collisions are reported rather than overwritten.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-conversational-skill\">3. Conversational skill\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Rewrite the canonical skill description and body with a when-to-use intent table mapping user intent to command and generated destination, compressing existing prose to hold the size budget.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Add tests asserting the intent table covers every content command and that the rendered skill stays within its byte budget.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Extend release verification to require the command template in the package payload.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Update \u003ccode>README.md\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, and \u003ccode>docs/status.md\u003c/code> for the generated command surfaces and the skill's trigger contract.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Run \u003ccode>openspec validate agent-surface-triggers --strict\u003c/code> and the full release gate; regenerate local agent surfaces.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Surface installer\n\n- [x] 1.1 Generalize the managed-surface installer over a surface descriptor (template id, target, front matter, body) while preserving marker, digest, confinement, symlink, and atomic-write behavior and the existing result shape.\n- [x] 1.2 Add focused tests for descriptor-driven install, refresh, unchanged, edited-digest, unmarked, and escaping-target cases.\n\n## 2. Command surfaces\n\n- [x] 2.1 Add the packaged `iris-commands` template describing the generated content actions and their one-line bodies.\n- [x] 2.2 Generate `.claude/commands/iris/&lt;action&gt;.md` and `.github/prompts/iris-&lt;action&gt;.prompt.md` for each action with host-appropriate front matter; report results from init and update.\n- [x] 2.3 Add tests that every generated command names an existing catalog command, that unrelated sibling commands are preserved, and that collisions are reported rather than overwritten.\n\n## 3. Conversational skill\n\n- [x] 3.1 Rewrite the canonical skill description and body with a when-to-use intent table mapping user intent to command and generated destination, compressing existing prose to hold the size budget.\n- [x] 3.2 Add tests asserting the intent table covers every content command and that the rendered skill stays within its byte budget.\n\n## 4. Verification and documentation\n\n- [x] 4.1 Extend release verification to require the command template in the package payload.\n- [x] 4.2 Update `README.md`, `docs/cmds.md`, and `docs/status.md` for the generated command surfaces and the skill&#039;s trigger contract.\n- [x] 4.3 Run `openspec validate agent-surface-triggers --strict` and the full release gate; regenerate local agent surfaces.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-surface-triggers/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-agent-first-initialization-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · agent-first-initialization\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-agent-surface-triggers/specs/agent-first-initialization/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-agent-first-initialization-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-generated-agent-command-surfaces\">Requirement: generated agent command surfaces\u003c/h3>\n\u003cp>The system MUST generate typed command files for the supported agent surfaces from the same canonical Iris source that produces the skill, covering the frequent content actions, and MUST apply its managed-ownership rules to those files.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-command-surfaces-are-installed\">Scenario: command surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs agent guidance\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST generate one command file per supported content action into the Claude command directory and the GitHub prompt directory, each naming the exact Iris command, the file the agent edits, and the render step\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-generated-command-content-is-refreshed\">Scenario: generated command content is refreshed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a previously generated command file has valid ownership markers and its managed content matches the recorded digest\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-command-file-was-edited-or-is-user-owned\">Scenario: command file was edited or is user-owned\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a target command file is unmarked, partially marked, misordered, edited so its digest no longer matches, symlinked, or outside the repository boundary\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST preserve the file, MUST NOT truncate or follow it, and MUST report the affected surface\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-unrelated-command-content-exists\">Scenario: unrelated command content exists\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a supported command directory already contains unrelated commands or prompts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST leave that content unchanged\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"delta-agent-first-initialization-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-canonical-multi-agent-skill-installation\">Requirement: canonical multi-agent skill installation\u003c/h3>\n\u003cp>The system MUST generate supported agent skill files from one canonical Iris-owned skill source rather than maintaining independent instruction copies, and that source MUST state when to use Iris in conversational terms and map common user intents to the exact command and generated destination.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-supported-skill-surfaces-are-installed\">Scenario: supported skill surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs agent guidance\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST provide the \u003ccode>iris-workspace\u003c/code> skill to the generic/Codex, Claude, and GitHub Copilot repository skill locations\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-unrelated-agent-content-exists\">Scenario: unrelated agent content exists\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a supported skill directory already contains sibling skills or unrelated files\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> initialization MUST leave that content unchanged\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-agent-decides-whether-iris-applies\">Scenario: agent decides whether Iris applies\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an agent reads the installed skill while deciding how to record completed work\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the skill MUST name the situations that call for Iris and map each supported intent to its command and generated output location without requiring the agent to read any other file\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## ADDED Requirements\n\n### Requirement: generated agent command surfaces\n\nThe system MUST generate typed command files for the supported agent surfaces from the same canonical Iris source that produces the skill, covering the frequent content actions, and MUST apply its managed-ownership rules to those files.\n\n#### Scenario: command surfaces are installed\n\n- **WHEN** `iris init` installs agent guidance\n- **THEN** it MUST generate one command file per supported content action into the Claude command directory and the GitHub prompt directory, each naming the exact Iris command, the file the agent edits, and the render step\n\n#### Scenario: generated command content is refreshed\n\n- **WHEN** a previously generated command file has valid ownership markers and its managed content matches the recorded digest\n- **THEN** initialization MUST update only that managed content and preserve user-authored bytes outside the managed boundary\n\n#### Scenario: command file was edited or is user-owned\n\n- **WHEN** a target command file is unmarked, partially marked, misordered, edited so its digest no longer matches, symlinked, or outside the repository boundary\n- **THEN** initialization MUST preserve the file, MUST NOT truncate or follow it, and MUST report the affected surface\n\n#### Scenario: unrelated command content exists\n\n- **WHEN** a supported command directory already contains unrelated commands or prompts\n- **THEN** initialization MUST leave that content unchanged\n\n## MODIFIED Requirements\n\n### Requirement: canonical multi-agent skill installation\n\nThe system MUST generate supported agent skill files from one canonical Iris-owned skill source rather than maintaining independent instruction copies, and that source MUST state when to use Iris in conversational terms and map common user intents to the exact command and generated destination.\n\n#### Scenario: supported skill surfaces are installed\n\n- **WHEN** `iris init` installs agent guidance\n- **THEN** it MUST provide the `iris-workspace` skill to the generic/Codex, Claude, and GitHub Copilot repository skill locations\n\n#### Scenario: unrelated agent content exists\n\n- **WHEN** a supported skill directory already contains sibling skills or unrelated files\n- **THEN** initialization MUST leave that content unchanged\n\n#### Scenario: agent decides whether Iris applies\n\n- **WHEN** an agent reads the installed skill while deciding how to record completed work\n- **THEN** the skill MUST name the situations that call for Iris and map each supported intent to its command and generated output location without requiring the agent to read any other file\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-generalize-the-installer-over-a-surface-descriptor\">Generalize the installer over a surface descriptor\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-one-packaged-command-template-projected-per-action\">One packaged command template, projected per action\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-conversational-skill-trigger\">Conversational skill trigger\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-packaging-and-verification\">Packaging and verification\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-surface-installer\">1. Surface installer\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-command-surfaces\">2. Command surfaces\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-conversational-skill\">3. Conversational skill\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-artifact\">Delta spec · agent-first-initialization\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-generated-agent-command-surfaces\">Requirement: generated agent command surfaces\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-canonical-multi-agent-skill-installation\">Requirement: canonical multi-agent skill installation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-brew-formula-and-installability":{"kind":"change","name":"2026-08-21-brew-formula-and-installability","title":"2026-08-21-brew-formula-and-installability","path":"changes/archive/2026-08-21-brew-formula-and-installability","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-brew-formula-and-installability\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-brew-formula-and-installability\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">9\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-brew-formula-and-installability: 9 of 9 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">9/9 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-brew-formula-and-installability/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The CLI and render pipeline are now working, but the project still lacks a reliable install story for everyday use. A local-first tool is easier to trust when users can install it consistently, verify the command surface quickly, and upgrade without manual repo cloning or shell hacks.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Define a supported install path for the \u003ccode>iris\u003c/code> CLI using Homebrew and/or a standard Node package entrypoint.\u003c/li>\n\u003cli>Package the release metadata so the CLI can be versioned and upgraded predictably.\u003c/li>\n\u003cli>Add a smoke-testable install path that verifies the binary and core render commands work immediately after installation.\u003c/li>\n\u003cli>Keep the install story aligned with the repo's local-first and offline-safe design principles.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>brew-formula-and-installability: establish a distribution path for the CLI that is easy to install, verify, and upgrade.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>None\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>CLI adoption: easier setup for users who want to try or use the tool without a repo checkout.\u003c/li>\n\u003cli>Release quality: versioning and install metadata become explicit and testable.\u003c/li>\n\u003cli>Documentation: users get a clear first-run path and a consistent command reference.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe CLI and render pipeline are now working, but the project still lacks a reliable install story for everyday use. A local-first tool is easier to trust when users can install it consistently, verify the command surface quickly, and upgrade without manual repo cloning or shell hacks.\n\n## What Changes\n\n- Define a supported install path for the `iris` CLI using Homebrew and/or a standard Node package entrypoint.\n- Package the release metadata so the CLI can be versioned and upgraded predictably.\n- Add a smoke-testable install path that verifies the binary and core render commands work immediately after installation.\n- Keep the install story aligned with the repo&#039;s local-first and offline-safe design principles.\n\n## Capabilities\n\n### New Capabilities\n- brew-formula-and-installability: establish a distribution path for the CLI that is easy to install, verify, and upgrade.\n\n### Modified Capabilities\n- None\n\n## Impact\n\n- CLI adoption: easier setup for users who want to try or use the tool without a repo checkout.\n- Release quality: versioning and install metadata become explicit and testable.\n- Documentation: users get a clear first-run path and a consistent command reference.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-brew-formula-and-installability/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The project has evolved from a local CLI plus render-first workflow into a tool that can generate deterministic HTML docs and publish artifacts. The remaining gap is distribution: users still need a clear, repeatable install path that matches the local-first model rather than a hosted service or remote backend.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Provide a stable install flow for the \u003ccode>iris\u003c/code> CLI.\u003c/li>\n\u003cli>Support a Homebrew-friendly equivalent packaging entrypoint for easy setup.\u003c/li>\n\u003cli>Keep the install story simple, offline-safe, and consistent with the Node-based project runtime.\u003c/li>\n\u003cli>Add release validation around the key CLI commands.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>A hosted SaaS runtime or cloud-managed rendering service.\u003c/li>\n\u003cli>Full platform marketing or marketplace expansion.\u003c/li>\n\u003cli>Broad feature work unrelated to installability, release quality, and distribution.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Keep the CLI as the primary product surface: installs should provide the same command interface users already know from the repo checkout.\u003c/li>\n\u003cli>Use npm as the primary distribution path on macOS, Linux, and Windows with Node.js 22.13.0 or newer. The package entrypoint is implemented, cross-platform, versioned, and already exercised from a packed artifact outside the repository.\u003c/li>\n\u003cli>Defer a real Homebrew formula until a published release supplies a stable source tarball URL and SHA-256 checksum. Do not mark formula work complete or ship placeholder metadata before those inputs exist.\u003c/li>\n\u003cli>Publish from GitHub Releases through \u003ccode>.github/workflows/release.yml\u003c/code>. The workflow requires an exact \u003ccode>v&lt;package version&gt;\u003c/code> tag, repeats the full project gate, inspects the package payload, and uses npm trusted publishing with provenance instead of a long-lived automation token.\u003c/li>\n\u003cli>Validate the installation path via a focused smoke test around \u003ccode>iris --help\u003c/code> and a minimal render command.\u003c/li>\n\u003cli>Keep packaging offline-safe and local-first; the installer should not require any remote backend to work.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Install friction] → Mitigation: keep the install syntax minimal and document the supported path clearly.\u003c/li>\n\u003cli>[Version drift] → Mitigation: tie the release and formula metadata to the package version and CI validation.\u003c/li>\n\u003cli>[Packaging complexity] → Mitigation: start with a single, narrow distribution path instead of expanding into a wider ecosystem.\u003c/li>\n\u003cli>[First-release account gate] → Mitigation: document the one-time npm package/trusted-publisher and GitHub environment setup; the repository contains no credentials and does not claim publication before that owner action.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Treat npm as the supported install and upgrade path; retain Node.js 22.13.0 as the minimum runtime.\u003c/li>\n\u003cli>Validate release tags, package metadata, the packed payload, and the installed command before publication.\u003c/li>\n\u003cli>Publish from a GitHub Release using npm trusted publishing and provenance after the owner configures the external trust relationship.\u003c/li>\n\u003cli>Add Homebrew only after a real release URL and checksum can be tested end to end.\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"design-open-questions\">Open Questions\u003c/h2>\n\u003cp>None. npm-first is decided; Homebrew is explicitly deferred on missing release artifacts rather than left ambiguous.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe project has evolved from a local CLI plus render-first workflow into a tool that can generate deterministic HTML docs and publish artifacts. The remaining gap is distribution: users still need a clear, repeatable install path that matches the local-first model rather than a hosted service or remote backend.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Provide a stable install flow for the `iris` CLI.\n- Support a Homebrew-friendly equivalent packaging entrypoint for easy setup.\n- Keep the install story simple, offline-safe, and consistent with the Node-based project runtime.\n- Add release validation around the key CLI commands.\n\n**Non-Goals:**\n- A hosted SaaS runtime or cloud-managed rendering service.\n- Full platform marketing or marketplace expansion.\n- Broad feature work unrelated to installability, release quality, and distribution.\n\n## Decisions\n\n- Keep the CLI as the primary product surface: installs should provide the same command interface users already know from the repo checkout.\n- Use npm as the primary distribution path on macOS, Linux, and Windows with Node.js 22.13.0 or newer. The package entrypoint is implemented, cross-platform, versioned, and already exercised from a packed artifact outside the repository.\n- Defer a real Homebrew formula until a published release supplies a stable source tarball URL and SHA-256 checksum. Do not mark formula work complete or ship placeholder metadata before those inputs exist.\n- Publish from GitHub Releases through `.github/workflows/release.yml`. The workflow requires an exact `v&lt;package version&gt;` tag, repeats the full project gate, inspects the package payload, and uses npm trusted publishing with provenance instead of a long-lived automation token.\n- Validate the installation path via a focused smoke test around `iris --help` and a minimal render command.\n- Keep packaging offline-safe and local-first; the installer should not require any remote backend to work.\n\n## Risks / Trade-offs\n\n- [Install friction] → Mitigation: keep the install syntax minimal and document the supported path clearly.\n- [Version drift] → Mitigation: tie the release and formula metadata to the package version and CI validation.\n- [Packaging complexity] → Mitigation: start with a single, narrow distribution path instead of expanding into a wider ecosystem.\n- [First-release account gate] → Mitigation: document the one-time npm package/trusted-publisher and GitHub environment setup; the repository contains no credentials and does not claim publication before that owner action.\n\n## Migration Plan\n\n1. Treat npm as the supported install and upgrade path; retain Node.js 22.13.0 as the minimum runtime.\n2. Validate release tags, package metadata, the packed payload, and the installed command before publication.\n3. Publish from a GitHub Release using npm trusted publishing and provenance after the owner configures the external trust relationship.\n4. Add Homebrew only after a real release URL and checksum can be tested end to end.\n\n## Open Questions\n\nNone. npm-first is decided; Homebrew is explicitly deferred on missing release artifacts rather than left ambiguous.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-brew-formula-and-installability/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-install-path-and-packaging-definition\">1. Install path and packaging definition\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Decide the primary install story for the CLI and confirm the supported target environments.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Add or formalize the package metadata needed for a repeatable install and upgrade flow.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Confirm the formula / package surface matches the project's local-first design goals.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-distribution-and-smoke-validation\">2. Distribution and smoke validation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add the release packaging or formula definition needed for easy install.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Add a smoke-check command or script that validates the CLI after installation.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Validate the flow against the minimum supported Node environment.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-documentation-and-rollout\">3. Documentation and rollout\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Document the install and upgrade steps in the project docs.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Confirm the CLI documentation reflects the actual installed command surface.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Review the change against the roadmap to keep scope limited to installability and distribution.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Install path and packaging definition\n\n- [x] 1.1 Decide the primary install story for the CLI and confirm the supported target environments.\n- [x] 1.2 Add or formalize the package metadata needed for a repeatable install and upgrade flow.\n- [x] 1.3 Confirm the formula / package surface matches the project&#039;s local-first design goals.\n\n## 2. Distribution and smoke validation\n\n- [x] 2.1 Add the release packaging or formula definition needed for easy install.\n- [x] 2.2 Add a smoke-check command or script that validates the CLI after installation.\n- [x] 2.3 Validate the flow against the minimum supported Node environment.\n\n## 3. Documentation and rollout\n\n- [x] 3.1 Document the install and upgrade steps in the project docs.\n- [x] 3.2 Confirm the CLI documentation reflects the actual installed command surface.\n- [x] 3.3 Review the change against the roadmap to keep scope limited to installability and distribution.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-brew-formula-and-installability/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-18\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-brew-formula-and-installability-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · brew-formula-and-installability\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-brew-formula-and-installability/specs/brew-formula-and-installability/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-brew-formula-and-installability-purpose\">Purpose\u003c/h2>\n\u003cp>Provide a reliable, low-friction installation and upgrade path for the \u003ccode>iris\u003c/code> CLI so the project remains easy to adopt without depending on a repo checkout or manual build steps.\u003c/p>\n\u003ch2 id=\"delta-brew-formula-and-installability-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-brew-formula-and-installability-requirement-supported-cli-installation-path\">Requirement: supported CLI installation path\u003c/h3>\n\u003cp>The system MUST provide a clear installation path for the \u003ccode>iris\u003c/code> command that works across supported environments.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-user-installs-the-cli\">Scenario: user installs the CLI\u003c/h4>\n\u003cp>WHEN a user follows the documented install flow\nTHEN the CLI MUST be available as \u003ccode>iris\u003c/code> and MUST expose the expected command surface.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-install-fails-with-clear-guidance\">Scenario: install fails with clear guidance\u003c/h4>\n\u003cp>WHEN installation prerequisites are missing or unsupported\nTHEN the system MUST fail with a simple, actionable message instead of a vague runtime error.\u003c/p>\n\u003ch3 id=\"delta-brew-formula-and-installability-requirement-versioned-package-distribution\">Requirement: versioned package distribution\u003c/h3>\n\u003cp>The project MUST distribute the CLI through a package entrypoint that installs the same binary and command surface as the repository build on macOS, Linux, and Windows, and MUST NOT publish package-manager metadata for a distribution channel whose release inputs do not yet exist.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-package-install-path-is-available\">Scenario: package install path is available\u003c/h4>\n\u003cp>WHEN a user installs the published package with the documented package manager\nTHEN the command MUST resolve to the same project binary and versioned CLI behavior as the repository build\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-upgrade-path-is-clear\">Scenario: upgrade path is clear\u003c/h4>\n\u003cp>WHEN a user upgrades the installed CLI\nTHEN the system MUST expose a predictable version and install flow that keeps the CLI current\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-a-distribution-channel-lacks-release-inputs\">Scenario: a distribution channel lacks release inputs\u003c/h4>\n\u003cp>WHEN a channel such as a Homebrew formula would require a published release URL and checksum that do not exist yet\nTHEN the project MUST document that channel as unavailable and MUST NOT ship a formula or manifest that claims otherwise\u003c/p>\n\u003ch3 id=\"delta-brew-formula-and-installability-requirement-minimal-install-smoke-validation\">Requirement: minimal install smoke validation\u003c/h3>\n\u003cp>The release workflow MUST include a minimal verification that the installed CLI works for a basic command path.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-install-smoke-test-passes\">Scenario: install smoke test passes\u003c/h4>\n\u003cp>WHEN the package or formula is installed\nTHEN a fast smoke check such as \u003ccode>iris --help\u003c/code> or a minimal render run MUST succeed without additional repo setup.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-invalid-installation-is-surfaced-early\">Scenario: invalid installation is surfaced early\u003c/h4>\n\u003cp>WHEN the install path is broken or incompatible\nTHEN the validation step MUST fail early and identify the missing or unsupported prerequisite.\u003c/p>\n\u003ch3 id=\"delta-brew-formula-and-installability-requirement-documentation-and-upgrade-guidance\">Requirement: documentation and upgrade guidance\u003c/h3>\n\u003cp>The project MUST provide install instructions that align with the local-first design and current project conventions.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-user-can-self-serve-setup\">Scenario: user can self-serve setup\u003c/h4>\n\u003cp>WHEN a new user reads the docs\nTHEN they MUST be able to install and verify the CLI with a single, documented path.\u003c/p>\n\u003ch4 id=\"delta-brew-formula-and-installability-scenario-command-reference-stays-current\">Scenario: command reference stays current\u003c/h4>\n\u003cp>WHEN package distribution changes\nTHEN the install docs and command reference MUST stay aligned with the actual CLI behavior.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nProvide a reliable, low-friction installation and upgrade path for the `iris` CLI so the project remains easy to adopt without depending on a repo checkout or manual build steps.\n\n## ADDED Requirements\n\n### Requirement: supported CLI installation path\nThe system MUST provide a clear installation path for the `iris` command that works across supported environments.\n\n#### Scenario: user installs the CLI\nWHEN a user follows the documented install flow\nTHEN the CLI MUST be available as `iris` and MUST expose the expected command surface.\n\n#### Scenario: install fails with clear guidance\nWHEN installation prerequisites are missing or unsupported\nTHEN the system MUST fail with a simple, actionable message instead of a vague runtime error.\n\n### Requirement: versioned package distribution\nThe project MUST distribute the CLI through a package entrypoint that installs the same binary and command surface as the repository build on macOS, Linux, and Windows, and MUST NOT publish package-manager metadata for a distribution channel whose release inputs do not yet exist.\n\n#### Scenario: package install path is available\nWHEN a user installs the published package with the documented package manager\nTHEN the command MUST resolve to the same project binary and versioned CLI behavior as the repository build\n\n#### Scenario: upgrade path is clear\nWHEN a user upgrades the installed CLI\nTHEN the system MUST expose a predictable version and install flow that keeps the CLI current\n\n#### Scenario: a distribution channel lacks release inputs\nWHEN a channel such as a Homebrew formula would require a published release URL and checksum that do not exist yet\nTHEN the project MUST document that channel as unavailable and MUST NOT ship a formula or manifest that claims otherwise\n\n### Requirement: minimal install smoke validation\nThe release workflow MUST include a minimal verification that the installed CLI works for a basic command path.\n\n#### Scenario: install smoke test passes\nWHEN the package or formula is installed\nTHEN a fast smoke check such as `iris --help` or a minimal render run MUST succeed without additional repo setup.\n\n#### Scenario: invalid installation is surfaced early\nWHEN the install path is broken or incompatible\nTHEN the validation step MUST fail early and identify the missing or unsupported prerequisite.\n\n### Requirement: documentation and upgrade guidance\nThe project MUST provide install instructions that align with the local-first design and current project conventions.\n\n#### Scenario: user can self-serve setup\nWHEN a new user reads the docs\nTHEN they MUST be able to install and verify the CLI with a single, documented path.\n\n#### Scenario: command reference stays current\nWHEN package distribution changes\nTHEN the install docs and command reference MUST stay aligned with the actual CLI behavior.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-open-questions\">Open Questions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-install-path-and-packaging-definition\">1. Install path and packaging definition\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-distribution-and-smoke-validation\">2. Distribution and smoke validation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-documentation-and-rollout\">3. Documentation and rollout\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-brew-formula-and-installability-artifact\">Delta spec · brew-formula-and-installability\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-brew-formula-and-installability-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-brew-formula-and-installability-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-brew-formula-and-installability-requirement-supported-cli-installation-path\">Requirement: supported CLI installation path\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-brew-formula-and-installability-requirement-versioned-package-distribution\">Requirement: versioned package distribution\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-brew-formula-and-installability-requirement-minimal-install-smoke-validation\">Requirement: minimal install smoke validation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-brew-formula-and-installability-requirement-documentation-and-upgrade-guidance\">Requirement: documentation and upgrade guidance\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-dashboard-shell-redesign":{"kind":"change","name":"2026-08-21-dashboard-shell-redesign","title":"2026-08-21-dashboard-shell-redesign","path":"changes/archive/2026-08-21-dashboard-shell-redesign","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-dashboard-shell-redesign\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-dashboard-shell-redesign\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">18\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-dashboard-shell-redesign: 18 of 18 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">18/18 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-dashboard-shell-redesign/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The dashboard packs every surface (briefing, health strip, architecture, Work browser, Spec browser, project docs) into one tabbed \u003ccode>index.html\u003c/code> that is already 5,800 lines with zero pages; as OpenSpec, command, and research content grows it becomes slow to scan, expensive for agents to read, and impossible to deep-link by section. Users want Jira/Trello-style organization: a persistent navigation shell, one page per section with its own summary strip, and a cleaner product palette.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Replace the single tabbed dashboard with a multi-page workspace: \u003ccode>iris/index.html\u003c/code> (Overview), \u003ccode>iris/work.html\u003c/code> (Work), \u003ccode>iris/spec.html\u003c/code> (Spec), \u003ccode>iris/commands.html\u003c/code> (Commands), plus the existing contract pages and project docs, all sharing one generated navigation shell (collapsible sidebar + top bar).\u003c/li>\n\u003cli>Introduce Aperture 3 &quot;Electra&quot; tokens: cooler neutral surfaces, an electric indigo interactive accent, a white sidebar in the light theme, and light as a first-class peer of dark. The type, status, and priority spectrum is retained; a \u003ccode>research\u003c/code> type color is reserved for the follow-up research capability.\u003c/li>\n\u003cli>Add a generated Commands page driven by one command catalog (purpose groups, synopsis, usage, explicit status: implemented / partial / stubbed). \u003ccode>iris --help\u003c/code> is generated from the same catalog.\u003c/li>\n\u003cli>Every section page opens with its own summary strip. Overview shows cross-section summaries (briefing, counts, recent work, active spec movement, quick-start commands) and links out instead of embedding each section's full content.\u003c/li>\n\u003cli>Navigation shell: sidebar collapses to a rail (control + \u003ccode>b\u003c/code> key, persisted locally), top bar carries breadcrumb, filter when relevant, and theme toggle; at narrow widths the sections sit behind a menu control. The shell is stripped from published and exported artifacts.\u003c/li>\n\u003cli>\u003cstrong>BREAKING\u003c/strong> (generated output only): Work and Spec leave \u003ccode>index.html\u003c/code> for their own pages, and the managed \u003ccode>iris/project/commands.html\u003c/code> placeholder is retired in favor of \u003ccode>iris/commands.html\u003c/code>. Contracts, \u003ccode>state.json\u003c/code>, schemas, and CLI commands are unchanged; every page stays reachable from the dashboard and links back to it.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cp>None.\u003c/p>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: token contract gains navigation-shell tokens and the Electra palette; component language gains the navigation shell, page header, summary strip, progress bar, and command card; the dashboard hierarchy becomes a multi-page workspace with an Overview page; deterministic interaction gains sidebar collapse, the \u003ccode>b\u003c/code> shortcut, cross-page theme persistence, and a narrow-width menu; new requirements cover the navigation shell, the generated command reference, and per-section summary strips.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>\u003ccode>src/templates/design.ts\u003c/code> is split into tokens, shell, component CSS, classic script, and per-page generators; \u003ccode>src/commands/render.ts\u003c/code> writes the additional section pages; \u003ccode>src/commands/lifecycle.ts\u003c/code> retires the commands placeholder safely; \u003ccode>src/cli.ts\u003c/code> derives help from a new \u003ccode>src/lib/command-catalog.ts\u003c/code>.\u003c/li>\n\u003cli>\u003ccode>publish\u003c/code>/\u003ccode>export\u003c/code> strip the shell; \u003ccode>html-check\u003c/code> covers the new cross-page links; \u003ccode>token-lint\u003c/code>/\u003ccode>token-contract\u003c/code> validate the new token pairs.\u003c/li>\n\u003cli>Tests touching \u003ccode>index.html\u003c/code> structure (\u003ccode>html-navigation\u003c/code>, \u003ccode>work-board\u003c/code>, \u003ccode>openspec-browser\u003c/code>, \u003ccode>cli-help\u003c/code>, \u003ccode>publish-export\u003c/code>, \u003ccode>token-contract\u003c/code>) are updated; shell, overview, and commands-page tests are added.\u003c/li>\n\u003cli>Docs (\u003ccode>README\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/design-system.md\u003c/code>, \u003ccode>docs/status.md\u003c/code>, the canonical skill template's dashboard mention) and the checked-in \u003ccode>iris/\u003c/code> dogfood output are regenerated. No new runtime dependency, framework, network request, or server.\u003c/li>\n\u003cli>Sequencing: \u003ccode>redesign-work-board\u003c/code> modifies the same Aperture requirements and must be archived before this change so the delta applies on top of it.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe dashboard packs every surface (briefing, health strip, architecture, Work browser, Spec browser, project docs) into one tabbed `index.html` that is already 5,800 lines with zero pages; as OpenSpec, command, and research content grows it becomes slow to scan, expensive for agents to read, and impossible to deep-link by section. Users want Jira/Trello-style organization: a persistent navigation shell, one page per section with its own summary strip, and a cleaner product palette.\n\n## What Changes\n\n- Replace the single tabbed dashboard with a multi-page workspace: `iris/index.html` (Overview), `iris/work.html` (Work), `iris/spec.html` (Spec), `iris/commands.html` (Commands), plus the existing contract pages and project docs, all sharing one generated navigation shell (collapsible sidebar + top bar).\n- Introduce Aperture 3 &quot;Electra&quot; tokens: cooler neutral surfaces, an electric indigo interactive accent, a white sidebar in the light theme, and light as a first-class peer of dark. The type, status, and priority spectrum is retained; a `research` type color is reserved for the follow-up research capability.\n- Add a generated Commands page driven by one command catalog (purpose groups, synopsis, usage, explicit status: implemented / partial / stubbed). `iris --help` is generated from the same catalog.\n- Every section page opens with its own summary strip. Overview shows cross-section summaries (briefing, counts, recent work, active spec movement, quick-start commands) and links out instead of embedding each section&#039;s full content.\n- Navigation shell: sidebar collapses to a rail (control + `b` key, persisted locally), top bar carries breadcrumb, filter when relevant, and theme toggle; at narrow widths the sections sit behind a menu control. The shell is stripped from published and exported artifacts.\n- **BREAKING** (generated output only): Work and Spec leave `index.html` for their own pages, and the managed `iris/project/commands.html` placeholder is retired in favor of `iris/commands.html`. Contracts, `state.json`, schemas, and CLI commands are unchanged; every page stays reachable from the dashboard and links back to it.\n\n## Capabilities\n\n### New Capabilities\n\nNone.\n\n### Modified Capabilities\n\n- `aperture-design-system`: token contract gains navigation-shell tokens and the Electra palette; component language gains the navigation shell, page header, summary strip, progress bar, and command card; the dashboard hierarchy becomes a multi-page workspace with an Overview page; deterministic interaction gains sidebar collapse, the `b` shortcut, cross-page theme persistence, and a narrow-width menu; new requirements cover the navigation shell, the generated command reference, and per-section summary strips.\n\n## Impact\n\n- `src/templates/design.ts` is split into tokens, shell, component CSS, classic script, and per-page generators; `src/commands/render.ts` writes the additional section pages; `src/commands/lifecycle.ts` retires the commands placeholder safely; `src/cli.ts` derives help from a new `src/lib/command-catalog.ts`.\n- `publish`/`export` strip the shell; `html-check` covers the new cross-page links; `token-lint`/`token-contract` validate the new token pairs.\n- Tests touching `index.html` structure (`html-navigation`, `work-board`, `openspec-browser`, `cli-help`, `publish-export`, `token-contract`) are updated; shell, overview, and commands-page tests are added.\n- Docs (`README`, `docs/cmds.md`, `docs/design-system.md`, `docs/status.md`, the canonical skill template&#039;s dashboard mention) and the checked-in `iris/` dogfood output are regenerated. No new runtime dependency, framework, network request, or server.\n- Sequencing: `redesign-work-board` modifies the same Aperture requirements and must be archived before this change so the delta applies on top of it.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-dashboard-shell-redesign/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See proposal.md for motivation. Today \u003ccode>src/templates/design.ts\u003c/code> (1,522 lines) owns tokens, component CSS, the classic script, contract-page templates, and one \u003ccode>dashboardHtml()\u003c/code> that emits a single \u003ccode>index.html\u003c/code> with Work/Spec tabs. \u003ccode>refreshDashboard()\u003c/code> in \u003ccode>src/commands/render.ts\u003c/code> builds the \u003ccode>DashboardPage[]\u003c/code> projection and writes that one file. \u003ccode>redesign-work-board\u003c/code> (complete, not yet archived) already delivered the dense List/Table/Kanban browser and drawer inside that page.\u003c/p>\n\u003cp>Hard constraints that shape the approach: deterministic static HTML, opens from \u003ccode>file://\u003c/code>, classic deferred scripts only, no runtime framework or new dependency, token-only CSS enforced by \u003ccode>token-lint\u003c/code>/\u003ccode>token-contract\u003c/code>, generated link integrity enforced by \u003ccode>html-check\u003c/code>, and \u003ccode>publish\u003c/code>/\u003ccode>export --single\u003c/code> must stay self-contained with nav chrome stripped via \u003ccode>data-iris-nav\u003c/code>.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Each section is its own small HTML file with its own summary strip; Overview links out instead of embedding.\u003c/li>\n\u003cli>One shared shell generator so sidebar, top bar, breadcrumb, and theme behave identically on every page, including contract pages and project docs.\u003c/li>\n\u003cli>A cleaner &quot;Electra&quot; palette that still passes the contrast validator in both themes.\u003c/li>\n\u003cli>One command catalog that feeds \u003ccode>--help\u003c/code>, the Commands page, and (later) docs.\u003c/li>\n\u003cli>Smaller per-page HTML so agents that read generated output spend fewer tokens.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Research/Markdown pages (separate change \u003ccode>research-markdown-pages\u003c/code>, which adds its own nav entry).\u003c/li>\n\u003cli>Search across pages, saved filters, drag-and-drop, persistence beyond theme and sidebar state.\u003c/li>\n\u003cli>Architecture projection and chart blocks (still roadmap step 5).\u003c/li>\n\u003cli>Changing contracts, schemas, \u003ccode>state.json\u003c/code> shape, or CLI command names.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-multi-page-static-output-not-a-hash-routed-single-page\">Multi-page static output, not a hash-routed single page\u003c/h3>\n\u003cp>Each section is a separate generated file (\u003ccode>index.html\u003c/code>, \u003ccode>work.html\u003c/code>, \u003ccode>spec.html\u003c/code>, \u003ccode>commands.html\u003c/code>). A hash-routed shell in one file was rejected: it reproduces the &quot;everything on the home page&quot; problem the user named, keeps one enormous file that agents must read whole, and complicates no-JS behavior and per-section publish.\u003c/p>\n\u003ch3 id=\"design-one-rendershell-wraps-every-page\">One \u003ccode>renderShell()\u003c/code> wraps every page\u003c/h3>\n\u003cp>A single function takes \u003ccode>{ current, depth, title, crumbs, topbarSlot, content }\u003c/code> and returns the full document. \u003ccode>depth\u003c/code> drives relative asset and navigation paths (\u003ccode>./\u003c/code> at root, \u003ccode>../\u003c/code> for \u003ccode>project/\u003c/code>, \u003ccode>../../\u003c/code> for \u003ccode>pages/&lt;id&gt;/\u003c/code> and \u003ccode>archive/&lt;id&gt;/\u003c/code>). The sidebar and top bar are marked \u003ccode>data-iris-nav\u003c/code> so the existing publish stripper removes them; the published page body keeps its own heading and back link. Contract pages (\u003ccode>renderPageShell\u003c/code>) and project placeholders adopt the same shell so the app feels continuous.\u003c/p>\n\u003cp>Alternative: keep contract pages on the old header-only layout. Rejected — inconsistent chrome is exactly what makes the current output feel assembled rather than designed.\u003c/p>\n\u003ch3 id=\"design-split-design-ts-into-focused-modules\">Split \u003ccode>design.ts\u003c/code> into focused modules\u003c/h3>\n\u003cp>\u003ccode>src/templates/tokens.ts\u003c/code> (TOKENS_CSS), \u003ccode>shell.ts\u003c/code> (shell markup + nav model), \u003ccode>styles.ts\u003c/code> (component CSS), \u003ccode>script.ts\u003c/code> (classic JS), \u003ccode>pages/overview.ts\u003c/code>, \u003ccode>pages/work.ts\u003c/code>, \u003ccode>pages/spec.ts\u003c/code>, \u003ccode>pages/commands.ts\u003c/code>, \u003ccode>contract-page.ts\u003c/code>. \u003ccode>design.ts\u003c/code> becomes a thin re-export barrel so existing imports and tests keep working. \u003ccode>token-lint\u003c/code> is pointed at \u003ccode>tokens.ts\u003c/code>. This is the targeted cleanup the redesign needs; no unrelated refactor.\u003c/p>\n\u003ch3 id=\"design-electra-palette-aperture-3\">Electra palette (Aperture 3)\u003c/h3>\n\u003cp>Dark stays the default; light becomes a true peer with a white sidebar on a cool gray ground (the Jira/Trello convention). Interactive accent moves from amber to electric indigo; amber is retained only as the \u003ccode>plan\u003c/code> type color. New tokens: \u003ccode>--nav-bg\u003c/code>, \u003ccode>--nav-text\u003c/code>, \u003ccode>--nav-active-bg\u003c/code>, \u003ccode>--nav-active-text\u003c/code>, \u003ccode>--accent-soft\u003c/code>, \u003ccode>--type-research\u003c/code>, \u003ccode>--type-research-soft\u003c/code>. Final literals are fixed in \u003ccode>tokens.ts\u003c/code> and validated by \u003ccode>token-contract\u003c/code>; the intended values are:\u003c/p>\n\u003ctable>\n\u003cthead>\n\u003ctr>\n\u003cth>Token\u003c/th>\n\u003cth>Dark\u003c/th>\n\u003cth>Light\u003c/th>\n\u003c/tr>\n\u003c/thead>\n\u003ctbody>\n\u003ctr>\n\u003ctd>\u003ccode>--bg\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#0e1117\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#f4f5f7\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--surface-1\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#151923\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#ffffff\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--surface-2\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#1b2030\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#f1f2f4\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--surface-3\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#242a3b\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#e4e6ea\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--line-1\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#2a3143\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#dfe1e6\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--text-1\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#e7eaf2\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#172b4d\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--text-2\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#a4adc2\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#44546f\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--text-3\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#7a8399\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#626f86\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--accent\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#6f8cff\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#3b5bdb\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--accent-text\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#93a8ff\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#2f4ac0\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--accent-ink\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#0b0e14\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#ffffff\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--nav-bg\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#0a0d13\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#ffffff\u003c/code>\u003c/td>\n\u003c/tr>\n\u003ctr>\n\u003ctd>\u003ccode>--type-research\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#2dd4bf\u003c/code>\u003c/td>\n\u003ctd>\u003ccode>#0f766e\u003c/code>\u003c/td>\n\u003c/tr>\n\u003c/tbody>\n\u003c/table>\n\u003cp>\u003ccode>iris/config.yaml\u003c/code> already carries \u003ccode>theme:\u003c/code>; generated pages emit \u003ccode>data-theme\u003c/code> from it as the initial theme, and the existing \u003ccode>localStorage\u003c/code> toggle still overrides per browser.\u003c/p>\n\u003ch3 id=\"design-command-catalog-as-the-single-source\">Command catalog as the single source\u003c/h3>\n\u003cp>\u003ccode>src/lib/command-catalog.ts\u003c/code> exports groups (\u003ccode>setup\u003c/code>, \u003ccode>content\u003c/code>, \u003ccode>render\u003c/code>, \u003ccode>lifecycle\u003c/code>, \u003ccode>share\u003c/code>) of entries \u003ccode>{ name, usage, synopsis, flags, status }\u003c/code> with \u003ccode>status ∈ implemented | partial | stubbed\u003c/code>. \u003ccode>HELP_TEXT\u003c/code> is generated from it (grouped), \u003ccode>commands.html\u003c/code> renders one card per entry with a textual status chip, and the Overview quick-start reads the \u003ccode>content\u003c/code> group. \u003ccode>docs/cmds.md\u003c/code> stays the long-form reference.\u003c/p>\n\u003ch3 id=\"design-overview-composition\">Overview composition\u003c/h3>\n\u003cp>Briefing hero (aperture ring + repo name + one line + quick-start commands) → four summary tiles linking to Work, Spec, Commands, project docs → two columns: recent work (five most recently updated records by ISO \u003ccode>updated\u003c/code>, &quot;not set&quot; last) and spec movement (active changes with a task progress bar) → compact architecture pane (existing empty state) → project-docs strip. Every block ends in a link to its section page.\u003c/p>\n\u003ch3 id=\"design-work-and-spec-relocate-unchanged\">Work and Spec relocate unchanged\u003c/h3>\n\u003cp>\u003ccode>work.html\u003c/code> hosts exactly the \u003ccode>redesign-work-board\u003c/code> browser, toolbar, and drawer; \u003ccode>spec.html\u003c/code> hosts the existing \u003ccode>specView()\u003c/code>. Their stat strips come from the same projections. Only the wrapper and relative paths change, which keeps the drawer/keyboard test surface stable.\u003c/p>\n\u003ch3 id=\"design-sidebar-state-and-theme-persistence\">Sidebar state and theme persistence\u003c/h3>\n\u003cp>\u003ccode>iris-nav\u003c/code> (\u003ccode>expanded|collapsed\u003c/code>) and the existing \u003ccode>iris-theme\u003c/code> keys in \u003ccode>localStorage\u003c/code>. Browsers differ on whether \u003ccode>file://\u003c/code> pages in different directories share storage; the default (expanded, config theme) is always correct, so persistence is best-effort and never required for correctness. At \u003ccode>≤ 40rem\u003c/code> the sidebar becomes an overlay opened by a top-bar menu button; without JavaScript it renders expanded above the content.\u003c/p>\n\u003ch3 id=\"design-retiring-the-commands-placeholder\">Retiring the commands placeholder\u003c/h3>\n\u003cp>\u003ccode>PROJECT_DOC_NAMES\u003c/code> drops \u003ccode>commands\u003c/code>; \u003ccode>refreshProjectPlaceholders\u003c/code> removes \u003ccode>iris/project/commands.html\u003c/code> only when the file carries \u003ccode>data-iris-managed\u003c/code> (same ownership rule it already uses to refresh), otherwise leaves it and reports it.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Tests assert on the single-page structure] → Update \u003ccode>html-navigation\u003c/code>, \u003ccode>work-board\u003c/code>, \u003ccode>openspec-browser\u003c/code>, \u003ccode>cli-help\u003c/code>, \u003ccode>publish-export\u003c/code>, \u003ccode>token-contract\u003c/code>; add shell/overview/commands tests. Work-board drawer tests move to \u003ccode>work.html\u003c/code> with no behavioral change.\u003c/li>\n\u003cli>[More files to keep linked] → \u003ccode>html-check\u003c/code> already walks every generated file; add a test that every nav entry resolves at every depth.\u003c/li>\n\u003cli>[Publish strips the shell but page loses context] → Published body keeps its H1, type/status, and a plain back link (already the \u003ccode>data-iris-nav\u003c/code> pattern).\u003c/li>\n\u003cli>[Cross-page storage on \u003ccode>file://\u003c/code> varies by browser] → Correct defaults without storage; document as best-effort.\u003c/li>\n\u003cli>[Palette change invalidates screenshots/docs] → Regenerate dogfood and update \u003ccode>docs/design-system.md\u003c/code> tokens table in the same change.\u003c/li>\n\u003cli>[Aperture delta collides with \u003ccode>redesign-work-board\u003c/code>] → Archive that change first; this delta's MODIFIED blocks are written on top of its text.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Archive \u003ccode>redesign-work-board\u003c/code> (complete) so \u003ccode>openspec/specs/aperture-design-system\u003c/code> contains its requirements.\u003c/li>\n\u003cli>Land tokens + module split with the existing single page still rendering (tests green).\u003c/li>\n\u003cli>Add the shell and section pages; switch \u003ccode>refreshDashboard\u003c/code> to write all section files; retire the commands placeholder.\u003c/li>\n\u003cli>Update tests, regenerate \u003ccode>iris/\u003c/code>, run the release gate, update docs.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback: revert the change; \u003ccode>iris init\u003c/code>/\u003ccode>iris update\u003c/code> rewrite managed surfaces, and stale section files are harmless static HTML that the next render overwrites.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee proposal.md for motivation. Today `src/templates/design.ts` (1,522 lines) owns tokens, component CSS, the classic script, contract-page templates, and one `dashboardHtml()` that emits a single `index.html` with Work/Spec tabs. `refreshDashboard()` in `src/commands/render.ts` builds the `DashboardPage[]` projection and writes that one file. `redesign-work-board` (complete, not yet archived) already delivered the dense List/Table/Kanban browser and drawer inside that page.\n\nHard constraints that shape the approach: deterministic static HTML, opens from `file://`, classic deferred scripts only, no runtime framework or new dependency, token-only CSS enforced by `token-lint`/`token-contract`, generated link integrity enforced by `html-check`, and `publish`/`export --single` must stay self-contained with nav chrome stripped via `data-iris-nav`.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Each section is its own small HTML file with its own summary strip; Overview links out instead of embedding.\n- One shared shell generator so sidebar, top bar, breadcrumb, and theme behave identically on every page, including contract pages and project docs.\n- A cleaner &quot;Electra&quot; palette that still passes the contrast validator in both themes.\n- One command catalog that feeds `--help`, the Commands page, and (later) docs.\n- Smaller per-page HTML so agents that read generated output spend fewer tokens.\n\n**Non-Goals:**\n\n- Research/Markdown pages (separate change `research-markdown-pages`, which adds its own nav entry).\n- Search across pages, saved filters, drag-and-drop, persistence beyond theme and sidebar state.\n- Architecture projection and chart blocks (still roadmap step 5).\n- Changing contracts, schemas, `state.json` shape, or CLI command names.\n\n## Decisions\n\n### Multi-page static output, not a hash-routed single page\n\nEach section is a separate generated file (`index.html`, `work.html`, `spec.html`, `commands.html`). A hash-routed shell in one file was rejected: it reproduces the &quot;everything on the home page&quot; problem the user named, keeps one enormous file that agents must read whole, and complicates no-JS behavior and per-section publish.\n\n### One `renderShell()` wraps every page\n\nA single function takes `{ current, depth, title, crumbs, topbarSlot, content }` and returns the full document. `depth` drives relative asset and navigation paths (`./` at root, `../` for `project/`, `../../` for `pages/&lt;id&gt;/` and `archive/&lt;id&gt;/`). The sidebar and top bar are marked `data-iris-nav` so the existing publish stripper removes them; the published page body keeps its own heading and back link. Contract pages (`renderPageShell`) and project placeholders adopt the same shell so the app feels continuous.\n\nAlternative: keep contract pages on the old header-only layout. Rejected — inconsistent chrome is exactly what makes the current output feel assembled rather than designed.\n\n### Split `design.ts` into focused modules\n\n`src/templates/tokens.ts` (TOKENS_CSS), `shell.ts` (shell markup + nav model), `styles.ts` (component CSS), `script.ts` (classic JS), `pages/overview.ts`, `pages/work.ts`, `pages/spec.ts`, `pages/commands.ts`, `contract-page.ts`. `design.ts` becomes a thin re-export barrel so existing imports and tests keep working. `token-lint` is pointed at `tokens.ts`. This is the targeted cleanup the redesign needs; no unrelated refactor.\n\n### Electra palette (Aperture 3)\n\nDark stays the default; light becomes a true peer with a white sidebar on a cool gray ground (the Jira/Trello convention). Interactive accent moves from amber to electric indigo; amber is retained only as the `plan` type color. New tokens: `--nav-bg`, `--nav-text`, `--nav-active-bg`, `--nav-active-text`, `--accent-soft`, `--type-research`, `--type-research-soft`. Final literals are fixed in `tokens.ts` and validated by `token-contract`; the intended values are:\n\n| Token             | Dark      | Light     |\n| ----------------- | --------- | --------- |\n| `--bg`            | `#0e1117` | `#f4f5f7` |\n| `--surface-1`     | `#151923` | `#ffffff` |\n| `--surface-2`     | `#1b2030` | `#f1f2f4` |\n| `--surface-3`     | `#242a3b` | `#e4e6ea` |\n| `--line-1`        | `#2a3143` | `#dfe1e6` |\n| `--text-1`        | `#e7eaf2` | `#172b4d` |\n| `--text-2`        | `#a4adc2` | `#44546f` |\n| `--text-3`        | `#7a8399` | `#626f86` |\n| `--accent`        | `#6f8cff` | `#3b5bdb` |\n| `--accent-text`   | `#93a8ff` | `#2f4ac0` |\n| `--accent-ink`    | `#0b0e14` | `#ffffff` |\n| `--nav-bg`        | `#0a0d13` | `#ffffff` |\n| `--type-research` | `#2dd4bf` | `#0f766e` |\n\n`iris/config.yaml` already carries `theme:`; generated pages emit `data-theme` from it as the initial theme, and the existing `localStorage` toggle still overrides per browser.\n\n### Command catalog as the single source\n\n`src/lib/command-catalog.ts` exports groups (`setup`, `content`, `render`, `lifecycle`, `share`) of entries `{ name, usage, synopsis, flags, status }` with `status ∈ implemented | partial | stubbed`. `HELP_TEXT` is generated from it (grouped), `commands.html` renders one card per entry with a textual status chip, and the Overview quick-start reads the `content` group. `docs/cmds.md` stays the long-form reference.\n\n### Overview composition\n\nBriefing hero (aperture ring + repo name + one line + quick-start commands) → four summary tiles linking to Work, Spec, Commands, project docs → two columns: recent work (five most recently updated records by ISO `updated`, &quot;not set&quot; last) and spec movement (active changes with a task progress bar) → compact architecture pane (existing empty state) → project-docs strip. Every block ends in a link to its section page.\n\n### Work and Spec relocate unchanged\n\n`work.html` hosts exactly the `redesign-work-board` browser, toolbar, and drawer; `spec.html` hosts the existing `specView()`. Their stat strips come from the same projections. Only the wrapper and relative paths change, which keeps the drawer/keyboard test surface stable.\n\n### Sidebar state and theme persistence\n\n`iris-nav` (`expanded|collapsed`) and the existing `iris-theme` keys in `localStorage`. Browsers differ on whether `file://` pages in different directories share storage; the default (expanded, config theme) is always correct, so persistence is best-effort and never required for correctness. At `≤ 40rem` the sidebar becomes an overlay opened by a top-bar menu button; without JavaScript it renders expanded above the content.\n\n### Retiring the commands placeholder\n\n`PROJECT_DOC_NAMES` drops `commands`; `refreshProjectPlaceholders` removes `iris/project/commands.html` only when the file carries `data-iris-managed` (same ownership rule it already uses to refresh), otherwise leaves it and reports it.\n\n## Risks / Trade-offs\n\n- [Tests assert on the single-page structure] → Update `html-navigation`, `work-board`, `openspec-browser`, `cli-help`, `publish-export`, `token-contract`; add shell/overview/commands tests. Work-board drawer tests move to `work.html` with no behavioral change.\n- [More files to keep linked] → `html-check` already walks every generated file; add a test that every nav entry resolves at every depth.\n- [Publish strips the shell but page loses context] → Published body keeps its H1, type/status, and a plain back link (already the `data-iris-nav` pattern).\n- [Cross-page storage on `file://` varies by browser] → Correct defaults without storage; document as best-effort.\n- [Palette change invalidates screenshots/docs] → Regenerate dogfood and update `docs/design-system.md` tokens table in the same change.\n- [Aperture delta collides with `redesign-work-board`] → Archive that change first; this delta&#039;s MODIFIED blocks are written on top of its text.\n\n## Migration Plan\n\n1. Archive `redesign-work-board` (complete) so `openspec/specs/aperture-design-system` contains its requirements.\n2. Land tokens + module split with the existing single page still rendering (tests green).\n3. Add the shell and section pages; switch `refreshDashboard` to write all section files; retire the commands placeholder.\n4. Update tests, regenerate `iris/`, run the release gate, update docs.\n\nRollback: revert the change; `iris init`/`iris update` rewrite managed surfaces, and stale section files are harmless static HTML that the next render overwrites.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-dashboard-shell-redesign/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-foundations\">1. Foundations\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Archive the completed \u003ccode>redesign-work-board\u003c/code> change so the Aperture delta in this change applies on top of its requirements.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Split \u003ccode>src/templates/design.ts\u003c/code> into \u003ccode>tokens.ts\u003c/code>, \u003ccode>shell.ts\u003c/code>, \u003ccode>styles.ts\u003c/code>, \u003ccode>script.ts\u003c/code>, \u003ccode>contract-page.ts\u003c/code>, and \u003ccode>pages/*.ts\u003c/code>, keeping \u003ccode>design.ts\u003c/code> as a re-export barrel; point \u003ccode>token-lint\u003c/code> at the new tokens module.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Replace the token values with the Electra palette, add navigation-shell, accent-soft, and research type tokens in both themes, extend \u003ccode>token-contract\u003c/code> pairs, and make \u003ccode>token-lint\u003c/code> pass.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.4 Emit the initial \u003ccode>data-theme\u003c/code> from \u003ccode>iris/config.yaml\u003c/code> \u003ccode>theme:\u003c/code> on every generated page while keeping the local-storage toggle override.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-navigation-shell\">2. Navigation shell\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Implement \u003ccode>renderShell()\u003c/code> with the nav model (Overview, Work, Spec, Commands, project docs group), depth-aware relative paths, current-section marking, breadcrumb, top-bar slot, and \u003ccode>data-iris-nav\u003c/code> marking.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Add shell CSS: sidebar, collapsed rail, top bar, page header, summary strip tiles, progress bar, command card, narrow-width overlay, print, reduced motion.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add classic-script behavior: collapse control, \u003ccode>b\u003c/code> shortcut, \u003ccode>iris-nav\u003c/code> persistence, narrow-width menu toggle, focus management; keep \u003ccode>/\u003c/code> and \u003ccode>t\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Adopt the shell in contract pages and project placeholders and verify publish/export strip it while the body keeps a heading and back link.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-section-pages\">3. Section pages\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Add \u003ccode>src/lib/command-catalog.ts\u003c/code> with grouped entries and statuses; generate \u003ccode>HELP_TEXT\u003c/code> from it and update the help test.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Generate \u003ccode>iris/commands.html\u003c/code> from the catalog with grouped command cards and textual status chips.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Move the Work browser, toolbar, and drawer to \u003ccode>iris/work.html\u003c/code> with a Work summary strip; keep behavior and data attributes unchanged.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.4 Move the Spec view to \u003ccode>iris/spec.html\u003c/code> with the existing overview counts as its summary strip.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.5 Generate the Overview at \u003ccode>iris/index.html\u003c/code>: briefing hero with quick-start commands, four section tiles, recent work, spec movement with progress bars, architecture pane, project-docs strip, all linking to section pages.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.6 Retire \u003ccode>iris/project/commands.html\u003c/code> from \u003ccode>PROJECT_DOC_NAMES\u003c/code>, remove it during init/update only when it carries the managed marker, and report unmanaged copies.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Update \u003ccode>html-navigation\u003c/code>, \u003ccode>work-board\u003c/code>, \u003ccode>openspec-browser\u003c/code>, \u003ccode>cli-help\u003c/code>, \u003ccode>publish-export\u003c/code>, and \u003ccode>token-contract\u003c/code> tests for the new page set; add tests for shell links at every depth, collapsed-state markup, commands-page statuses, overview summaries, and empty states.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Regenerate the checked-in \u003ccode>iris/\u003c/code> output, run \u003ccode>pnpm html-check\u003c/code>, and inspect Overview, Work, Spec, Commands, and one contract page at desktop and 360 px in both themes.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Update \u003ccode>README.md\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/design-system.md\u003c/code> (tokens, IA, component inventory), \u003ccode>docs/status.md\u003c/code>, and the canonical skill template's dashboard sentence.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 Run \u003ccode>openspec validate dashboard-shell-redesign --strict\u003c/code> and the full release gate; reconcile task evidence with the implementation.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Foundations\n\n- [x] 1.1 Archive the completed `redesign-work-board` change so the Aperture delta in this change applies on top of its requirements.\n- [x] 1.2 Split `src/templates/design.ts` into `tokens.ts`, `shell.ts`, `styles.ts`, `script.ts`, `contract-page.ts`, and `pages/*.ts`, keeping `design.ts` as a re-export barrel; point `token-lint` at the new tokens module.\n- [x] 1.3 Replace the token values with the Electra palette, add navigation-shell, accent-soft, and research type tokens in both themes, extend `token-contract` pairs, and make `token-lint` pass.\n- [x] 1.4 Emit the initial `data-theme` from `iris/config.yaml` `theme:` on every generated page while keeping the local-storage toggle override.\n\n## 2. Navigation shell\n\n- [x] 2.1 Implement `renderShell()` with the nav model (Overview, Work, Spec, Commands, project docs group), depth-aware relative paths, current-section marking, breadcrumb, top-bar slot, and `data-iris-nav` marking.\n- [x] 2.2 Add shell CSS: sidebar, collapsed rail, top bar, page header, summary strip tiles, progress bar, command card, narrow-width overlay, print, reduced motion.\n- [x] 2.3 Add classic-script behavior: collapse control, `b` shortcut, `iris-nav` persistence, narrow-width menu toggle, focus management; keep `/` and `t`.\n- [x] 2.4 Adopt the shell in contract pages and project placeholders and verify publish/export strip it while the body keeps a heading and back link.\n\n## 3. Section pages\n\n- [x] 3.1 Add `src/lib/command-catalog.ts` with grouped entries and statuses; generate `HELP_TEXT` from it and update the help test.\n- [x] 3.2 Generate `iris/commands.html` from the catalog with grouped command cards and textual status chips.\n- [x] 3.3 Move the Work browser, toolbar, and drawer to `iris/work.html` with a Work summary strip; keep behavior and data attributes unchanged.\n- [x] 3.4 Move the Spec view to `iris/spec.html` with the existing overview counts as its summary strip.\n- [x] 3.5 Generate the Overview at `iris/index.html`: briefing hero with quick-start commands, four section tiles, recent work, spec movement with progress bars, architecture pane, project-docs strip, all linking to section pages.\n- [x] 3.6 Retire `iris/project/commands.html` from `PROJECT_DOC_NAMES`, remove it during init/update only when it carries the managed marker, and report unmanaged copies.\n\n## 4. Verification and documentation\n\n- [x] 4.1 Update `html-navigation`, `work-board`, `openspec-browser`, `cli-help`, `publish-export`, and `token-contract` tests for the new page set; add tests for shell links at every depth, collapsed-state markup, commands-page statuses, overview summaries, and empty states.\n- [x] 4.2 Regenerate the checked-in `iris/` output, run `pnpm html-check`, and inspect Overview, Work, Spec, Commands, and one contract page at desktop and 360 px in both themes.\n- [x] 4.3 Update `README.md`, `docs/cmds.md`, `docs/design-system.md` (tokens, IA, component inventory), `docs/status.md`, and the canonical skill template&#039;s dashboard sentence.\n- [x] 4.4 Run `openspec validate dashboard-shell-redesign --strict` and the full release gate; reconcile task evidence with the implementation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-dashboard-shell-redesign/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-dashboard-shell-redesign/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-workspace-navigation-shell\">Requirement: workspace navigation shell\u003c/h3>\n\u003cp>Every generated Iris HTML page MUST share one navigation shell that lists the workspace sections (Overview, Work, Spec, Commands, and the project docs group), marks the current section, links each section with a relative \u003ccode>file://\u003c/code>-safe path, and can be collapsed and restored without any network request.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-moves-between-sections\">Scenario: user moves between sections\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates a navigation entry on any generated page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the browser MUST open that section's own page and the shell on that page MUST mark it as current\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-sidebar-is-collapsed\">Scenario: sidebar is collapsed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user activates the collapse control or presses \u003ccode>b\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell MUST collapse to a compact rail in which every section remains reachable with an accessible label, and the same collapsed or expanded state MUST be restored on the next generated page opened in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport\">Scenario: narrow viewport\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell MUST present the sections behind a menu control in the top bar and the page MUST have no horizontal overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-artifact-is-published\">Scenario: artifact is published\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a page is published or exported as a standalone artifact\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the navigation shell MUST be omitted and the page content MUST remain readable and self-contained\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-generated-command-reference-page\">Requirement: generated command reference page\u003c/h3>\n\u003cp>The system MUST generate a Commands page that lists every CLI command from one command catalog, grouped by purpose, with synopsis, usage, and an explicit implementation status, and \u003ccode>iris --help\u003c/code> MUST present the same groups and commands from that catalog.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-command-surface-changes\">Scenario: command surface changes\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a command is added, removed, regrouped, or its status changes in the catalog\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated Commands page and \u003ccode>iris --help\u003c/code> MUST both reflect the change without a separate edit\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-command-is-not-fully-implemented\">Scenario: command is not fully implemented\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a catalog entry is partial or stubbed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Commands page MUST label it with a textual status and MUST NOT present it as fully available\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-section-summary-strips\">Requirement: section summary strips\u003c/h3>\n\u003cp>Each section page MUST open with a summary strip of counts derived only from generated data for that section, and the Overview page MUST summarize every section with links to the owning section page rather than embedding that section's full content.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-overview-is-opened\">Scenario: overview is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens \u003ccode>iris/index.html\u003c/code> from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST show the repository briefing, per-section counts, the most recently updated work records, active spec changes with task progress, and quick-start commands, each linking to the owning section page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-section-has-no-data\">Scenario: section has no data\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a section page has no records\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST name the exact command that populates it instead of showing an empty area\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/h3>\n\u003cp>The system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, MUST validate text contrast at 4.5:1 or better, and MUST define navigation-shell surface, text, and selection tokens alongside the surface, text, accent, type, status, and priority tokens so that every generated page and the shell render from one token set.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-generated-styles-are-validated\">Scenario: generated styles are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a contributor runs the token validation command\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme, including navigation-shell pairs\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-theme-is-switched\">Scenario: theme is switched\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user toggles the theme on any generated page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that page and its navigation shell MUST render in the selected theme from the same tokens, and the preference MUST be restored on other generated pages in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/h3>\n\u003cp>The system MUST provide generated component styles and semantic markup for aperture marks, the navigation shell, page headers, summary strip tiles, progress bars, command cards, restrained status/type/priority indicators, compact work rows, structured tables, Kanban columns and cards, detail drawers, callouts, timelines, keyboard hints, and command-specific empty states.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-meaning-is-communicated-accessibly\">Scenario: meaning is communicated accessibly\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page uses type, status, or priority color, interactive controls, selection, current-section marking, or motion\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide a non-color signal, visible keyboard focus, semantic state, and a static reduced-motion presentation\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated workspace MUST consist of an Overview page at \u003ccode>iris/index.html\u003c/code>, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page organized as overview, canonical specs, active changes with artifacts and delta specs, and archive, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Overview from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-generated-work-pages\">Scenario: repository has generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Work page with one or more page contracts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec page MUST show OpenSpec overview counts and navigable canonical, active-change, and archive sections while the shell keeps every other section reachable\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/h3>\n\u003cp>The generated workspace MUST retain classic deferred-script interactions for navigation-shell collapse, Work layout selection, shared filtering, theme switching, detail-drawer behavior, and keyboard navigation, MUST keep every page usable without JavaScript, and MUST remain operable at a 360 px viewport.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-keyboard-navigation-is-used\">Scenario: keyboard navigation is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses \u003ccode>/\u003c/code>, \u003ccode>t\u003c/code>, or \u003ccode>b\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> \u003ccode>/\u003c/code> MUST focus the relevant visible filter, \u003ccode>t\u003c/code> MUST toggle the theme, and \u003ccode>b\u003c/code> MUST toggle the navigation shell, none of which loads external assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-top-level-tabs-are-keyboard-operated\">Scenario: top-level tabs are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on a navigation-shell entry and the user presses Tab or Enter, or focus is on the List/Table/Kanban tablist and the user presses supported arrow, Home, or End keys\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> section navigation MUST follow standard link behavior with visible focus, and the layout tablist MUST move focus and selection according to the tab pattern with correct tab/panel semantics\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-work-items-are-keyboard-operated\">Scenario: work items are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on a work-item link and the user presses Enter or Space\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the corresponding detail drawer MUST open without navigating away\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-javascript-is-unavailable\">Scenario: JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page opens without executing JavaScript\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the navigation shell MUST remain expanded and fully linked, the default Work List MUST remain readable, and every work item MUST keep its full-page link\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport-is-used\">Scenario: narrow viewport is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> any generated page viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell menu, Work List rows, prioritized Table columns, stacked Kanban columns, full-screen drawer, Spec content, Commands cards, warnings, and source fallbacks MUST remain readable without horizontal page overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-reduced-motion-is-requested\">Scenario: reduced motion is requested\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user enables reduced-motion preferences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> collapsing the shell, switching layouts, opening the drawer, and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## ADDED Requirements\n\n### Requirement: workspace navigation shell\n\nEvery generated Iris HTML page MUST share one navigation shell that lists the workspace sections (Overview, Work, Spec, Commands, and the project docs group), marks the current section, links each section with a relative `file://`-safe path, and can be collapsed and restored without any network request.\n\n#### Scenario: user moves between sections\n\n- **WHEN** a user activates a navigation entry on any generated page\n- **THEN** the browser MUST open that section&#039;s own page and the shell on that page MUST mark it as current\n\n#### Scenario: sidebar is collapsed\n\n- **WHEN** the user activates the collapse control or presses `b` outside an editable field\n- **THEN** the shell MUST collapse to a compact rail in which every section remains reachable with an accessible label, and the same collapsed or expanded state MUST be restored on the next generated page opened in that browser when local storage is available\n\n#### Scenario: narrow viewport\n\n- **WHEN** the viewport is 360 px wide\n- **THEN** the shell MUST present the sections behind a menu control in the top bar and the page MUST have no horizontal overflow\n\n#### Scenario: artifact is published\n\n- **WHEN** a page is published or exported as a standalone artifact\n- **THEN** the navigation shell MUST be omitted and the page content MUST remain readable and self-contained\n\n### Requirement: generated command reference page\n\nThe system MUST generate a Commands page that lists every CLI command from one command catalog, grouped by purpose, with synopsis, usage, and an explicit implementation status, and `iris --help` MUST present the same groups and commands from that catalog.\n\n#### Scenario: command surface changes\n\n- **WHEN** a command is added, removed, regrouped, or its status changes in the catalog\n- **THEN** the generated Commands page and `iris --help` MUST both reflect the change without a separate edit\n\n#### Scenario: command is not fully implemented\n\n- **WHEN** a catalog entry is partial or stubbed\n- **THEN** the Commands page MUST label it with a textual status and MUST NOT present it as fully available\n\n### Requirement: section summary strips\n\nEach section page MUST open with a summary strip of counts derived only from generated data for that section, and the Overview page MUST summarize every section with links to the owning section page rather than embedding that section&#039;s full content.\n\n#### Scenario: overview is opened\n\n- **WHEN** a newcomer opens `iris/index.html` from `file://`\n- **THEN** it MUST show the repository briefing, per-section counts, the most recently updated work records, active spec changes with task progress, and quick-start commands, each linking to the owning section page\n\n#### Scenario: section has no data\n\n- **WHEN** a section page has no records\n- **THEN** it MUST name the exact command that populates it instead of showing an empty area\n\n## MODIFIED Requirements\n\n### Requirement: Aperture token contract\n\nThe system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, MUST validate text contrast at 4.5:1 or better, and MUST define navigation-shell surface, text, and selection tokens alongside the surface, text, accent, type, status, and priority tokens so that every generated page and the shell render from one token set.\n\n#### Scenario: generated styles are validated\n\n- **WHEN** a contributor runs the token validation command\n- **THEN** the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme, including navigation-shell pairs\n\n#### Scenario: theme is switched\n\n- **WHEN** the user toggles the theme on any generated page\n- **THEN** that page and its navigation shell MUST render in the selected theme from the same tokens, and the preference MUST be restored on other generated pages in that browser when local storage is available\n\n### Requirement: Aperture component language\n\nThe system MUST provide generated component styles and semantic markup for aperture marks, the navigation shell, page headers, summary strip tiles, progress bars, command cards, restrained status/type/priority indicators, compact work rows, structured tables, Kanban columns and cards, detail drawers, callouts, timelines, keyboard hints, and command-specific empty states.\n\n#### Scenario: meaning is communicated accessibly\n\n- **WHEN** a generated page uses type, status, or priority color, interactive controls, selection, current-section marking, or motion\n- **THEN** the page MUST provide a non-color signal, visible keyboard focus, semantic state, and a static reduced-motion presentation\n\n### Requirement: newcomer-first dashboard hierarchy\n\nThe generated workspace MUST consist of an Overview page at `iris/index.html`, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page organized as overview, canonical specs, active changes with artifacts and delta specs, and archive, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\n\n#### Scenario: repository has no generated work pages\n\n- **WHEN** a newcomer opens the Overview from `file://`\n- **THEN** it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n#### Scenario: repository has generated work pages\n\n- **WHEN** a newcomer opens the Work page with one or more page contracts\n- **THEN** the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\n\n#### Scenario: user opens the Spec view\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the Spec page MUST show OpenSpec overview counts and navigable canonical, active-change, and archive sections while the shell keeps every other section reachable\n\n#### Scenario: state is communicated in the Spec view\n\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n### Requirement: deterministic responsive interaction\n\nThe generated workspace MUST retain classic deferred-script interactions for navigation-shell collapse, Work layout selection, shared filtering, theme switching, detail-drawer behavior, and keyboard navigation, MUST keep every page usable without JavaScript, and MUST remain operable at a 360 px viewport.\n\n#### Scenario: keyboard navigation is used\n\n- **WHEN** the user presses `/`, `t`, or `b` outside an editable field\n- **THEN** `/` MUST focus the relevant visible filter, `t` MUST toggle the theme, and `b` MUST toggle the navigation shell, none of which loads external assets\n\n#### Scenario: top-level tabs are keyboard operated\n\n- **WHEN** focus is on a navigation-shell entry and the user presses Tab or Enter, or focus is on the List/Table/Kanban tablist and the user presses supported arrow, Home, or End keys\n- **THEN** section navigation MUST follow standard link behavior with visible focus, and the layout tablist MUST move focus and selection according to the tab pattern with correct tab/panel semantics\n\n#### Scenario: work items are keyboard operated\n\n- **WHEN** focus is on a work-item link and the user presses Enter or Space\n- **THEN** the corresponding detail drawer MUST open without navigating away\n\n#### Scenario: JavaScript is unavailable\n\n- **WHEN** a generated page opens without executing JavaScript\n- **THEN** the navigation shell MUST remain expanded and fully linked, the default Work List MUST remain readable, and every work item MUST keep its full-page link\n\n#### Scenario: narrow viewport is used\n\n- **WHEN** any generated page viewport is 360 px wide\n- **THEN** the shell menu, Work List rows, prioritized Table columns, stacked Kanban columns, full-screen drawer, Spec content, Commands cards, warnings, and source fallbacks MUST remain readable without horizontal page overflow\n\n#### Scenario: reduced motion is requested\n\n- **WHEN** the user enables reduced-motion preferences\n- **THEN** collapsing the shell, switching layouts, opening the drawer, and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-multi-page-static-output-not-a-hash-routed-single-page\">Multi-page static output, not a hash-routed single page\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-one-rendershell-wraps-every-page\">One `renderShell()` wraps every page\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-split-design-ts-into-focused-modules\">Split `design.ts` into focused modules\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-electra-palette-aperture-3\">Electra palette (Aperture 3)\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-command-catalog-as-the-single-source\">Command catalog as the single source\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-overview-composition\">Overview composition\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-work-and-spec-relocate-unchanged\">Work and Spec relocate unchanged\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-sidebar-state-and-theme-persistence\">Sidebar state and theme persistence\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-retiring-the-commands-placeholder\">Retiring the commands placeholder\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-foundations\">1. Foundations\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-navigation-shell\">2. Navigation shell\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-section-pages\">3. Section pages\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-workspace-navigation-shell\">Requirement: workspace navigation shell\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-generated-command-reference-page\">Requirement: generated command reference page\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-section-summary-strips\">Requirement: section summary strips\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-design-system-aperture":{"kind":"change","name":"2026-08-21-design-system-aperture","title":"2026-08-21-design-system-aperture","path":"changes/archive/2026-08-21-design-system-aperture","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-design-system-aperture\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-design-system-aperture\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">11\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-design-system-aperture: 11 of 11 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">11/11 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-design-system-aperture/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The generated dashboard is functional but visually generic and leads with an empty work list instead of the repository briefing a newcomer needs. Aperture establishes an offline-safe visual identity and information hierarchy while preserving deterministic \u003ccode>file://\u003c/code> rendering.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Replace the generated token palette and typography/motion values with the Aperture Tokens 2.0 contract, including automated contrast checks for both themes.\u003c/li>\n\u003cli>Restyle and extend the generated base component set with aperture marks, stat tiles, pills, callouts, timelines, keyboard hints, and command-specific empty states.\u003c/li>\n\u003cli>Reorder the generated dashboard into briefing hero, health strip, architecture placeholder, work surface, and project-docs strip.\u003c/li>\n\u003cli>Preserve keyboard shortcuts, visible focus, reduced-motion behavior, 360 px responsiveness, classic deferred scripts, and zero network requests.\u003c/li>\n\u003cli>Exclude vendored assets, Mermaid rendering, and chart blocks; those remain later changes.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: deterministic tokens, components, accessibility behavior, and dashboard information architecture for generated iris HTML.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cp>None.\u003c/p>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Affects \u003ccode>src/templates/design.ts\u003c/code>, \u003ccode>src/commands/init.ts\u003c/code>, token validation, generated \u003ccode>iris/design/*\u003c/code>, generated dashboard/project HTML, and focused tests.\u003c/li>\n\u003cli>Adds no runtime dependency or network request and does not change page data contracts.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe generated dashboard is functional but visually generic and leads with an empty work list instead of the repository briefing a newcomer needs. Aperture establishes an offline-safe visual identity and information hierarchy while preserving deterministic `file://` rendering.\n\n## What Changes\n\n- Replace the generated token palette and typography/motion values with the Aperture Tokens 2.0 contract, including automated contrast checks for both themes.\n- Restyle and extend the generated base component set with aperture marks, stat tiles, pills, callouts, timelines, keyboard hints, and command-specific empty states.\n- Reorder the generated dashboard into briefing hero, health strip, architecture placeholder, work surface, and project-docs strip.\n- Preserve keyboard shortcuts, visible focus, reduced-motion behavior, 360 px responsiveness, classic deferred scripts, and zero network requests.\n- Exclude vendored assets, Mermaid rendering, and chart blocks; those remain later changes.\n\n## Capabilities\n\n### New Capabilities\n- `aperture-design-system`: deterministic tokens, components, accessibility behavior, and dashboard information architecture for generated iris HTML.\n\n### Modified Capabilities\n\nNone.\n\n## Impact\n\n- Affects `src/templates/design.ts`, `src/commands/init.ts`, token validation, generated `iris/design/*`, generated dashboard/project HTML, and focused tests.\n- Adds no runtime dependency or network request and does not change page data contracts.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-design-system-aperture/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The generated HTML already uses a token stylesheet, a shared base stylesheet, and one classic deferred script. The redesign must preserve those boundaries, work from \u003ccode>file://\u003c/code>, and regenerate the checked-in \u003ccode>iris/\u003c/code> dogfood output from source templates rather than editing output by hand.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Make dark theme the source palette and derive a contrast-safe light theme.\u003c/li>\n\u003cli>Keep every generated CSS literal in the token stylesheet and strengthen validation around the expanded contract.\u003c/li>\n\u003cli>Establish the Aperture component vocabulary and newcomer-first dashboard sequence without changing page contracts.\u003c/li>\n\u003cli>Preserve deterministic markup, keyboard behavior, reduced-motion fallback, and narrow-screen usability.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Implement \u003ccode>iris vendor\u003c/code>, load tier-1 fonts, or introduce third-party assets.\u003c/li>\n\u003cli>Add Mermaid execution, diagram snapshots, chart contracts, or chart renderers.\u003c/li>\n\u003cli>Add a browser renderer or change PNG/PDF export behavior.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Keep \u003ccode>src/templates/design.ts\u003c/code> as the sole source for generated tokens, component CSS/JS, and HTML. This preserves the current generator contract and avoids a second styling pipeline.\u003c/li>\n\u003cli>Expand token lint with a declared-token reference pass plus explicit WCAG contrast pairs for both themes. A generic CSS engine would add disproportionate dependency and maintenance cost for a fixed token contract.\u003c/li>\n\u003cli>Render the architecture area as a semantic placeholder that names \u003ccode>iris vendor\u003c/code> and the later diagram workflow. Fabricating a diagram would blur the intentionally deferred step 4–5 boundary.\u003c/li>\n\u003cli>Derive briefing copy from deterministic local project metadata and adopted README presence; use an instructional fallback when no adopted source exists. No prose generation or network access is introduced.\u003c/li>\n\u003cli>Use CSS transforms only for the one-time aperture opening cue, with segments already visible in the reduced-motion static frame. All routine transitions use motion tokens.\u003c/li>\n\u003cli>Keep \u003ccode>/\u003c/code> and \u003ccode>t\u003c/code> in the existing classic script, and represent their shortcuts with visible \u003ccode>kbd\u003c/code> hints. No module script or framework is introduced.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Fixed contrast-pair validation can miss future component combinations] → Validate every foreground/background pairing introduced by this change and require new pairs to be registered alongside new tokens.\u003c/li>\n\u003cli>[Dashboard briefing has limited source data] → Prefer explicit local metadata and command-specific empty states over inferred prose.\u003c/li>\n\u003cli>[Generated dogfood drift] → Re-run the source CLI against this repository and retain HTML/link/token checks in the phase gate.\u003c/li>\n\u003cli>[Animation obscures content] → Keep the ring readable at frame zero and disable animation/transitions under reduced motion.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Replace generated token values and extend token validation.\u003c/li>\n\u003cli>Replace generated component styles/markup and preserve existing interactions.\u003c/li>\n\u003cli>Reorder dashboard markup and add deterministic briefing/health/architecture content.\u003c/li>\n\u003cli>Regenerate checked-in dogfood output and run the full phase gate.\u003c/li>\n\u003cli>Roll back by reverting the phase commit; page contracts and state remain compatible.\u003c/li>\n\u003c/ol>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe generated HTML already uses a token stylesheet, a shared base stylesheet, and one classic deferred script. The redesign must preserve those boundaries, work from `file://`, and regenerate the checked-in `iris/` dogfood output from source templates rather than editing output by hand.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Make dark theme the source palette and derive a contrast-safe light theme.\n- Keep every generated CSS literal in the token stylesheet and strengthen validation around the expanded contract.\n- Establish the Aperture component vocabulary and newcomer-first dashboard sequence without changing page contracts.\n- Preserve deterministic markup, keyboard behavior, reduced-motion fallback, and narrow-screen usability.\n\n**Non-Goals:**\n- Implement `iris vendor`, load tier-1 fonts, or introduce third-party assets.\n- Add Mermaid execution, diagram snapshots, chart contracts, or chart renderers.\n- Add a browser renderer or change PNG/PDF export behavior.\n\n## Decisions\n\n- Keep `src/templates/design.ts` as the sole source for generated tokens, component CSS/JS, and HTML. This preserves the current generator contract and avoids a second styling pipeline.\n- Expand token lint with a declared-token reference pass plus explicit WCAG contrast pairs for both themes. A generic CSS engine would add disproportionate dependency and maintenance cost for a fixed token contract.\n- Render the architecture area as a semantic placeholder that names `iris vendor` and the later diagram workflow. Fabricating a diagram would blur the intentionally deferred step 4–5 boundary.\n- Derive briefing copy from deterministic local project metadata and adopted README presence; use an instructional fallback when no adopted source exists. No prose generation or network access is introduced.\n- Use CSS transforms only for the one-time aperture opening cue, with segments already visible in the reduced-motion static frame. All routine transitions use motion tokens.\n- Keep `/` and `t` in the existing classic script, and represent their shortcuts with visible `kbd` hints. No module script or framework is introduced.\n\n## Risks / Trade-offs\n\n- [Fixed contrast-pair validation can miss future component combinations] → Validate every foreground/background pairing introduced by this change and require new pairs to be registered alongside new tokens.\n- [Dashboard briefing has limited source data] → Prefer explicit local metadata and command-specific empty states over inferred prose.\n- [Generated dogfood drift] → Re-run the source CLI against this repository and retain HTML/link/token checks in the phase gate.\n- [Animation obscures content] → Keep the ring readable at frame zero and disable animation/transitions under reduced motion.\n\n## Migration Plan\n\n1. Replace generated token values and extend token validation.\n2. Replace generated component styles/markup and preserve existing interactions.\n3. Reorder dashboard markup and add deterministic briefing/health/architecture content.\n4. Regenerate checked-in dogfood output and run the full phase gate.\n5. Roll back by reverting the phase commit; page contracts and state remain compatible.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-design-system-aperture/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-tokens-2-0\">1. Tokens 2.0\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Replace the generated dark/light palette, spectrum, type, radius, leading, elevation, and motion tokens with the Aperture contract.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Extend token lint to validate declared references and 4.5:1 contrast pairs in both themes.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Add regression coverage for the expanded token contract and contrast failures.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-components-2-0\">2. Components 2.0\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Restyle the shared base components and add aperture, stat-tile, pill, callout, timeline, kbd, and command-specific empty-state styles.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Update page/dashboard markup so type and status color always has a text, icon, or position signal.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Preserve visible focus, reduced-motion static presentation, classic-script behavior, and print styling.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-dashboard-information-architecture\">3. Dashboard information architecture\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Reorder the dashboard into briefing hero, health strip, architecture placeholder, work surface, and docs strip.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Preserve \u003ccode>/\u003c/code> filter and \u003ccode>t\u003c/code> theme shortcuts and make the layout usable at 360 px.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Update initialization/render inputs and tests for deterministic briefing and empty-state content.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-dogfood-and-verification\">4. Dogfood and verification\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Regenerate the checked-in \u003ccode>iris/\u003c/code> tree through the CLI without hand-editing generated output.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Verify OpenSpec, token lint, HTML integrity, tests, and install smoke gates.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Tokens 2.0\n\n- [x] 1.1 Replace the generated dark/light palette, spectrum, type, radius, leading, elevation, and motion tokens with the Aperture contract.\n- [x] 1.2 Extend token lint to validate declared references and 4.5:1 contrast pairs in both themes.\n- [x] 1.3 Add regression coverage for the expanded token contract and contrast failures.\n\n## 2. Components 2.0\n\n- [x] 2.1 Restyle the shared base components and add aperture, stat-tile, pill, callout, timeline, kbd, and command-specific empty-state styles.\n- [x] 2.2 Update page/dashboard markup so type and status color always has a text, icon, or position signal.\n- [x] 2.3 Preserve visible focus, reduced-motion static presentation, classic-script behavior, and print styling.\n\n## 3. Dashboard information architecture\n\n- [x] 3.1 Reorder the dashboard into briefing hero, health strip, architecture placeholder, work surface, and docs strip.\n- [x] 3.2 Preserve `/` filter and `t` theme shortcuts and make the layout usable at 360 px.\n- [x] 3.3 Update initialization/render inputs and tests for deterministic briefing and empty-state content.\n\n## 4. Dogfood and verification\n\n- [x] 4.1 Regenerate the checked-in `iris/` tree through the CLI without hand-editing generated output.\n- [x] 4.2 Verify OpenSpec, token lint, HTML integrity, tests, and install smoke gates.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-design-system-aperture/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-design-system-aperture/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-purpose\">Purpose\u003c/h2>\n\u003cp>Define the deterministic visual language and dashboard hierarchy that lets a newcomer understand an iris-enabled repository from a single offline HTML entry point.\u003c/p>\n\u003ch2 id=\"delta-aperture-design-system-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/h3>\n\u003cp>The system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, and MUST validate text contrast at 4.5:1 or better.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-generated-styles-are-validated\">Scenario: generated styles are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a contributor runs the token validation command\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/h3>\n\u003cp>The system MUST provide generated component styles and semantic markup for aperture marks, stat tiles, pills, callouts, timelines, keyboard hints, and command-specific empty states.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-meaning-is-communicated-accessibly\">Scenario: meaning is communicated accessibly\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page uses type or status color, interactive controls, or motion\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide a non-color signal, visible keyboard focus, and a static reduced-motion presentation\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated dashboard MUST order its primary content as briefing hero, health strip, architecture pane, work surface, and project-docs strip.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the dashboard from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/h3>\n\u003cp>The generated dashboard MUST retain classic deferred-script interactions for filtering and theme switching and MUST remain operable at a 360 px viewport.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-keyboard-navigation-is-used\">Scenario: keyboard navigation is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses \u003ccode>/\u003c/code> or \u003ccode>t\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> \u003ccode>/\u003c/code> MUST focus the work filter and \u003ccode>t\u003c/code> MUST toggle the theme without loading external assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport-is-used\">Scenario: narrow viewport is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the dashboard viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the hero, health strip, architecture pane, work surface, and docs strip MUST remain readable without horizontal page overflow\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nDefine the deterministic visual language and dashboard hierarchy that lets a newcomer understand an iris-enabled repository from a single offline HTML entry point.\n\n## ADDED Requirements\n\n### Requirement: Aperture token contract\nThe system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, and MUST validate text contrast at 4.5:1 or better.\n\n#### Scenario: generated styles are validated\n- **WHEN** a contributor runs the token validation command\n- **THEN** the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme\n\n### Requirement: Aperture component language\nThe system MUST provide generated component styles and semantic markup for aperture marks, stat tiles, pills, callouts, timelines, keyboard hints, and command-specific empty states.\n\n#### Scenario: meaning is communicated accessibly\n- **WHEN** a generated page uses type or status color, interactive controls, or motion\n- **THEN** the page MUST provide a non-color signal, visible keyboard focus, and a static reduced-motion presentation\n\n### Requirement: newcomer-first dashboard hierarchy\nThe generated dashboard MUST order its primary content as briefing hero, health strip, architecture pane, work surface, and project-docs strip.\n\n#### Scenario: repository has no generated work pages\n- **WHEN** a newcomer opens the dashboard from `file://`\n- **THEN** the dashboard MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n### Requirement: deterministic responsive interaction\nThe generated dashboard MUST retain classic deferred-script interactions for filtering and theme switching and MUST remain operable at a 360 px viewport.\n\n#### Scenario: keyboard navigation is used\n- **WHEN** the user presses `/` or `t` outside an editable field\n- **THEN** `/` MUST focus the work filter and `t` MUST toggle the theme without loading external assets\n\n#### Scenario: narrow viewport is used\n- **WHEN** the dashboard viewport is 360 px wide\n- **THEN** the hero, health strip, architecture pane, work surface, and docs strip MUST remain readable without horizontal page overflow\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-tokens-2-0\">1. Tokens 2.0\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-components-2-0\">2. Components 2.0\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-dashboard-information-architecture\">3. Dashboard information architecture\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-dogfood-and-verification\">4. Dogfood and verification\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-enterprise-chrome-and-openspec-signal":{"kind":"change","name":"2026-08-21-enterprise-chrome-and-openspec-signal","title":"2026-08-21-enterprise-chrome-and-openspec-signal","path":"changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-enterprise-chrome-and-openspec-signal\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">19\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">3\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-enterprise-chrome-and-openspec-signal: 19 of 19 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">19/19 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/proposal.md\u003c/p>\n        \n        \u003ch1 id=\"proposal-enterprise-chrome-and-openspec-signal\">Enterprise chrome and OpenSpec signal\u003c/h1>\n\u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Three defects surfaced when the generated workspace was reviewed against a real repository that already uses OpenSpec.\u003c/p>\n\u003cp>\u003cstrong>The dashboard reports that OpenSpec is absent when it is not.\u003c/strong> \u003ccode>iris/spec.json\u003c/code> records \u003ccode>detected: true\u003c/code> with nine canonical specs and seventeen archived changes, and the Spec page renders all of it. The sidebar badge still reads \u003ccode>Spec 0\u003c/code> because it counts only active changes, while the \u003ccode>work\u003c/code>, \u003ccode>research\u003c/code>, and \u003ccode>commands\u003c/code> badges all count totals. The Overview &quot;Spec movement&quot; card compounds this: with no active change it prints &quot;No active OpenSpec changes&quot; and stops, never mentioning the specs it is already holding. A reader concludes Iris cannot see OpenSpec.\u003c/p>\n\u003cp>\u003ccode>iris/config.yaml\u003c/code> states this outright: \u003ccode>detected_tools.openspec: false\u003c/code>. That key is written once by \u003ccode>writeIfMissing\u003c/code> and never revisited, so it is frozen at whatever was true on the day the workspace was created. Nothing reads it. It is derived state stored in a user-owned file, and it contradicts the snapshot that is actually authoritative.\u003c/p>\n\u003cp>\u003cstrong>Initialization installs fifteen agent files and says nothing.\u003c/strong> \u003ccode>iris init\u003c/code> writes the \u003ccode>iris-workspace\u003c/code> skill to the generic/Codex, Claude, and Copilot skill roots and six command surfaces to \u003ccode>.claude/commands/iris/\u003c/code> and \u003ccode>.github/prompts/\u003c/code>, then prints \u003ccode>iris initialized\u003c/code>. \u003ccode>updateManagedSurfaces\u003c/code> already returns created, updated, unchanged, and conflict lists; initialization discards all of it. The workspace has no view of what Iris installed either, so the product's central promise — set up skills for agents — is invisible in both the terminal and the dashboard.\u003c/p>\n\u003cp>\u003cstrong>Card borders are unvalidated and read as muddy.\u003c/strong> \u003ccode>--line-1\u003c/code> measures 1.46:1 against \u003ccode>--surface-1\u003c/code> and 1.23:1 against \u003ccode>--surface-2\u003c/code> in the dark theme. The token contract enforces 4.5:1 across twenty-one text pairs and does not check border or control boundaries at all, so \u003ccode>pnpm token-lint\u003c/code> stayed green while the boundaries were effectively invisible. The accent is a soft periwinkle rather than a primary blue, and the top bar is painted \u003ccode>var(--bg)\u003c/code>, so the chrome has no identity of its own.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Section badges report totals consistently; the Spec badge counts canonical specs plus active changes.\u003c/li>\n\u003cli>The Overview spec card always reports canonical and archived counts, and names active changes as a separate line rather than as the card's only content.\u003c/li>\n\u003cli>\u003ccode>detected_tools\u003c/code> is removed from the generated \u003ccode>config.yaml\u003c/code> template; OpenSpec detection is read from the snapshot, which is where it is already derived.\u003c/li>\n\u003cli>\u003ccode>iris init\u003c/code> reports the agent surfaces it installed, updated, left unchanged, or could not write.\u003c/li>\n\u003cli>The Commands page gains an agent-surfaces section listing every installed surface and its destination.\u003c/li>\n\u003cli>The token contract validates non-text contrast: control boundaries at 3:1 and border visibility at a declared floor, in both themes.\u003c/li>\n\u003cli>The palette moves to neutral chrome with a strong blue primary: crisper borders, a distinct top-bar surface, and a blue active rail.\u003c/li>\n\u003cli>The Overview project-docs shelf becomes a card grid carrying each document's icon and purpose.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Affected specs: \u003ccode>aperture-design-system\u003c/code>, \u003ccode>agent-first-initialization\u003c/code>, \u003ccode>openspec-spec-browser\u003c/code>\u003c/li>\n\u003cli>Affected code: \u003ccode>src/templates/tokens.ts\u003c/code>, \u003ccode>src/templates/styles.ts\u003c/code>, \u003ccode>src/templates/workspace.ts\u003c/code>, \u003ccode>src/templates/pages/overview.ts\u003c/code>, \u003ccode>src/templates/pages/commands.ts\u003c/code>, \u003ccode>src/commands/init.ts\u003c/code>, \u003ccode>scripts/token-contract.mjs\u003c/code>\u003c/li>\n\u003cli>\u003ccode>navCounts\u003c/code> carries a CRITICAL impact rating (14 impacted symbols, 3 direct callers, 14 execution flows). The signature is unchanged; only the reported value moves, so every caller keeps compiling and rendering.\u003c/li>\n\u003cli>Existing workspaces keep their \u003ccode>detected_tools\u003c/code> key until the owner removes it. Nothing reads it, so a stale value stops being able to mislead the dashboard.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Enterprise chrome and OpenSpec signal\n\n## Why\n\nThree defects surfaced when the generated workspace was reviewed against a real repository that already uses OpenSpec.\n\n**The dashboard reports that OpenSpec is absent when it is not.** `iris/spec.json` records `detected: true` with nine canonical specs and seventeen archived changes, and the Spec page renders all of it. The sidebar badge still reads `Spec 0` because it counts only active changes, while the `work`, `research`, and `commands` badges all count totals. The Overview &quot;Spec movement&quot; card compounds this: with no active change it prints &quot;No active OpenSpec changes&quot; and stops, never mentioning the specs it is already holding. A reader concludes Iris cannot see OpenSpec.\n\n`iris/config.yaml` states this outright: `detected_tools.openspec: false`. That key is written once by `writeIfMissing` and never revisited, so it is frozen at whatever was true on the day the workspace was created. Nothing reads it. It is derived state stored in a user-owned file, and it contradicts the snapshot that is actually authoritative.\n\n**Initialization installs fifteen agent files and says nothing.** `iris init` writes the `iris-workspace` skill to the generic/Codex, Claude, and Copilot skill roots and six command surfaces to `.claude/commands/iris/` and `.github/prompts/`, then prints `iris initialized`. `updateManagedSurfaces` already returns created, updated, unchanged, and conflict lists; initialization discards all of it. The workspace has no view of what Iris installed either, so the product&#039;s central promise — set up skills for agents — is invisible in both the terminal and the dashboard.\n\n**Card borders are unvalidated and read as muddy.** `--line-1` measures 1.46:1 against `--surface-1` and 1.23:1 against `--surface-2` in the dark theme. The token contract enforces 4.5:1 across twenty-one text pairs and does not check border or control boundaries at all, so `pnpm token-lint` stayed green while the boundaries were effectively invisible. The accent is a soft periwinkle rather than a primary blue, and the top bar is painted `var(--bg)`, so the chrome has no identity of its own.\n\n## What Changes\n\n- Section badges report totals consistently; the Spec badge counts canonical specs plus active changes.\n- The Overview spec card always reports canonical and archived counts, and names active changes as a separate line rather than as the card&#039;s only content.\n- `detected_tools` is removed from the generated `config.yaml` template; OpenSpec detection is read from the snapshot, which is where it is already derived.\n- `iris init` reports the agent surfaces it installed, updated, left unchanged, or could not write.\n- The Commands page gains an agent-surfaces section listing every installed surface and its destination.\n- The token contract validates non-text contrast: control boundaries at 3:1 and border visibility at a declared floor, in both themes.\n- The palette moves to neutral chrome with a strong blue primary: crisper borders, a distinct top-bar surface, and a blue active rail.\n- The Overview project-docs shelf becomes a card grid carrying each document&#039;s icon and purpose.\n\n## Impact\n\n- Affected specs: `aperture-design-system`, `agent-first-initialization`, `openspec-spec-browser`\n- Affected code: `src/templates/tokens.ts`, `src/templates/styles.ts`, `src/templates/workspace.ts`, `src/templates/pages/overview.ts`, `src/templates/pages/commands.ts`, `src/commands/init.ts`, `scripts/token-contract.mjs`\n- `navCounts` carries a CRITICAL impact rating (14 impacted symbols, 3 direct callers, 14 execution flows). The signature is unchanged; only the reported value moves, so every caller keeps compiling and rendering.\n- Existing workspaces keep their `detected_tools` key until the owner removes it. Nothing reads it, so a stale value stops being able to mislead the dashboard.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/design.md\u003c/p>\n        \n        \u003ch1 id=\"design-design\">Design\u003c/h1>\n\u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The workspace is generated once and opened from \u003ccode>file://\u003c/code>. Every decision below is constrained by that: there is no runtime to re-derive state, so anything the reader sees was decided at generation time and frozen into the output.\u003c/p>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-section-counts-are-totals-measured-the-same-way\">Section counts are totals, measured the same way\u003c/h3>\n\u003cp>\u003ccode>navCounts\u003c/code> reported \u003ccode>work\u003c/code>, \u003ccode>research\u003c/code>, and \u003ccode>commands\u003c/code> as totals and \u003ccode>spec\u003c/code> as \u003ccode>active_changes.length\u003c/code>. Mixing measures inside one badge row is what produced \u003ccode>Spec 0\u003c/code> beside a Spec page listing nine canonical specs. The badge becomes canonical specs plus active changes: the records the section holds.\u003c/p>\n\u003cp>Archived changes are deliberately excluded from the badge. They are reachable and counted on the Spec page, but a badge that grows forever as history accumulates stops carrying signal about the current workspace. The badge answers &quot;how much is here now&quot;; the page answers &quot;what is here in total&quot;.\u003c/p>\n\u003cp>\u003ccode>navCounts\u003c/code> carries a CRITICAL impact rating — three direct callers, fourteen execution flows, both the Commands and Templates modules. The rating reflects that every page renders the shell. The \u003ccode>NavCounts\u003c/code> shape is unchanged and \u003ccode>spec\u003c/code> remains a number, so the blast radius is the displayed value on eleven pages, which is the intent.\u003c/p>\n\u003ch3 id=\"design-derived-detection-is-removed-from-user-configuration-not-re-\">Derived detection is removed from user configuration, not re-derived\u003c/h3>\n\u003cp>\u003ccode>detected_tools.openspec\u003c/code> was written into \u003ccode>iris/config.yaml\u003c/code> by \u003ccode>writeIfMissing\u003c/code>, so it recorded whatever was true when the workspace was first created and was never revisited. Nothing reads it.\u003c/p>\n\u003cp>Re-deriving it on every \u003ccode>init\u003c/code> was rejected. \u003ccode>config.yaml\u003c/code> is user-owned: it carries \u003ccode>theme\u003c/code>, which \u003ccode>loadWorkspaceTheme\u003c/code> reads, and \u003ccode>budgets\u003c/code>. Rewriting a user-owned file to refresh a value nothing consumes trades a harmless stale key for a real risk of clobbering hand edits. Detection already has a correct home in \u003ccode>iris/spec.json\u003c/code>, which is regenerated wholesale on every \u003ccode>init\u003c/code> and \u003ccode>render\u003c/code>, and which the renderer actually reads.\u003c/p>\n\u003cp>The key is dropped from the template rather than migrated out of existing files. Removing a key from a file the user owns is a change to their file; leaving it costs nothing once no code path consults it.\u003c/p>\n\u003ch3 id=\"design-non-text-contrast-is-validated-with-two-thresholds-named-sep\">Non-text contrast is validated with two thresholds, named separately\u003c/h3>\n\u003cp>The token contract enforced 4.5:1 across twenty-one text pairs and checked no border or control boundary. Measured against that silence, \u003ccode>--line-1\u003c/code> sat at 1.46:1 on \u003ccode>--surface-1\u003c/code> and 1.23:1 on \u003ccode>--surface-2\u003c/code>.\u003c/p>\n\u003cp>Two thresholds apply, because two different things are being measured:\u003c/p>\n\u003cul>\n\u003cli>\u003cstrong>Control boundaries at 3:1.\u003c/strong> \u003ccode>--accent\u003c/code> carries buttons, the active navigation rail, and focus rings. These are user interface components under WCAG 1.4.11, and 3:1 is that criterion's threshold.\u003c/li>\n\u003cli>\u003cstrong>Border visibility at 1.45:1.\u003c/strong> A card border is not a control and no accessibility criterion governs it. The floor exists to make &quot;the boundary is visible&quot; a measurable property rather than a matter of taste. It is declared as a project floor and the validator says so in the failure message, because reporting a 1.45 floor in the same voice as a WCAG result would misrepresent what was checked.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"design-neutral-chrome-with-a-blue-primary\">Neutral chrome with a blue primary\u003c/h3>\n\u003cp>A navy chrome band was considered and rejected: the light theme paints \u003ccode>--nav-bg: #ffffff\u003c/code>, so a navy top bar would either force the sidebar navy in both themes or leave the two halves of one chrome unit unrelated.\u003c/p>\n\u003cp>Instead the chrome stays neutral and the blue carries meaning: the active navigation rail, links, buttons, and focus. The top bar takes its own surface token rather than \u003ccode>var(--bg)\u003c/code> so it reads as a band without colour, and \u003ccode>--elevation-1\u003c/code> stays off in the dark theme, where a shadow under a now-visible border only muddies the edge it sits on.\u003c/p>\n\u003ch3 id=\"design-codex-coverage-already-exists\">Codex coverage already exists\u003c/h3>\n\u003cp>\u003ccode>iris init\u003c/code> writes the \u003ccode>iris-workspace\u003c/code> skill to \u003ccode>.agents/skills/\u003c/code>, \u003ccode>.claude/skills/\u003c/code>, and \u003ccode>.github/skills/\u003c/code> — the same three roots OpenSpec v1.9.0 writes in this repository — plus six command surfaces under \u003ccode>.claude/commands/iris/\u003c/code> and \u003ccode>.github/prompts/\u003c/code>. The canonical spec already names \u003ccode>.agents/\u003c/code> as the generic/Codex location, and neither tool writes \u003ccode>AGENTS.md\u003c/code>.\u003c/p>\n\u003cp>No new surface descriptor is added. The gap was that fifteen installed files were reported nowhere, so initialization looked like it had done nothing. The fix is reporting, in the terminal and on the Commands page.\u003c/p>\n\u003ch2 id=\"design-risks\">Risks\u003c/h2>\n\u003cul>\n\u003cli>Existing workspaces keep a stale \u003ccode>detected_tools\u003c/code> key. It is inert once unread, and removing it from a user-owned file without being asked is the larger harm.\u003c/li>\n\u003cli>Raising border contrast changes the appearance of every generated page. The token contract now covers it, so a regression is caught by \u003ccode>pnpm token-lint\u003c/code> rather than by eye.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Design\n\n## Context\n\nThe workspace is generated once and opened from `file://`. Every decision below is constrained by that: there is no runtime to re-derive state, so anything the reader sees was decided at generation time and frozen into the output.\n\n## Decisions\n\n### Section counts are totals, measured the same way\n\n`navCounts` reported `work`, `research`, and `commands` as totals and `spec` as `active_changes.length`. Mixing measures inside one badge row is what produced `Spec 0` beside a Spec page listing nine canonical specs. The badge becomes canonical specs plus active changes: the records the section holds.\n\nArchived changes are deliberately excluded from the badge. They are reachable and counted on the Spec page, but a badge that grows forever as history accumulates stops carrying signal about the current workspace. The badge answers &quot;how much is here now&quot;; the page answers &quot;what is here in total&quot;.\n\n`navCounts` carries a CRITICAL impact rating — three direct callers, fourteen execution flows, both the Commands and Templates modules. The rating reflects that every page renders the shell. The `NavCounts` shape is unchanged and `spec` remains a number, so the blast radius is the displayed value on eleven pages, which is the intent.\n\n### Derived detection is removed from user configuration, not re-derived\n\n`detected_tools.openspec` was written into `iris/config.yaml` by `writeIfMissing`, so it recorded whatever was true when the workspace was first created and was never revisited. Nothing reads it.\n\nRe-deriving it on every `init` was rejected. `config.yaml` is user-owned: it carries `theme`, which `loadWorkspaceTheme` reads, and `budgets`. Rewriting a user-owned file to refresh a value nothing consumes trades a harmless stale key for a real risk of clobbering hand edits. Detection already has a correct home in `iris/spec.json`, which is regenerated wholesale on every `init` and `render`, and which the renderer actually reads.\n\nThe key is dropped from the template rather than migrated out of existing files. Removing a key from a file the user owns is a change to their file; leaving it costs nothing once no code path consults it.\n\n### Non-text contrast is validated with two thresholds, named separately\n\nThe token contract enforced 4.5:1 across twenty-one text pairs and checked no border or control boundary. Measured against that silence, `--line-1` sat at 1.46:1 on `--surface-1` and 1.23:1 on `--surface-2`.\n\nTwo thresholds apply, because two different things are being measured:\n\n- **Control boundaries at 3:1.** `--accent` carries buttons, the active navigation rail, and focus rings. These are user interface components under WCAG 1.4.11, and 3:1 is that criterion&#039;s threshold.\n- **Border visibility at 1.45:1.** A card border is not a control and no accessibility criterion governs it. The floor exists to make &quot;the boundary is visible&quot; a measurable property rather than a matter of taste. It is declared as a project floor and the validator says so in the failure message, because reporting a 1.45 floor in the same voice as a WCAG result would misrepresent what was checked.\n\n### Neutral chrome with a blue primary\n\nA navy chrome band was considered and rejected: the light theme paints `--nav-bg: #ffffff`, so a navy top bar would either force the sidebar navy in both themes or leave the two halves of one chrome unit unrelated.\n\nInstead the chrome stays neutral and the blue carries meaning: the active navigation rail, links, buttons, and focus. The top bar takes its own surface token rather than `var(--bg)` so it reads as a band without colour, and `--elevation-1` stays off in the dark theme, where a shadow under a now-visible border only muddies the edge it sits on.\n\n### Codex coverage already exists\n\n`iris init` writes the `iris-workspace` skill to `.agents/skills/`, `.claude/skills/`, and `.github/skills/` — the same three roots OpenSpec v1.9.0 writes in this repository — plus six command surfaces under `.claude/commands/iris/` and `.github/prompts/`. The canonical spec already names `.agents/` as the generic/Codex location, and neither tool writes `AGENTS.md`.\n\nNo new surface descriptor is added. The gap was that fifteen installed files were reported nowhere, so initialization looked like it had done nothing. The fix is reporting, in the terminal and on the Commands page.\n\n## Risks\n\n- Existing workspaces keep a stale `detected_tools` key. It is inert once unread, and removing it from a user-owned file without being asked is the larger harm.\n- Raising border contrast changes the appearance of every generated page. The token contract now covers it, so a regression is caught by `pnpm token-lint` rather than by eye.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/tasks.md\u003c/p>\n        \n        \u003ch1 id=\"tasks-tasks\">Tasks\u003c/h1>\n\u003ch2 id=\"tasks-1-openspec-signal\">1. OpenSpec signal\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Count the Spec badge as canonical specs plus active changes in \u003ccode>navCounts\u003c/code>\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Report canonical and archived totals in the Overview spec card whether or not an active change exists\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Remove \u003ccode>detected_tools\u003c/code> from the generated \u003ccode>config.yaml\u003c/code> template\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.4 Cover the populated-but-inactive case in tests\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-initialization-visibility\">2. Initialization visibility\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Report created, updated, unchanged, and conflicted agent surfaces from \u003ccode>iris init\u003c/code>\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 List installed agent surfaces and their destinations on the Commands page\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Mark a conflicted surface as not installed with its reason\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Cover the reporting output in tests\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-non-text-contrast-contract\">3. Non-text contrast contract\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Add control-boundary pairs validated at 3:1\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Add border pairs validated at a declared visibility floor, named as such in failures\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Cover both thresholds in the token contract tests\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-enterprise-chrome\">4. Enterprise chrome\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Raise border contrast above the floor against every surface it is drawn on, in both themes\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Move the accent to a primary blue that clears the control-boundary threshold\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Give the top bar its own surface token instead of \u003ccode>var(--bg)\u003c/code>\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 Render the active navigation entry with a blue rail\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.5 Rebuild the Overview project-docs shelf as a card grid with each document's icon and purpose\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-5-verification\">5. Verification\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.1 \u003ccode>pnpm token-lint\u003c/code> passes with the new pairs\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.2 \u003ccode>pnpm release:check\u003c/code> passes\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.3 Regenerate with \u003ccode>iris init\u003c/code> and confirm the workspace diff is only the intended changes\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># Tasks\n\n## 1. OpenSpec signal\n\n- [x] 1.1 Count the Spec badge as canonical specs plus active changes in `navCounts`\n- [x] 1.2 Report canonical and archived totals in the Overview spec card whether or not an active change exists\n- [x] 1.3 Remove `detected_tools` from the generated `config.yaml` template\n- [x] 1.4 Cover the populated-but-inactive case in tests\n\n## 2. Initialization visibility\n\n- [x] 2.1 Report created, updated, unchanged, and conflicted agent surfaces from `iris init`\n- [x] 2.2 List installed agent surfaces and their destinations on the Commands page\n- [x] 2.3 Mark a conflicted surface as not installed with its reason\n- [x] 2.4 Cover the reporting output in tests\n\n## 3. Non-text contrast contract\n\n- [x] 3.1 Add control-boundary pairs validated at 3:1\n- [x] 3.2 Add border pairs validated at a declared visibility floor, named as such in failures\n- [x] 3.3 Cover both thresholds in the token contract tests\n\n## 4. Enterprise chrome\n\n- [x] 4.1 Raise border contrast above the floor against every surface it is drawn on, in both themes\n- [x] 4.2 Move the accent to a primary blue that clears the control-boundary threshold\n- [x] 4.3 Give the top bar its own surface token instead of `var(--bg)`\n- [x] 4.4 Render the active navigation entry with a blue rail\n- [x] 4.5 Rebuild the Overview project-docs shelf as a card grid with each document&#039;s icon and purpose\n\n## 5. Verification\n\n- [x] 5.1 `pnpm token-lint` passes with the new pairs\n- [x] 5.2 `pnpm release:check` passes\n- [x] 5.3 Regenerate with `iris init` and confirm the workspace diff is only the intended changes\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-agent-first-initialization-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · agent-first-initialization\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/specs/agent-first-initialization/spec.md\u003c/p>\n        \n        \u003ch1 id=\"delta-agent-first-initialization-agent-first-initialization\">agent-first-initialization\u003c/h1>\n\u003ch2 id=\"delta-agent-first-initialization-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-one-command-agent-first-setup\">Requirement: one-command agent-first setup\u003c/h3>\n\u003cp>The system MUST make \u003ccode>iris init\u003c/code> the complete project setup and upgrade flow for an installed Iris CLI, MUST report the agent surfaces that operation installed, and MUST NOT record derived tool detection in the user-owned workspace configuration.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-first-initialization\">Scenario: first initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> in a repository with no Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST create the usable local workspace, install the supported agent-facing Iris skill surfaces, and render the dashboard in that operation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-repeated-initialization\">Scenario: repeated initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> again in an initialized repository\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST refresh Iris-managed outputs idempotently without requiring \u003ccode>iris update\u003c/code>, \u003ccode>iris adopt\u003c/code>, or \u003ccode>iris sync\u003c/code> as a setup step\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-initialized-repository-has-content-commands\">Scenario: initialized repository has content commands\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> initialization completes successfully\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> agents and users MUST still be able to use the existing draft, render, report, archive, publish, export, and open commands\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-initialization-installs-agent-surfaces\">Scenario: initialization installs agent surfaces\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> finishes installing agent surfaces\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST report how many surfaces were created, updated, and left unchanged, and MUST name every surface it could not write, so that the operator can see what was installed without inspecting the filesystem\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-generated-configuration-is-written\">Scenario: generated configuration is written\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the system writes the workspace configuration file\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that file MUST NOT contain detection results derived from the repository's contents, so that a value frozen at creation time can never contradict the detection the dashboard actually reads\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"delta-agent-first-initialization-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-agent-first-initialization-requirement-installed-agent-surface-visibility\">Requirement: installed agent surface visibility\u003c/h3>\n\u003cp>The generated workspace MUST show which agent surfaces Iris installed and where each one lives, so that the setup Iris performs is inspectable from the dashboard rather than only from the filesystem.\u003c/p>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-surfaces-are-installed\">Scenario: surfaces are installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the generated command reference after initialization\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST list each installed agent surface with its destination path and the agent tooling that reads it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-agent-first-initialization-scenario-a-surface-could-not-be-written\">Scenario: a surface could not be written\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a surface was preserved as a conflict instead of being written\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the listing MUST mark that surface as not installed and give the reason, rather than presenting it as available\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># agent-first-initialization\n\n## MODIFIED Requirements\n\n### Requirement: one-command agent-first setup\nThe system MUST make `iris init` the complete project setup and upgrade flow for an installed Iris CLI, MUST report the agent surfaces that operation installed, and MUST NOT record derived tool detection in the user-owned workspace configuration.\n\n#### Scenario: first initialization\n- **WHEN** a user runs `iris init` in a repository with no Iris workspace\n- **THEN** the system MUST create the usable local workspace, install the supported agent-facing Iris skill surfaces, and render the dashboard in that operation\n\n#### Scenario: repeated initialization\n- **WHEN** a user runs `iris init` again in an initialized repository\n- **THEN** the system MUST refresh Iris-managed outputs idempotently without requiring `iris update`, `iris adopt`, or `iris sync` as a setup step\n\n#### Scenario: initialized repository has content commands\n- **WHEN** initialization completes successfully\n- **THEN** agents and users MUST still be able to use the existing draft, render, report, archive, publish, export, and open commands\n\n#### Scenario: initialization installs agent surfaces\n- **WHEN** `iris init` finishes installing agent surfaces\n- **THEN** it MUST report how many surfaces were created, updated, and left unchanged, and MUST name every surface it could not write, so that the operator can see what was installed without inspecting the filesystem\n\n#### Scenario: generated configuration is written\n- **WHEN** the system writes the workspace configuration file\n- **THEN** that file MUST NOT contain detection results derived from the repository&#039;s contents, so that a value frozen at creation time can never contradict the detection the dashboard actually reads\n\n## ADDED Requirements\n\n### Requirement: installed agent surface visibility\n\nThe generated workspace MUST show which agent surfaces Iris installed and where each one lives, so that the setup Iris performs is inspectable from the dashboard rather than only from the filesystem.\n\n#### Scenario: surfaces are installed\n\n- **WHEN** a user opens the generated command reference after initialization\n- **THEN** it MUST list each installed agent surface with its destination path and the agent tooling that reads it\n\n#### Scenario: a surface could not be written\n\n- **WHEN** a surface was preserved as a conflict instead of being written\n- **THEN** the listing MUST mark that surface as not installed and give the reason, rather than presenting it as available\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch1 id=\"delta-aperture-design-system-aperture-design-system\">aperture-design-system\u003c/h1>\n\u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/h3>\n\u003cp>The system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, MUST validate text contrast at 4.5:1 or better, MUST validate non-text contrast for control boundaries and border visibility, and MUST define navigation-shell surface, text, and selection tokens alongside the surface, text, accent, type, status, and priority tokens so that every generated page and the shell render from one token set.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-generated-styles-are-validated\">Scenario: generated styles are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a contributor runs the token validation command\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme, including navigation-shell pairs\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-theme-is-switched\">Scenario: theme is switched\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user toggles the theme on any generated page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that page and its navigation shell MUST render in the selected theme from the same tokens, and the preference MUST be restored on other generated pages in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-non-text-surfaces-are-validated\">Scenario: non-text surfaces are validated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the token validation command checks a configured non-text pair\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST reject a control-boundary pair below 3:1 and a border pair below the declared visibility floor in either theme, and the failure message MUST state which threshold applied so a border floor is never read as a text contrast result\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-section-summary-strips\">Requirement: section summary strips\u003c/h3>\n\u003cp>Each section page MUST open with a summary strip of counts derived only from generated data for that section, and the Overview page MUST summarize every section with links to the owning section page rather than embedding that section's full content. A summary MUST report the totals it holds even when the section's most active measure is zero.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-overview-is-opened\">Scenario: overview is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens \u003ccode>iris/index.html\u003c/code> from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST show the repository briefing, per-section counts, the most recently updated work records, active spec changes with task progress, and quick-start commands, each linking to the owning section page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-section-has-no-data\">Scenario: section has no data\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a section page has no records\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST name the exact command that populates it instead of showing an empty area\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-detected-source-has-totals-but-no-active-items\">Scenario: a detected source has totals but no active items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Overview summarizes a source that is present and non-empty but has no active items\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST report the totals it holds and describe the absence of active items as one part of that summary, and it MUST NOT present the summary in a form a reader could mistake for the source being absent or unread\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-workspace-navigation-shell\">Requirement: workspace navigation shell\u003c/h3>\n\u003cp>Every generated Iris HTML page MUST share one navigation shell that lists the workspace sections (Overview, Work, Spec, Commands, and the project docs group), marks the current section, links each section with a relative \u003ccode>file://\u003c/code>-safe path, and can be collapsed and restored without any network request. Every section count shown in the shell MUST be a total for that section, measured the same way across sections.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-moves-between-sections\">Scenario: user moves between sections\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates a navigation entry on any generated page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the browser MUST open that section's own page and the shell on that page MUST mark it as current\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-sidebar-is-collapsed\">Scenario: sidebar is collapsed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user activates the collapse control or presses \u003ccode>b\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell MUST collapse to a compact rail in which every section remains reachable with an accessible label, and the same collapsed or expanded state MUST be restored on the next generated page opened in that browser when local storage is available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport\">Scenario: narrow viewport\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the shell MUST present the sections behind a menu control in the top bar and the page MUST have no horizontal overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-artifact-is-published\">Scenario: artifact is published\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a page is published or exported as a standalone artifact\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the navigation shell MUST be omitted and the page content MUST remain readable and self-contained\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-section-holds-records-but-has-no-active-ones\">Scenario: a section holds records but has no active ones\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the shell shows the count for a section whose records exist but none are active\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that count MUST reflect the records the section holds rather than the active subset, so that a populated section is never labelled zero\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># aperture-design-system\n\n## MODIFIED Requirements\n\n### Requirement: Aperture token contract\n\nThe system MUST generate dark and light themes from the Aperture token contract, MUST keep style literals confined to the generated token stylesheet, MUST validate text contrast at 4.5:1 or better, MUST validate non-text contrast for control boundaries and border visibility, and MUST define navigation-shell surface, text, and selection tokens alongside the surface, text, accent, type, status, and priority tokens so that every generated page and the shell render from one token set.\n\n#### Scenario: generated styles are validated\n\n- **WHEN** a contributor runs the token validation command\n- **THEN** the validator MUST reject undeclared token names, forbidden style literals outside the token stylesheet, and configured foreground/background pairs below 4.5:1 in either theme, including navigation-shell pairs\n\n#### Scenario: theme is switched\n\n- **WHEN** the user toggles the theme on any generated page\n- **THEN** that page and its navigation shell MUST render in the selected theme from the same tokens, and the preference MUST be restored on other generated pages in that browser when local storage is available\n\n#### Scenario: non-text surfaces are validated\n\n- **WHEN** the token validation command checks a configured non-text pair\n- **THEN** it MUST reject a control-boundary pair below 3:1 and a border pair below the declared visibility floor in either theme, and the failure message MUST state which threshold applied so a border floor is never read as a text contrast result\n\n### Requirement: section summary strips\n\nEach section page MUST open with a summary strip of counts derived only from generated data for that section, and the Overview page MUST summarize every section with links to the owning section page rather than embedding that section&#039;s full content. A summary MUST report the totals it holds even when the section&#039;s most active measure is zero.\n\n#### Scenario: overview is opened\n\n- **WHEN** a newcomer opens `iris/index.html` from `file://`\n- **THEN** it MUST show the repository briefing, per-section counts, the most recently updated work records, active spec changes with task progress, and quick-start commands, each linking to the owning section page\n\n#### Scenario: section has no data\n\n- **WHEN** a section page has no records\n- **THEN** it MUST name the exact command that populates it instead of showing an empty area\n\n#### Scenario: a detected source has totals but no active items\n\n- **WHEN** the Overview summarizes a source that is present and non-empty but has no active items\n- **THEN** it MUST report the totals it holds and describe the absence of active items as one part of that summary, and it MUST NOT present the summary in a form a reader could mistake for the source being absent or unread\n\n### Requirement: workspace navigation shell\n\nEvery generated Iris HTML page MUST share one navigation shell that lists the workspace sections (Overview, Work, Spec, Commands, and the project docs group), marks the current section, links each section with a relative `file://`-safe path, and can be collapsed and restored without any network request. Every section count shown in the shell MUST be a total for that section, measured the same way across sections.\n\n#### Scenario: user moves between sections\n\n- **WHEN** a user activates a navigation entry on any generated page\n- **THEN** the browser MUST open that section&#039;s own page and the shell on that page MUST mark it as current\n\n#### Scenario: sidebar is collapsed\n\n- **WHEN** the user activates the collapse control or presses `b` outside an editable field\n- **THEN** the shell MUST collapse to a compact rail in which every section remains reachable with an accessible label, and the same collapsed or expanded state MUST be restored on the next generated page opened in that browser when local storage is available\n\n#### Scenario: narrow viewport\n\n- **WHEN** the viewport is 360 px wide\n- **THEN** the shell MUST present the sections behind a menu control in the top bar and the page MUST have no horizontal overflow\n\n#### Scenario: artifact is published\n\n- **WHEN** a page is published or exported as a standalone artifact\n- **THEN** the navigation shell MUST be omitted and the page content MUST remain readable and self-contained\n\n#### Scenario: a section holds records but has no active ones\n\n- **WHEN** the shell shows the count for a section whose records exist but none are active\n- **THEN** that count MUST reflect the records the section holds rather than the active subset, so that a populated section is never labelled zero\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-openspec-spec-browser-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · openspec-spec-browser\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-enterprise-chrome-and-openspec-signal/specs/openspec-spec-browser/spec.md\u003c/p>\n        \n        \u003ch1 id=\"delta-openspec-spec-browser-openspec-spec-browser\">openspec-spec-browser\u003c/h1>\n\u003ch2 id=\"delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-graceful-unknown-and-empty-states\">Requirement: graceful unknown and empty states\u003c/h3>\n\u003cp>The system MUST render readable fallback states for absent, empty, malformed, unsupported, or partially complete OpenSpec workspaces, and MUST keep a detected workspace visually distinguishable from an absent one wherever it is summarized.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-repository-has-no-openspec-directory\">Scenario: repository has no OpenSpec directory\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the dashboard snapshot is generated without an \u003ccode>openspec/\u003c/code> directory\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST explain that no OpenSpec workspace was detected without suggesting general-document ingestion\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-openspec-workspace-is-empty\">Scenario: OpenSpec workspace is empty\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>openspec/\u003c/code> exists but contains no supported project, spec, or change records\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST show a distinct empty-workspace state\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-artifact-is-malformed-or-unknown\">Scenario: artifact is malformed or unknown\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a recognized path contains malformed Markdown or an unsupported layout\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST render escaped readable source when available, identify the affected path, and show an actionable warning without hiding other valid records\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-a-detected-workspace-has-no-active-changes\">Scenario: a detected workspace has no active changes\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the snapshot records a detected OpenSpec workspace that holds canonical specs or archived changes but no active change\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every summary of it MUST report the canonical and archived totals it holds, and MUST NOT reduce to a message whose only content is the absence of active changes\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># openspec-spec-browser\n\n## MODIFIED Requirements\n\n### Requirement: graceful unknown and empty states\nThe system MUST render readable fallback states for absent, empty, malformed, unsupported, or partially complete OpenSpec workspaces, and MUST keep a detected workspace visually distinguishable from an absent one wherever it is summarized.\n\n#### Scenario: repository has no OpenSpec directory\n- **WHEN** the dashboard snapshot is generated without an `openspec/` directory\n- **THEN** the Spec tab MUST explain that no OpenSpec workspace was detected without suggesting general-document ingestion\n\n#### Scenario: OpenSpec workspace is empty\n- **WHEN** `openspec/` exists but contains no supported project, spec, or change records\n- **THEN** the Spec tab MUST show a distinct empty-workspace state\n\n#### Scenario: artifact is malformed or unknown\n- **WHEN** a recognized path contains malformed Markdown or an unsupported layout\n- **THEN** the Spec tab MUST render escaped readable source when available, identify the affected path, and show an actionable warning without hiding other valid records\n\n#### Scenario: a detected workspace has no active changes\n- **WHEN** the snapshot records a detected OpenSpec workspace that holds canonical specs or archived changes but no active change\n- **THEN** every summary of it MUST report the canonical and archived totals it holds, and MUST NOT reduce to a message whose only content is the absence of active changes\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-section-counts-are-totals-measured-the-same-way\">Section counts are totals, measured the same way\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-derived-detection-is-removed-from-user-configuration-not-re-\">Derived detection is removed from user configuration, not re-derived\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-non-text-contrast-is-validated-with-two-thresholds-named-sep\">Non-text contrast is validated with two thresholds, named separately\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-neutral-chrome-with-a-blue-primary\">Neutral chrome with a blue primary\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-codex-coverage-already-exists\">Codex coverage already exists\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks\">Risks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-openspec-signal\">1. OpenSpec signal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-initialization-visibility\">2. Initialization visibility\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-non-text-contrast-contract\">3. Non-text contrast contract\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-enterprise-chrome\">4. Enterprise chrome\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-5-verification\">5. Verification\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-artifact\">Delta spec · agent-first-initialization\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-one-command-agent-first-setup\">Requirement: one-command agent-first setup\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-agent-first-initialization-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-agent-first-initialization-requirement-installed-agent-surface-visibility\">Requirement: installed agent surface visibility\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-token-contract\">Requirement: Aperture token contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-section-summary-strips\">Requirement: section summary strips\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-workspace-navigation-shell\">Requirement: workspace navigation shell\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-artifact\">Delta spec · openspec-spec-browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-graceful-unknown-and-empty-states\">Requirement: graceful unknown and empty states\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-openspec-spec-browser":{"kind":"change","name":"2026-08-21-openspec-spec-browser","title":"2026-08-21-openspec-spec-browser","path":"changes/archive/2026-08-21-openspec-spec-browser","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-openspec-spec-browser\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">25\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">2\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-openspec-spec-browser: 25 of 25 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">25/25 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Iris can render intentional work pages, but its dashboard does not expose the repository's actual OpenSpec workspace. Agents and maintainers need an offline, deterministic view of canonical specs and change progress without running OpenSpec, ingesting general documentation, or introducing background synchronization.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Add a top-level dashboard tab labelled \u003ccode>Spec\u003c/code> that summarizes canonical specs, active changes, archived changes, project/config context, artifact availability, delta specs, and real Markdown task progress.\u003c/li>\n\u003cli>Parse the \u003ccode>openspec/\u003c/code> filesystem directly during \u003ccode>iris init\u003c/code> and \u003ccode>iris render --all\u003c/code>; keep single-page renders focused on page output and avoid a new refresh command because the existing explicit full-render flow has a clear user-facing purpose.\u003c/li>\n\u003cli>Support the repository's structured active/archive layouts and legacy archived Markdown while preserving nested capability paths and representing invalid, incomplete, or unknown layouts with escaped source and path-specific warnings.\u003c/li>\n\u003cli>Extend the Aperture dashboard hierarchy with accessible Work/Spec top-level navigation that remains deterministic, token-only, keyboard-operable, theme-safe, reduced-motion-safe, responsive at 360 px, and functional from \u003ccode>file://\u003c/code> without network requests or runtime modules.\u003c/li>\n\u003cli>Add bounded, symlink-safe filesystem traversal and fixture coverage for every observed layout plus nested capabilities and all delta-operation headings.\u003c/li>\n\u003cli>Regenerate Iris dogfood only through the CLI and update the user/technical/design documentation for the new surface and refresh contract.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>openspec-spec-browser\u003c/code>: Deterministic parsing, normalization, refresh, fallback, and offline visualization of a repository's complete OpenSpec workspace.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: Extend the dashboard interaction and responsive hierarchy with a top-level accessible \u003ccode>Spec\u003c/code> tab while retaining the existing Work experience.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Affects initialization and full-render orchestration, dashboard view data, generated HTML/CSS/classic-script behavior, tests/fixtures, dogfood output, and project documentation.\u003c/li>\n\u003cli>Adds no runtime dependency on OpenSpec, no server, no framework, no network request, no watcher, and no new content-ingestion command.\u003c/li>\n\u003cli>OpenSpec files remain repository-owned inputs; Iris writes only its generated \u003ccode>iris/\u003c/code> output.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nIris can render intentional work pages, but its dashboard does not expose the repository&#039;s actual OpenSpec workspace. Agents and maintainers need an offline, deterministic view of canonical specs and change progress without running OpenSpec, ingesting general documentation, or introducing background synchronization.\n\n## What Changes\n\n- Add a top-level dashboard tab labelled `Spec` that summarizes canonical specs, active changes, archived changes, project/config context, artifact availability, delta specs, and real Markdown task progress.\n- Parse the `openspec/` filesystem directly during `iris init` and `iris render --all`; keep single-page renders focused on page output and avoid a new refresh command because the existing explicit full-render flow has a clear user-facing purpose.\n- Support the repository&#039;s structured active/archive layouts and legacy archived Markdown while preserving nested capability paths and representing invalid, incomplete, or unknown layouts with escaped source and path-specific warnings.\n- Extend the Aperture dashboard hierarchy with accessible Work/Spec top-level navigation that remains deterministic, token-only, keyboard-operable, theme-safe, reduced-motion-safe, responsive at 360 px, and functional from `file://` without network requests or runtime modules.\n- Add bounded, symlink-safe filesystem traversal and fixture coverage for every observed layout plus nested capabilities and all delta-operation headings.\n- Regenerate Iris dogfood only through the CLI and update the user/technical/design documentation for the new surface and refresh contract.\n\n## Capabilities\n\n### New Capabilities\n\n- `openspec-spec-browser`: Deterministic parsing, normalization, refresh, fallback, and offline visualization of a repository&#039;s complete OpenSpec workspace.\n\n### Modified Capabilities\n\n- `aperture-design-system`: Extend the dashboard interaction and responsive hierarchy with a top-level accessible `Spec` tab while retaining the existing Work experience.\n\n## Impact\n\n- Affects initialization and full-render orchestration, dashboard view data, generated HTML/CSS/classic-script behavior, tests/fixtures, dogfood output, and project documentation.\n- Adds no runtime dependency on OpenSpec, no server, no framework, no network request, no watcher, and no new content-ingestion command.\n- OpenSpec files remain repository-owned inputs; Iris writes only its generated `iris/` output.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See \u003ccode>proposal.md\u003c/code> for motivation and the two delta specs for observable behavior. The current renderer rebuilds \u003ccode>iris/index.html\u003c/code> after page operations from page registry data, but it has no durable generated representation of OpenSpec. A single-page render, archive, report, or update may refresh the dashboard even though Phase 2 permits OpenSpec filesystem reads only during explicit workspace refreshes.\u003c/p>\n\u003cp>The repository currently proves four forms: canonical \u003ccode>specs/**/spec.md\u003c/code>, structured active change directories, structured archive directories, and legacy archived single Markdown files. Current capability paths are flat and current delta files primarily use \u003ccode>ADDED Requirements\u003c/code>; nested paths and the other delta operations need contract fixtures rather than claims of observed use.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Define one versioned normalized snapshot shared by parsing, rendering, and tests.\u003c/li>\n\u003cli>Keep filesystem reads deterministic, bounded, confined, symlink-safe, and tolerant of isolated failures.\u003c/li>\n\u003cli>Preserve the exact repository-relative identity of every supported OpenSpec record.\u003c/li>\n\u003cli>Make Work and Spec peer dashboard views without regressing file-based navigation or accessibility.\u003c/li>\n\u003cli>Make refresh ownership explicit so unrelated page operations reuse the last generated Spec snapshot.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Semantic validation equivalent to \u003ccode>openspec validate\u003c/code> or use of private OpenSpec APIs.\u003c/li>\n\u003cli>A full Markdown engine, editable spec UI, search index, watcher, server, or dedicated refresh command.\u003c/li>\n\u003cli>Mermaid, charts, browser rendering, or general repository-document ingestion.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-1-persist-a-generated-iris-spec-json-snapshot\">1. Persist a generated \u003ccode>iris/spec.json\u003c/code> snapshot\u003c/h3>\n\u003cp>Add a versioned derived snapshot below \u003ccode>iris/\u003c/code> rather than re-reading \u003ccode>openspec/\u003c/code> every time the dashboard is regenerated. \u003ccode>iris init\u003c/code> and an explicit full render (\u003ccode>iris render --all\u003c/code>, plus the existing compatible bare \u003ccode>iris render\u003c/code> form) parse and atomically replace the snapshot before rendering the dashboard. Single-page render, report, archive, and update paths load the existing snapshot without touching OpenSpec. A missing snapshot normalizes to an undetected/empty view until the next authorized refresh.\u003c/p>\n\u003cp>This keeps the refresh contract observable and testable. Re-parsing inside the dashboard renderer was rejected because that renderer is called by operations that are not OpenSpec refresh actions. Extracting state back out of old HTML was rejected because generated markup is an output, not a data store.\u003c/p>\n\u003cp>The snapshot shape is deliberately UI-neutral:\u003c/p>\n\u003cpre>\u003ccode class=\"language-ts\">type OpenSpecSnapshot = {\n  version: 1;\n  detected: boolean;\n  generated_at: null;\n  context: { project?: SourceDocument; config?: SourceDocument };\n  canonical_specs: CapabilityRecord[];\n  active_changes: ChangeRecord[];\n  archived_changes: ChangeRecord[];\n  legacy_archives: SourceDocument[];\n  warnings: ParseWarning[];\n};\n\ntype ChangeRecord = {\n  name: string;\n  path: string;\n  lifecycle: 'active' | 'archived';\n  layout: 'structured' | 'legacy';\n  artifacts: { proposal?: SourceDocument; design?: SourceDocument; tasks?: TaskDocument };\n  delta_specs: CapabilityRecord[];\n  completeness: 'complete' | 'incomplete' | 'unknown';\n  health: 'valid' | 'warning' | 'invalid';\n};\n\u003c/code>\u003c/pre>\n\u003cp>\u003ccode>generated_at\u003c/code> stays \u003ccode>null\u003c/code>; timestamps would make unchanged renders nondeterministic. Arrays and warnings sort by normalized repository-relative path. Lifecycle, completeness, task progress, and parser health are never collapsed into one status.\u003c/p>\n\u003ch3 id=\"design-2-traverse-only-a-documented-allowlist-with-hard-resource-bo\">2. Traverse only a documented allowlist with hard resource bounds\u003c/h3>\n\u003cp>Implement a framework-free parser module that begins at the lexical and real \u003ccode>openspec/\u003c/code> root, uses \u003ccode>lstat\u003c/code>, refuses symlinks at every visited component, and never follows entries outside that root. It recognizes only:\u003c/p>\n\u003cul>\n\u003cli>\u003ccode>config.yaml\u003c/code> and \u003ccode>project.md\u003c/code>;\u003c/li>\n\u003cli>\u003ccode>specs/**/spec.md\u003c/code> with the full relative capability path;\u003c/li>\n\u003cli>\u003ccode>changes/&lt;name&gt;/.openspec.yaml\u003c/code>, \u003ccode>proposal.md\u003c/code>, \u003ccode>design.md\u003c/code>, \u003ccode>tasks.md\u003c/code>, and \u003ccode>specs/**/spec.md\u003c/code>;\u003c/li>\n\u003cli>the same structured files below \u003ccode>changes/archive/&lt;dated-name&gt;/\u003c/code>;\u003c/li>\n\u003cli>top-level \u003ccode>changes/archive/*.md\u003c/code> as legacy records.\u003c/li>\n\u003c/ul>\n\u003cp>Traversal is sorted and capped by named constants for depth, supported file count, and bytes per file. The implementation will start with depth 16, 1,000 supported files, and 1 MiB per file; hitting a bound yields a warning and preserves records already read. Unrecognized files remain untouched and are not treated as general documentation.\u003c/p>\n\u003cp>Recursive convenience APIs and glob libraries were rejected: neither provides the required per-component symlink checks and bounded failure reporting as clearly as a small explicit walker.\u003c/p>\n\u003ch3 id=\"design-3-use-a-conservative-line-parser-not-a-markdown-runtime\">3. Use a conservative line parser, not a Markdown runtime\u003c/h3>\n\u003cp>Parse only structural evidence Iris needs: headings, requirement/scenario headings, delta section labels, artifact presence, and task list items. A fence state machine excludes checkbox-like lines inside backtick or tilde fences. Supported task markers are Markdown list items with \u003ccode>[ ]\u003c/code>, \u003ccode>[x]\u003c/code>, or \u003ccode>[X]\u003c/code>; directory names and archive location never affect counts.\u003c/p>\n\u003cp>Every source document retains its bounded raw text for a readable fallback. The renderer escapes text and attribute values before inserting them into templates. It may turn recognized structures into semantic HTML, but it never passes raw source through as HTML and never evaluates YAML, Markdown, scripts, links, or code fences. YAML is displayed as escaped source and used only for simple presence/context labels; OpenSpec semantic validity remains outside Iris's claim.\u003c/p>\n\u003cp>Adding a Markdown package was rejected because the required view is structural, not a general documentation renderer, and plugin/sanitizer policy would add dependency and execution surface for little benefit.\u003c/p>\n\u003ch3 id=\"design-4-recover-per-path-and-make-unknowns-visible\">4. Recover per path and make unknowns visible\u003c/h3>\n\u003cp>Parsing returns records plus warnings instead of throwing for content problems. Missing expected artifacts make a structured change incomplete. Malformed headings, unreadable files, unsafe paths, bounds, and unsupported layouts attach stable warning codes and repository-relative paths. If raw text was read safely, the UI exposes it in an escaped \u003ccode>&lt;pre&gt;\u003c/code> disclosure. A failure to read one file cannot remove other valid records.\u003c/p>\n\u003cp>Only inability to write the CLI-owned snapshot/dashboard remains a command-level error. This separates untrusted input quality from Iris output integrity.\u003c/p>\n\u003ch3 id=\"design-5-add-peer-work-spec-tab-panels-to-the-generated-dashboard\">5. Add peer Work/Spec tab panels to the generated dashboard\u003c/h3>\n\u003cp>Extend \u003ccode>dashboardHtml\u003c/code> to accept the normalized snapshot and render:\u003c/p>\n\u003cpre>\u003ccode class=\"language-text\">Work | Spec\n       Overview: canonical, active, archived, open/completed tasks, warnings\n       Canonical specs\n       Active changes: proposal, design, tasks, delta specs\n       Archive: structured changes, then legacy Markdown\n\u003c/code>\u003c/pre>\n\u003cp>Within Spec, native headings, lists, links/anchors, and \u003ccode>&lt;details&gt;\u003c/code> disclosures provide most navigation without JavaScript. The existing classic deferred script owns only tab selection, roving keyboard focus, filter focus, and theme state. Work and Spec use \u003ccode>role=&quot;tablist&quot;\u003c/code>, \u003ccode>role=&quot;tab&quot;\u003c/code>, and linked \u003ccode>tabpanel\u003c/code> elements; List/Board remains nested inside Work with unique control ids. Color is paired with lifecycle labels, warning text/icons, and section position.\u003c/p>\n\u003cp>Spec styles extend the existing generated token/component strings and introduce no literal outside \u003ccode>TOKENS_CSS\u003c/code>. Long paths and source blocks wrap or scroll inside their component without causing page-level overflow at 360 px. Reduced motion disables view transitions while retaining immediate state changes.\u003c/p>\n\u003cp>A separate \u003ccode>iris/spec.html\u003c/code> was rejected because the user contract calls for a top-level dashboard tab and a second entry point would fragment navigation and offline integrity checks.\u003c/p>\n\u003ch3 id=\"design-6-verify-with-real-layout-copies-plus-synthetic-contract-fix\">6. Verify with real-layout copies plus synthetic contract fixtures\u003c/h3>\n\u003cp>Create temporary-repository fixtures for every observed layout: project/config, canonical specs, both current structured active states, structured archives, and legacy archive Markdown. Add synthetic fixtures for nested capabilities; ADDED, MODIFIED, REMOVED, and RENAMED sections; fenced fake checkboxes; absent/empty workspaces; partial/malformed changes; unsafe symlinks; oversized/deep/excess entries; unreadable paths where portable; and executable-looking source.\u003c/p>\n\u003cp>Parser tests assert normalized values and deterministic ordering. Orchestration tests prove only init/full render refresh the snapshot. Template/navigation tests assert escaping, unique ARIA relationships, keyboard behavior contracts, both themes, reduced-motion CSS, 360 px containment hooks, classic deferred scripts, and no remote URL. Dogfood is regenerated through the built CLI and checked by the existing HTML/link gate.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[OpenSpec formats evolve beyond the recognized subset] → retain escaped source, stable unknown-layout warnings, and path identity so the dashboard degrades honestly.\u003c/li>\n\u003cli>[Large workspaces make initialization slow or output huge] → enforce explicit bounds, skip only excess inputs, and report what was omitted.\u003c/li>\n\u003cli>[Persisted snapshots become stale after manual spec edits] → label refresh behavior in the UI/docs and refresh only on \u003ccode>init\u003c/code> or a full render as specified.\u003c/li>\n\u003cli>[Nested tab behavior creates duplicate ids or confusing focus] → namespace Work/Spec and List/Board controls and cover ARIA/keyboard behavior in generated HTML tests.\u003c/li>\n\u003cli>[Platform filesystem permissions differ] → keep portable parser fixtures primary and conditionally test permission-specific failures where the platform supports them.\u003c/li>\n\u003cli>[Raw fallback text enlarges \u003ccode>iris/index.html\u003c/code>] → bound every source file and total file count; prefer collapsed disclosures and deterministic truncation warnings if the aggregate render budget is reached.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add the parser, normalized model, atomic snapshot writer/loader, and fixtures without changing dashboard output.\u003c/li>\n\u003cli>Wire snapshot refresh to init and full-render entry points while other render callers reuse the stored snapshot.\u003c/li>\n\u003cli>Add Work/Spec information architecture, safe artifact rendering, and classic-script accessibility behavior.\u003c/li>\n\u003cli>Update documentation and regenerate \u003ccode>iris/spec.json\u003c/code> plus \u003ccode>iris/index.html\u003c/code> through the built CLI.\u003c/li>\n\u003cli>Verify the delta requirements, sync canonical specs, archive the change, and run the complete repository/install gate.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback removes the parser/snapshot integration and regenerates \u003ccode>iris/\u003c/code> with the prior CLI. \u003ccode>openspec/\u003c/code> is read-only throughout, so no source migration or recovery is required.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee `proposal.md` for motivation and the two delta specs for observable behavior. The current renderer rebuilds `iris/index.html` after page operations from page registry data, but it has no durable generated representation of OpenSpec. A single-page render, archive, report, or update may refresh the dashboard even though Phase 2 permits OpenSpec filesystem reads only during explicit workspace refreshes.\n\nThe repository currently proves four forms: canonical `specs/**/spec.md`, structured active change directories, structured archive directories, and legacy archived single Markdown files. Current capability paths are flat and current delta files primarily use `ADDED Requirements`; nested paths and the other delta operations need contract fixtures rather than claims of observed use.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Define one versioned normalized snapshot shared by parsing, rendering, and tests.\n- Keep filesystem reads deterministic, bounded, confined, symlink-safe, and tolerant of isolated failures.\n- Preserve the exact repository-relative identity of every supported OpenSpec record.\n- Make Work and Spec peer dashboard views without regressing file-based navigation or accessibility.\n- Make refresh ownership explicit so unrelated page operations reuse the last generated Spec snapshot.\n\n**Non-Goals:**\n\n- Semantic validation equivalent to `openspec validate` or use of private OpenSpec APIs.\n- A full Markdown engine, editable spec UI, search index, watcher, server, or dedicated refresh command.\n- Mermaid, charts, browser rendering, or general repository-document ingestion.\n\n## Decisions\n\n### 1. Persist a generated `iris/spec.json` snapshot\n\nAdd a versioned derived snapshot below `iris/` rather than re-reading `openspec/` every time the dashboard is regenerated. `iris init` and an explicit full render (`iris render --all`, plus the existing compatible bare `iris render` form) parse and atomically replace the snapshot before rendering the dashboard. Single-page render, report, archive, and update paths load the existing snapshot without touching OpenSpec. A missing snapshot normalizes to an undetected/empty view until the next authorized refresh.\n\nThis keeps the refresh contract observable and testable. Re-parsing inside the dashboard renderer was rejected because that renderer is called by operations that are not OpenSpec refresh actions. Extracting state back out of old HTML was rejected because generated markup is an output, not a data store.\n\nThe snapshot shape is deliberately UI-neutral:\n\n```ts\ntype OpenSpecSnapshot = {\n  version: 1;\n  detected: boolean;\n  generated_at: null;\n  context: { project?: SourceDocument; config?: SourceDocument };\n  canonical_specs: CapabilityRecord[];\n  active_changes: ChangeRecord[];\n  archived_changes: ChangeRecord[];\n  legacy_archives: SourceDocument[];\n  warnings: ParseWarning[];\n};\n\ntype ChangeRecord = {\n  name: string;\n  path: string;\n  lifecycle: &#039;active&#039; | &#039;archived&#039;;\n  layout: &#039;structured&#039; | &#039;legacy&#039;;\n  artifacts: { proposal?: SourceDocument; design?: SourceDocument; tasks?: TaskDocument };\n  delta_specs: CapabilityRecord[];\n  completeness: &#039;complete&#039; | &#039;incomplete&#039; | &#039;unknown&#039;;\n  health: &#039;valid&#039; | &#039;warning&#039; | &#039;invalid&#039;;\n};\n```\n\n`generated_at` stays `null`; timestamps would make unchanged renders nondeterministic. Arrays and warnings sort by normalized repository-relative path. Lifecycle, completeness, task progress, and parser health are never collapsed into one status.\n\n### 2. Traverse only a documented allowlist with hard resource bounds\n\nImplement a framework-free parser module that begins at the lexical and real `openspec/` root, uses `lstat`, refuses symlinks at every visited component, and never follows entries outside that root. It recognizes only:\n\n- `config.yaml` and `project.md`;\n- `specs/**/spec.md` with the full relative capability path;\n- `changes/&lt;name&gt;/.openspec.yaml`, `proposal.md`, `design.md`, `tasks.md`, and `specs/**/spec.md`;\n- the same structured files below `changes/archive/&lt;dated-name&gt;/`;\n- top-level `changes/archive/*.md` as legacy records.\n\nTraversal is sorted and capped by named constants for depth, supported file count, and bytes per file. The implementation will start with depth 16, 1,000 supported files, and 1 MiB per file; hitting a bound yields a warning and preserves records already read. Unrecognized files remain untouched and are not treated as general documentation.\n\nRecursive convenience APIs and glob libraries were rejected: neither provides the required per-component symlink checks and bounded failure reporting as clearly as a small explicit walker.\n\n### 3. Use a conservative line parser, not a Markdown runtime\n\nParse only structural evidence Iris needs: headings, requirement/scenario headings, delta section labels, artifact presence, and task list items. A fence state machine excludes checkbox-like lines inside backtick or tilde fences. Supported task markers are Markdown list items with `[ ]`, `[x]`, or `[X]`; directory names and archive location never affect counts.\n\nEvery source document retains its bounded raw text for a readable fallback. The renderer escapes text and attribute values before inserting them into templates. It may turn recognized structures into semantic HTML, but it never passes raw source through as HTML and never evaluates YAML, Markdown, scripts, links, or code fences. YAML is displayed as escaped source and used only for simple presence/context labels; OpenSpec semantic validity remains outside Iris&#039;s claim.\n\nAdding a Markdown package was rejected because the required view is structural, not a general documentation renderer, and plugin/sanitizer policy would add dependency and execution surface for little benefit.\n\n### 4. Recover per path and make unknowns visible\n\nParsing returns records plus warnings instead of throwing for content problems. Missing expected artifacts make a structured change incomplete. Malformed headings, unreadable files, unsafe paths, bounds, and unsupported layouts attach stable warning codes and repository-relative paths. If raw text was read safely, the UI exposes it in an escaped `&lt;pre&gt;` disclosure. A failure to read one file cannot remove other valid records.\n\nOnly inability to write the CLI-owned snapshot/dashboard remains a command-level error. This separates untrusted input quality from Iris output integrity.\n\n### 5. Add peer Work/Spec tab panels to the generated dashboard\n\nExtend `dashboardHtml` to accept the normalized snapshot and render:\n\n```text\nWork | Spec\n       Overview: canonical, active, archived, open/completed tasks, warnings\n       Canonical specs\n       Active changes: proposal, design, tasks, delta specs\n       Archive: structured changes, then legacy Markdown\n```\n\nWithin Spec, native headings, lists, links/anchors, and `&lt;details&gt;` disclosures provide most navigation without JavaScript. The existing classic deferred script owns only tab selection, roving keyboard focus, filter focus, and theme state. Work and Spec use `role=&quot;tablist&quot;`, `role=&quot;tab&quot;`, and linked `tabpanel` elements; List/Board remains nested inside Work with unique control ids. Color is paired with lifecycle labels, warning text/icons, and section position.\n\nSpec styles extend the existing generated token/component strings and introduce no literal outside `TOKENS_CSS`. Long paths and source blocks wrap or scroll inside their component without causing page-level overflow at 360 px. Reduced motion disables view transitions while retaining immediate state changes.\n\nA separate `iris/spec.html` was rejected because the user contract calls for a top-level dashboard tab and a second entry point would fragment navigation and offline integrity checks.\n\n### 6. Verify with real-layout copies plus synthetic contract fixtures\n\nCreate temporary-repository fixtures for every observed layout: project/config, canonical specs, both current structured active states, structured archives, and legacy archive Markdown. Add synthetic fixtures for nested capabilities; ADDED, MODIFIED, REMOVED, and RENAMED sections; fenced fake checkboxes; absent/empty workspaces; partial/malformed changes; unsafe symlinks; oversized/deep/excess entries; unreadable paths where portable; and executable-looking source.\n\nParser tests assert normalized values and deterministic ordering. Orchestration tests prove only init/full render refresh the snapshot. Template/navigation tests assert escaping, unique ARIA relationships, keyboard behavior contracts, both themes, reduced-motion CSS, 360 px containment hooks, classic deferred scripts, and no remote URL. Dogfood is regenerated through the built CLI and checked by the existing HTML/link gate.\n\n## Risks / Trade-offs\n\n- [OpenSpec formats evolve beyond the recognized subset] → retain escaped source, stable unknown-layout warnings, and path identity so the dashboard degrades honestly.\n- [Large workspaces make initialization slow or output huge] → enforce explicit bounds, skip only excess inputs, and report what was omitted.\n- [Persisted snapshots become stale after manual spec edits] → label refresh behavior in the UI/docs and refresh only on `init` or a full render as specified.\n- [Nested tab behavior creates duplicate ids or confusing focus] → namespace Work/Spec and List/Board controls and cover ARIA/keyboard behavior in generated HTML tests.\n- [Platform filesystem permissions differ] → keep portable parser fixtures primary and conditionally test permission-specific failures where the platform supports them.\n- [Raw fallback text enlarges `iris/index.html`] → bound every source file and total file count; prefer collapsed disclosures and deterministic truncation warnings if the aggregate render budget is reached.\n\n## Migration Plan\n\n1. Add the parser, normalized model, atomic snapshot writer/loader, and fixtures without changing dashboard output.\n2. Wire snapshot refresh to init and full-render entry points while other render callers reuse the stored snapshot.\n3. Add Work/Spec information architecture, safe artifact rendering, and classic-script accessibility behavior.\n4. Update documentation and regenerate `iris/spec.json` plus `iris/index.html` through the built CLI.\n5. Verify the delta requirements, sync canonical specs, archive the change, and run the complete repository/install gate.\n\nRollback removes the parser/snapshot integration and regenerates `iris/` with the prior CLI. `openspec/` is read-only throughout, so no source migration or recovery is required.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-build-the-normalized-openspec-parser\">1. Build the normalized OpenSpec parser\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Define the versioned snapshot, source-document, capability, change, task-progress, artifact-completeness, lifecycle, health, and warning types with deterministic empty defaults.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Implement sorted allowlisted discovery for project/config, canonical specs, structured active/archive changes, change-local specs, and legacy archive Markdown while preserving nested capability paths.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Add per-component confinement and symlink refusal plus explicit depth, supported-file-count, per-file-size, and aggregate-output bounds with stable path-specific warnings.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.4 Implement conservative line parsing for headings, requirements, scenarios, delta operations, and Markdown task checkboxes while excluding fenced examples.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.5 Preserve bounded raw source for fallback rendering and isolate malformed, unreadable, unknown, or partial inputs without losing valid sibling records.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-persist-and-orchestrate-explicit-snapshots\">2. Persist and orchestrate explicit snapshots\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add deterministic atomic write/load handling for the CLI-owned \u003ccode>iris/spec.json\u003c/code> snapshot without timestamps or OpenSpec runtime dependencies.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Refresh the snapshot during \u003ccode>iris init\u003c/code> before dashboard generation and represent missing or empty \u003ccode>openspec/\u003c/code> distinctly.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Refresh the snapshot for explicit full renders, including \u003ccode>iris render --all\u003c/code> and the compatible bare full-render form, before rebuilding the dashboard.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Make single-page render, report, archive, and update paths reuse the stored snapshot without reading \u003ccode>openspec/\u003c/code>.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-implement-the-work-and-spec-information-architecture\">3. Implement the Work and Spec information architecture\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Extend dashboard view data and templates with peer Work/\u003ccode>Spec\u003c/code> tab panels and unique nested List/Board control identities.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Render Spec overview counts, canonical specs, active changes with proposal/design/tasks/delta specs, structured archives, and legacy archives in deterministic order.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Render lifecycle, completeness, task progress, and parser health with independent textual/structural indicators and command-specific absent, empty, malformed, and partial states.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.4 Escape every source and attribute value, expose readable fallback source in bounded disclosures, and prove executable-looking content cannot become active markup.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.5 Extend the token-owned Aperture CSS and classic deferred script for tab semantics, roving keyboard focus, visible focus, theme parity, reduced-motion fallback, long-path handling, and 360 px responsiveness.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-cover-real-and-adversarial-filesystem-layouts\">4. Cover real and adversarial filesystem layouts\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Add fixtures matching every repository-observed OpenSpec layout: project/config, canonical specs, structured active changes, structured archives, and legacy archived Markdown.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Add synthetic contract fixtures for nested capability paths and ADDED, MODIFIED, REMOVED, and RENAMED delta sections without presenting them as repository-observed formats.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Add parser tests for deterministic ordering, artifact availability, independent status dimensions, real checkbox totals, fenced fake checkboxes, malformed Markdown, and partial changes.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 Add safety tests for symlinks, traversal/confinement, depth/count/size bounds, isolated filesystem failures where portable, raw-source escaping, and no content execution.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.5 Add orchestration tests proving init/full render refresh snapshots while single-page render, report, archive, and update preserve the prior snapshot with no implicit OpenSpec read.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.6 Extend generated-HTML tests for Work/Spec navigation, ARIA relationships, keyboard contracts, both themes, reduced motion, classic deferred scripts, zero remote requests, 360 px layout hooks, and \u003ccode>file://\u003c/code> link integrity.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-5-document-and-dogfood-the-spec-browser\">5. Document and dogfood the Spec browser\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.1 Update \u003ccode>README.md\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/tech.md\u003c/code>, \u003ccode>docs/design-system.md\u003c/code>, \u003ccode>docs/status.md\u003c/code>, and the installed \u003ccode>iris-workspace\u003c/code> skill for supported layouts, fallback limits, explicit refresh behavior, and OpenSpec CLI independence.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.2 Build the CLI and regenerate this repository's \u003ccode>iris/spec.json\u003c/code>, managed assets, and dashboard through \u003ccode>iris init\u003c/code> and \u003ccode>iris render --all\u003c/code> without hand-editing generated output.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.3 Inspect the generated diff and run HTML/link integrity checks to confirm readable Spec navigation, retained Work behavior, and no unexpected repository-document ingestion.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-6-complete-lifecycle-verification\">6. Complete lifecycle verification\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.1 Run lint, token lint, typecheck, the full Vitest suite, HTML integrity, and packed-install smoke as the complete repository gate.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 6.2 Run strict OpenSpec validation and the verification workflow, reconcile every requirement and scenario with code/test evidence, and leave no incomplete task before spec sync and archive.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Build the normalized OpenSpec parser\n\n- [x] 1.1 Define the versioned snapshot, source-document, capability, change, task-progress, artifact-completeness, lifecycle, health, and warning types with deterministic empty defaults.\n- [x] 1.2 Implement sorted allowlisted discovery for project/config, canonical specs, structured active/archive changes, change-local specs, and legacy archive Markdown while preserving nested capability paths.\n- [x] 1.3 Add per-component confinement and symlink refusal plus explicit depth, supported-file-count, per-file-size, and aggregate-output bounds with stable path-specific warnings.\n- [x] 1.4 Implement conservative line parsing for headings, requirements, scenarios, delta operations, and Markdown task checkboxes while excluding fenced examples.\n- [x] 1.5 Preserve bounded raw source for fallback rendering and isolate malformed, unreadable, unknown, or partial inputs without losing valid sibling records.\n\n## 2. Persist and orchestrate explicit snapshots\n\n- [x] 2.1 Add deterministic atomic write/load handling for the CLI-owned `iris/spec.json` snapshot without timestamps or OpenSpec runtime dependencies.\n- [x] 2.2 Refresh the snapshot during `iris init` before dashboard generation and represent missing or empty `openspec/` distinctly.\n- [x] 2.3 Refresh the snapshot for explicit full renders, including `iris render --all` and the compatible bare full-render form, before rebuilding the dashboard.\n- [x] 2.4 Make single-page render, report, archive, and update paths reuse the stored snapshot without reading `openspec/`.\n\n## 3. Implement the Work and Spec information architecture\n\n- [x] 3.1 Extend dashboard view data and templates with peer Work/`Spec` tab panels and unique nested List/Board control identities.\n- [x] 3.2 Render Spec overview counts, canonical specs, active changes with proposal/design/tasks/delta specs, structured archives, and legacy archives in deterministic order.\n- [x] 3.3 Render lifecycle, completeness, task progress, and parser health with independent textual/structural indicators and command-specific absent, empty, malformed, and partial states.\n- [x] 3.4 Escape every source and attribute value, expose readable fallback source in bounded disclosures, and prove executable-looking content cannot become active markup.\n- [x] 3.5 Extend the token-owned Aperture CSS and classic deferred script for tab semantics, roving keyboard focus, visible focus, theme parity, reduced-motion fallback, long-path handling, and 360 px responsiveness.\n\n## 4. Cover real and adversarial filesystem layouts\n\n- [x] 4.1 Add fixtures matching every repository-observed OpenSpec layout: project/config, canonical specs, structured active changes, structured archives, and legacy archived Markdown.\n- [x] 4.2 Add synthetic contract fixtures for nested capability paths and ADDED, MODIFIED, REMOVED, and RENAMED delta sections without presenting them as repository-observed formats.\n- [x] 4.3 Add parser tests for deterministic ordering, artifact availability, independent status dimensions, real checkbox totals, fenced fake checkboxes, malformed Markdown, and partial changes.\n- [x] 4.4 Add safety tests for symlinks, traversal/confinement, depth/count/size bounds, isolated filesystem failures where portable, raw-source escaping, and no content execution.\n- [x] 4.5 Add orchestration tests proving init/full render refresh snapshots while single-page render, report, archive, and update preserve the prior snapshot with no implicit OpenSpec read.\n- [x] 4.6 Extend generated-HTML tests for Work/Spec navigation, ARIA relationships, keyboard contracts, both themes, reduced motion, classic deferred scripts, zero remote requests, 360 px layout hooks, and `file://` link integrity.\n\n## 5. Document and dogfood the Spec browser\n\n- [x] 5.1 Update `README.md`, `docs/cmds.md`, `docs/tech.md`, `docs/design-system.md`, `docs/status.md`, and the installed `iris-workspace` skill for supported layouts, fallback limits, explicit refresh behavior, and OpenSpec CLI independence.\n- [x] 5.2 Build the CLI and regenerate this repository&#039;s `iris/spec.json`, managed assets, and dashboard through `iris init` and `iris render --all` without hand-editing generated output.\n- [x] 5.3 Inspect the generated diff and run HTML/link integrity checks to confirm readable Spec navigation, retained Work behavior, and no unexpected repository-document ingestion.\n\n## 6. Complete lifecycle verification\n\n- [x] 6.1 Run lint, token lint, typecheck, the full Vitest suite, HTML integrity, and packed-install smoke as the complete repository gate.\n- [x] 6.2 Run strict OpenSpec validation and the verification workflow, reconcile every requirement and scenario with code/test evidence, and leave no incomplete task before spec sync and archive.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated dashboard MUST provide top-level Work and \u003ccode>Spec\u003c/code> views, preserve the Work view's briefing hero, health strip, architecture pane, work surface, and project-docs strip, and organize the Spec view as overview, canonical specs, active changes with artifacts and delta specs, and archive.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the dashboard from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the top-level tab labelled \u003ccode>Spec\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST show OpenSpec overview counts and navigable canonical, active-change, and archive sections while keeping the Work view available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/h3>\n\u003cp>The generated dashboard MUST retain classic deferred-script interactions for top-level view selection, filtering, theme switching, and keyboard navigation and MUST remain operable at a 360 px viewport.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-keyboard-navigation-is-used\">Scenario: keyboard navigation is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses \u003ccode>/\u003c/code> or \u003ccode>t\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> \u003ccode>/\u003c/code> MUST focus the relevant visible filter and \u003ccode>t\u003c/code> MUST toggle the theme without loading external assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-top-level-tabs-are-keyboard-operated\">Scenario: top-level tabs are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on the Work/Spec tablist and the user presses supported arrow, Home, or End keys\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> focus and selection MUST move according to the tab pattern with visible focus and correct tab/panel semantics\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport-is-used\">Scenario: narrow viewport is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the dashboard viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Work and Spec views, their navigation, summaries, artifact content, warnings, and source fallbacks MUST remain readable without horizontal page overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-reduced-motion-is-requested\">Scenario: reduced motion is requested\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user enables reduced-motion preferences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> switching views and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: newcomer-first dashboard hierarchy\nThe generated dashboard MUST provide top-level Work and `Spec` views, preserve the Work view&#039;s briefing hero, health strip, architecture pane, work surface, and project-docs strip, and organize the Spec view as overview, canonical specs, active changes with artifacts and delta specs, and archive.\n\n#### Scenario: repository has no generated work pages\n- **WHEN** a newcomer opens the dashboard from `file://`\n- **THEN** the dashboard MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n#### Scenario: user opens the Spec view\n- **WHEN** a user activates the top-level tab labelled `Spec`\n- **THEN** the dashboard MUST show OpenSpec overview counts and navigable canonical, active-change, and archive sections while keeping the Work view available\n\n#### Scenario: state is communicated in the Spec view\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n### Requirement: deterministic responsive interaction\nThe generated dashboard MUST retain classic deferred-script interactions for top-level view selection, filtering, theme switching, and keyboard navigation and MUST remain operable at a 360 px viewport.\n\n#### Scenario: keyboard navigation is used\n- **WHEN** the user presses `/` or `t` outside an editable field\n- **THEN** `/` MUST focus the relevant visible filter and `t` MUST toggle the theme without loading external assets\n\n#### Scenario: top-level tabs are keyboard operated\n- **WHEN** focus is on the Work/Spec tablist and the user presses supported arrow, Home, or End keys\n- **THEN** focus and selection MUST move according to the tab pattern with visible focus and correct tab/panel semantics\n\n#### Scenario: narrow viewport is used\n- **WHEN** the dashboard viewport is 360 px wide\n- **THEN** the Work and Spec views, their navigation, summaries, artifact content, warnings, and source fallbacks MUST remain readable without horizontal page overflow\n\n#### Scenario: reduced motion is requested\n- **WHEN** the user enables reduced-motion preferences\n- **THEN** switching views and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-openspec-spec-browser-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · openspec-spec-browser\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-openspec-spec-browser/specs/openspec-spec-browser/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-openspec-spec-browser-purpose\">Purpose\u003c/h2>\n\u003cp>Provide a deterministic, offline dashboard view of the repository's complete OpenSpec filesystem so users and agents can inspect specifications, change artifacts, and real task progress without running OpenSpec.\u003c/p>\n\u003ch2 id=\"delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-explicit-openspec-workspace-snapshots\">Requirement: explicit OpenSpec workspace snapshots\u003c/h3>\n\u003cp>The system MUST detect and read the repository's \u003ccode>openspec/\u003c/code> workspace during \u003ccode>iris init\u003c/code> and \u003ccode>iris render --all\u003c/code>, and MUST NOT introduce implicit synchronization, background monitoring, or a separate refresh command for the same operation.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-repository-is-initialized-with-openspec\">Scenario: repository is initialized with OpenSpec\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris init\u003c/code> in a repository containing \u003ccode>openspec/\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated dashboard MUST contain a snapshot of that workspace from the files read during initialization\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-explicitly-refreshes-all-output\">Scenario: user explicitly refreshes all output\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris render --all\u003c/code> after OpenSpec files change\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST re-read the current OpenSpec filesystem and replace the previous generated snapshot\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-renders-one-work-page\">Scenario: user renders one work page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris render &lt;id&gt;\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST render that page without treating the operation as an OpenSpec workspace refresh\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-complete-normalized-workspace-view\">Requirement: complete normalized workspace view\u003c/h3>\n\u003cp>The system MUST represent canonical specs, active changes, structured archived changes, legacy archived Markdown, project/config context, proposal, design, tasks, and change-local delta specs while keeping canonical, active, archived, legacy, invalid, and incomplete states distinct.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-repository-contains-current-structured-layouts\">Scenario: repository contains current structured layouts\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical specs and structured active or archived changes are present\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST preserve their repository-relative identities, lifecycle state, artifact availability, requirements, scenarios, and delta-operation labels\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-capability-path-is-nested\">Scenario: capability path is nested\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a canonical or change-local spec exists below a nested capability path\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST preserve and display the complete capability path without flattening ambiguous names\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-archive-contains-a-legacy-markdown-record\">Scenario: archive contains a legacy Markdown record\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>openspec/changes/archive/\u003c/code> contains an archived single Markdown file rather than a structured change directory\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST identify it as legacy archive content and keep it separate from structured archived changes\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-evidence-based-task-and-artifact-status\">Requirement: evidence-based task and artifact status\u003c/h3>\n\u003cp>The system MUST derive task totals only from actual Markdown task checkboxes and MUST keep task progress, artifact completeness, lifecycle state, and parser health as independent dimensions.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-tasks-contain-checked-and-unchecked-items\">Scenario: tasks contain checked and unchecked items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a structured change has task list items using supported Markdown checkbox syntax outside fenced code\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST report completed, open, and total task counts from those items without inferring completion from the directory name or archive location\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-change-is-partially-complete\">Scenario: change is partially complete\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a change is missing an expected artifact or contains open tasks\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST label the available evidence and incompleteness without presenting the change as invalid or complete by assumption\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-checkbox-like-text-appears-in-a-code-fence\">Scenario: checkbox-like text appears in a code fence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact contains checkbox syntax inside fenced source examples\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that text MUST NOT contribute to task progress\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-safe-bounded-filesystem-parsing\">Requirement: safe bounded filesystem parsing\u003c/h3>\n\u003cp>The system MUST parse OpenSpec as untrusted local content using deterministic sorted traversal, repository confinement, bounded input, and symlink refusal, and MUST never execute file content.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-content-contains-active-markup-or-script-text\">Scenario: content contains active markup or script text\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an OpenSpec file contains HTML, script text, or other executable-looking content\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every rendered representation MUST escape the content so opening the dashboard cannot execute it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-path-is-unsafe-or-input-exceeds-a-bound\">Scenario: path is unsafe or input exceeds a bound\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> traversal encounters a symlink, path escape, unsupported file location, excessive depth, excessive file count, or oversized file\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the parser MUST skip the unsafe input, retain the rest of the workspace, and expose a path-specific actionable warning\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-filesystem-entry-cannot-be-read\">Scenario: filesystem entry cannot be read\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> one OpenSpec file or directory produces a filesystem error\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the parser MUST preserve successfully read entries and report the isolated failure rather than crashing the entire dashboard refresh\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-graceful-unknown-and-empty-states\">Requirement: graceful unknown and empty states\u003c/h3>\n\u003cp>The system MUST render readable fallback states for absent, empty, malformed, unsupported, or partially complete OpenSpec workspaces.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-repository-has-no-openspec-directory\">Scenario: repository has no OpenSpec directory\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the dashboard snapshot is generated without an \u003ccode>openspec/\u003c/code> directory\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST explain that no OpenSpec workspace was detected without suggesting general-document ingestion\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-openspec-workspace-is-empty\">Scenario: OpenSpec workspace is empty\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>openspec/\u003c/code> exists but contains no supported project, spec, or change records\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST show a distinct empty-workspace state\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-artifact-is-malformed-or-unknown\">Scenario: artifact is malformed or unknown\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a recognized path contains malformed Markdown or an unsupported layout\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec tab MUST render escaped readable source when available, identify the affected path, and show an actionable warning without hiding other valid records\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-offline-runtime-independence\">Requirement: offline runtime independence\u003c/h3>\n\u003cp>The system MUST generate and operate the Spec view without requiring the OpenSpec CLI, a server, network access, runtime ES modules, telemetry, or general project-document ingestion.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-dashboard-opens-directly-from-disk\">Scenario: dashboard opens directly from disk\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens \u003ccode>iris/index.html\u003c/code> through \u003ccode>file://\u003c/code> after generation\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST remain navigable and readable using only local generated assets and classic deferred scripts with zero network requests\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-openspec-executable-is-unavailable\">Scenario: OpenSpec executable is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Iris initializes or renders in a repository whose \u003ccode>openspec/\u003c/code> files exist but no OpenSpec CLI is installed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the filesystem snapshot and Spec view MUST still be generated successfully\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nProvide a deterministic, offline dashboard view of the repository&#039;s complete OpenSpec filesystem so users and agents can inspect specifications, change artifacts, and real task progress without running OpenSpec.\n\n## ADDED Requirements\n\n### Requirement: explicit OpenSpec workspace snapshots\nThe system MUST detect and read the repository&#039;s `openspec/` workspace during `iris init` and `iris render --all`, and MUST NOT introduce implicit synchronization, background monitoring, or a separate refresh command for the same operation.\n\n#### Scenario: repository is initialized with OpenSpec\n- **WHEN** a user runs `iris init` in a repository containing `openspec/`\n- **THEN** the generated dashboard MUST contain a snapshot of that workspace from the files read during initialization\n\n#### Scenario: user explicitly refreshes all output\n- **WHEN** a user runs `iris render --all` after OpenSpec files change\n- **THEN** the dashboard MUST re-read the current OpenSpec filesystem and replace the previous generated snapshot\n\n#### Scenario: user renders one work page\n- **WHEN** a user runs `iris render &lt;id&gt;`\n- **THEN** the system MUST render that page without treating the operation as an OpenSpec workspace refresh\n\n### Requirement: complete normalized workspace view\nThe system MUST represent canonical specs, active changes, structured archived changes, legacy archived Markdown, project/config context, proposal, design, tasks, and change-local delta specs while keeping canonical, active, archived, legacy, invalid, and incomplete states distinct.\n\n#### Scenario: repository contains current structured layouts\n- **WHEN** canonical specs and structured active or archived changes are present\n- **THEN** the Spec view MUST preserve their repository-relative identities, lifecycle state, artifact availability, requirements, scenarios, and delta-operation labels\n\n#### Scenario: capability path is nested\n- **WHEN** a canonical or change-local spec exists below a nested capability path\n- **THEN** the Spec view MUST preserve and display the complete capability path without flattening ambiguous names\n\n#### Scenario: archive contains a legacy Markdown record\n- **WHEN** `openspec/changes/archive/` contains an archived single Markdown file rather than a structured change directory\n- **THEN** the Spec view MUST identify it as legacy archive content and keep it separate from structured archived changes\n\n### Requirement: evidence-based task and artifact status\nThe system MUST derive task totals only from actual Markdown task checkboxes and MUST keep task progress, artifact completeness, lifecycle state, and parser health as independent dimensions.\n\n#### Scenario: tasks contain checked and unchecked items\n- **WHEN** a structured change has task list items using supported Markdown checkbox syntax outside fenced code\n- **THEN** the Spec view MUST report completed, open, and total task counts from those items without inferring completion from the directory name or archive location\n\n#### Scenario: change is partially complete\n- **WHEN** a change is missing an expected artifact or contains open tasks\n- **THEN** the Spec view MUST label the available evidence and incompleteness without presenting the change as invalid or complete by assumption\n\n#### Scenario: checkbox-like text appears in a code fence\n- **WHEN** an artifact contains checkbox syntax inside fenced source examples\n- **THEN** that text MUST NOT contribute to task progress\n\n### Requirement: safe bounded filesystem parsing\nThe system MUST parse OpenSpec as untrusted local content using deterministic sorted traversal, repository confinement, bounded input, and symlink refusal, and MUST never execute file content.\n\n#### Scenario: content contains active markup or script text\n- **WHEN** an OpenSpec file contains HTML, script text, or other executable-looking content\n- **THEN** every rendered representation MUST escape the content so opening the dashboard cannot execute it\n\n#### Scenario: path is unsafe or input exceeds a bound\n- **WHEN** traversal encounters a symlink, path escape, unsupported file location, excessive depth, excessive file count, or oversized file\n- **THEN** the parser MUST skip the unsafe input, retain the rest of the workspace, and expose a path-specific actionable warning\n\n#### Scenario: filesystem entry cannot be read\n- **WHEN** one OpenSpec file or directory produces a filesystem error\n- **THEN** the parser MUST preserve successfully read entries and report the isolated failure rather than crashing the entire dashboard refresh\n\n### Requirement: graceful unknown and empty states\nThe system MUST render readable fallback states for absent, empty, malformed, unsupported, or partially complete OpenSpec workspaces.\n\n#### Scenario: repository has no OpenSpec directory\n- **WHEN** the dashboard snapshot is generated without an `openspec/` directory\n- **THEN** the Spec tab MUST explain that no OpenSpec workspace was detected without suggesting general-document ingestion\n\n#### Scenario: OpenSpec workspace is empty\n- **WHEN** `openspec/` exists but contains no supported project, spec, or change records\n- **THEN** the Spec tab MUST show a distinct empty-workspace state\n\n#### Scenario: artifact is malformed or unknown\n- **WHEN** a recognized path contains malformed Markdown or an unsupported layout\n- **THEN** the Spec tab MUST render escaped readable source when available, identify the affected path, and show an actionable warning without hiding other valid records\n\n### Requirement: offline runtime independence\nThe system MUST generate and operate the Spec view without requiring the OpenSpec CLI, a server, network access, runtime ES modules, telemetry, or general project-document ingestion.\n\n#### Scenario: dashboard opens directly from disk\n- **WHEN** a user opens `iris/index.html` through `file://` after generation\n- **THEN** the Spec view MUST remain navigable and readable using only local generated assets and classic deferred scripts with zero network requests\n\n#### Scenario: OpenSpec executable is unavailable\n- **WHEN** Iris initializes or renders in a repository whose `openspec/` files exist but no OpenSpec CLI is installed\n- **THEN** the filesystem snapshot and Spec view MUST still be generated successfully\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-1-persist-a-generated-iris-spec-json-snapshot\">1. Persist a generated `iris/spec.json` snapshot\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-2-traverse-only-a-documented-allowlist-with-hard-resource-bo\">2. Traverse only a documented allowlist with hard resource bounds\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-3-use-a-conservative-line-parser-not-a-markdown-runtime\">3. Use a conservative line parser, not a Markdown runtime\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-4-recover-per-path-and-make-unknowns-visible\">4. Recover per path and make unknowns visible\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-5-add-peer-work-spec-tab-panels-to-the-generated-dashboard\">5. Add peer Work/Spec tab panels to the generated dashboard\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-6-verify-with-real-layout-copies-plus-synthetic-contract-fix\">6. Verify with real-layout copies plus synthetic contract fixtures\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-build-the-normalized-openspec-parser\">1. Build the normalized OpenSpec parser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-persist-and-orchestrate-explicit-snapshots\">2. Persist and orchestrate explicit snapshots\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-implement-the-work-and-spec-information-architecture\">3. Implement the Work and Spec information architecture\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-cover-real-and-adversarial-filesystem-layouts\">4. Cover real and adversarial filesystem layouts\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-5-document-and-dogfood-the-spec-browser\">5. Document and dogfood the Spec browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-6-complete-lifecycle-verification\">6. Complete lifecycle verification\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-artifact\">Delta spec · openspec-spec-browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-explicit-openspec-workspace-snapshots\">Requirement: explicit OpenSpec workspace snapshots\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-complete-normalized-workspace-view\">Requirement: complete normalized workspace view\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-evidence-based-task-and-artifact-status\">Requirement: evidence-based task and artifact status\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-safe-bounded-filesystem-parsing\">Requirement: safe bounded filesystem parsing\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-graceful-unknown-and-empty-states\">Requirement: graceful unknown and empty states\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-offline-runtime-independence\">Requirement: offline runtime independence\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-project-lifecycle-automation":{"kind":"change","name":"2026-08-21-project-lifecycle-automation","title":"2026-08-21-project-lifecycle-automation","path":"changes/archive/2026-08-21-project-lifecycle-automation","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-project-lifecycle-automation\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-project-lifecycle-automation\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">15\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-project-lifecycle-automation: 15 of 15 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">15/15 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-project-lifecycle-automation/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The CLI contract already includes lifecycle operations such as \u003ccode>sync\u003c/code>, \u003ccode>adopt\u003c/code>, \u003ccode>archive\u003c/code>, \u003ccode>update\u003c/code>, \u003ccode>vendor\u003c/code>, and \u003ccode>open\u003c/code>, but the project still treats most of these as planned work rather than a connected operational flow. Without an explicit lifecycle layer, the generated dashboard and page set can drift from the repo, the docs, and the user’s real working state.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Implement the project lifecycle commands that keep the generated \u003ccode>iris\u003c/code> structure aligned with the repository.\u003c/li>\n\u003cli>Add a sync path for incremental updates and stale-state detection.\u003c/li>\n\u003cli>Add a doc-adoption flow for mirroring README and related markdown artifacts into managed pages.\u003c/li>\n\u003cli>Add archive and update steps for keeping the dashboard clean while preserving historical context.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>project-lifecycle-automation: manage the local project surface, dashboard state, and page lifecycle without manual drift.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>init: expand the bootstrap flow to set up a more complete lifecycle-aware project skeleton.\u003c/li>\n\u003cli>sync: formalize the incremental update behavior for feed, stale marks, and re-render decisions.\u003c/li>\n\u003cli>adopt: add a concrete doc-mirroring workflow for markdown assets.\u003c/li>\n\u003cli>archive: define when a page is archived and how the registry is updated.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>The project gains a coherent operational loop rather than isolated one-off commands.\u003c/li>\n\u003cli>Local documentation and generated pages stay closer to the actual repo state.\u003c/li>\n\u003cli>Users can manage the dashboard and generated artifact set without manually editing page metadata.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe CLI contract already includes lifecycle operations such as `sync`, `adopt`, `archive`, `update`, `vendor`, and `open`, but the project still treats most of these as planned work rather than a connected operational flow. Without an explicit lifecycle layer, the generated dashboard and page set can drift from the repo, the docs, and the user’s real working state.\n\n## What Changes\n\n- Implement the project lifecycle commands that keep the generated `iris` structure aligned with the repository.\n- Add a sync path for incremental updates and stale-state detection.\n- Add a doc-adoption flow for mirroring README and related markdown artifacts into managed pages.\n- Add archive and update steps for keeping the dashboard clean while preserving historical context.\n\n## Capabilities\n\n### New Capabilities\n- project-lifecycle-automation: manage the local project surface, dashboard state, and page lifecycle without manual drift.\n\n### Modified Capabilities\n- init: expand the bootstrap flow to set up a more complete lifecycle-aware project skeleton.\n- sync: formalize the incremental update behavior for feed, stale marks, and re-render decisions.\n- adopt: add a concrete doc-mirroring workflow for markdown assets.\n- archive: define when a page is archived and how the registry is updated.\n\n## Impact\n\n- The project gains a coherent operational loop rather than isolated one-off commands.\n- Local documentation and generated pages stay closer to the actual repo state.\n- Users can manage the dashboard and generated artifact set without manually editing page metadata.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-project-lifecycle-automation/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The project has a clear product identity and a documented command surface, but several lifecycle commands are intentionally stubbed out. The design goal is to keep the generated \u003ccode>iris\u003c/code> workspace active, up-to-date, and understandable without forcing users to manually maintain page metadata, dashboard links, or markdown-derived pages.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Turn the CLI lifecycle commands into a coherent operational model.\u003c/li>\n\u003cli>Keep the generated project state aligned with repo files and documentation updates.\u003c/li>\n\u003cli>Archive stale pages without losing the underlying page data.\u003c/li>\n\u003cli>Maintain managed content boundaries so generated blocks do not wipe user work.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Full multi-repo orchestration or remote sync.\u003c/li>\n\u003cli>Automatic opinionated content rewriting beyond the project’s own lifecycle operations.\u003c/li>\n\u003cli>A hosted backup or SaaS-driven project lifecycle.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Treat lifecycle commands as the project’s operational layer rather than separate feature hacks.\u003c/li>\n\u003cli>Keep \u003ccode>iris\u003c/code> pages and the dashboard as the canonical project memory, with repo markdown and other artifacts providing data inputs.\u003c/li>\n\u003cli>Prefer selective refresh and stale-state toggles over broad re-renders after every shell event.\u003c/li>\n\u003cli>Preserve user-authored files outside managed boundaries while updating generated integration code or scaffolding.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Project drift] → Mitigation: add sync and stale-state checks before re-rendering or archiving pages.\u003c/li>\n\u003cli>[User edits getting clobbered] → Mitigation: keep managed/unmanaged boundaries explicit and update only managed blocks.\u003c/li>\n\u003cli>[Lack of strong lifecycle semantics] → Mitigation: define a narrow set of lifecycle commands and keep them modest and deterministic.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Complete the project scaffold and baseline lifecycle configuration.\u003c/li>\n\u003cli>Implement the sync and stale-state decisions for the existing dashboard and page registry.\u003c/li>\n\u003cli>Add the adopt flow for README/docs-driven pages.\u003c/li>\n\u003cli>Add archive and update actions with boundary-safe behavior.\u003c/li>\n\u003cli>Validate the lifecycle flow with a real repo example and smoke tests.\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"design-open-questions\">Open Questions\u003c/h2>\n\u003cp>None at this stage; the command set is already defined in the CLI contract and should guide the implementation.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe project has a clear product identity and a documented command surface, but several lifecycle commands are intentionally stubbed out. The design goal is to keep the generated `iris` workspace active, up-to-date, and understandable without forcing users to manually maintain page metadata, dashboard links, or markdown-derived pages.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Turn the CLI lifecycle commands into a coherent operational model.\n- Keep the generated project state aligned with repo files and documentation updates.\n- Archive stale pages without losing the underlying page data.\n- Maintain managed content boundaries so generated blocks do not wipe user work.\n\n**Non-Goals:**\n- Full multi-repo orchestration or remote sync.\n- Automatic opinionated content rewriting beyond the project’s own lifecycle operations.\n- A hosted backup or SaaS-driven project lifecycle.\n\n## Decisions\n\n- Treat lifecycle commands as the project’s operational layer rather than separate feature hacks.\n- Keep `iris` pages and the dashboard as the canonical project memory, with repo markdown and other artifacts providing data inputs.\n- Prefer selective refresh and stale-state toggles over broad re-renders after every shell event.\n- Preserve user-authored files outside managed boundaries while updating generated integration code or scaffolding.\n\n## Risks / Trade-offs\n\n- [Project drift] → Mitigation: add sync and stale-state checks before re-rendering or archiving pages.\n- [User edits getting clobbered] → Mitigation: keep managed/unmanaged boundaries explicit and update only managed blocks.\n- [Lack of strong lifecycle semantics] → Mitigation: define a narrow set of lifecycle commands and keep them modest and deterministic.\n\n## Migration Plan\n\n1. Complete the project scaffold and baseline lifecycle configuration.\n2. Implement the sync and stale-state decisions for the existing dashboard and page registry.\n3. Add the adopt flow for README/docs-driven pages.\n4. Add archive and update actions with boundary-safe behavior.\n5. Validate the lifecycle flow with a real repo example and smoke tests.\n\n## Open Questions\n\nNone at this stage; the command set is already defined in the CLI contract and should guide the implementation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-project-lifecycle-automation/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-lifecycle-scaffold-and-setup\">1. Lifecycle scaffold and setup\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Define the baseline \u003ccode>iris\u003c/code> project structure created by \u003ccode>init\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Ensure the scaffold includes the page registry, dashboard surfaces, and lifecycle metadata needed for future commands.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Add a minimal validation pass for a fresh project init.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-sync-and-stale-state-detection\">2. Sync and stale-state detection\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Implement the project sync rule set for incremental updates and stale marks.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Decide when a page should be refreshed versus left alone.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add CLI output to make the state transitions discoverable.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-document-adoption-and-mirroring\">3. Document adoption and mirroring\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Define the docs-to-page adoption contract for README and markdown assets.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Add the \u003ccode>adopt\u003c/code> flow so generated pages are created from repo documentation.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Ensure adopted content remains traceable back to the source document.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-archive-and-update-management\">4. Archive and update management\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Add the archive flow for stale or superseded pages.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Implement the update flow for managed blocks without clobbering user content.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Keep dashboard and registry state consistent after lifecycle actions.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-5-validation-and-rollout\">5. Validation and rollout\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.1 Add smoke tests covering init, sync, adopt, archive, and update.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.2 Document the command usage and expected lifecycle behavior.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.3 Review the change against the project roadmap to confirm it stays scoped to lifecycle automation.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Lifecycle scaffold and setup\n\n- [x] 1.1 Define the baseline `iris` project structure created by `init`.\n- [x] 1.2 Ensure the scaffold includes the page registry, dashboard surfaces, and lifecycle metadata needed for future commands.\n- [x] 1.3 Add a minimal validation pass for a fresh project init.\n\n## 2. Sync and stale-state detection\n\n- [x] 2.1 Implement the project sync rule set for incremental updates and stale marks.\n- [x] 2.2 Decide when a page should be refreshed versus left alone.\n- [x] 2.3 Add CLI output to make the state transitions discoverable.\n\n## 3. Document adoption and mirroring\n\n- [x] 3.1 Define the docs-to-page adoption contract for README and markdown assets.\n- [x] 3.2 Add the `adopt` flow so generated pages are created from repo documentation.\n- [x] 3.3 Ensure adopted content remains traceable back to the source document.\n\n## 4. Archive and update management\n\n- [x] 4.1 Add the archive flow for stale or superseded pages.\n- [x] 4.2 Implement the update flow for managed blocks without clobbering user content.\n- [x] 4.3 Keep dashboard and registry state consistent after lifecycle actions.\n\n## 5. Validation and rollout\n\n- [x] 5.1 Add smoke tests covering init, sync, adopt, archive, and update.\n- [x] 5.2 Document the command usage and expected lifecycle behavior.\n- [x] 5.3 Review the change against the project roadmap to confirm it stays scoped to lifecycle automation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-project-lifecycle-automation/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-18\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-project-lifecycle-automation-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · project-lifecycle-automation\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-project-lifecycle-automation/specs/project-lifecycle-automation/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-project-lifecycle-automation-purpose\">Purpose\u003c/h2>\n\u003cp>Make the \u003ccode>iris\u003c/code> project lifecycle explicit and reproducible by aligning the generated dashboard and page registry with the repository state and user workflow. The system must provide a safe path for boosting, syncing, archiving, and updating local project artifacts while preserving user-owned content outside managed blocks.\u003c/p>\n\u003ch2 id=\"delta-project-lifecycle-automation-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-lifecycle-aware-project-bootstrap\">Requirement: lifecycle-aware project bootstrap\u003c/h3>\n\u003cp>The system MUST provide a project bootstrap flow that creates the local structure needed for page generation, dashboard rendering, and managed updates.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-user-initializes-the-project\">Scenario: user initializes the project\u003c/h4>\n\u003cp>WHEN a user runs the init flow in a repository\nTHEN the system MUST create the required \u003ccode>iris/\u003c/code> structure, scaffold task metadata, and establish the baseline workflow for later lifecycle commands.\u003c/p>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-incremental-sync-and-stale-state-awareness\">Requirement: incremental sync and stale-state awareness\u003c/h3>\n\u003cp>The system MUST support a sync workflow that detects drift between repository state and the generated dashboard or page artifacts.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-repo-changes-after-a-page-was-generated\">Scenario: repo changes after a page was generated\u003c/h4>\n\u003cp>WHEN a project changes or a document is updated\nTHEN the sync flow MUST decide whether to refresh, flag stale state, or skip re-rendering when the data has not meaningfully changed.\u003c/p>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-doc-adoption-and-mirroring\">Requirement: doc adoption and mirroring\u003c/h3>\n\u003cp>The system MUST provide a workflow for adopting repository markdown or docs into managed page records without losing the original source.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-user-runs-adopt-on-the-project-root\">Scenario: user runs adopt on the project root\u003c/h4>\n\u003cp>WHEN the project contains markdown docs or starter README content\nTHEN the CLI MUST create or update the corresponding pages and keep them in the dashboard with the right metadata.\u003c/p>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-archival-and-cleanup\">Requirement: archival and cleanup\u003c/h3>\n\u003cp>The system MUST provide a clear archiving path that removes stale pages from the active feed while preserving historical references and generated artifacts.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-a-page-becomes-outdated\">Scenario: a page becomes outdated\u003c/h4>\n\u003cp>WHEN a page is no longer relevant to the active workflow\nTHEN the archive command MUST move it into an archive state and update the page registry or index consistently.\u003c/p>\n\u003ch3 id=\"delta-project-lifecycle-automation-requirement-managed-updates-without-user-content-loss\">Requirement: managed updates without user-content loss\u003c/h3>\n\u003cp>The system MUST support update actions that refresh generated shims or managed file blocks without clobbering user-authored content outside those managed boundaries.\u003c/p>\n\u003ch4 id=\"delta-project-lifecycle-automation-scenario-a-project-is-updated-after-scaffold-generation\">Scenario: a project is updated after scaffold generation\u003c/h4>\n\u003cp>WHEN a user runs the update flow\nTHEN the system MUST preserve user edits outside managed sections and refresh only the managed integration points.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nMake the `iris` project lifecycle explicit and reproducible by aligning the generated dashboard and page registry with the repository state and user workflow. The system must provide a safe path for boosting, syncing, archiving, and updating local project artifacts while preserving user-owned content outside managed blocks.\n\n## ADDED Requirements\n\n### Requirement: lifecycle-aware project bootstrap\nThe system MUST provide a project bootstrap flow that creates the local structure needed for page generation, dashboard rendering, and managed updates.\n\n#### Scenario: user initializes the project\nWHEN a user runs the init flow in a repository\nTHEN the system MUST create the required `iris/` structure, scaffold task metadata, and establish the baseline workflow for later lifecycle commands.\n\n### Requirement: incremental sync and stale-state awareness\nThe system MUST support a sync workflow that detects drift between repository state and the generated dashboard or page artifacts.\n\n#### Scenario: repo changes after a page was generated\nWHEN a project changes or a document is updated\nTHEN the sync flow MUST decide whether to refresh, flag stale state, or skip re-rendering when the data has not meaningfully changed.\n\n### Requirement: doc adoption and mirroring\nThe system MUST provide a workflow for adopting repository markdown or docs into managed page records without losing the original source.\n\n#### Scenario: user runs adopt on the project root\nWHEN the project contains markdown docs or starter README content\nTHEN the CLI MUST create or update the corresponding pages and keep them in the dashboard with the right metadata.\n\n### Requirement: archival and cleanup\nThe system MUST provide a clear archiving path that removes stale pages from the active feed while preserving historical references and generated artifacts.\n\n#### Scenario: a page becomes outdated\nWHEN a page is no longer relevant to the active workflow\nTHEN the archive command MUST move it into an archive state and update the page registry or index consistently.\n\n### Requirement: managed updates without user-content loss\nThe system MUST support update actions that refresh generated shims or managed file blocks without clobbering user-authored content outside those managed boundaries.\n\n#### Scenario: a project is updated after scaffold generation\nWHEN a user runs the update flow\nTHEN the system MUST preserve user edits outside managed sections and refresh only the managed integration points.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-open-questions\">Open Questions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-lifecycle-scaffold-and-setup\">1. Lifecycle scaffold and setup\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-sync-and-stale-state-detection\">2. Sync and stale-state detection\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-document-adoption-and-mirroring\">3. Document adoption and mirroring\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-archive-and-update-management\">4. Archive and update management\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-5-validation-and-rollout\">5. Validation and rollout\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-project-lifecycle-automation-artifact\">Delta spec · project-lifecycle-automation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-project-lifecycle-automation-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-project-lifecycle-automation-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-lifecycle-aware-project-bootstrap\">Requirement: lifecycle-aware project bootstrap\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-incremental-sync-and-stale-state-awareness\">Requirement: incremental sync and stale-state awareness\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-doc-adoption-and-mirroring\">Requirement: doc adoption and mirroring\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-archival-and-cleanup\">Requirement: archival and cleanup\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-project-lifecycle-automation-requirement-managed-updates-without-user-content-loss\">Requirement: managed updates without user-content loss\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-redesign-work-board":{"kind":"change","name":"2026-08-21-redesign-work-board","title":"2026-08-21-redesign-work-board","path":"changes/archive/2026-08-21-redesign-work-board","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-redesign-work-board\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-redesign-work-board\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">11\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-redesign-work-board: 11 of 11 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">11/11 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-redesign-work-board/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Iris's Work surface is visually spacious, exposes only coarse List/Board layouts, and navigates away from the dashboard before users can inspect an item. A denser, context-preserving work browser is needed so repositories with many agent-authored records remain quickly scannable across desktop and narrow screens.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Replace the current List/Board switcher with peer List, Table, and Kanban views that share one filter and the same real Iris contract data.\u003c/li>\n\u003cli>Introduce compact work-item hierarchy for type, ID, title, status, priority, update time, and owner, while displaying unavailable fields honestly rather than inventing values.\u003c/li>\n\u003cli>Open work items in an accessible right-side detail drawer with description/evidence, an explicit full-page action, Escape/backdrop close behavior, focus containment and return, and a full-screen 360 px treatment.\u003c/li>\n\u003cli>Evolve Aperture semantic tokens and component styles for neutral density, selected/hover states, status, priority, work type, focus, and responsive column priority.\u003c/li>\n\u003cli>Preserve deterministic static HTML, \u003ccode>file://\u003c/code>, classic deferred JavaScript, both themes, reduced motion, no drag-and-drop, no remote assets, and no unsupported data mutation.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cp>None.\u003c/p>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: Expand the Work surface and responsive interaction requirements to cover dense List/Table/Kanban presentation and an accessible side-detail drawer.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Changes generated dashboard markup, Aperture tokens/components, classic interaction script, navigation behavior, tests, design documentation, and checked-in Iris output.\u003c/li>\n\u003cli>Extends the internal dashboard page projection with bounded optional metadata derived from existing contracts; schemas and editable source contracts remain unchanged.\u003c/li>\n\u003cli>Adds no framework, dependency, network request, persistence layer, drag-and-drop behavior, or Jira-owned asset.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nIris&#039;s Work surface is visually spacious, exposes only coarse List/Board layouts, and navigates away from the dashboard before users can inspect an item. A denser, context-preserving work browser is needed so repositories with many agent-authored records remain quickly scannable across desktop and narrow screens.\n\n## What Changes\n\n- Replace the current List/Board switcher with peer List, Table, and Kanban views that share one filter and the same real Iris contract data.\n- Introduce compact work-item hierarchy for type, ID, title, status, priority, update time, and owner, while displaying unavailable fields honestly rather than inventing values.\n- Open work items in an accessible right-side detail drawer with description/evidence, an explicit full-page action, Escape/backdrop close behavior, focus containment and return, and a full-screen 360 px treatment.\n- Evolve Aperture semantic tokens and component styles for neutral density, selected/hover states, status, priority, work type, focus, and responsive column priority.\n- Preserve deterministic static HTML, `file://`, classic deferred JavaScript, both themes, reduced motion, no drag-and-drop, no remote assets, and no unsupported data mutation.\n\n## Capabilities\n\n### New Capabilities\n\nNone.\n\n### Modified Capabilities\n\n- `aperture-design-system`: Expand the Work surface and responsive interaction requirements to cover dense List/Table/Kanban presentation and an accessible side-detail drawer.\n\n## Impact\n\n- Changes generated dashboard markup, Aperture tokens/components, classic interaction script, navigation behavior, tests, design documentation, and checked-in Iris output.\n- Extends the internal dashboard page projection with bounded optional metadata derived from existing contracts; schemas and editable source contracts remain unchanged.\n- Adds no framework, dependency, network request, persistence layer, drag-and-drop behavior, or Jira-owned asset.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-redesign-work-board/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The Work surface currently projects each page into a minimal \u003ccode>DashboardPage\u003c/code> containing ID, type, title, status, and link, then duplicates a spacious card in List and Board views. The validated envelope also supplies agent, timestamps, and tags, while type-specific sections contain bounded description and evidence fields. The static output must remain deterministic, open from \u003ccode>file://\u003c/code>, use classic scripts, and preserve full-page links when JavaScript is unavailable.\u003c/p>\n\u003cp>Public Jira guidance supports the transferable interaction model: list views prioritize type/key/summary/status/priority/updated/assignee fields; boards map status to columns and expose selected fields on compact cards; compact density helps busy backlogs; and preview panels keep users in context with a full-page escape hatch. Iris will implement those principles through Aperture tokens and its own component language rather than copy Atlassian branding, fonts, icons, or exact styling.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Make 20–100 Iris records scannable without decorative card padding dominating the viewport.\u003c/li>\n\u003cli>Provide one data projection shared by List, Table, Kanban, filtering, and drawer detail.\u003c/li>\n\u003cli>Preserve navigation and source honesty when scripts fail or metadata is absent.\u003c/li>\n\u003cli>Meet keyboard, focus, reduced-motion, light/dark, print, and 360 px requirements.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Drag-and-drop, inline editing, persistence, sorting, saved filters, pagination, or invented workflow transitions.\u003c/li>\n\u003cli>New contract fields, schema changes, user accounts, remote fonts, Jira assets, or a frontend framework.\u003c/li>\n\u003cli>Replacing the Spec view or redesigning full contract pages.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-derive-a-bounded-work-projection-during-generation\">Derive a bounded Work projection during generation\u003c/h3>\n\u003cp>\u003ccode>refreshDashboard\u003c/code> will derive updated date, agent, tags, description, evidence, and priority from validated contract payloads. Bug severity maps directly to display priority (\u003ccode>p0\u003c/code> urgent through \u003ccode>p3\u003c/code> low); other types report priority as unavailable because effort, impact, status, and type are not interchangeable with priority. Description and evidence strings are bounded before entering generated attributes.\u003c/p>\n\u003cp>Archived state currently retains only ID/type/title/status/link, so optional fields remain unavailable instead of reading moved source opportunistically or inventing values.\u003c/p>\n\u003cp>Alternative: expand the schema with owner and priority. Rejected because this redesign is a read-only presentation change and schema semantics require a separate capability decision.\u003c/p>\n\u003ch3 id=\"design-keep-all-three-views-in-static-semantic-html\">Keep all three views in static semantic HTML\u003c/h3>\n\u003cp>List uses compact linked rows with a strong ID/title pair and one-line metadata. Table uses a real table with type, ID, title, status, priority, updated, and agent columns; responsive CSS hides agent, updated, priority, then type in that order before allowing page overflow. Kanban uses four fixed columns matching Iris's actual statuses: Draft, Active, Done, Archived. Cards stay compact and are not draggable.\u003c/p>\n\u003cp>All representations use full-page anchors. JavaScript intercepts activation for the detail drawer, while no-JS users follow the existing page link. A single filter hides matching representations across all views and computes unique results from the List representation.\u003c/p>\n\u003cp>Alternative: render only the active layout and rebuild on tab changes. Rejected because it complicates no-JS behavior and introduces client-side templating into a deterministic static artifact.\u003c/p>\n\u003ch3 id=\"design-use-one-reusable-modal-side-drawer\">Use one reusable modal side drawer\u003c/h3>\n\u003cp>The dashboard emits one drawer after the main content. Activating any work anchor copies escaped data attributes into text-only drawer slots, updates \u003ccode>#work=&lt;id&gt;\u003c/code>, marks the dashboard main content inert, locks body scrolling, focuses Close, and traps Tab within the drawer. Escape, backdrop, Close, or hash removal closes it and restores the opener focus.\u003c/p>\n\u003cp>The drawer is \u003ccode>min(38rem, 52vw)\u003c/code> on wide screens and the full viewport below 40rem. Its content order is ID/type, title, status, metadata, description, evidence/tags, then actions. “Open full page” uses the original static link.\u003c/p>\n\u003cp>Alternative: CSS \u003ccode>:target\u003c/code> only. Rejected because it cannot reliably trap/restore focus, populate one reusable drawer, or provide Escape behavior.\u003c/p>\n\u003ch3 id=\"design-evolve-aperture-toward-neutral-compact-density\">Evolve Aperture toward neutral compact density\u003c/h3>\n\u003cp>The existing system stack remains. Work UI uses 0.75–0.875rem metadata/body text, 1.25rem section headings, 32–40 px interactive row targets, 4–8 px radii for compact elements, hairline separators, and neutral surfaces. Spectrum colors appear only in aperture/type, status, and priority signals, always paired with text. Hover, selected, and focus states use existing accent/border semantics.\u003c/p>\n\u003ch3 id=\"design-keep-deep-links-local-and-deterministic\">Keep deep links local and deterministic\u003c/h3>\n\u003cp>The drawer hash contains only the encoded work ID. On load or \u003ccode>hashchange\u003c/code>, the script opens a matching item; unknown IDs leave the dashboard unchanged. Closing uses history replacement for direct closes, while browser Back naturally removes a pushed hash. The full page remains the authoritative deep link and no data is serialized into the URL.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Three DOM representations increase generated HTML size] → Keep markup compact, reuse one drawer, and avoid client-side templates or duplicated descriptions.\u003c/li>\n\u003cli>[Data attributes can become large or unsafe] → Bound derived text and HTML-escape every attribute; populate drawer fields with \u003ccode>textContent\u003c/code> only.\u003c/li>\n\u003cli>[A modal drawer can create focus bugs] → Test initial focus, Tab boundaries, Escape, backdrop, hash navigation, focus return, and inert cleanup in a real browser.\u003c/li>\n\u003cli>[Responsive tables can hide useful context] → Preserve ID, title, and status longest; every row opens the drawer where all available metadata remains visible.\u003c/li>\n\u003cli>[The checked-in repository has no work pages] → Use temporary validated fixture contracts for visual/runtime proof; do not add demo records to user-owned \u003ccode>iris/pages/\u003c/code>.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Extend the dashboard projection and add focused generator tests for real and unavailable metadata.\u003c/li>\n\u003cli>Replace Work markup and styles while preserving existing full-page links and empty state.\u003c/li>\n\u003cli>Add shared filtering, drawer, hash, focus, and keyboard behavior in the classic base script.\u003c/li>\n\u003cli>Regenerate managed output, validate at desktop and 360 px with temporary populated fixtures, and run the full release gate.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback restores the prior projection and List/Board markup; contracts and state require no migration.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe Work surface currently projects each page into a minimal `DashboardPage` containing ID, type, title, status, and link, then duplicates a spacious card in List and Board views. The validated envelope also supplies agent, timestamps, and tags, while type-specific sections contain bounded description and evidence fields. The static output must remain deterministic, open from `file://`, use classic scripts, and preserve full-page links when JavaScript is unavailable.\n\nPublic Jira guidance supports the transferable interaction model: list views prioritize type/key/summary/status/priority/updated/assignee fields; boards map status to columns and expose selected fields on compact cards; compact density helps busy backlogs; and preview panels keep users in context with a full-page escape hatch. Iris will implement those principles through Aperture tokens and its own component language rather than copy Atlassian branding, fonts, icons, or exact styling.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Make 20–100 Iris records scannable without decorative card padding dominating the viewport.\n- Provide one data projection shared by List, Table, Kanban, filtering, and drawer detail.\n- Preserve navigation and source honesty when scripts fail or metadata is absent.\n- Meet keyboard, focus, reduced-motion, light/dark, print, and 360 px requirements.\n\n**Non-Goals:**\n\n- Drag-and-drop, inline editing, persistence, sorting, saved filters, pagination, or invented workflow transitions.\n- New contract fields, schema changes, user accounts, remote fonts, Jira assets, or a frontend framework.\n- Replacing the Spec view or redesigning full contract pages.\n\n## Decisions\n\n### Derive a bounded Work projection during generation\n\n`refreshDashboard` will derive updated date, agent, tags, description, evidence, and priority from validated contract payloads. Bug severity maps directly to display priority (`p0` urgent through `p3` low); other types report priority as unavailable because effort, impact, status, and type are not interchangeable with priority. Description and evidence strings are bounded before entering generated attributes.\n\nArchived state currently retains only ID/type/title/status/link, so optional fields remain unavailable instead of reading moved source opportunistically or inventing values.\n\nAlternative: expand the schema with owner and priority. Rejected because this redesign is a read-only presentation change and schema semantics require a separate capability decision.\n\n### Keep all three views in static semantic HTML\n\nList uses compact linked rows with a strong ID/title pair and one-line metadata. Table uses a real table with type, ID, title, status, priority, updated, and agent columns; responsive CSS hides agent, updated, priority, then type in that order before allowing page overflow. Kanban uses four fixed columns matching Iris&#039;s actual statuses: Draft, Active, Done, Archived. Cards stay compact and are not draggable.\n\nAll representations use full-page anchors. JavaScript intercepts activation for the detail drawer, while no-JS users follow the existing page link. A single filter hides matching representations across all views and computes unique results from the List representation.\n\nAlternative: render only the active layout and rebuild on tab changes. Rejected because it complicates no-JS behavior and introduces client-side templating into a deterministic static artifact.\n\n### Use one reusable modal side drawer\n\nThe dashboard emits one drawer after the main content. Activating any work anchor copies escaped data attributes into text-only drawer slots, updates `#work=&lt;id&gt;`, marks the dashboard main content inert, locks body scrolling, focuses Close, and traps Tab within the drawer. Escape, backdrop, Close, or hash removal closes it and restores the opener focus.\n\nThe drawer is `min(38rem, 52vw)` on wide screens and the full viewport below 40rem. Its content order is ID/type, title, status, metadata, description, evidence/tags, then actions. “Open full page” uses the original static link.\n\nAlternative: CSS `:target` only. Rejected because it cannot reliably trap/restore focus, populate one reusable drawer, or provide Escape behavior.\n\n### Evolve Aperture toward neutral compact density\n\nThe existing system stack remains. Work UI uses 0.75–0.875rem metadata/body text, 1.25rem section headings, 32–40 px interactive row targets, 4–8 px radii for compact elements, hairline separators, and neutral surfaces. Spectrum colors appear only in aperture/type, status, and priority signals, always paired with text. Hover, selected, and focus states use existing accent/border semantics.\n\n### Keep deep links local and deterministic\n\nThe drawer hash contains only the encoded work ID. On load or `hashchange`, the script opens a matching item; unknown IDs leave the dashboard unchanged. Closing uses history replacement for direct closes, while browser Back naturally removes a pushed hash. The full page remains the authoritative deep link and no data is serialized into the URL.\n\n## Risks / Trade-offs\n\n- [Three DOM representations increase generated HTML size] → Keep markup compact, reuse one drawer, and avoid client-side templates or duplicated descriptions.\n- [Data attributes can become large or unsafe] → Bound derived text and HTML-escape every attribute; populate drawer fields with `textContent` only.\n- [A modal drawer can create focus bugs] → Test initial focus, Tab boundaries, Escape, backdrop, hash navigation, focus return, and inert cleanup in a real browser.\n- [Responsive tables can hide useful context] → Preserve ID, title, and status longest; every row opens the drawer where all available metadata remains visible.\n- [The checked-in repository has no work pages] → Use temporary validated fixture contracts for visual/runtime proof; do not add demo records to user-owned `iris/pages/`.\n\n## Migration Plan\n\n1. Extend the dashboard projection and add focused generator tests for real and unavailable metadata.\n2. Replace Work markup and styles while preserving existing full-page links and empty state.\n3. Add shared filtering, drawer, hash, focus, and keyboard behavior in the classic base script.\n4. Regenerate managed output, validate at desktop and 360 px with temporary populated fixtures, and run the full release gate.\n\nRollback restores the prior projection and List/Board markup; contracts and state require no migration.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-redesign-work-board/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-work-data-projection\">1. Work data projection\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Extend the generated dashboard page projection with bounded update, agent, tag, priority, description, and evidence metadata derived only from validated contracts.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Cover supported type-specific derivation and honest unavailable values with focused generator tests.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-dense-work-views\">2. Dense Work views\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Replace the current List/Board markup with semantic List, Table, and Kanban representations that retain full-page links and share one filter.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Add Aperture tokens and compact responsive styles for work rows, table columns, Kanban columns/cards, type/status/priority signals, both themes, print, reduced motion, and 360 px layouts.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Implement shared filtering, unique result counts, layout tabs, and keyboard navigation without adding persistence, remote assets, or runtime modules.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-work-detail-drawer\">3. Work detail drawer\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Emit one reusable detail drawer populated from escaped bounded data attributes with a full-page action and local item hash.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Implement click, Enter, Space, Escape, backdrop, hash, focus containment, focus return, inert cleanup, and responsive full-screen behavior.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Add focused markup and interaction tests for dialog semantics, keyboard behavior, no-JavaScript links, and offline dependency constraints.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-documentation-and-verification\">4. Documentation and verification\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Record the Jira-inspired interaction research and Aperture implementation decisions without copying Jira branding or assets.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Regenerate checked-in output, inspect populated temporary fixture output in Safari and a 360 px WebKit rendering, and fix visible layout/accessibility defects.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Run strict OpenSpec validation and the full release gate, then reconcile task evidence with the implementation.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Work data projection\n\n- [x] 1.1 Extend the generated dashboard page projection with bounded update, agent, tag, priority, description, and evidence metadata derived only from validated contracts.\n- [x] 1.2 Cover supported type-specific derivation and honest unavailable values with focused generator tests.\n\n## 2. Dense Work views\n\n- [x] 2.1 Replace the current List/Board markup with semantic List, Table, and Kanban representations that retain full-page links and share one filter.\n- [x] 2.2 Add Aperture tokens and compact responsive styles for work rows, table columns, Kanban columns/cards, type/status/priority signals, both themes, print, reduced motion, and 360 px layouts.\n- [x] 2.3 Implement shared filtering, unique result counts, layout tabs, and keyboard navigation without adding persistence, remote assets, or runtime modules.\n\n## 3. Work detail drawer\n\n- [x] 3.1 Emit one reusable detail drawer populated from escaped bounded data attributes with a full-page action and local item hash.\n- [x] 3.2 Implement click, Enter, Space, Escape, backdrop, hash, focus containment, focus return, inert cleanup, and responsive full-screen behavior.\n- [x] 3.3 Add focused markup and interaction tests for dialog semantics, keyboard behavior, no-JavaScript links, and offline dependency constraints.\n\n## 4. Documentation and verification\n\n- [x] 4.1 Record the Jira-inspired interaction research and Aperture implementation decisions without copying Jira branding or assets.\n- [x] 4.2 Regenerate checked-in output, inspect populated temporary fixture output in Safari and a 360 px WebKit rendering, and fix visible layout/accessibility defects.\n- [x] 4.3 Run strict OpenSpec validation and the full release gate, then reconcile task evidence with the implementation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-redesign-work-board/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-redesign-work-board/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-dense-context-preserving-work-browser\">Requirement: dense context-preserving Work browser\u003c/h3>\n\u003cp>The generated Work surface MUST provide peer List, Table, and Kanban views over the same Iris page records, MUST share one text filter across all views, and MUST use only metadata available from validated contracts or explicit unavailable labels.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-changes-work-layout\">Scenario: user changes Work layout\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user selects List, Table, or Kanban\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the corresponding view MUST present the same matching records with type, ID, title, status, and every supported optional field without changing source data\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-filters-work\">Scenario: user filters work\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user enters text matching a title, ID, type, status, priority, agent, or tag\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every Work view MUST hide nonmatching records and report the matching unique-record count\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-optional-metadata-is-unavailable\">Scenario: optional metadata is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a page contract does not define priority or another requested field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> List, Table, Kanban, and detail views MUST use a neutral unavailable value and MUST NOT infer or fabricate data\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-javascript-is-unavailable\">Scenario: JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the generated dashboard opens without executing JavaScript\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every work item MUST retain a navigable full-page link and the default List view MUST remain readable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-accessible-work-item-detail-drawer\">Requirement: accessible work-item detail drawer\u003c/h3>\n\u003cp>Selecting a work item MUST open one right-side detail drawer that preserves the current Work view, displays available contract metadata, description, evidence, and a full-page link, and provides deterministic close and focus behavior.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-item-is-activated\">Scenario: item is activated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user clicks a work item or activates its link with Enter or Space\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the drawer MUST open with dialog semantics, identify the selected ID and title, move focus to its close control, and update a local hash that can reopen the same item\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-drawer-is-closed\">Scenario: drawer is closed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses Escape, activates the close control, activates the backdrop, or navigates back from the item hash\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the drawer MUST close, remove its modal state, and return focus to the item that opened it when that item remains available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-focus-moves-within-the-drawer\">Scenario: focus moves within the drawer\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the drawer is open and the user presses Tab or Shift+Tab at its focus boundaries\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> focus MUST remain within the drawer until it is closed\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport-opens-a-drawer\">Scenario: narrow viewport opens a drawer\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the viewport is 360 px wide and a work item is activated\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the drawer MUST occupy the viewport without horizontal page overflow and MUST keep its close and full-page actions reachable\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/h3>\n\u003cp>The system MUST provide generated component styles and semantic markup for aperture marks, stat tiles, restrained status/type/priority indicators, compact work rows, structured tables, Kanban columns and cards, detail drawers, callouts, timelines, keyboard hints, and command-specific empty states.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-meaning-is-communicated-accessibly\">Scenario: meaning is communicated accessibly\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page uses type, status, or priority color, interactive controls, selection, or motion\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide a non-color signal, visible keyboard focus, semantic state, and a static reduced-motion presentation\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated dashboard MUST provide top-level Work and \u003ccode>Spec\u003c/code> views, preserve the Work view's briefing hero, health strip, architecture pane, dense List/Table/Kanban work browser, and project-docs strip, and organize the Spec view as overview, canonical specs, active changes with artifacts and delta specs, and archive.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the dashboard from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-generated-work-pages\">Scenario: repository has generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Work view with one or more page contracts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from the dashboard\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the top-level tab labelled \u003ccode>Spec\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the dashboard MUST show OpenSpec overview counts and navigable canonical, active-change, and archive sections while keeping the Work view available\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/h3>\n\u003cp>The generated dashboard MUST retain classic deferred-script interactions for top-level view selection, Work layout selection, shared filtering, theme switching, detail-drawer behavior, and keyboard navigation and MUST remain operable at a 360 px viewport.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-keyboard-navigation-is-used\">Scenario: keyboard navigation is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user presses \u003ccode>/\u003c/code> or \u003ccode>t\u003c/code> outside an editable field\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> \u003ccode>/\u003c/code> MUST focus the relevant visible filter and \u003ccode>t\u003c/code> MUST toggle the theme without loading external assets\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-top-level-tabs-are-keyboard-operated\">Scenario: top-level tabs are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on the Work/Spec or List/Table/Kanban tablist and the user presses supported arrow, Home, or End keys\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> focus and selection MUST move according to the tab pattern with visible focus and correct tab/panel semantics\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-work-items-are-keyboard-operated\">Scenario: work items are keyboard operated\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> focus is on a work-item link and the user presses Enter or Space\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the corresponding detail drawer MUST open without navigating away\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-narrow-viewport-is-used\">Scenario: narrow viewport is used\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the dashboard viewport is 360 px wide\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> Work List rows, the prioritized Table columns, stacked Kanban columns, full-screen drawer, Spec content, warnings, and source fallbacks MUST remain readable without horizontal page overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-reduced-motion-is-requested\">Scenario: reduced motion is requested\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the user enables reduced-motion preferences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> switching views, opening the drawer, and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## ADDED Requirements\n\n### Requirement: dense context-preserving Work browser\n\nThe generated Work surface MUST provide peer List, Table, and Kanban views over the same Iris page records, MUST share one text filter across all views, and MUST use only metadata available from validated contracts or explicit unavailable labels.\n\n#### Scenario: user changes Work layout\n\n- **WHEN** a user selects List, Table, or Kanban\n- **THEN** the corresponding view MUST present the same matching records with type, ID, title, status, and every supported optional field without changing source data\n\n#### Scenario: user filters work\n\n- **WHEN** a user enters text matching a title, ID, type, status, priority, agent, or tag\n- **THEN** every Work view MUST hide nonmatching records and report the matching unique-record count\n\n#### Scenario: optional metadata is unavailable\n\n- **WHEN** a page contract does not define priority or another requested field\n- **THEN** List, Table, Kanban, and detail views MUST use a neutral unavailable value and MUST NOT infer or fabricate data\n\n#### Scenario: JavaScript is unavailable\n\n- **WHEN** the generated dashboard opens without executing JavaScript\n- **THEN** every work item MUST retain a navigable full-page link and the default List view MUST remain readable\n\n### Requirement: accessible work-item detail drawer\n\nSelecting a work item MUST open one right-side detail drawer that preserves the current Work view, displays available contract metadata, description, evidence, and a full-page link, and provides deterministic close and focus behavior.\n\n#### Scenario: item is activated\n\n- **WHEN** a user clicks a work item or activates its link with Enter or Space\n- **THEN** the drawer MUST open with dialog semantics, identify the selected ID and title, move focus to its close control, and update a local hash that can reopen the same item\n\n#### Scenario: drawer is closed\n\n- **WHEN** the user presses Escape, activates the close control, activates the backdrop, or navigates back from the item hash\n- **THEN** the drawer MUST close, remove its modal state, and return focus to the item that opened it when that item remains available\n\n#### Scenario: focus moves within the drawer\n\n- **WHEN** the drawer is open and the user presses Tab or Shift+Tab at its focus boundaries\n- **THEN** focus MUST remain within the drawer until it is closed\n\n#### Scenario: narrow viewport opens a drawer\n\n- **WHEN** the viewport is 360 px wide and a work item is activated\n- **THEN** the drawer MUST occupy the viewport without horizontal page overflow and MUST keep its close and full-page actions reachable\n\n## MODIFIED Requirements\n\n### Requirement: Aperture component language\n\nThe system MUST provide generated component styles and semantic markup for aperture marks, stat tiles, restrained status/type/priority indicators, compact work rows, structured tables, Kanban columns and cards, detail drawers, callouts, timelines, keyboard hints, and command-specific empty states.\n\n#### Scenario: meaning is communicated accessibly\n\n- **WHEN** a generated page uses type, status, or priority color, interactive controls, selection, or motion\n- **THEN** the page MUST provide a non-color signal, visible keyboard focus, semantic state, and a static reduced-motion presentation\n\n### Requirement: newcomer-first dashboard hierarchy\n\nThe generated dashboard MUST provide top-level Work and `Spec` views, preserve the Work view&#039;s briefing hero, health strip, architecture pane, dense List/Table/Kanban work browser, and project-docs strip, and organize the Spec view as overview, canonical specs, active changes with artifacts and delta specs, and archive.\n\n#### Scenario: repository has no generated work pages\n\n- **WHEN** a newcomer opens the dashboard from `file://`\n- **THEN** the dashboard MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n#### Scenario: repository has generated work pages\n\n- **WHEN** a newcomer opens the Work view with one or more page contracts\n- **THEN** the dashboard MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from the dashboard\n\n#### Scenario: user opens the Spec view\n\n- **WHEN** a user activates the top-level tab labelled `Spec`\n- **THEN** the dashboard MUST show OpenSpec overview counts and navigable canonical, active-change, and archive sections while keeping the Work view available\n\n#### Scenario: state is communicated in the Spec view\n\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n### Requirement: deterministic responsive interaction\n\nThe generated dashboard MUST retain classic deferred-script interactions for top-level view selection, Work layout selection, shared filtering, theme switching, detail-drawer behavior, and keyboard navigation and MUST remain operable at a 360 px viewport.\n\n#### Scenario: keyboard navigation is used\n\n- **WHEN** the user presses `/` or `t` outside an editable field\n- **THEN** `/` MUST focus the relevant visible filter and `t` MUST toggle the theme without loading external assets\n\n#### Scenario: top-level tabs are keyboard operated\n\n- **WHEN** focus is on the Work/Spec or List/Table/Kanban tablist and the user presses supported arrow, Home, or End keys\n- **THEN** focus and selection MUST move according to the tab pattern with visible focus and correct tab/panel semantics\n\n#### Scenario: work items are keyboard operated\n\n- **WHEN** focus is on a work-item link and the user presses Enter or Space\n- **THEN** the corresponding detail drawer MUST open without navigating away\n\n#### Scenario: narrow viewport is used\n\n- **WHEN** the dashboard viewport is 360 px wide\n- **THEN** Work List rows, the prioritized Table columns, stacked Kanban columns, full-screen drawer, Spec content, warnings, and source fallbacks MUST remain readable without horizontal page overflow\n\n#### Scenario: reduced motion is requested\n\n- **WHEN** the user enables reduced-motion preferences\n- **THEN** switching views, opening the drawer, and revealing Spec content MUST have an equivalent static presentation without meaning-bearing information loss\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-derive-a-bounded-work-projection-during-generation\">Derive a bounded Work projection during generation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-keep-all-three-views-in-static-semantic-html\">Keep all three views in static semantic HTML\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-use-one-reusable-modal-side-drawer\">Use one reusable modal side drawer\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-evolve-aperture-toward-neutral-compact-density\">Evolve Aperture toward neutral compact density\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-keep-deep-links-local-and-deterministic\">Keep deep links local and deterministic\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-work-data-projection\">1. Work data projection\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-dense-work-views\">2. Dense Work views\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-work-detail-drawer\">3. Work detail drawer\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-documentation-and-verification\">4. Documentation and verification\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-dense-context-preserving-work-browser\">Requirement: dense context-preserving Work browser\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-accessible-work-item-detail-drawer\">Requirement: accessible work-item detail drawer\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-aperture-component-language\">Requirement: Aperture component language\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-deterministic-responsive-interaction\">Requirement: deterministic responsive interaction\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-render-mermaid-diagrams":{"kind":"change","name":"2026-08-21-render-mermaid-diagrams","title":"2026-08-21-render-mermaid-diagrams","path":"changes/archive/2026-08-21-render-mermaid-diagrams","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-render-mermaid-diagrams\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">12\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">2\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-render-mermaid-diagrams: 12 of 12 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">12/12 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Iris renders Markdown semantically but leaves Mermaid fences as inert code, so architecture and workflow diagrams cannot be inspected visually in the offline workspace. Mermaid support should preserve Iris's safe-source evidence and \u003ccode>file://\u003c/code> contract instead of introducing a network loader or allowing diagram failures to break an entire document.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Recognize fenced Markdown blocks whose language is exactly \u003ccode>mermaid\u003c/code> and emit an accessible diagram host with an escaped-source fallback.\u003c/li>\n\u003cli>Render each diagram independently in the browser from a locally installed, pinned Mermaid asset; make invalid diagrams fail locally while leaving their source readable.\u003c/li>\n\u003cli>Make the local Mermaid runtime available through the existing \u003ccode>iris vendor\u003c/code> lifecycle command and keep generated pages free of remote requests and runtime modules.\u003c/li>\n\u003cli>Document and test the Markdown authoring, offline asset, security, fallback, theme, and standalone-publish boundaries.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>markdown-diagram-rendering\u003c/code>: Safe, offline rendering and fallback behavior for Mermaid fences embedded in Iris Markdown.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>openspec-spec-browser\u003c/code>: OpenSpec Markdown artifacts may progressively render recognized Mermaid fences while preserving exact source and inert untrusted content.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Affects \u003ccode>src/lib/markdown.ts\u003c/code>, static page templates and client script, CLI lifecycle handling, generated design assets, tests, and rendering documentation.\u003c/li>\n\u003cli>Adds Mermaid as a pinned runtime dependency whose browser bundle and license are copied into each Iris workspace on explicit vendoring.\u003c/li>\n\u003cli>Does not enable embedded HTML, remote assets, arbitrary Markdown plugins, or server-side/browser automation.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nIris renders Markdown semantically but leaves Mermaid fences as inert code, so architecture and workflow diagrams cannot be inspected visually in the offline workspace. Mermaid support should preserve Iris&#039;s safe-source evidence and `file://` contract instead of introducing a network loader or allowing diagram failures to break an entire document.\n\n## What Changes\n\n- Recognize fenced Markdown blocks whose language is exactly `mermaid` and emit an accessible diagram host with an escaped-source fallback.\n- Render each diagram independently in the browser from a locally installed, pinned Mermaid asset; make invalid diagrams fail locally while leaving their source readable.\n- Make the local Mermaid runtime available through the existing `iris vendor` lifecycle command and keep generated pages free of remote requests and runtime modules.\n- Document and test the Markdown authoring, offline asset, security, fallback, theme, and standalone-publish boundaries.\n\n## Capabilities\n\n### New Capabilities\n\n- `markdown-diagram-rendering`: Safe, offline rendering and fallback behavior for Mermaid fences embedded in Iris Markdown.\n\n### Modified Capabilities\n\n- `openspec-spec-browser`: OpenSpec Markdown artifacts may progressively render recognized Mermaid fences while preserving exact source and inert untrusted content.\n\n## Impact\n\n- Affects `src/lib/markdown.ts`, static page templates and client script, CLI lifecycle handling, generated design assets, tests, and rendering documentation.\n- Adds Mermaid as a pinned runtime dependency whose browser bundle and license are copied into each Iris workspace on explicit vendoring.\n- Does not enable embedded HTML, remote assets, arbitrary Markdown plugins, or server-side/browser automation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The generation-time Markdown renderer currently escapes HTML and active images, rejects unsafe links, and emits ordinary fences as \u003ccode>&lt;pre&gt;&lt;code&gt;\u003c/code>. Generated pages load one local classic deferred script and work directly from \u003ccode>file://\u003c/code>. The CLI already creates \u003ccode>iris/design/vendor/\u003c/code>, registers a stubbed \u003ccode>iris vendor\u003c/code> command, and documents Mermaid as a later local-only enhancement. See \u003ccode>proposal.md\u003c/code> for motivation and the two delta specs for observable behavior.\u003c/p>\n\u003cp>Mermaid's browser runtime is necessarily client-side if Iris is to avoid a browser automation dependency. Diagram source is untrusted repository input, so the integration must preserve the Markdown renderer's existing trust boundary and must not treat successful JavaScript execution as the only readable state.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Add one safe Markdown extension for exact \u003ccode>mermaid\u003c/code> fences.\u003c/li>\n\u003cli>Keep source-first no-JS behavior, per-diagram failure isolation, and deterministic generated markup.\u003c/li>\n\u003cli>Install a pinned Mermaid classic browser bundle from Iris's already installed dependency, without a second network operation.\u003c/li>\n\u003cli>Use the same behavior in contract Markdown and OpenSpec Markdown.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Rendering diagrams to SVG during CLI generation or embedding SVG snapshots in standalone exports.\u003c/li>\n\u003cli>Supporting remote Mermaid loaders, ES modules at view time, click interactions, HTML labels, custom icons, external images, arbitrary plugins, or theme directives that weaken global security configuration.\u003c/li>\n\u003cli>Replacing fenced source with a diagram-only representation.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-extend-the-existing-markdown-renderer-at-the-fence-rule\">Extend the existing Markdown renderer at the fence rule\u003c/h3>\n\u003cp>The \u003ccode>markdown-it\u003c/code> fence renderer will delegate all non-Mermaid fences to its existing default and emit a semantic figure for exact normalized \u003ccode>mermaid\u003c/code> info strings. The figure contains a dedicated runtime host plus an always-present escaped \u003ccode>&lt;pre&gt;&lt;code&gt;\u003c/code> fallback. This keeps escaping centralized and makes no-JS output complete.\u003c/p>\n\u003cp>Alternative considered: preprocess Markdown with regular expressions. Rejected because fence parsing, nesting, and escaping already belong to the Markdown parser; duplicating them would create divergent edge cases.\u003c/p>\n\u003ch3 id=\"design-use-a-pinned-runtime-dependency-and-explicit-offline-vendori\">Use a pinned runtime dependency and explicit offline vendoring\u003c/h3>\n\u003cp>Mermaid will be pinned as a production dependency. \u003ccode>iris vendor\u003c/code> will resolve the installed package, verify the expected browser bundle and license, then atomically copy those bytes into \u003ccode>iris/design/vendor/\u003c/code>. It will not download from the CDN. Generated pages may reference only that relative classic-script path.\u003c/p>\n\u003cp>Alternative considered: load the documented CDN ESM build. Rejected because it violates \u003ccode>file://\u003c/code>, offline, no-network, and no-runtime-module constraints. A CLI-side SVG renderer was also rejected for this slice because it would introduce DOM/browser emulation or headless-browser policy that the project has explicitly deferred.\u003c/p>\n\u003ch3 id=\"design-initialize-once-then-render-one-node-at-a-time\">Initialize once, then render one node at a time\u003c/h3>\n\u003cp>The shared base script will call \u003ccode>mermaid.initialize\u003c/code> once with \u003ccode>startOnLoad: false\u003c/code>, \u003ccode>securityLevel: 'strict'\u003c/code>, \u003ccode>htmlLabels: false\u003c/code>, fixed text/edge bounds, suppressed built-in error SVGs, and a system font stack. It will then await \u003ccode>mermaid.run({ nodes: [node], suppressErrors: true })\u003c/code> separately for every host. Success hides only that host's fallback and adds accessible SVG metadata; rejection marks only that figure as failed and leaves its source visible.\u003c/p>\n\u003cp>This follows Mermaid's current official security contract: strict mode encodes HTML and disables click behavior, secure configuration keys prevent diagram directives from weakening core limits, and \u003ccode>run\u003c/code> supports explicit node arrays. Theme-specific color remapping is deferred; Mermaid's neutral theme will sit on Iris surfaces in both themes while source remains authoritative.\u003c/p>\n\u003cp>Alternative considered: render all nodes in one \u003ccode>run\u003c/code> call. Rejected because a single invalid diagram can make failure attribution and fallback state ambiguous.\u003c/p>\n\u003ch3 id=\"design-treat-contract-fields-and-openspec-artifacts-as-one-markdown\">Treat contract fields and OpenSpec artifacts as one Markdown surface\u003c/h3>\n\u003cp>Contract section Markdown will use \u003ccode>renderSafeMarkdown\u003c/code> instead of its small parallel paragraph/list converter. This avoids implementing Mermaid twice and brings the same embedded-HTML/link/image protections to both inputs. Exact OpenSpec source disclosure remains unchanged.\u003c/p>\n\u003ch3 id=\"design-keep-standalone-publishing-source-safe\">Keep standalone publishing source-safe\u003c/h3>\n\u003cp>Published single-file contract pages will remove external Mermaid and base-script tags as they already do for local shared assets. The embedded diagram source fallback remains visible; runtime SVG snapshotting remains out of scope until Iris adopts a deterministic browser renderer.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[The full Mermaid bundle materially increases installed dependency and per-workspace size] → Keep installation explicit through \u003ccode>iris vendor\u003c/code>, pin the version, copy one minified browser file plus its license, and document the size.\u003c/li>\n\u003cli>[Mermaid is a large untrusted-input parser with its own security history] → Pin exact versions, use strict mode and secure bounds, disable interactions/HTML labels, retain source fallback, and include dependency/security review in upgrades.\u003c/li>\n\u003cli>[Theme changes after rendering do not recolor an existing SVG] → Use a neutral, surface-compatible first implementation and retain accessible source; theme-aware rerendering can be a later independently specified enhancement.\u003c/li>\n\u003cli>[A missing vendor file produces a local resource load error] → The base script detects the absent global, keeps all fallbacks visible, and reports a concise local status in each figure.\u003c/li>\n\u003cli>[Standalone artifacts do not include rendered SVG] → Preserve the escaped source honestly; do not embed a multi-megabyte runtime or claim visual standalone diagrams without snapshot evidence.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add the pinned dependency and the idempotent local vendor command.\u003c/li>\n\u003cli>Extend safe Markdown output, styles, base script initialization, templates, and standalone stripping.\u003c/li>\n\u003cli>Add unit, CLI, HTML integrity, malicious-input, multiple-diagram, offline, and packaging tests.\u003c/li>\n\u003cli>Update authoring/technical documentation, run \u003ccode>iris vendor\u003c/code>, then regenerate checked-in Iris output with \u003ccode>iris render --all\u003c/code>.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback removes the Mermaid fence override, vendor command implementation, script references, dependency, and generated vendor files; existing Markdown remains ordinary fenced code with no data migration.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe generation-time Markdown renderer currently escapes HTML and active images, rejects unsafe links, and emits ordinary fences as `&lt;pre&gt;&lt;code&gt;`. Generated pages load one local classic deferred script and work directly from `file://`. The CLI already creates `iris/design/vendor/`, registers a stubbed `iris vendor` command, and documents Mermaid as a later local-only enhancement. See `proposal.md` for motivation and the two delta specs for observable behavior.\n\nMermaid&#039;s browser runtime is necessarily client-side if Iris is to avoid a browser automation dependency. Diagram source is untrusted repository input, so the integration must preserve the Markdown renderer&#039;s existing trust boundary and must not treat successful JavaScript execution as the only readable state.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Add one safe Markdown extension for exact `mermaid` fences.\n- Keep source-first no-JS behavior, per-diagram failure isolation, and deterministic generated markup.\n- Install a pinned Mermaid classic browser bundle from Iris&#039;s already installed dependency, without a second network operation.\n- Use the same behavior in contract Markdown and OpenSpec Markdown.\n\n**Non-Goals:**\n\n- Rendering diagrams to SVG during CLI generation or embedding SVG snapshots in standalone exports.\n- Supporting remote Mermaid loaders, ES modules at view time, click interactions, HTML labels, custom icons, external images, arbitrary plugins, or theme directives that weaken global security configuration.\n- Replacing fenced source with a diagram-only representation.\n\n## Decisions\n\n### Extend the existing Markdown renderer at the fence rule\n\nThe `markdown-it` fence renderer will delegate all non-Mermaid fences to its existing default and emit a semantic figure for exact normalized `mermaid` info strings. The figure contains a dedicated runtime host plus an always-present escaped `&lt;pre&gt;&lt;code&gt;` fallback. This keeps escaping centralized and makes no-JS output complete.\n\nAlternative considered: preprocess Markdown with regular expressions. Rejected because fence parsing, nesting, and escaping already belong to the Markdown parser; duplicating them would create divergent edge cases.\n\n### Use a pinned runtime dependency and explicit offline vendoring\n\nMermaid will be pinned as a production dependency. `iris vendor` will resolve the installed package, verify the expected browser bundle and license, then atomically copy those bytes into `iris/design/vendor/`. It will not download from the CDN. Generated pages may reference only that relative classic-script path.\n\nAlternative considered: load the documented CDN ESM build. Rejected because it violates `file://`, offline, no-network, and no-runtime-module constraints. A CLI-side SVG renderer was also rejected for this slice because it would introduce DOM/browser emulation or headless-browser policy that the project has explicitly deferred.\n\n### Initialize once, then render one node at a time\n\nThe shared base script will call `mermaid.initialize` once with `startOnLoad: false`, `securityLevel: &#039;strict&#039;`, `htmlLabels: false`, fixed text/edge bounds, suppressed built-in error SVGs, and a system font stack. It will then await `mermaid.run({ nodes: [node], suppressErrors: true })` separately for every host. Success hides only that host&#039;s fallback and adds accessible SVG metadata; rejection marks only that figure as failed and leaves its source visible.\n\nThis follows Mermaid&#039;s current official security contract: strict mode encodes HTML and disables click behavior, secure configuration keys prevent diagram directives from weakening core limits, and `run` supports explicit node arrays. Theme-specific color remapping is deferred; Mermaid&#039;s neutral theme will sit on Iris surfaces in both themes while source remains authoritative.\n\nAlternative considered: render all nodes in one `run` call. Rejected because a single invalid diagram can make failure attribution and fallback state ambiguous.\n\n### Treat contract fields and OpenSpec artifacts as one Markdown surface\n\nContract section Markdown will use `renderSafeMarkdown` instead of its small parallel paragraph/list converter. This avoids implementing Mermaid twice and brings the same embedded-HTML/link/image protections to both inputs. Exact OpenSpec source disclosure remains unchanged.\n\n### Keep standalone publishing source-safe\n\nPublished single-file contract pages will remove external Mermaid and base-script tags as they already do for local shared assets. The embedded diagram source fallback remains visible; runtime SVG snapshotting remains out of scope until Iris adopts a deterministic browser renderer.\n\n## Risks / Trade-offs\n\n- [The full Mermaid bundle materially increases installed dependency and per-workspace size] → Keep installation explicit through `iris vendor`, pin the version, copy one minified browser file plus its license, and document the size.\n- [Mermaid is a large untrusted-input parser with its own security history] → Pin exact versions, use strict mode and secure bounds, disable interactions/HTML labels, retain source fallback, and include dependency/security review in upgrades.\n- [Theme changes after rendering do not recolor an existing SVG] → Use a neutral, surface-compatible first implementation and retain accessible source; theme-aware rerendering can be a later independently specified enhancement.\n- [A missing vendor file produces a local resource load error] → The base script detects the absent global, keeps all fallbacks visible, and reports a concise local status in each figure.\n- [Standalone artifacts do not include rendered SVG] → Preserve the escaped source honestly; do not embed a multi-megabyte runtime or claim visual standalone diagrams without snapshot evidence.\n\n## Migration Plan\n\n1. Add the pinned dependency and the idempotent local vendor command.\n2. Extend safe Markdown output, styles, base script initialization, templates, and standalone stripping.\n3. Add unit, CLI, HTML integrity, malicious-input, multiple-diagram, offline, and packaging tests.\n4. Update authoring/technical documentation, run `iris vendor`, then regenerate checked-in Iris output with `iris render --all`.\n\nRollback removes the Mermaid fence override, vendor command implementation, script references, dependency, and generated vendor files; existing Markdown remains ordinary fenced code with no data migration.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-offline-runtime\">1. Offline Runtime\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Add and lock the pinned Mermaid production dependency, including its runtime rationale.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Implement idempotent \u003ccode>iris vendor\u003c/code> copying of the classic browser bundle and upstream license with actionable precondition and asset errors.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Add CLI, package, and offline asset tests for successful vendoring, refresh, missing workspace, and installed artifact contents.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-markdown-and-browser-rendering\">2. Markdown and Browser Rendering\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Extend the safe Markdown renderer so exact Mermaid fences emit escaped, accessible diagram hosts while other fences remain literal code.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Use the safe Markdown renderer for contract Markdown fields and load only relative classic Mermaid assets from generated HTML.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add isolated Mermaid initialization, strict security and complexity configuration, success accessibility, missing-runtime fallback, and per-diagram error handling to the shared base script.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Add responsive, theme-compatible, print-safe diagram and fallback styles without page-level overflow.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.5 Keep standalone publish/export artifacts self-contained by removing the local runtime reference and retaining diagram source fallback.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-verification-and-documentation\">3. Verification and Documentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Add renderer, OpenSpec dashboard, hostile-input, multiple-diagram, invalid-diagram, accessibility, offline, and HTML-integrity coverage.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Update README, command, technical, design-system, status, and dependency documentation for the implemented authoring and fallback contract.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Validate the OpenSpec change strictly and run focused tests followed by the repository release checks.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.4 Vendor Mermaid locally, regenerate managed Iris output, and verify generated HTML and assets without modifying unrelated user-owned files.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Offline Runtime\n\n- [x] 1.1 Add and lock the pinned Mermaid production dependency, including its runtime rationale.\n- [x] 1.2 Implement idempotent `iris vendor` copying of the classic browser bundle and upstream license with actionable precondition and asset errors.\n- [x] 1.3 Add CLI, package, and offline asset tests for successful vendoring, refresh, missing workspace, and installed artifact contents.\n\n## 2. Markdown and Browser Rendering\n\n- [x] 2.1 Extend the safe Markdown renderer so exact Mermaid fences emit escaped, accessible diagram hosts while other fences remain literal code.\n- [x] 2.2 Use the safe Markdown renderer for contract Markdown fields and load only relative classic Mermaid assets from generated HTML.\n- [x] 2.3 Add isolated Mermaid initialization, strict security and complexity configuration, success accessibility, missing-runtime fallback, and per-diagram error handling to the shared base script.\n- [x] 2.4 Add responsive, theme-compatible, print-safe diagram and fallback styles without page-level overflow.\n- [x] 2.5 Keep standalone publish/export artifacts self-contained by removing the local runtime reference and retaining diagram source fallback.\n\n## 3. Verification and Documentation\n\n- [x] 3.1 Add renderer, OpenSpec dashboard, hostile-input, multiple-diagram, invalid-diagram, accessibility, offline, and HTML-integrity coverage.\n- [x] 3.2 Update README, command, technical, design-system, status, and dependency documentation for the implemented authoring and fallback contract.\n- [x] 3.3 Validate the OpenSpec change strictly and run focused tests followed by the repository release checks.\n- [x] 3.4 Vendor Mermaid locally, regenerate managed Iris output, and verify generated HTML and assets without modifying unrelated user-owned files.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-markdown-diagram-rendering-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · markdown-diagram-rendering\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams/specs/markdown-diagram-rendering/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-markdown-diagram-rendering-purpose\">Purpose\u003c/h2>\n\u003cp>Provide safe, offline visual rendering for Mermaid fences in Iris Markdown while retaining readable source evidence whenever rendering is unavailable or invalid.\u003c/p>\n\u003ch2 id=\"delta-markdown-diagram-rendering-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-explicit-mermaid-fence-rendering\">Requirement: explicit Mermaid fence rendering\u003c/h3>\n\u003cp>The system SHALL recognize only fenced Markdown code blocks whose normalized language identifier is exactly \u003ccode>mermaid\u003c/code> as diagrams and SHALL leave every other fenced block as literal code.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-markdown-contains-a-mermaid-fence\">Scenario: Markdown contains a Mermaid fence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> rendered Markdown contains a closed fenced block labeled \u003ccode>mermaid\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated HTML SHALL contain a diagram host and the exact escaped diagram source as its fallback\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-another-code-fence-resembles-a-diagram\">Scenario: another code fence resembles a diagram\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> rendered Markdown contains an unlabeled fence or a fence with another language identifier\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated HTML SHALL preserve it as literal code and SHALL NOT submit it to the diagram runtime\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-offline-progressive-enhancement\">Requirement: offline progressive enhancement\u003c/h3>\n\u003cp>The system MUST render Mermaid diagrams using only a pinned local classic-script asset and MUST keep the Markdown readable through \u003ccode>file://\u003c/code> with no server, runtime module, or network request.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-local-mermaid-asset-is-installed\">Scenario: local Mermaid asset is installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Iris page containing a valid Mermaid fence opens from \u003ccode>file://\u003c/code> and the vendored runtime exists\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page SHALL replace the diagram host with an accessible inline SVG without requesting a remote resource\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-local-mermaid-asset-is-absent-or-javascript-is-unav\">Scenario: local Mermaid asset is absent or JavaScript is unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated page cannot load or execute the local diagram runtime\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each diagram SHALL retain its readable escaped-source fallback and the rest of the Markdown SHALL remain usable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-isolated-safe-failure\">Requirement: isolated safe failure\u003c/h3>\n\u003cp>The system MUST process each Mermaid fence independently under strict untrusted-content settings, MUST disable diagram click behavior and active HTML, and MUST preserve a diagram's escaped source when parsing or rendering that diagram fails.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-one-document-contains-valid-and-invalid-diagrams\">Scenario: one document contains valid and invalid diagrams\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Markdown document contains a valid Mermaid fence and a separate invalid Mermaid fence\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the valid diagram SHALL render, the invalid diagram SHALL show an actionable local error and its escaped source, and neither outcome SHALL hide the surrounding document\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-diagram-source-requests-active-behavior\">Scenario: diagram source requests active behavior\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Mermaid source includes HTML labels, click callbacks, links, scripts, or configuration intended to weaken the renderer\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the rendered page MUST keep active HTML and click behavior disabled and MUST NOT execute repository-authored JavaScript\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-bounded-and-accessible-diagram-presentation\">Requirement: bounded and accessible diagram presentation\u003c/h3>\n\u003cp>The system MUST give each diagram an accessible name, MUST bound accepted source and graph complexity, and MUST keep both rendered output and fallback source usable in light, dark, print, reduced-motion, and 360-pixel layouts.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-diagram-renders-successfully\">Scenario: diagram renders successfully\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Mermaid fence becomes an inline SVG\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the diagram SHALL expose a non-empty accessible label and fit its content region without forcing page-level horizontal overflow\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-diagram-input-exceeds-a-configured-bound\">Scenario: diagram input exceeds a configured bound\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> Mermaid source or graph complexity exceeds the renderer's configured limit\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> that diagram SHALL fail locally with its escaped source and an actionable error while other content remains available\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-markdown-diagram-rendering-requirement-explicit-local-asset-installation\">Requirement: explicit local asset installation\u003c/h3>\n\u003cp>The system SHALL provide an idempotent \u003ccode>iris vendor\u003c/code> operation that copies the pinned Mermaid browser bundle and upstream license from the installed Iris package into the workspace without fetching from the network.\u003c/p>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-user-vendors-diagram-assets\">Scenario: user vendors diagram assets\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris vendor\u003c/code> in an initialized Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command SHALL install or refresh the exact pinned Mermaid runtime and license under \u003ccode>iris/design/vendor/\u003c/code> and report the installed version\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-markdown-diagram-rendering-scenario-user-vendors-before-initialization\">Scenario: user vendors before initialization\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris vendor\u003c/code> without an initialized Iris workspace\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the command SHALL fail with an actionable instruction to run \u003ccode>iris init\u003c/code> and SHALL NOT create a partial workspace\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nProvide safe, offline visual rendering for Mermaid fences in Iris Markdown while retaining readable source evidence whenever rendering is unavailable or invalid.\n\n## ADDED Requirements\n\n### Requirement: explicit Mermaid fence rendering\n\nThe system SHALL recognize only fenced Markdown code blocks whose normalized language identifier is exactly `mermaid` as diagrams and SHALL leave every other fenced block as literal code.\n\n#### Scenario: Markdown contains a Mermaid fence\n\n- **WHEN** rendered Markdown contains a closed fenced block labeled `mermaid`\n- **THEN** the generated HTML SHALL contain a diagram host and the exact escaped diagram source as its fallback\n\n#### Scenario: another code fence resembles a diagram\n\n- **WHEN** rendered Markdown contains an unlabeled fence or a fence with another language identifier\n- **THEN** the generated HTML SHALL preserve it as literal code and SHALL NOT submit it to the diagram runtime\n\n### Requirement: offline progressive enhancement\n\nThe system MUST render Mermaid diagrams using only a pinned local classic-script asset and MUST keep the Markdown readable through `file://` with no server, runtime module, or network request.\n\n#### Scenario: local Mermaid asset is installed\n\n- **WHEN** a generated Iris page containing a valid Mermaid fence opens from `file://` and the vendored runtime exists\n- **THEN** the page SHALL replace the diagram host with an accessible inline SVG without requesting a remote resource\n\n#### Scenario: local Mermaid asset is absent or JavaScript is unavailable\n\n- **WHEN** a generated page cannot load or execute the local diagram runtime\n- **THEN** each diagram SHALL retain its readable escaped-source fallback and the rest of the Markdown SHALL remain usable\n\n### Requirement: isolated safe failure\n\nThe system MUST process each Mermaid fence independently under strict untrusted-content settings, MUST disable diagram click behavior and active HTML, and MUST preserve a diagram&#039;s escaped source when parsing or rendering that diagram fails.\n\n#### Scenario: one document contains valid and invalid diagrams\n\n- **WHEN** a Markdown document contains a valid Mermaid fence and a separate invalid Mermaid fence\n- **THEN** the valid diagram SHALL render, the invalid diagram SHALL show an actionable local error and its escaped source, and neither outcome SHALL hide the surrounding document\n\n#### Scenario: diagram source requests active behavior\n\n- **WHEN** Mermaid source includes HTML labels, click callbacks, links, scripts, or configuration intended to weaken the renderer\n- **THEN** the rendered page MUST keep active HTML and click behavior disabled and MUST NOT execute repository-authored JavaScript\n\n### Requirement: bounded and accessible diagram presentation\n\nThe system MUST give each diagram an accessible name, MUST bound accepted source and graph complexity, and MUST keep both rendered output and fallback source usable in light, dark, print, reduced-motion, and 360-pixel layouts.\n\n#### Scenario: diagram renders successfully\n\n- **WHEN** a Mermaid fence becomes an inline SVG\n- **THEN** the diagram SHALL expose a non-empty accessible label and fit its content region without forcing page-level horizontal overflow\n\n#### Scenario: diagram input exceeds a configured bound\n\n- **WHEN** Mermaid source or graph complexity exceeds the renderer&#039;s configured limit\n- **THEN** that diagram SHALL fail locally with its escaped source and an actionable error while other content remains available\n\n### Requirement: explicit local asset installation\n\nThe system SHALL provide an idempotent `iris vendor` operation that copies the pinned Mermaid browser bundle and upstream license from the installed Iris package into the workspace without fetching from the network.\n\n#### Scenario: user vendors diagram assets\n\n- **WHEN** a user runs `iris vendor` in an initialized Iris workspace\n- **THEN** the command SHALL install or refresh the exact pinned Mermaid runtime and license under `iris/design/vendor/` and report the installed version\n\n#### Scenario: user vendors before initialization\n\n- **WHEN** a user runs `iris vendor` without an initialized Iris workspace\n- **THEN** the command SHALL fail with an actionable instruction to run `iris init` and SHALL NOT create a partial workspace\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-openspec-spec-browser-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · openspec-spec-browser\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-mermaid-diagrams/specs/openspec-spec-browser/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/h3>\n\u003cp>The system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST progressively render fenced blocks labeled exactly \u003ccode>mermaid\u003c/code> as safe offline diagrams with escaped-source fallback, MUST keep the exact escaped document source available as a secondary disclosure, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-supported-markdown-structures-are-present\">Scenario: supported Markdown structures are present\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, fenced code, or Mermaid fences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST render those structures with readable semantic markup, progressively enhance Mermaid fences when the local runtime is available, and preserve escaped fallback source rather than displaying Markdown punctuation as the primary presentation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-needs-exact-source-evidence\">Scenario: user needs exact source evidence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a rendered Markdown artifact is displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the user MUST be able to reveal the exact escaped source without leaving the dashboard\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-configuration-artifact-is-displayed\">Scenario: configuration artifact is displayed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Spec view presents an OpenSpec YAML manifest or workspace configuration\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown or submitted to the diagram runtime\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-inert-markdown-generation\">Requirement: inert Markdown generation\u003c/h3>\n\u003cp>The system MUST generate Markdown presentation without allowing repository content or Mermaid diagram source to inject active HTML, unsafe URL schemes, executable click behavior, runtime modules, or implicit network requests.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-markdown-contains-embedded-html\">Scenario: Markdown contains embedded HTML\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact contains an HTML element, event handler, script, iframe, style, or other executable-looking markup outside or inside a Mermaid fence\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated document MUST keep that input inert and MUST NOT add it as executable DOM content\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-markdown-contains-an-unsafe-link\">Scenario: Markdown contains an unsafe link\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Markdown link or Mermaid interaction uses an unsafe scheme such as \u003ccode>javascript:\u003c/code> or \u003ccode>data:\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated document MUST not emit a navigable link or executable interaction for that destination\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-dashboard-opens-offline\">Scenario: dashboard opens offline\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the generated dashboard through \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> rendered Markdown and diagram fallbacks MUST remain readable without loading a Markdown library, module, stylesheet, font, script, or other asset from the network\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: safe semantic Markdown presentation\n\nThe system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST progressively render fenced blocks labeled exactly `mermaid` as safe offline diagrams with escaped-source fallback, MUST keep the exact escaped document source available as a secondary disclosure, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\n\n#### Scenario: supported Markdown structures are present\n\n- **WHEN** an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, fenced code, or Mermaid fences\n- **THEN** the Spec view MUST render those structures with readable semantic markup, progressively enhance Mermaid fences when the local runtime is available, and preserve escaped fallback source rather than displaying Markdown punctuation as the primary presentation\n\n#### Scenario: user needs exact source evidence\n\n- **WHEN** a rendered Markdown artifact is displayed\n- **THEN** the user MUST be able to reveal the exact escaped source without leaving the dashboard\n\n#### Scenario: configuration artifact is displayed\n\n- **WHEN** the Spec view presents an OpenSpec YAML manifest or workspace configuration\n- **THEN** the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown or submitted to the diagram runtime\n\n### Requirement: inert Markdown generation\n\nThe system MUST generate Markdown presentation without allowing repository content or Mermaid diagram source to inject active HTML, unsafe URL schemes, executable click behavior, runtime modules, or implicit network requests.\n\n#### Scenario: Markdown contains embedded HTML\n\n- **WHEN** an artifact contains an HTML element, event handler, script, iframe, style, or other executable-looking markup outside or inside a Mermaid fence\n- **THEN** the generated document MUST keep that input inert and MUST NOT add it as executable DOM content\n\n#### Scenario: Markdown contains an unsafe link\n\n- **WHEN** a Markdown link or Mermaid interaction uses an unsafe scheme such as `javascript:` or `data:`\n- **THEN** the generated document MUST not emit a navigable link or executable interaction for that destination\n\n#### Scenario: dashboard opens offline\n\n- **WHEN** a user opens the generated dashboard through `file://`\n- **THEN** rendered Markdown and diagram fallbacks MUST remain readable without loading a Markdown library, module, stylesheet, font, script, or other asset from the network\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-extend-the-existing-markdown-renderer-at-the-fence-rule\">Extend the existing Markdown renderer at the fence rule\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-use-a-pinned-runtime-dependency-and-explicit-offline-vendori\">Use a pinned runtime dependency and explicit offline vendoring\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-initialize-once-then-render-one-node-at-a-time\">Initialize once, then render one node at a time\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-treat-contract-fields-and-openspec-artifacts-as-one-markdown\">Treat contract fields and OpenSpec artifacts as one Markdown surface\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-keep-standalone-publishing-source-safe\">Keep standalone publishing source-safe\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-offline-runtime\">1. Offline Runtime\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-markdown-and-browser-rendering\">2. Markdown and Browser Rendering\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-verification-and-documentation\">3. Verification and Documentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-markdown-diagram-rendering-artifact\">Delta spec · markdown-diagram-rendering\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-markdown-diagram-rendering-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-markdown-diagram-rendering-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-explicit-mermaid-fence-rendering\">Requirement: explicit Mermaid fence rendering\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-offline-progressive-enhancement\">Requirement: offline progressive enhancement\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-isolated-safe-failure\">Requirement: isolated safe failure\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-bounded-and-accessible-diagram-presentation\">Requirement: bounded and accessible diagram presentation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-markdown-diagram-rendering-requirement-explicit-local-asset-installation\">Requirement: explicit local asset installation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-artifact\">Delta spec · openspec-spec-browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-inert-markdown-generation\">Requirement: inert Markdown generation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-render-openspec-markdown":{"kind":"change","name":"2026-08-21-render-openspec-markdown","title":"2026-08-21-render-openspec-markdown","path":"changes/archive/2026-08-21-render-openspec-markdown","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-render-openspec-markdown\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-render-openspec-markdown\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">12\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-render-openspec-markdown: 12 of 12 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">12/12 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-openspec-markdown/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The Spec browser currently presents every OpenSpec artifact as escaped source, which is safe but forces users to read Markdown syntax instead of the document hierarchy it describes. Iris should render Markdown as semantic offline HTML while preserving the existing untrusted-content boundary and an exact raw-source fallback.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Render Markdown OpenSpec artifacts with headings, paragraphs, lists, task lists, links, blockquotes, tables, and fenced code at generation time.\u003c/li>\n\u003cli>Disable embedded HTML and unsafe URL schemes so repository content cannot become executable markup or trigger remote requests implicitly.\u003c/li>\n\u003cli>Keep YAML manifests as escaped code and retain a collapsed raw-source disclosure for every document.\u003c/li>\n\u003cli>Style rendered documents through the existing Aperture token contract and cover readable light/dark, reduced-motion, narrow-screen, and print-safe behavior.\u003c/li>\n\u003cli>Add dependency, packaging, security, snapshot, and generated-HTML tests for the renderer.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cp>None.\u003c/p>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>openspec-spec-browser\u003c/code>: Replace raw-only Markdown presentation with safe semantic rendering while retaining exact source fallback and offline independence.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Affects the dashboard template, generated Aperture CSS, OpenSpec browser tests/fixtures, package dependencies and lockfile, dogfood output, and documentation.\u003c/li>\n\u003cli>Adds \u003ccode>markdown-it\u003c/code> as an installed generation-time dependency plus its TypeScript declarations for development.\u003c/li>\n\u003cli>Does not add browser runtime code, a server, network access, background processing, or Markdown execution.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe Spec browser currently presents every OpenSpec artifact as escaped source, which is safe but forces users to read Markdown syntax instead of the document hierarchy it describes. Iris should render Markdown as semantic offline HTML while preserving the existing untrusted-content boundary and an exact raw-source fallback.\n\n## What Changes\n\n- Render Markdown OpenSpec artifacts with headings, paragraphs, lists, task lists, links, blockquotes, tables, and fenced code at generation time.\n- Disable embedded HTML and unsafe URL schemes so repository content cannot become executable markup or trigger remote requests implicitly.\n- Keep YAML manifests as escaped code and retain a collapsed raw-source disclosure for every document.\n- Style rendered documents through the existing Aperture token contract and cover readable light/dark, reduced-motion, narrow-screen, and print-safe behavior.\n- Add dependency, packaging, security, snapshot, and generated-HTML tests for the renderer.\n\n## Capabilities\n\n### New Capabilities\n\nNone.\n\n### Modified Capabilities\n\n- `openspec-spec-browser`: Replace raw-only Markdown presentation with safe semantic rendering while retaining exact source fallback and offline independence.\n\n## Impact\n\n- Affects the dashboard template, generated Aperture CSS, OpenSpec browser tests/fixtures, package dependencies and lockfile, dogfood output, and documentation.\n- Adds `markdown-it` as an installed generation-time dependency plus its TypeScript declarations for development.\n- Does not add browser runtime code, a server, network access, background processing, or Markdown execution.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-openspec-markdown/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See \u003ccode>proposal.md\u003c/code> for motivation. The Spec browser currently stores bounded raw source in \u003ccode>iris/spec.json\u003c/code> and inserts an escaped copy into \u003ccode>&lt;pre&gt;&lt;code&gt;\u003c/code>. Iris pages also have a deliberately tiny paragraph/list formatter, but it is not a CommonMark renderer and cannot represent OpenSpec document hierarchy. All output must remain deterministic, offline, classic-script compatible, and safe for untrusted repository content.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Render Markdown artifacts into semantic HTML during CLI generation.\u003c/li>\n\u003cli>Make embedded HTML and unsafe links inert by construction.\u003c/li>\n\u003cli>Retain exact escaped source and parser metadata for evidence/debugging.\u003c/li>\n\u003cli>Style the generated document with Aperture tokens at 360 px, in both themes, reduced motion, and print.\u003c/li>\n\u003cli>Keep package/install smoke proof representative of the new runtime dependency.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Runtime Markdown rendering in the browser.\u003c/li>\n\u003cli>Executing embedded HTML, JavaScript, directives, plugins, diagrams, or syntax highlighters.\u003c/li>\n\u003cli>Interpreting YAML as Markdown.\u003c/li>\n\u003cli>Fetching remote link targets, images, styles, fonts, or previews.\u003c/li>\n\u003cli>Replacing the structural OpenSpec parser or snapshot format.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-1-use-markdown-it-at-generation-time-with-embedded-html-disa\">1. Use \u003ccode>markdown-it\u003c/code> at generation time with embedded HTML disabled\u003c/h3>\n\u003cp>Add the pinned MIT-licensed \u003ccode>markdown-it\u003c/code> package and TypeScript declarations. Configure one renderer with \u003ccode>html: false\u003c/code>, \u003ccode>linkify: false\u003c/code>, and \u003ccode>typographer: false\u003c/code>. This supports the required document structures while making raw HTML text inert before template insertion. \u003ccode>marked\u003c/code> plus a sanitizer was rejected because it would require a second security dependency and a larger policy surface. Expanding Iris's tiny formatter was rejected because edge-complete Markdown parsing is mature-library work, not a charming weekend regex accident.\u003c/p>\n\u003ch3 id=\"design-2-apply-an-explicit-output-link-policy\">2. Apply an explicit output/link policy\u003c/h3>\n\u003cp>Use the renderer's default URL normalization/validation and override link output only to add \u003ccode>rel=&quot;noopener noreferrer&quot;\u003c/code>. Do not enable automatic URL linkification. Render all Markdown image syntax as escaped textual evidence (alt text plus source) rather than an active \u003ccode>&lt;img&gt;\u003c/code>, so neither remote nor unresolved relative references can initiate requests or break generated-file integrity. Tests must prove unsafe schemes do not become \u003ccode>href\u003c/code> or \u003ccode>src\u003c/code> attributes and generated output contains no new remote asset dependency.\u003c/p>\n\u003cp>The generated HTML returned by the configured renderer is trusted only as renderer output. Repository text must never bypass that renderer into an HTML context except through the existing escaping helper.\u003c/p>\n\u003ch3 id=\"design-3-separate-rendered-document-and-exact-source\">3. Separate rendered document and exact source\u003c/h3>\n\u003cp>For Markdown document kinds, each artifact disclosure contains a \u003ccode>.spec-document\u003c/code> semantic rendering followed by a nested, collapsed \u003ccode>Source\u003c/code> disclosure with the escaped raw bytes. Config/YAML documents continue to use only the escaped source block. Parser warnings and requirement/scenario/operation summaries remain outside the rendered body.\u003c/p>\n\u003cp>This keeps the primary view readable while preserving auditable evidence and graceful fallback when Markdown is malformed.\u003c/p>\n\u003ch3 id=\"design-4-keep-generated-styling-token-owned-and-bounded\">4. Keep generated styling token-owned and bounded\u003c/h3>\n\u003cp>Add styles for document headings, paragraphs, lists, task inputs, blockquotes, tables, links, and code using only Aperture tokens. Tables and code blocks scroll inside their container; long prose and URLs wrap without page-level horizontal overflow. Native controls and static content require no new animation, so reduced-motion behavior remains unchanged. Print styling avoids clipping source or document content.\u003c/p>\n\u003ch3 id=\"design-5-verify-security-at-both-helper-and-generated-dashboard-bou\">5. Verify security at both helper and generated-dashboard boundaries\u003c/h3>\n\u003cp>Add unit-visible fixtures containing the supported syntax plus embedded script/style/iframe/event-handler markup and unsafe links. Assert semantic output, escaped hostile HTML, rejected unsafe destinations, exact raw-source availability, no remote runtime references, and unchanged YAML literal behavior. Retain the full install smoke gate so the packed CLI proves the dependency ships correctly.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Markdown library behavior changes] → Pin exact versions, retain security regression fixtures, and review upgrades deliberately.\u003c/li>\n\u003cli>[Rendered HTML enlarges the dashboard] → Source was already embedded; semantic markup adds bounded overhead under existing input limits.\u003c/li>\n\u003cli>[Remote images create implicit requests] → Do not support remote image rendering in the Spec view; render their alt text/link evidence without an active remote \u003ccode>src\u003c/code>.\u003c/li>\n\u003cli>[Malformed Markdown renders surprisingly] → Keep exact source and parser warnings available; never treat presentation as semantic validation.\u003c/li>\n\u003cli>[CSS makes wide structures overflow] → Contain tables/code blocks locally and verify narrow-screen hooks.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add and pin the parser dependency and declarations.\u003c/li>\n\u003cli>Introduce a focused safe Markdown renderer and unit/security tests.\u003c/li>\n\u003cli>Wire Markdown document kinds into the Spec artifact presentation while retaining YAML/source fallback.\u003c/li>\n\u003cli>Extend Aperture styles and generated-HTML assertions.\u003c/li>\n\u003cli>Update docs, regenerate dogfood through the built CLI, run full packaging/OpenSpec gates, sync, and archive.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback removes the dependency and renderer integration, then regenerates \u003ccode>iris/\u003c/code> with the prior raw-only template. The snapshot format and repository-owned OpenSpec inputs do not change.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee `proposal.md` for motivation. The Spec browser currently stores bounded raw source in `iris/spec.json` and inserts an escaped copy into `&lt;pre&gt;&lt;code&gt;`. Iris pages also have a deliberately tiny paragraph/list formatter, but it is not a CommonMark renderer and cannot represent OpenSpec document hierarchy. All output must remain deterministic, offline, classic-script compatible, and safe for untrusted repository content.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Render Markdown artifacts into semantic HTML during CLI generation.\n- Make embedded HTML and unsafe links inert by construction.\n- Retain exact escaped source and parser metadata for evidence/debugging.\n- Style the generated document with Aperture tokens at 360 px, in both themes, reduced motion, and print.\n- Keep package/install smoke proof representative of the new runtime dependency.\n\n**Non-Goals:**\n\n- Runtime Markdown rendering in the browser.\n- Executing embedded HTML, JavaScript, directives, plugins, diagrams, or syntax highlighters.\n- Interpreting YAML as Markdown.\n- Fetching remote link targets, images, styles, fonts, or previews.\n- Replacing the structural OpenSpec parser or snapshot format.\n\n## Decisions\n\n### 1. Use `markdown-it` at generation time with embedded HTML disabled\n\nAdd the pinned MIT-licensed `markdown-it` package and TypeScript declarations. Configure one renderer with `html: false`, `linkify: false`, and `typographer: false`. This supports the required document structures while making raw HTML text inert before template insertion. `marked` plus a sanitizer was rejected because it would require a second security dependency and a larger policy surface. Expanding Iris&#039;s tiny formatter was rejected because edge-complete Markdown parsing is mature-library work, not a charming weekend regex accident.\n\n### 2. Apply an explicit output/link policy\n\nUse the renderer&#039;s default URL normalization/validation and override link output only to add `rel=&quot;noopener noreferrer&quot;`. Do not enable automatic URL linkification. Render all Markdown image syntax as escaped textual evidence (alt text plus source) rather than an active `&lt;img&gt;`, so neither remote nor unresolved relative references can initiate requests or break generated-file integrity. Tests must prove unsafe schemes do not become `href` or `src` attributes and generated output contains no new remote asset dependency.\n\nThe generated HTML returned by the configured renderer is trusted only as renderer output. Repository text must never bypass that renderer into an HTML context except through the existing escaping helper.\n\n### 3. Separate rendered document and exact source\n\nFor Markdown document kinds, each artifact disclosure contains a `.spec-document` semantic rendering followed by a nested, collapsed `Source` disclosure with the escaped raw bytes. Config/YAML documents continue to use only the escaped source block. Parser warnings and requirement/scenario/operation summaries remain outside the rendered body.\n\nThis keeps the primary view readable while preserving auditable evidence and graceful fallback when Markdown is malformed.\n\n### 4. Keep generated styling token-owned and bounded\n\nAdd styles for document headings, paragraphs, lists, task inputs, blockquotes, tables, links, and code using only Aperture tokens. Tables and code blocks scroll inside their container; long prose and URLs wrap without page-level horizontal overflow. Native controls and static content require no new animation, so reduced-motion behavior remains unchanged. Print styling avoids clipping source or document content.\n\n### 5. Verify security at both helper and generated-dashboard boundaries\n\nAdd unit-visible fixtures containing the supported syntax plus embedded script/style/iframe/event-handler markup and unsafe links. Assert semantic output, escaped hostile HTML, rejected unsafe destinations, exact raw-source availability, no remote runtime references, and unchanged YAML literal behavior. Retain the full install smoke gate so the packed CLI proves the dependency ships correctly.\n\n## Risks / Trade-offs\n\n- [Markdown library behavior changes] → Pin exact versions, retain security regression fixtures, and review upgrades deliberately.\n- [Rendered HTML enlarges the dashboard] → Source was already embedded; semantic markup adds bounded overhead under existing input limits.\n- [Remote images create implicit requests] → Do not support remote image rendering in the Spec view; render their alt text/link evidence without an active remote `src`.\n- [Malformed Markdown renders surprisingly] → Keep exact source and parser warnings available; never treat presentation as semantic validation.\n- [CSS makes wide structures overflow] → Contain tables/code blocks locally and verify narrow-screen hooks.\n\n## Migration Plan\n\n1. Add and pin the parser dependency and declarations.\n2. Introduce a focused safe Markdown renderer and unit/security tests.\n3. Wire Markdown document kinds into the Spec artifact presentation while retaining YAML/source fallback.\n4. Extend Aperture styles and generated-HTML assertions.\n5. Update docs, regenerate dogfood through the built CLI, run full packaging/OpenSpec gates, sync, and archive.\n\nRollback removes the dependency and renderer integration, then regenerates `iris/` with the prior raw-only template. The snapshot format and repository-owned OpenSpec inputs do not change.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-openspec-markdown/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-add-the-safe-generation-time-renderer\">1. Add the safe generation-time renderer\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Pin \u003ccode>markdown-it\u003c/code> and its TypeScript declarations, record the runtime rationale, and refresh the lockfile.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Add a focused renderer module with embedded HTML, automatic linkification, typographic rewriting, unsafe destinations, and active remote images disabled.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Add renderer tests for supported Markdown structure, inert HTML, safe/unsafe links, local and remote image policy, deterministic output, and exact source independence.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-integrate-semantic-artifact-presentation\">2. Integrate semantic artifact presentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Distinguish Markdown documents from YAML/config documents in the normalized source model without changing snapshot compatibility.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Render Markdown as the primary artifact body, preserve parser summaries, and add a nested exact-source disclosure while keeping YAML literal-only.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Extend Aperture token-owned styles for prose, task lists, blockquotes, tables, links, inline/fenced code, nested source, narrow screens, both themes, and print.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-prove-generated-safety-and-usability\">3. Prove generated safety and usability\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Extend fixtures with representative CommonMark/GFM-style structures plus script, iframe, event-handler, unsafe-link, and remote-image payloads.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Extend generated-dashboard tests for semantic markup, inert hostile content, rejected unsafe/remote destinations, exact source, YAML literal behavior, offline assets, and classic deferred scripts.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Run focused parser/template/navigation tests and resolve any regression in Work/Spec interaction or file-link integrity.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-document-dogfood-and-verify\">4. Document, dogfood, and verify\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Update dependency rationale, user commands, technical/design/status docs, and the installed Iris skill for rendered Markdown and source fallback behavior.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Build the CLI and regenerate \u003ccode>iris/spec.json\u003c/code>, managed assets, and the dashboard through \u003ccode>iris init\u003c/code> and \u003ccode>iris render --all\u003c/code>; inspect the generated presentation without hand edits.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Run lint, token lint, typecheck, the full test suite, HTML integrity, packed-install smoke, strict OpenSpec validation, and the verification workflow before spec sync/archive.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Add the safe generation-time renderer\n\n- [x] 1.1 Pin `markdown-it` and its TypeScript declarations, record the runtime rationale, and refresh the lockfile.\n- [x] 1.2 Add a focused renderer module with embedded HTML, automatic linkification, typographic rewriting, unsafe destinations, and active remote images disabled.\n- [x] 1.3 Add renderer tests for supported Markdown structure, inert HTML, safe/unsafe links, local and remote image policy, deterministic output, and exact source independence.\n\n## 2. Integrate semantic artifact presentation\n\n- [x] 2.1 Distinguish Markdown documents from YAML/config documents in the normalized source model without changing snapshot compatibility.\n- [x] 2.2 Render Markdown as the primary artifact body, preserve parser summaries, and add a nested exact-source disclosure while keeping YAML literal-only.\n- [x] 2.3 Extend Aperture token-owned styles for prose, task lists, blockquotes, tables, links, inline/fenced code, nested source, narrow screens, both themes, and print.\n\n## 3. Prove generated safety and usability\n\n- [x] 3.1 Extend fixtures with representative CommonMark/GFM-style structures plus script, iframe, event-handler, unsafe-link, and remote-image payloads.\n- [x] 3.2 Extend generated-dashboard tests for semantic markup, inert hostile content, rejected unsafe/remote destinations, exact source, YAML literal behavior, offline assets, and classic deferred scripts.\n- [x] 3.3 Run focused parser/template/navigation tests and resolve any regression in Work/Spec interaction or file-link integrity.\n\n## 4. Document, dogfood, and verify\n\n- [x] 4.1 Update dependency rationale, user commands, technical/design/status docs, and the installed Iris skill for rendered Markdown and source fallback behavior.\n- [x] 4.2 Build the CLI and regenerate `iris/spec.json`, managed assets, and the dashboard through `iris init` and `iris render --all`; inspect the generated presentation without hand edits.\n- [x] 4.3 Run lint, token lint, typecheck, the full test suite, HTML integrity, packed-install smoke, strict OpenSpec validation, and the verification workflow before spec sync/archive.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-openspec-markdown/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-openspec-spec-browser-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · openspec-spec-browser\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-render-openspec-markdown/specs/openspec-spec-browser/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/h3>\n\u003cp>The system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST keep the exact escaped source available as a secondary disclosure, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-supported-markdown-structures-are-present\">Scenario: supported Markdown structures are present\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, or fenced code\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec view MUST render those structures with readable semantic markup rather than displaying Markdown punctuation as the primary presentation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-needs-exact-source-evidence\">Scenario: user needs exact source evidence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a rendered Markdown artifact is displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the user MUST be able to reveal the exact escaped source without leaving the dashboard\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-configuration-artifact-is-displayed\">Scenario: configuration artifact is displayed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Spec view presents an OpenSpec YAML manifest or workspace configuration\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-inert-markdown-generation\">Requirement: inert Markdown generation\u003c/h3>\n\u003cp>The system MUST generate Markdown presentation without allowing repository content to inject active HTML, unsafe URL schemes, browser runtime dependencies, or implicit network requests.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-markdown-contains-embedded-html\">Scenario: Markdown contains embedded HTML\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact contains an HTML element, event handler, script, iframe, style, or other executable-looking markup\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated document MUST display that input as inert text and MUST NOT add it as an active DOM element\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-markdown-contains-an-unsafe-link\">Scenario: Markdown contains an unsafe link\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a Markdown link uses an unsafe scheme such as \u003ccode>javascript:\u003c/code> or \u003ccode>data:\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated document MUST not emit a navigable link for that destination\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-dashboard-opens-offline\">Scenario: dashboard opens offline\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the generated dashboard through \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> rendered Markdown MUST remain readable without loading a Markdown library, module, stylesheet, font, script, or other asset from the network\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## ADDED Requirements\n\n### Requirement: safe semantic Markdown presentation\nThe system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST keep the exact escaped source available as a secondary disclosure, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\n\n#### Scenario: supported Markdown structures are present\n- **WHEN** an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, or fenced code\n- **THEN** the Spec view MUST render those structures with readable semantic markup rather than displaying Markdown punctuation as the primary presentation\n\n#### Scenario: user needs exact source evidence\n- **WHEN** a rendered Markdown artifact is displayed\n- **THEN** the user MUST be able to reveal the exact escaped source without leaving the dashboard\n\n#### Scenario: configuration artifact is displayed\n- **WHEN** the Spec view presents an OpenSpec YAML manifest or workspace configuration\n- **THEN** the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown\n\n### Requirement: inert Markdown generation\nThe system MUST generate Markdown presentation without allowing repository content to inject active HTML, unsafe URL schemes, browser runtime dependencies, or implicit network requests.\n\n#### Scenario: Markdown contains embedded HTML\n- **WHEN** an artifact contains an HTML element, event handler, script, iframe, style, or other executable-looking markup\n- **THEN** the generated document MUST display that input as inert text and MUST NOT add it as an active DOM element\n\n#### Scenario: Markdown contains an unsafe link\n- **WHEN** a Markdown link uses an unsafe scheme such as `javascript:` or `data:`\n- **THEN** the generated document MUST not emit a navigable link for that destination\n\n#### Scenario: dashboard opens offline\n- **WHEN** a user opens the generated dashboard through `file://`\n- **THEN** rendered Markdown MUST remain readable without loading a Markdown library, module, stylesheet, font, script, or other asset from the network\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-1-use-markdown-it-at-generation-time-with-embedded-html-disa\">1. Use `markdown-it` at generation time with embedded HTML disabled\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-2-apply-an-explicit-output-link-policy\">2. Apply an explicit output/link policy\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-3-separate-rendered-document-and-exact-source\">3. Separate rendered document and exact source\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-4-keep-generated-styling-token-owned-and-bounded\">4. Keep generated styling token-owned and bounded\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-5-verify-security-at-both-helper-and-generated-dashboard-bou\">5. Verify security at both helper and generated-dashboard boundaries\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-add-the-safe-generation-time-renderer\">1. Add the safe generation-time renderer\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-integrate-semantic-artifact-presentation\">2. Integrate semantic artifact presentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-prove-generated-safety-and-usability\">3. Prove generated safety and usability\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-document-dogfood-and-verify\">4. Document, dogfood, and verify\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-artifact\">Delta spec · openspec-spec-browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-inert-markdown-generation\">Requirement: inert Markdown generation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-research-markdown-pages":{"kind":"change","name":"2026-08-21-research-markdown-pages","title":"2026-08-21-research-markdown-pages","path":"changes/archive/2026-08-21-research-markdown-pages","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-research-markdown-pages\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-research-markdown-pages\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">14\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-research-markdown-pages: 14 of 14 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">14/14 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-research-markdown-pages/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>Coding agents produce research, investigation, and hand-off output as Markdown, but Iris can only render JSON contracts with Markdown embedded in string fields — clumsy to write, escape, and review — so that output ends up as loose \u003ccode>.md\u003c/code> files with no index, navigation, or consistent rendering. OpenSpec already shows the right model: read Markdown files directly, render them deterministically, and organize them. Iris should do the same for agent research so the workspace, not a folder of Markdown, becomes the organized record.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Add a \u003ccode>research\u003c/code> content type whose source is one Markdown file per item at \u003ccode>iris/research/&lt;id&gt;/index.md\u003c/code>, with optional front matter (\u003ccode>title\u003c/code>, \u003ccode>status\u003c/code>, \u003ccode>tags\u003c/code>, \u003ccode>agent\u003c/code>, \u003ccode>updated\u003c/code>) and a free Markdown body.\u003c/li>\n\u003cli>Add \u003ccode>iris research &lt;id&gt;\u003c/code> to create a compact skeleton (front matter plus Question / Findings / Evidence / Next steps headings) that agents fill in directly in Markdown.\u003c/li>\n\u003cli>Render each research item to \u003ccode>iris/research/&lt;id&gt;/page.html\u003c/code> with a document template: shared navigation shell, page header from front matter, generated table of contents, the existing safe Markdown renderer, Mermaid fences, callouts, print styles.\u003c/li>\n\u003cli>Generate \u003ccode>iris/research.html\u003c/code>: a Research section page with a summary strip, filterable list, and tag signals; add a Research entry to the navigation shell.\u003c/li>\n\u003cli>Include research records in the Work browser projection as type \u003ccode>research\u003c/code> (status from front matter, priority unavailable) so List/Table/Kanban, filter, and drawer cover them; extend archive, publish, and export to research pages.\u003c/li>\n\u003cli>Update the canonical \u003ccode>iris-workspace\u003c/code> skill with the research workflow in a few lines so agents know to write Markdown, not JSON, for research responses.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>research-markdown-pages\u003c/code>: Markdown-sourced research pages — source contract, rendering, section page, navigation, work-browser inclusion, lifecycle, and agent guidance.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cp>None. Navigation and work-browser inclusion are stated as requirements of the new capability; the shell itself comes from \u003ccode>dashboard-shell-redesign\u003c/code>, which this change depends on.\u003c/p>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>New \u003ccode>src/lib/front-matter.ts\u003c/code> (bounded parser) and \u003ccode>src/lib/research-workspace.ts\u003c/code> (discovery, bounds, projection); new \u003ccode>src/templates/pages/research.ts\u003c/code> and document template; \u003ccode>draft.ts\u003c/code> gains \u003ccode>research\u003c/code>; \u003ccode>render.ts\u003c/code>, \u003ccode>lifecycle.ts\u003c/code> (archive), \u003ccode>publish.ts\u003c/code>, \u003ccode>export.ts\u003c/code> resolve research paths; \u003ccode>project-state.ts\u003c/code> records the source root per entry (additive, version stays 2); \u003ccode>command-catalog.ts\u003c/code> and \u003ccode>templates/agents/iris-workspace.md\u003c/code> gain the command.\u003c/li>\n\u003cli>\u003ccode>renderSafeMarkdown\u003c/code> gains an opt-in heading-id mode for the table of contents; OpenSpec rendering is unchanged.\u003c/li>\n\u003cli>Tests for parser, draft, render, projection, archive, publish, hostile input, and the skill template; docs (\u003ccode>README\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/status.md\u003c/code>, \u003ccode>docs/tech.md\u003c/code>) and regenerated \u003ccode>iris/\u003c/code> output.\u003c/li>\n\u003cli>No new dependency; reading stays inside \u003ccode>iris/research/\u003c/code>, sorted, bounded, symlink-refusing, and network-free. Depends on \u003ccode>dashboard-shell-redesign\u003c/code> for the shell and the Research nav entry.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nCoding agents produce research, investigation, and hand-off output as Markdown, but Iris can only render JSON contracts with Markdown embedded in string fields — clumsy to write, escape, and review — so that output ends up as loose `.md` files with no index, navigation, or consistent rendering. OpenSpec already shows the right model: read Markdown files directly, render them deterministically, and organize them. Iris should do the same for agent research so the workspace, not a folder of Markdown, becomes the organized record.\n\n## What Changes\n\n- Add a `research` content type whose source is one Markdown file per item at `iris/research/&lt;id&gt;/index.md`, with optional front matter (`title`, `status`, `tags`, `agent`, `updated`) and a free Markdown body.\n- Add `iris research &lt;id&gt;` to create a compact skeleton (front matter plus Question / Findings / Evidence / Next steps headings) that agents fill in directly in Markdown.\n- Render each research item to `iris/research/&lt;id&gt;/page.html` with a document template: shared navigation shell, page header from front matter, generated table of contents, the existing safe Markdown renderer, Mermaid fences, callouts, print styles.\n- Generate `iris/research.html`: a Research section page with a summary strip, filterable list, and tag signals; add a Research entry to the navigation shell.\n- Include research records in the Work browser projection as type `research` (status from front matter, priority unavailable) so List/Table/Kanban, filter, and drawer cover them; extend archive, publish, and export to research pages.\n- Update the canonical `iris-workspace` skill with the research workflow in a few lines so agents know to write Markdown, not JSON, for research responses.\n\n## Capabilities\n\n### New Capabilities\n\n- `research-markdown-pages`: Markdown-sourced research pages — source contract, rendering, section page, navigation, work-browser inclusion, lifecycle, and agent guidance.\n\n### Modified Capabilities\n\nNone. Navigation and work-browser inclusion are stated as requirements of the new capability; the shell itself comes from `dashboard-shell-redesign`, which this change depends on.\n\n## Impact\n\n- New `src/lib/front-matter.ts` (bounded parser) and `src/lib/research-workspace.ts` (discovery, bounds, projection); new `src/templates/pages/research.ts` and document template; `draft.ts` gains `research`; `render.ts`, `lifecycle.ts` (archive), `publish.ts`, `export.ts` resolve research paths; `project-state.ts` records the source root per entry (additive, version stays 2); `command-catalog.ts` and `templates/agents/iris-workspace.md` gain the command.\n- `renderSafeMarkdown` gains an opt-in heading-id mode for the table of contents; OpenSpec rendering is unchanged.\n- Tests for parser, draft, render, projection, archive, publish, hostile input, and the skill template; docs (`README`, `docs/cmds.md`, `docs/status.md`, `docs/tech.md`) and regenerated `iris/` output.\n- No new dependency; reading stays inside `iris/research/`, sorted, bounded, symlink-refusing, and network-free. Depends on `dashboard-shell-redesign` for the shell and the Research nav entry.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-research-markdown-pages/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See proposal.md. Iris renders five JSON contract types from \u003ccode>iris/pages/&lt;id&gt;/data.json\u003c/code> and visualizes OpenSpec Markdown read directly from \u003ccode>openspec/\u003c/code> through a bounded walker plus \u003ccode>renderSafeMarkdown\u003c/code> (markdown-it, embedded HTML off, Mermaid fences as source-first hosts). \u003ccode>agent-first-initialization\u003c/code> deliberately removed implicit ingestion of \u003ccode>README.md\u003c/code>/\u003ccode>docs/**/*.md\u003c/code>. This change depends on \u003ccode>dashboard-shell-redesign\u003c/code> for \u003ccode>renderShell()\u003c/code>, the nav model, and section pages.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Agents write one Markdown file per research item; no JSON escaping.\u003c/li>\n\u003cli>Reuse the OpenSpec reading discipline (sorted, allowlisted, bounded, symlink-refusing) and the existing safe renderer.\u003c/li>\n\u003cli>Research joins the same Work projection so one browser, filter, and drawer cover everything.\u003c/li>\n\u003cli>Skill guidance stays a few lines; skeleton is small.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Reading Markdown from outside \u003ccode>iris/research/\u003c/code> (for example \u003ccode>docs/agent-handoffs/*.md\u003c/code>). Explicit opt-in source globs in \u003ccode>config.yaml\u003c/code> are a possible later capability; implicit ingestion stays removed.\u003c/li>\n\u003cli>Images or attachments next to \u003ccode>index.md\u003c/code>, Markdown-to-contract promotion, full-text search, or editing in the browser.\u003c/li>\n\u003cli>A YAML dependency; the parser covers the documented subset only.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-markdown-file-with-front-matter-not-a-sixth-json-contract\">Markdown file with front matter, not a sixth JSON contract\u003c/h3>\n\u003cp>A \u003ccode>research\u003c/code> JSON type with \u003ccode>sections.body.md\u003c/code> would reuse every existing code path but forces agents to escape Markdown inside JSON strings and makes diffs unreadable. A plain file matches how agents already produce research and how OpenSpec artifacts are authored; the price is a small front-matter parser and a second source root, both bounded.\u003c/p>\n\u003ch3 id=\"design-bounded-hand-rolled-front-matter-parser\">Bounded hand-rolled front matter parser\u003c/h3>\n\u003cp>\u003ccode>src/lib/front-matter.ts\u003c/code> accepts a leading \u003ccode>---\u003c/code> block and parses \u003ccode>key: scalar\u003c/code>, \u003ccode>key: [a, b]\u003c/code>, and \u003ccode>key:\u003c/code> followed by \u003ccode>- item\u003c/code> lines; quotes are stripped, unknown keys ignored, duplicate keys last-wins, anything else yields a warning. Values are validated (\u003ccode>status\u003c/code> enum, \u003ccode>updated\u003c/code> \u003ccode>YYYY-MM-DD\u003c/code> prefix, tag strings bounded to 80 chars, at most 20 tags). No YAML library: the subset is tiny and determinism matters more than YAML fidelity.\u003c/p>\n\u003ch3 id=\"design-discovery-mirrors-the-openspec-walker\">Discovery mirrors the OpenSpec walker\u003c/h3>\n\u003cp>\u003ccode>src/lib/research-workspace.ts\u003c/code> lists \u003ccode>iris/research/*/index.md\u003c/code> sorted, refuses symlinks and path escapes, caps file bytes (256 KB) and item count, and returns \u003ccode>{ id, frontMatter, body, warnings }\u003c/code>. Warnings surface on the Research page and in \u003ccode>--json\u003c/code> output, not as failures.\u003c/p>\n\u003ch3 id=\"design-document-template-and-table-of-contents\">Document template and table of contents\u003c/h3>\n\u003cp>\u003ccode>renderSafeMarkdown\u003c/code> gains an options object with \u003ccode>headingIds: true\u003c/code>; when set, heading tokens get deterministic slug ids (lowercased, non-alphanumerics collapsed to \u003ccode>-\u003c/code>, numeric suffix on collision) and the renderer returns the heading list alongside HTML. OpenSpec rendering keeps the default (no ids) so multiple documents on the Spec page cannot collide. The research page shows a right-rail TOC (sticky on wide screens, inline above the body at narrow widths) when two or more h2/h3 headings exist.\u003c/p>\n\u003ch3 id=\"design-one-projection-two-source-roots\">One projection, two source roots\u003c/h3>\n\u003cp>\u003ccode>refreshDashboard\u003c/code> builds \u003ccode>DashboardPage[]\u003c/code> from \u003ccode>pages/\u003c/code> (contracts) and \u003ccode>research/\u003c/code> (Markdown), tagging research entries \u003ccode>type: 'research'\u003c/code>, \u003ccode>priority: 'not set'\u003c/code>, \u003ccode>description\u003c/code> = first paragraph bounded to 1200 chars, \u003ccode>evidence\u003c/code> = \u003ccode>&quot;&lt;n&gt; headings · &lt;m&gt; words&quot;\u003c/code>. Work List/Table/Kanban, filter, and drawer need no change; \u003ccode>typeClass\u003c/code> gains \u003ccode>tp-research\u003c/code>. The Overview and Work summary strips count research.\u003c/p>\n\u003ch3 id=\"design-registry-and-lifecycle\">Registry and lifecycle\u003c/h3>\n\u003cp>\u003ccode>PageRegistryEntry\u003c/code> gains optional \u003ccode>source: 'pages' | 'research'\u003c/code> (default \u003ccode>pages\u003c/code>); state stays version 2. \u003ccode>archive\u003c/code> resolves the source root from the entry (falling back to whichever directory exists), moves the whole folder under \u003ccode>iris/archive/&lt;id&gt;/\u003c/code>, and re-renders. \u003ccode>publish\u003c/code>/\u003ccode>export\u003c/code> resolve \u003ccode>page.html\u003c/code> across \u003ccode>pages/\u003c/code>, \u003ccode>research/\u003c/code>, and \u003ccode>archive/\u003c/code>. Research pages carry the same \u003ccode>data-iris-nav\u003c/code> shell so the existing stripper works.\u003c/p>\n\u003ch3 id=\"design-skill-and-catalog-wording\">Skill and catalog wording\u003c/h3>\n\u003cp>\u003ccode>templates/agents/iris-workspace.md\u003c/code> adds one numbered item (&quot;\u003ccode>iris research &lt;id&gt;\u003c/code> creates \u003ccode>iris/research/&lt;id&gt;/index.md\u003c/code>; write Markdown there, then \u003ccode>iris render --all\u003c/code>&quot;) and one sentence on front matter keys. \u003ccode>command-catalog.ts\u003c/code> adds \u003ccode>research\u003c/code> to the \u003ccode>content\u003c/code> group with status \u003ccode>implemented\u003c/code>.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Two source roots complicate id uniqueness] → \u003ccode>draft\u003c/code>/\u003ccode>research\u003c/code> refuse an id that exists in \u003ccode>pages/\u003c/code>, \u003ccode>research/\u003c/code>, or \u003ccode>archive/\u003c/code>; the render reports duplicates and keeps the first.\u003c/li>\n\u003cli>[Front matter subset surprises users] → Warnings name the unsupported line; docs list the exact grammar.\u003c/li>\n\u003cli>[Heading ids could leak into Spec rendering] → Opt-in option only; a test asserts OpenSpec output has no ids.\u003c/li>\n\u003cli>[Large Markdown bloats work-item data attributes] → Description bounded before projection, as for contracts.\u003c/li>\n\u003cli>[Shell dependency] → Implement after \u003ccode>dashboard-shell-redesign\u003c/code>; if it slips, render research pages with the shell function stubbed to the current header to stay unblocked, then switch.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Land parser and discovery with tests (no output change).\u003c/li>\n\u003cli>Add \u003ccode>iris research\u003c/code>, the document template, and the Research section page + nav entry; extend the projection.\u003c/li>\n\u003cli>Extend archive/publish/export and the registry; update the skill template and catalog.\u003c/li>\n\u003cli>Regenerate dogfood, run the release gate, update docs.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback: remove the command and pages; existing \u003ccode>iris/research/\u003c/code> Markdown is left untouched as plain files.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee proposal.md. Iris renders five JSON contract types from `iris/pages/&lt;id&gt;/data.json` and visualizes OpenSpec Markdown read directly from `openspec/` through a bounded walker plus `renderSafeMarkdown` (markdown-it, embedded HTML off, Mermaid fences as source-first hosts). `agent-first-initialization` deliberately removed implicit ingestion of `README.md`/`docs/**/*.md`. This change depends on `dashboard-shell-redesign` for `renderShell()`, the nav model, and section pages.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Agents write one Markdown file per research item; no JSON escaping.\n- Reuse the OpenSpec reading discipline (sorted, allowlisted, bounded, symlink-refusing) and the existing safe renderer.\n- Research joins the same Work projection so one browser, filter, and drawer cover everything.\n- Skill guidance stays a few lines; skeleton is small.\n\n**Non-Goals:**\n\n- Reading Markdown from outside `iris/research/` (for example `docs/agent-handoffs/*.md`). Explicit opt-in source globs in `config.yaml` are a possible later capability; implicit ingestion stays removed.\n- Images or attachments next to `index.md`, Markdown-to-contract promotion, full-text search, or editing in the browser.\n- A YAML dependency; the parser covers the documented subset only.\n\n## Decisions\n\n### Markdown file with front matter, not a sixth JSON contract\n\nA `research` JSON type with `sections.body.md` would reuse every existing code path but forces agents to escape Markdown inside JSON strings and makes diffs unreadable. A plain file matches how agents already produce research and how OpenSpec artifacts are authored; the price is a small front-matter parser and a second source root, both bounded.\n\n### Bounded hand-rolled front matter parser\n\n`src/lib/front-matter.ts` accepts a leading `---` block and parses `key: scalar`, `key: [a, b]`, and `key:` followed by `- item` lines; quotes are stripped, unknown keys ignored, duplicate keys last-wins, anything else yields a warning. Values are validated (`status` enum, `updated` `YYYY-MM-DD` prefix, tag strings bounded to 80 chars, at most 20 tags). No YAML library: the subset is tiny and determinism matters more than YAML fidelity.\n\n### Discovery mirrors the OpenSpec walker\n\n`src/lib/research-workspace.ts` lists `iris/research/*/index.md` sorted, refuses symlinks and path escapes, caps file bytes (256 KB) and item count, and returns `{ id, frontMatter, body, warnings }`. Warnings surface on the Research page and in `--json` output, not as failures.\n\n### Document template and table of contents\n\n`renderSafeMarkdown` gains an options object with `headingIds: true`; when set, heading tokens get deterministic slug ids (lowercased, non-alphanumerics collapsed to `-`, numeric suffix on collision) and the renderer returns the heading list alongside HTML. OpenSpec rendering keeps the default (no ids) so multiple documents on the Spec page cannot collide. The research page shows a right-rail TOC (sticky on wide screens, inline above the body at narrow widths) when two or more h2/h3 headings exist.\n\n### One projection, two source roots\n\n`refreshDashboard` builds `DashboardPage[]` from `pages/` (contracts) and `research/` (Markdown), tagging research entries `type: &#039;research&#039;`, `priority: &#039;not set&#039;`, `description` = first paragraph bounded to 1200 chars, `evidence` = `&quot;&lt;n&gt; headings · &lt;m&gt; words&quot;`. Work List/Table/Kanban, filter, and drawer need no change; `typeClass` gains `tp-research`. The Overview and Work summary strips count research.\n\n### Registry and lifecycle\n\n`PageRegistryEntry` gains optional `source: &#039;pages&#039; | &#039;research&#039;` (default `pages`); state stays version 2. `archive` resolves the source root from the entry (falling back to whichever directory exists), moves the whole folder under `iris/archive/&lt;id&gt;/`, and re-renders. `publish`/`export` resolve `page.html` across `pages/`, `research/`, and `archive/`. Research pages carry the same `data-iris-nav` shell so the existing stripper works.\n\n### Skill and catalog wording\n\n`templates/agents/iris-workspace.md` adds one numbered item (&quot;`iris research &lt;id&gt;` creates `iris/research/&lt;id&gt;/index.md`; write Markdown there, then `iris render --all`&quot;) and one sentence on front matter keys. `command-catalog.ts` adds `research` to the `content` group with status `implemented`.\n\n## Risks / Trade-offs\n\n- [Two source roots complicate id uniqueness] → `draft`/`research` refuse an id that exists in `pages/`, `research/`, or `archive/`; the render reports duplicates and keeps the first.\n- [Front matter subset surprises users] → Warnings name the unsupported line; docs list the exact grammar.\n- [Heading ids could leak into Spec rendering] → Opt-in option only; a test asserts OpenSpec output has no ids.\n- [Large Markdown bloats work-item data attributes] → Description bounded before projection, as for contracts.\n- [Shell dependency] → Implement after `dashboard-shell-redesign`; if it slips, render research pages with the shell function stubbed to the current header to stay unblocked, then switch.\n\n## Migration Plan\n\n1. Land parser and discovery with tests (no output change).\n2. Add `iris research`, the document template, and the Research section page + nav entry; extend the projection.\n3. Extend archive/publish/export and the registry; update the skill template and catalog.\n4. Regenerate dogfood, run the release gate, update docs.\n\nRollback: remove the command and pages; existing `iris/research/` Markdown is left untouched as plain files.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-research-markdown-pages/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-source-contract\">1. Source contract\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Implement the bounded front-matter parser (scalar, inline list, block list; validation of status, updated, tags) with warnings and focused tests including malformed and hostile input.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Implement research discovery under \u003ccode>iris/research/\u003c/code> (sorted, symlink-refusing, path-confined, size- and count-bounded) returning items plus path-specific warnings, with tests.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Add \u003ccode>iris research &lt;id&gt;\u003c/code> to the draft command: kebab-case validation, duplicate check across pages/research/archive, compact skeleton with front matter and the four headings; add the catalog entry and CLI test.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-rendering\">2. Rendering\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add opt-in heading ids and heading extraction to \u003ccode>renderSafeMarkdown\u003c/code> without changing OpenSpec output; test slug determinism and collision handling.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Implement the research document template inside the shared shell: header from front matter with unavailable labels, table of contents rule, safe body, Mermaid hosts, print and 360 px styles.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Generate \u003ccode>iris/research.html\u003c/code> with summary strip, filterable list, tag signals, and the populate-command empty state; add the Research entry to the navigation model.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Extend the dashboard projection with research records (type, status, bounded description, evidence) so Work views and drawer cover them; add \u003ccode>tp-research\u003c/code> styling and Overview/Work counts.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-lifecycle\">3. Lifecycle\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Record the source root in the page registry (additive field, version 2 retained) and resolve it in archive; move research folders under \u003ccode>iris/archive/&lt;id&gt;/\u003c/code> and keep links working.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Resolve research and archived research pages in publish and export; verify the standalone artifact omits the shell and stays self-contained.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Update the canonical \u003ccode>iris-workspace\u003c/code> skill template with the research workflow and regenerate the installed skill surfaces.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Add end-to-end tests: skeleton creation, render with and without front matter, malformed front matter warning, hostile body, TOC presence, Work projection, filter/drawer attributes, archive, publish, and html-check integrity.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Write a dogfood research page from the existing agent hand-off content under \u003ccode>iris/research/\u003c/code>, regenerate output, and inspect desktop and 360 px in both themes.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Update \u003ccode>README.md\u003c/code>, \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/tech.md\u003c/code>, \u003ccode>docs/status.md\u003c/code>, and \u003ccode>docs/design-system.md\u003c/code> for the research contract and page.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 Run \u003ccode>openspec validate research-markdown-pages --strict\u003c/code> and the full release gate; reconcile task evidence with the implementation.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Source contract\n\n- [x] 1.1 Implement the bounded front-matter parser (scalar, inline list, block list; validation of status, updated, tags) with warnings and focused tests including malformed and hostile input.\n- [x] 1.2 Implement research discovery under `iris/research/` (sorted, symlink-refusing, path-confined, size- and count-bounded) returning items plus path-specific warnings, with tests.\n- [x] 1.3 Add `iris research &lt;id&gt;` to the draft command: kebab-case validation, duplicate check across pages/research/archive, compact skeleton with front matter and the four headings; add the catalog entry and CLI test.\n\n## 2. Rendering\n\n- [x] 2.1 Add opt-in heading ids and heading extraction to `renderSafeMarkdown` without changing OpenSpec output; test slug determinism and collision handling.\n- [x] 2.2 Implement the research document template inside the shared shell: header from front matter with unavailable labels, table of contents rule, safe body, Mermaid hosts, print and 360 px styles.\n- [x] 2.3 Generate `iris/research.html` with summary strip, filterable list, tag signals, and the populate-command empty state; add the Research entry to the navigation model.\n- [x] 2.4 Extend the dashboard projection with research records (type, status, bounded description, evidence) so Work views and drawer cover them; add `tp-research` styling and Overview/Work counts.\n\n## 3. Lifecycle\n\n- [x] 3.1 Record the source root in the page registry (additive field, version 2 retained) and resolve it in archive; move research folders under `iris/archive/&lt;id&gt;/` and keep links working.\n- [x] 3.2 Resolve research and archived research pages in publish and export; verify the standalone artifact omits the shell and stays self-contained.\n- [x] 3.3 Update the canonical `iris-workspace` skill template with the research workflow and regenerate the installed skill surfaces.\n\n## 4. Verification and documentation\n\n- [x] 4.1 Add end-to-end tests: skeleton creation, render with and without front matter, malformed front matter warning, hostile body, TOC presence, Work projection, filter/drawer attributes, archive, publish, and html-check integrity.\n- [x] 4.2 Write a dogfood research page from the existing agent hand-off content under `iris/research/`, regenerate output, and inspect desktop and 360 px in both themes.\n- [x] 4.3 Update `README.md`, `docs/cmds.md`, `docs/tech.md`, `docs/status.md`, and `docs/design-system.md` for the research contract and page.\n- [x] 4.4 Run `openspec validate research-markdown-pages --strict` and the full release gate; reconcile task evidence with the implementation.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-research-markdown-pages/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-research-markdown-pages-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · research-markdown-pages\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-research-markdown-pages/specs/research-markdown-pages/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-research-markdown-pages-purpose\">Purpose\u003c/h2>\n\u003cp>Let agents record research and investigation output as plain Markdown files that Iris reads directly and renders into organized, navigable, offline HTML pages with their own section, so research stops living as loose Markdown.\u003c/p>\n\u003ch2 id=\"delta-research-markdown-pages-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-research-markdown-pages-requirement-markdown-research-source-contract\">Requirement: Markdown research source contract\u003c/h3>\n\u003cp>The system MUST treat \u003ccode>iris/research/&lt;id&gt;/index.md\u003c/code> as the editable source of a research page, where \u003ccode>&lt;id&gt;\u003c/code> is lowercase kebab-case, the file MAY begin with front matter providing \u003ccode>title\u003c/code>, \u003ccode>status\u003c/code> (\u003ccode>draft\u003c/code>, \u003ccode>active\u003c/code>, \u003ccode>done\u003c/code>, \u003ccode>archived\u003c/code>), \u003ccode>tags\u003c/code> (list of strings), \u003ccode>agent\u003c/code>, and \u003ccode>updated\u003c/code> (ISO date), and the remainder is the Markdown body. Missing front matter values MUST fall back to the first level-one heading or the id for \u003ccode>title\u003c/code>, \u003ccode>draft\u003c/code> for \u003ccode>status\u003c/code>, and an explicit unavailable value for the rest; malformed front matter MUST produce a path-specific warning and defaults, never a failed render of other pages. Reading MUST stay inside \u003ccode>iris/research/\u003c/code>, MUST refuse symlinks, MUST bound file size, and MUST NOT execute or fetch anything referenced by the file.\u003c/p>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-agent-creates-a-research-skeleton\">Scenario: agent creates a research skeleton\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user or agent runs \u003ccode>iris research &lt;id&gt;\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST create \u003ccode>iris/research/&lt;id&gt;/index.md\u003c/code> with front matter for the supported keys and Question, Findings, Evidence, and Next steps headings, and MUST refuse an id that already exists or is not kebab-case\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-front-matter-is-absent\">Scenario: front matter is absent\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a research file has no front matter\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST render with the first level-one heading or the id as its title, \u003ccode>draft\u003c/code> status, and unavailable tags, agent, and updated values\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-front-matter-is-malformed\">Scenario: front matter is malformed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> front matter cannot be parsed or contains an unsupported status value\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST render the page with defaults, report a warning naming the file, and continue rendering every other page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-file-exceeds-bounds-or-is-unsafe\">Scenario: file exceeds bounds or is unsafe\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>index.md\u003c/code> is larger than the configured bound, is a symlink, or escapes the research root\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the system MUST skip that item with a path-specific warning and MUST NOT read the target\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-research-markdown-pages-requirement-research-document-rendering\">Requirement: research document rendering\u003c/h3>\n\u003cp>Each research source MUST render to \u003ccode>iris/research/&lt;id&gt;/page.html\u003c/code> as a deterministic document page inside the shared navigation shell with a header showing title, type \u003ccode>research\u003c/code>, status, tags, agent, and updated, a generated table of contents when the body has two or more level-two or level-three headings, the body rendered by the safe Markdown renderer with embedded HTML, unsafe destinations, and active images disabled, Mermaid fences as source-first diagram hosts, and a print presentation.\u003c/p>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-body-contains-headings-and-a-mermaid-fence\">Scenario: body contains headings and a Mermaid fence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a research body has several level-two headings and an exact \u003ccode>mermaid\u003c/code> fence\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the rendered page MUST contain a table of contents linking to each heading and a diagram host that renders after \u003ccode>iris vendor\u003c/code> while keeping escaped source as fallback\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-body-contains-hostile-content\">Scenario: body contains hostile content\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the body contains raw HTML, script, remote image, or \u003ccode>javascript:\u003c/code> link Markdown\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the rendered page MUST show it as inert text or omit the active part and MUST NOT emit executable or network-loading markup\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-page-is-published-standalone\">Scenario: page is published standalone\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris publish &lt;id&gt;\u003c/code> or \u003ccode>iris export &lt;id&gt; --single\u003c/code> targets a research page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the artifact MUST be self-contained, omit the navigation shell, and keep the document readable\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-research-markdown-pages-requirement-research-section-page-and-navigation\">Requirement: research section page and navigation\u003c/h3>\n\u003cp>The system MUST generate \u003ccode>iris/research.html\u003c/code> listing every research item with a summary strip (total and per-status counts), a filterable list showing title, id, status, tags, agent, and updated with unavailable values labelled, and MUST add a Research entry to the navigation shell on every generated page.\u003c/p>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-repository-has-research-items\">Scenario: repository has research items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens the Research section\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST list every rendered research item with a link to its page and honest metadata, and the shell MUST mark Research as current\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-repository-has-no-research-items\">Scenario: repository has no research items\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Research section has no items\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST name \u003ccode>iris research &lt;id&gt;\u003c/code> and \u003ccode>iris render --all\u003c/code> as the commands that populate it\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-research-markdown-pages-requirement-research-records-in-the-work-browser-and-lifecyc\">Requirement: research records in the work browser and lifecycle\u003c/h3>\n\u003cp>Research items MUST appear in the Work browser projection as type \u003ccode>research\u003c/code> with status from front matter, priority unavailable, a bounded description from the first paragraph, and a full-page link, so List, Table, Kanban, filter, and the detail drawer cover them; \u003ccode>iris archive &lt;id&gt;\u003c/code> MUST move a research item into the archive while keeping its page reachable, and the Overview summary MUST count research items.\u003c/p>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-research-item-is-filtered-or-opened-from-work\">Scenario: research item is filtered or opened from Work\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user filters by a research tag or activates a research row\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the matching research record MUST be shown with type \u003ccode>research\u003c/code> and its drawer MUST link to the research page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-research-item-is-archived\">Scenario: research item is archived\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user runs \u003ccode>iris archive &lt;id&gt;\u003c/code> for a research item\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the source and page MUST move under \u003ccode>iris/archive/&lt;id&gt;/\u003c/code>, the registry MUST mark it archived, and the dashboard MUST keep a working link\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-research-markdown-pages-requirement-agent-guidance-for-research-pages\">Requirement: agent guidance for research pages\u003c/h3>\n\u003cp>The canonical \u003ccode>iris-workspace\u003c/code> skill MUST describe creating, writing, and rendering research pages as Markdown, naming the source path and the render command, so agents write Markdown rather than JSON for research responses.\u003c/p>\n\u003ch4 id=\"delta-research-markdown-pages-scenario-skill-is-installed\">Scenario: skill is installed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> \u003ccode>iris init\u003c/code> installs or refreshes the agent skill\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> every generated skill surface MUST include the research workflow alongside the existing content commands\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nLet agents record research and investigation output as plain Markdown files that Iris reads directly and renders into organized, navigable, offline HTML pages with their own section, so research stops living as loose Markdown.\n\n## ADDED Requirements\n\n### Requirement: Markdown research source contract\n\nThe system MUST treat `iris/research/&lt;id&gt;/index.md` as the editable source of a research page, where `&lt;id&gt;` is lowercase kebab-case, the file MAY begin with front matter providing `title`, `status` (`draft`, `active`, `done`, `archived`), `tags` (list of strings), `agent`, and `updated` (ISO date), and the remainder is the Markdown body. Missing front matter values MUST fall back to the first level-one heading or the id for `title`, `draft` for `status`, and an explicit unavailable value for the rest; malformed front matter MUST produce a path-specific warning and defaults, never a failed render of other pages. Reading MUST stay inside `iris/research/`, MUST refuse symlinks, MUST bound file size, and MUST NOT execute or fetch anything referenced by the file.\n\n#### Scenario: agent creates a research skeleton\n\n- **WHEN** a user or agent runs `iris research &lt;id&gt;`\n- **THEN** the system MUST create `iris/research/&lt;id&gt;/index.md` with front matter for the supported keys and Question, Findings, Evidence, and Next steps headings, and MUST refuse an id that already exists or is not kebab-case\n\n#### Scenario: front matter is absent\n\n- **WHEN** a research file has no front matter\n- **THEN** the page MUST render with the first level-one heading or the id as its title, `draft` status, and unavailable tags, agent, and updated values\n\n#### Scenario: front matter is malformed\n\n- **WHEN** front matter cannot be parsed or contains an unsupported status value\n- **THEN** the system MUST render the page with defaults, report a warning naming the file, and continue rendering every other page\n\n#### Scenario: file exceeds bounds or is unsafe\n\n- **WHEN** `index.md` is larger than the configured bound, is a symlink, or escapes the research root\n- **THEN** the system MUST skip that item with a path-specific warning and MUST NOT read the target\n\n### Requirement: research document rendering\n\nEach research source MUST render to `iris/research/&lt;id&gt;/page.html` as a deterministic document page inside the shared navigation shell with a header showing title, type `research`, status, tags, agent, and updated, a generated table of contents when the body has two or more level-two or level-three headings, the body rendered by the safe Markdown renderer with embedded HTML, unsafe destinations, and active images disabled, Mermaid fences as source-first diagram hosts, and a print presentation.\n\n#### Scenario: body contains headings and a Mermaid fence\n\n- **WHEN** a research body has several level-two headings and an exact `mermaid` fence\n- **THEN** the rendered page MUST contain a table of contents linking to each heading and a diagram host that renders after `iris vendor` while keeping escaped source as fallback\n\n#### Scenario: body contains hostile content\n\n- **WHEN** the body contains raw HTML, script, remote image, or `javascript:` link Markdown\n- **THEN** the rendered page MUST show it as inert text or omit the active part and MUST NOT emit executable or network-loading markup\n\n#### Scenario: page is published standalone\n\n- **WHEN** `iris publish &lt;id&gt;` or `iris export &lt;id&gt; --single` targets a research page\n- **THEN** the artifact MUST be self-contained, omit the navigation shell, and keep the document readable\n\n### Requirement: research section page and navigation\n\nThe system MUST generate `iris/research.html` listing every research item with a summary strip (total and per-status counts), a filterable list showing title, id, status, tags, agent, and updated with unavailable values labelled, and MUST add a Research entry to the navigation shell on every generated page.\n\n#### Scenario: repository has research items\n\n- **WHEN** a user opens the Research section\n- **THEN** it MUST list every rendered research item with a link to its page and honest metadata, and the shell MUST mark Research as current\n\n#### Scenario: repository has no research items\n\n- **WHEN** the Research section has no items\n- **THEN** it MUST name `iris research &lt;id&gt;` and `iris render --all` as the commands that populate it\n\n### Requirement: research records in the work browser and lifecycle\n\nResearch items MUST appear in the Work browser projection as type `research` with status from front matter, priority unavailable, a bounded description from the first paragraph, and a full-page link, so List, Table, Kanban, filter, and the detail drawer cover them; `iris archive &lt;id&gt;` MUST move a research item into the archive while keeping its page reachable, and the Overview summary MUST count research items.\n\n#### Scenario: research item is filtered or opened from Work\n\n- **WHEN** a user filters by a research tag or activates a research row\n- **THEN** the matching research record MUST be shown with type `research` and its drawer MUST link to the research page\n\n#### Scenario: research item is archived\n\n- **WHEN** a user runs `iris archive &lt;id&gt;` for a research item\n- **THEN** the source and page MUST move under `iris/archive/&lt;id&gt;/`, the registry MUST mark it archived, and the dashboard MUST keep a working link\n\n### Requirement: agent guidance for research pages\n\nThe canonical `iris-workspace` skill MUST describe creating, writing, and rendering research pages as Markdown, naming the source path and the render command, so agents write Markdown rather than JSON for research responses.\n\n#### Scenario: skill is installed\n\n- **WHEN** `iris init` installs or refreshes the agent skill\n- **THEN** every generated skill surface MUST include the research workflow alongside the existing content commands\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-markdown-file-with-front-matter-not-a-sixth-json-contract\">Markdown file with front matter, not a sixth JSON contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-bounded-hand-rolled-front-matter-parser\">Bounded hand-rolled front matter parser\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-discovery-mirrors-the-openspec-walker\">Discovery mirrors the OpenSpec walker\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-document-template-and-table-of-contents\">Document template and table of contents\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-one-projection-two-source-roots\">One projection, two source roots\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-registry-and-lifecycle\">Registry and lifecycle\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-skill-and-catalog-wording\">Skill and catalog wording\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-source-contract\">1. Source contract\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-rendering\">2. Rendering\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-lifecycle\">3. Lifecycle\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-research-markdown-pages-artifact\">Delta spec · research-markdown-pages\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-research-markdown-pages-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-research-markdown-pages-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-research-markdown-pages-requirement-markdown-research-source-contract\">Requirement: Markdown research source contract\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-research-markdown-pages-requirement-research-document-rendering\">Requirement: research document rendering\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-research-markdown-pages-requirement-research-section-page-and-navigation\">Requirement: research section page and navigation\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-research-markdown-pages-requirement-research-records-in-the-work-browser-and-lifecyc\">Requirement: research records in the work browser and lifecycle\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-research-markdown-pages-requirement-agent-guidance-for-research-pages\">Requirement: agent guidance for research pages\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-session-ingestion-and-reporting":{"kind":"change","name":"2026-08-21-session-ingestion-and-reporting","title":"2026-08-21-session-ingestion-and-reporting","path":"changes/archive/2026-08-21-session-ingestion-and-reporting","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-session-ingestion-and-reporting\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-session-ingestion-and-reporting\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">15\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-session-ingestion-and-reporting: 15 of 15 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">15/15 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-session-ingestion-and-reporting/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The project already models the page lifecycle around structured reports, but the workflow still depends on manual curation for session-derived work. Users are generating meaningful session artifacts locally yet the CLI does not fully convert that evidence into a reusable report contract, dashboard entry, or rendered artifact.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Add a session ingestion path that turns local session data into a consistent report contract.\u003c/li>\n\u003cli>Normalize session metadata, checkpoint summaries, and tool activity into a report-friendly structure.\u003c/li>\n\u003cli>Attach file and reference evidence so the generated page is traceable back to the original work.\u003c/li>\n\u003cli>Render the resulting report in the same dashboard-first flow used by other \u003ccode>iris\u003c/code> pages.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>session-ingestion-and-reporting: import session history into a structured page and render the output in the project dashboard.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>report: expand the report workflow to accept session-derived input in addition to manual page creation.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Session-to-report conversion becomes repeatable instead of ad hoc.\u003c/li>\n\u003cli>Users can preserve and review AI work without losing evidence from prompts, tools, and checkpoints.\u003c/li>\n\u003cli>Reports become more trustworthy because they are grounded in actual work artifacts and branch context.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe project already models the page lifecycle around structured reports, but the workflow still depends on manual curation for session-derived work. Users are generating meaningful session artifacts locally yet the CLI does not fully convert that evidence into a reusable report contract, dashboard entry, or rendered artifact.\n\n## What Changes\n\n- Add a session ingestion path that turns local session data into a consistent report contract.\n- Normalize session metadata, checkpoint summaries, and tool activity into a report-friendly structure.\n- Attach file and reference evidence so the generated page is traceable back to the original work.\n- Render the resulting report in the same dashboard-first flow used by other `iris` pages.\n\n## Capabilities\n\n### New Capabilities\n- session-ingestion-and-reporting: import session history into a structured page and render the output in the project dashboard.\n\n### Modified Capabilities\n- report: expand the report workflow to accept session-derived input in addition to manual page creation.\n\n## Impact\n\n- Session-to-report conversion becomes repeatable instead of ad hoc.\n- Users can preserve and review AI work without losing evidence from prompts, tools, and checkpoints.\n- Reports become more trustworthy because they are grounded in actual work artifacts and branch context.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-session-ingestion-and-reporting/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The repository already defines a report contract and exposes a \u003ccode>report --from-session\u003c/code> command in the CLI contract docs. The missing work is the implementation path: turning session evidence into a normalized report, grounding the output in session files or checkpoints, and keeping the output in the same local-first rendering pipeline as other \u003ccode>iris\u003c/code> pages.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Parse one or more session sources into a normal report record.\u003c/li>\n\u003cli>Extract branch names, work summaries, files touched, and references without requiring a PR to exist.\u003c/li>\n\u003cli>Keep the output deterministic and reviewable for later rendering.\u003c/li>\n\u003cli>Integrate the report into the existing page registry and dashboard flow.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Remote session syncing or SaaS ingestion.\u003c/li>\n\u003cli>Full AI summarization beyond local, rule-based evidence extraction.\u003c/li>\n\u003cli>Replacing the manual report path when a user prefers to author a page by hand.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Keep the ingestion pipeline local-first and file-based; support directories, dumped session JSON, and known session metadata tables.\u003c/li>\n\u003cli>Normalize the data into a canonical report schema before rendering so the rest of the engine can stay mostly unchanged.\u003c/li>\n\u003cli>Prefer factual extraction from session metadata over high-risk inference; the generated summary should be concise and evidence-backed.\u003c/li>\n\u003cli>Treat file paths, references, and checkpoints as first-class evidence rather than optional notes.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Noisy session data] → Mitigation: filter to known session tables and ignore irrelevant files while keeping the evidence path visible.\u003c/li>\n\u003cli>[Over-aggressive summarization] → Mitigation: use deterministic summaries based on session metadata and selective evidence, not freeform AI generation.\u003c/li>\n\u003cli>[Diverse session input formats] → Mitigation: support a small set of canonical adapters rather than a broad, unstable import matrix.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Map the session source format to a canonical internal report structure.\u003c/li>\n\u003cli>Add metadata extraction for branch, summary, files, and references.\u003c/li>\n\u003cli>Implement summary generation and validation rules for the resulting page contract.\u003c/li>\n\u003cli>Render and register the report in the standard page output flow.\u003c/li>\n\u003cli>Validate the CLI command with real session inputs and regression tests.\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"design-open-questions\">Open Questions\u003c/h2>\n\u003cp>None at this stage; the implementation should remain narrow and evidence-driven.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe repository already defines a report contract and exposes a `report --from-session` command in the CLI contract docs. The missing work is the implementation path: turning session evidence into a normalized report, grounding the output in session files or checkpoints, and keeping the output in the same local-first rendering pipeline as other `iris` pages.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Parse one or more session sources into a normal report record.\n- Extract branch names, work summaries, files touched, and references without requiring a PR to exist.\n- Keep the output deterministic and reviewable for later rendering.\n- Integrate the report into the existing page registry and dashboard flow.\n\n**Non-Goals:**\n- Remote session syncing or SaaS ingestion.\n- Full AI summarization beyond local, rule-based evidence extraction.\n- Replacing the manual report path when a user prefers to author a page by hand.\n\n## Decisions\n\n- Keep the ingestion pipeline local-first and file-based; support directories, dumped session JSON, and known session metadata tables.\n- Normalize the data into a canonical report schema before rendering so the rest of the engine can stay mostly unchanged.\n- Prefer factual extraction from session metadata over high-risk inference; the generated summary should be concise and evidence-backed.\n- Treat file paths, references, and checkpoints as first-class evidence rather than optional notes.\n\n## Risks / Trade-offs\n\n- [Noisy session data] → Mitigation: filter to known session tables and ignore irrelevant files while keeping the evidence path visible.\n- [Over-aggressive summarization] → Mitigation: use deterministic summaries based on session metadata and selective evidence, not freeform AI generation.\n- [Diverse session input formats] → Mitigation: support a small set of canonical adapters rather than a broad, unstable import matrix.\n\n## Migration Plan\n\n1. Map the session source format to a canonical internal report structure.\n2. Add metadata extraction for branch, summary, files, and references.\n3. Implement summary generation and validation rules for the resulting page contract.\n4. Render and register the report in the standard page output flow.\n5. Validate the CLI command with real session inputs and regression tests.\n\n## Open Questions\n\nNone at this stage; the implementation should remain narrow and evidence-driven.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-session-ingestion-and-reporting/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-session-source-parsing\">1. Session source parsing\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Define the supported input shapes for the session ingestion workflow.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Implement a source adapter for local session directories and exported session dumps.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Validate that the parser can discover session metadata, turns, checkpoints, and references without requiring a remote service.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-report-normalization\">2. Report normalization\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Map session evidence into a canonical report contract.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Preserve branch, repo, timestamps, files touched, and reference links as structured fields.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add a clean fallback for incomplete or partial data.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-summary-generation-and-validation\">3. Summary generation and validation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Build concise summary logic from session checkpoints and tool activity.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Surface workstream, status, and notable evidence in the report output.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Add validation rules to reject empty or malformed session sources.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-rendering-and-registration\">4. Rendering and registration\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Generate or update the page record in the local registry.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Render the session-derived report through the normal HTML pipeline.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Verify the resulting page is discoverable in the generated dashboard or index.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-5-cli-and-regression-coverage\">5. CLI and regression coverage\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.1 Wire the \u003ccode>report --from-session\u003c/code> command to the new adapter and output path.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.2 Add regression tests for supported session inputs and bad input handling.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 5.3 Document the expected usage and output contract for the session import flow.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Session source parsing\n\n- [x] 1.1 Define the supported input shapes for the session ingestion workflow.\n- [x] 1.2 Implement a source adapter for local session directories and exported session dumps.\n- [x] 1.3 Validate that the parser can discover session metadata, turns, checkpoints, and references without requiring a remote service.\n\n## 2. Report normalization\n\n- [x] 2.1 Map session evidence into a canonical report contract.\n- [x] 2.2 Preserve branch, repo, timestamps, files touched, and reference links as structured fields.\n- [x] 2.3 Add a clean fallback for incomplete or partial data.\n\n## 3. Summary generation and validation\n\n- [x] 3.1 Build concise summary logic from session checkpoints and tool activity.\n- [x] 3.2 Surface workstream, status, and notable evidence in the report output.\n- [x] 3.3 Add validation rules to reject empty or malformed session sources.\n\n## 4. Rendering and registration\n\n- [x] 4.1 Generate or update the page record in the local registry.\n- [x] 4.2 Render the session-derived report through the normal HTML pipeline.\n- [x] 4.3 Verify the resulting page is discoverable in the generated dashboard or index.\n\n## 5. CLI and regression coverage\n\n- [x] 5.1 Wire the `report --from-session` command to the new adapter and output path.\n- [x] 5.2 Add regression tests for supported session inputs and bad input handling.\n- [x] 5.3 Document the expected usage and output contract for the session import flow.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-session-ingestion-and-reporting/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-18\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-session-ingestion-and-reporting-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · session-ingestion-and-reporting\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-session-ingestion-and-reporting/specs/session-ingestion-and-reporting/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-session-ingestion-and-reporting-purpose\">Purpose\u003c/h2>\n\u003cp>Provide a reliable local-first path for converting session history into a visual, versioned report page. The system must preserve evidence from the user session, translate it into the project report schema, and render it in a way that remains useful for later review and sharing.\u003c/p>\n\u003ch2 id=\"delta-session-ingestion-and-reporting-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-session-ingestion-and-reporting-requirement-session-discovery-and-normalization\">Requirement: session discovery and normalization\u003c/h3>\n\u003cp>The system MUST support ingesting session data from a local directory or exported session bundle and MUST normalize the relevant files into a single report dataset.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-user-points-the-cli-at-a-session-directory\">Scenario: user points the CLI at a session directory\u003c/h4>\n\u003cp>WHEN a user runs the report workflow with a session source\nTHEN the CLI MUST discover session metadata, turns, checkpoints, references, and file activity and MUST ignore irrelevant files unless they are part of the session dataset.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-session-data-is-partial-or-noisy\">Scenario: session data is partial or noisy\u003c/h4>\n\u003cp>WHEN session data is incomplete or contains extra non-session artifacts\nTHEN the system MUST still build a usable report while preserving only the evidence that can be mapped to the report contract.\u003c/p>\n\u003ch3 id=\"delta-session-ingestion-and-reporting-requirement-report-content-derived-from-session-evidence\">Requirement: report content derived from session evidence\u003c/h3>\n\u003cp>The system MUST turn session metadata into a report summary that includes the workstream, branch, status, changed files, and notable references.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-session-shows-completed-work\">Scenario: session shows completed work\u003c/h4>\n\u003cp>WHEN a session contains concrete work, notes, or checkpoints\nTHEN the generated report MUST summarize the outcome in a concise, reviewable form and MUST keep the underlying evidence attached to the page.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-a-session-includes-pr-or-issue-references\">Scenario: a session includes PR or issue references\u003c/h4>\n\u003cp>WHEN evidence contains a linked PR, issue, or commit reference\nTHEN the report MUST surface the reference in a way that is searchable and visible in the generated contract.\u003c/p>\n\u003ch3 id=\"delta-session-ingestion-and-reporting-requirement-rendered-page-output\">Requirement: rendered page output\u003c/h3>\n\u003cp>The system MUST create or update a page record and render the resulting report so it appears in the local dashboard or page registry.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-report-is-generated-from-session-input\">Scenario: report is generated from session input\u003c/h4>\n\u003cp>WHEN the report pipeline succeeds\nTHEN the CLI MUST emit a rendered page artifact and the page SHOULD be discoverable through the same dashboard workflow as other iris pages.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-generation-fails-for-invalid-input\">Scenario: generation fails for invalid input\u003c/h4>\n\u003cp>WHEN the source input does not contain valid session evidence\nTHEN the system MUST stop with a clear validation error and MUST describe the missing or unsupported input format.\u003c/p>\n\u003ch3 id=\"delta-session-ingestion-and-reporting-requirement-local-first-offline-safe-operation\">Requirement: local-first, offline-safe operation\u003c/h3>\n\u003cp>The ingestion and reporting flow MUST require no external service call to generate the page and MUST operate purely from local files or a local session export.\u003c/p>\n\u003ch4 id=\"delta-session-ingestion-and-reporting-scenario-offline-usage\">Scenario: offline usage\u003c/h4>\n\u003cp>WHEN a user runs the tool without network access\nTHEN the session ingestion workflow MUST still create a report contract and rendered output from local data.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nProvide a reliable local-first path for converting session history into a visual, versioned report page. The system must preserve evidence from the user session, translate it into the project report schema, and render it in a way that remains useful for later review and sharing.\n\n## ADDED Requirements\n\n### Requirement: session discovery and normalization\nThe system MUST support ingesting session data from a local directory or exported session bundle and MUST normalize the relevant files into a single report dataset.\n\n#### Scenario: user points the CLI at a session directory\nWHEN a user runs the report workflow with a session source\nTHEN the CLI MUST discover session metadata, turns, checkpoints, references, and file activity and MUST ignore irrelevant files unless they are part of the session dataset.\n\n#### Scenario: session data is partial or noisy\nWHEN session data is incomplete or contains extra non-session artifacts\nTHEN the system MUST still build a usable report while preserving only the evidence that can be mapped to the report contract.\n\n### Requirement: report content derived from session evidence\nThe system MUST turn session metadata into a report summary that includes the workstream, branch, status, changed files, and notable references.\n\n#### Scenario: session shows completed work\nWHEN a session contains concrete work, notes, or checkpoints\nTHEN the generated report MUST summarize the outcome in a concise, reviewable form and MUST keep the underlying evidence attached to the page.\n\n#### Scenario: a session includes PR or issue references\nWHEN evidence contains a linked PR, issue, or commit reference\nTHEN the report MUST surface the reference in a way that is searchable and visible in the generated contract.\n\n### Requirement: rendered page output\nThe system MUST create or update a page record and render the resulting report so it appears in the local dashboard or page registry.\n\n#### Scenario: report is generated from session input\nWHEN the report pipeline succeeds\nTHEN the CLI MUST emit a rendered page artifact and the page SHOULD be discoverable through the same dashboard workflow as other iris pages.\n\n#### Scenario: generation fails for invalid input\nWHEN the source input does not contain valid session evidence\nTHEN the system MUST stop with a clear validation error and MUST describe the missing or unsupported input format.\n\n### Requirement: local-first, offline-safe operation\nThe ingestion and reporting flow MUST require no external service call to generate the page and MUST operate purely from local files or a local session export.\n\n#### Scenario: offline usage\nWHEN a user runs the tool without network access\nTHEN the session ingestion workflow MUST still create a report contract and rendered output from local data.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-open-questions\">Open Questions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-session-source-parsing\">1. Session source parsing\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-report-normalization\">2. Report normalization\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-summary-generation-and-validation\">3. Summary generation and validation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-rendering-and-registration\">4. Rendering and registration\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-5-cli-and-regression-coverage\">5. CLI and regression coverage\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-session-ingestion-and-reporting-artifact\">Delta spec · session-ingestion-and-reporting\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-session-ingestion-and-reporting-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-session-ingestion-and-reporting-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-session-ingestion-and-reporting-requirement-session-discovery-and-normalization\">Requirement: session discovery and normalization\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-session-ingestion-and-reporting-requirement-report-content-derived-from-session-evidence\">Requirement: report content derived from session evidence\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-session-ingestion-and-reporting-requirement-rendered-page-output\">Requirement: rendered page output\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-session-ingestion-and-reporting-requirement-local-first-offline-safe-operation\">Requirement: local-first, offline-safe operation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-spec-data-bundle":{"kind":"change","name":"2026-08-21-spec-data-bundle","title":"2026-08-21-spec-data-bundle","path":"changes/archive/2026-08-21-spec-data-bundle","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-spec-data-bundle\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">10\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">2\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-spec-data-bundle: 10 of 10 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">10/10 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>The Spec section generates one HTML file per record — 30 of the workspace's 41 pages — which grows linearly with the OpenSpec history and makes the generated tree dominated by archive material nobody opens. Browsers block \u003ccode>fetch\u003c/code> and \u003ccode>XMLHttpRequest\u003c/code> on \u003ccode>file://\u003c/code>, so a page cannot read a sibling Markdown file at view time; the one local mechanism that does work is a classic script assigning to a global, which is already how the vendored Mermaid runtime loads. Iris is an installed local CLI whose output is read in a real browser, so relying on that mechanism for the Spec section is acceptable where relying on it for shareable artifacts would not be.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Replace the per-record Spec detail pages with one generated classic-script data bundle at \u003ccode>iris/spec/data.js\u003c/code> holding each record's pre-rendered detail HTML, and render detail into \u003ccode>iris/spec.html\u003c/code> from a local hash route.\u003c/li>\n\u003cli>Keep the Spec index exactly as it is: summary strip, compact tables, project context, and warnings, with each row now addressing a record by hash instead of a separate file.\u003c/li>\n\u003cli>Escape the bundle so no record's content can terminate the script element or introduce executable markup, including artifacts that legitimately contain \u003ccode>&lt;/script&gt;\u003c/code> as escaped evidence.\u003c/li>\n\u003cli>State the JavaScript requirement honestly: without scripts the index remains complete and every row still names its on-disk source path, and the page says so rather than appearing broken.\u003c/li>\n\u003cli>\u003cstrong>BREAKING\u003c/strong> (generated output only): \u003ccode>iris/spec/**/page.html\u003c/code> files are no longer generated. Contracts, research pages, published artifacts, and every other section are unaffected.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>openspec-spec-browser\u003c/code>: the index-and-detail requirement changes from one file per record to one index plus a data bundle addressed by hash, and gains an explicit no-script behavior and a bundle-safety requirement.\u003c/li>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: the workspace hierarchy requirement changes because Spec is one page again rather than a page per record.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>\u003ccode>src/templates/pages/spec-detail.ts\u003c/code> becomes a bundle builder; \u003ccode>workspace.ts\u003c/code> emits \u003ccode>spec/data.js\u003c/code> instead of detail pages; \u003ccode>script.ts\u003c/code> gains the hash-routed spec browser; \u003ccode>render.ts\u003c/code> writes the bundle.\u003c/li>\n\u003cli>Generated HTML drops from 41 files to 11 plus one data file; the Spec index gains a detail region.\u003c/li>\n\u003cli>\u003ccode>html-check\u003c/code> sees fewer references; \u003ccode>openspec-browser\u003c/code> and \u003ccode>spec-detail-pages\u003c/code> tests move from per-record files to bundle records.\u003c/li>\n\u003cli>No new dependency, network access, schema change, or CLI command. Published and exported artifacts are untouched because the Spec section is never published.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nThe Spec section generates one HTML file per record — 30 of the workspace&#039;s 41 pages — which grows linearly with the OpenSpec history and makes the generated tree dominated by archive material nobody opens. Browsers block `fetch` and `XMLHttpRequest` on `file://`, so a page cannot read a sibling Markdown file at view time; the one local mechanism that does work is a classic script assigning to a global, which is already how the vendored Mermaid runtime loads. Iris is an installed local CLI whose output is read in a real browser, so relying on that mechanism for the Spec section is acceptable where relying on it for shareable artifacts would not be.\n\n## What Changes\n\n- Replace the per-record Spec detail pages with one generated classic-script data bundle at `iris/spec/data.js` holding each record&#039;s pre-rendered detail HTML, and render detail into `iris/spec.html` from a local hash route.\n- Keep the Spec index exactly as it is: summary strip, compact tables, project context, and warnings, with each row now addressing a record by hash instead of a separate file.\n- Escape the bundle so no record&#039;s content can terminate the script element or introduce executable markup, including artifacts that legitimately contain `&lt;/script&gt;` as escaped evidence.\n- State the JavaScript requirement honestly: without scripts the index remains complete and every row still names its on-disk source path, and the page says so rather than appearing broken.\n- **BREAKING** (generated output only): `iris/spec/**/page.html` files are no longer generated. Contracts, research pages, published artifacts, and every other section are unaffected.\n\n## Capabilities\n\n### Modified Capabilities\n\n- `openspec-spec-browser`: the index-and-detail requirement changes from one file per record to one index plus a data bundle addressed by hash, and gains an explicit no-script behavior and a bundle-safety requirement.\n- `aperture-design-system`: the workspace hierarchy requirement changes because Spec is one page again rather than a page per record.\n\n## Impact\n\n- `src/templates/pages/spec-detail.ts` becomes a bundle builder; `workspace.ts` emits `spec/data.js` instead of detail pages; `script.ts` gains the hash-routed spec browser; `render.ts` writes the bundle.\n- Generated HTML drops from 41 files to 11 plus one data file; the Spec index gains a detail region.\n- `html-check` sees fewer references; `openspec-browser` and `spec-detail-pages` tests move from per-record files to bundle records.\n- No new dependency, network access, schema change, or CLI command. Published and exported artifacts are untouched because the Spec section is never published.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See proposal.md. The measured constraint: from a \u003ccode>file://\u003c/code> page Chrome refuses \u003ccode>fetch\u003c/code> (\u003ccode>TypeError: Failed to fetch\u003c/code>) and \u003ccode>XMLHttpRequest\u003c/code> (\u003ccode>onerror\u003c/code>, \u003ccode>status 0\u003c/code>), because every local file is its own opaque origin. A classic \u003ccode>&lt;script src&gt;\u003c/code> still loads and executes in document order, which is how \u003ccode>mermaid.min.js\u003c/code> already works. That single exception is the only local-load mechanism available, so it is the one this change uses.\u003c/p>\n\u003cp>\u003ccode>spec-detail.ts\u003c/code> already produces the detail HTML per record; only its destination changes.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Collapse 30 generated files into one data file without losing any record content.\u003c/li>\n\u003cli>Keep the index complete and honest when scripts do not run.\u003c/li>\n\u003cli>Make it impossible for record content to escape the bundle.\u003c/li>\n\u003cli>Reuse the existing mermaid re-render hook rather than adding a second mechanism.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Bundling Work, Research, or contract pages. Those are published and exported, so they must stand alone without scripts.\u003c/li>\n\u003cli>Client-side Markdown parsing. Rendering stays at generation time; the bundle carries already-rendered HTML.\u003c/li>\n\u003cli>Lazy or chunked loading of the bundle.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-one-global-json-encoded-with-the-unsafe-sequences-escaped\">One global, JSON-encoded, with the unsafe sequences escaped\u003c/h3>\n\u003cp>The bundle is \u003ccode>globalThis.IRIS_SPEC = {...};\u003c/code> where the value is \u003ccode>JSON.stringify\u003c/code> output with \u003ccode>&lt;\u003c/code>, \u003ccode>U+2028\u003c/code>, and \u003ccode>U+2029\u003c/code> escaped as \u003ccode>&lt;\u003c/code>, \u003ccode>U+2028\u003c/code>, \u003ccode>U+2029\u003c/code>. Escaping every \u003ccode>&lt;\u003c/code> is the blunt instrument on purpose: it makes \u003ccode>&lt;/script&gt;\u003c/code>, \u003ccode>&lt;!--\u003c/code>, and \u003ccode>&lt;script\u003c/code> alike inexpressible in the source text while decoding back to the original characters at runtime, so a record that legitimately shows \u003ccode>&amp;lt;script&amp;gt;\u003c/code> as escaped evidence still displays correctly. The line separators are escaped because they are valid JSON but invalid JavaScript string literals.\u003c/p>\n\u003cp>Alternative: a JSON file loaded by \u003ccode>fetch\u003c/code>. Rejected — measured as blocked on \u003ccode>file://\u003c/code>, which is the whole reason this design exists.\u003c/p>\n\u003ch3 id=\"design-records-are-keyed-by-kind-and-name\">Records are keyed by kind and name\u003c/h3>\n\u003cp>Keys are \u003ccode>capability:&lt;path&gt;\u003c/code>, \u003ccode>change:&lt;name&gt;\u003c/code>, and \u003ccode>legacy:&lt;slug&gt;\u003c/code>, matching the three namespaces the previous per-file layout used. The hash route is \u003ccode>#/&lt;kind&gt;/&lt;name&gt;\u003c/code>, so an unknown or malformed hash simply matches no record and leaves the index displayed.\u003c/p>\n\u003ch3 id=\"design-the-index-owns-both-states\">The index owns both states\u003c/h3>\n\u003cp>\u003ccode>spec.html\u003c/code> renders the index plus an empty detail region carrying a \u003ccode>&lt;noscript&gt;\u003c/code> explanation. The script shows one and hides the other; with no script the index stands alone and every row still prints its on-disk path, so the page degrades to a directory rather than to a blank frame. Injection uses \u003ccode>innerHTML\u003c/code> with content generated and escaped at build time, then dispatches the existing \u003ccode>iris:visibilitychange\u003c/code> event so Mermaid figures inside a newly shown record render through the current code path.\u003c/p>\n\u003ch3 id=\"design-detail-html-keeps-its-own-anchors\">Detail HTML keeps its own anchors\u003c/h3>\n\u003cp>Record HTML retains the per-artifact id prefixes introduced by the previous change, so requirement anchors keep working inside the injected region and the table of contents resolves within the page.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Opening a record requires scripts] → Accepted for this section only, and stated on the page. The index — the part that answers &quot;what exists&quot; — needs no script.\u003c/li>\n\u003cli>[One large data file replaces many small ones] → It is a single deterministic artifact keyed by record; agents can read the index for structure and grep the bundle for one record.\u003c/li>\n\u003cli>[\u003ccode>innerHTML\u003c/code> injection] → Content is generated by Iris from already-escaped parser output; no untrusted string reaches the DOM unescaped, and inserted scripts do not execute by specification.\u003c/li>\n\u003cli>[Bundle drift from the index] → Both are produced from one snapshot in one pass, and a test asserts every index row resolves to a bundle key.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add the bundle builder and its escaping with tests, including a record containing \u003ccode>&lt;/script&gt;\u003c/code>.\u003c/li>\n\u003cli>Emit \u003ccode>spec/data.js\u003c/code>, add the detail region and hash routing, and stop generating per-record pages.\u003c/li>\n\u003cli>Move tests from per-record files to bundle records; regenerate output and run the release gate.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback: revert; stale \u003ccode>iris/spec/**/page.html\u003c/code> files are inert and the next render stops linking them.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee proposal.md. The measured constraint: from a `file://` page Chrome refuses `fetch` (`TypeError: Failed to fetch`) and `XMLHttpRequest` (`onerror`, `status 0`), because every local file is its own opaque origin. A classic `&lt;script src&gt;` still loads and executes in document order, which is how `mermaid.min.js` already works. That single exception is the only local-load mechanism available, so it is the one this change uses.\n\n`spec-detail.ts` already produces the detail HTML per record; only its destination changes.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Collapse 30 generated files into one data file without losing any record content.\n- Keep the index complete and honest when scripts do not run.\n- Make it impossible for record content to escape the bundle.\n- Reuse the existing mermaid re-render hook rather than adding a second mechanism.\n\n**Non-Goals:**\n\n- Bundling Work, Research, or contract pages. Those are published and exported, so they must stand alone without scripts.\n- Client-side Markdown parsing. Rendering stays at generation time; the bundle carries already-rendered HTML.\n- Lazy or chunked loading of the bundle.\n\n## Decisions\n\n### One global, JSON-encoded, with the unsafe sequences escaped\n\nThe bundle is `globalThis.IRIS_SPEC = {...};` where the value is `JSON.stringify` output with `&lt;`, `U+2028`, and `U+2029` escaped as `&lt;`, `U+2028`, `U+2029`. Escaping every `&lt;` is the blunt instrument on purpose: it makes `&lt;/script&gt;`, `&lt;!--`, and `&lt;script` alike inexpressible in the source text while decoding back to the original characters at runtime, so a record that legitimately shows `&amp;lt;script&amp;gt;` as escaped evidence still displays correctly. The line separators are escaped because they are valid JSON but invalid JavaScript string literals.\n\nAlternative: a JSON file loaded by `fetch`. Rejected — measured as blocked on `file://`, which is the whole reason this design exists.\n\n### Records are keyed by kind and name\n\nKeys are `capability:&lt;path&gt;`, `change:&lt;name&gt;`, and `legacy:&lt;slug&gt;`, matching the three namespaces the previous per-file layout used. The hash route is `#/&lt;kind&gt;/&lt;name&gt;`, so an unknown or malformed hash simply matches no record and leaves the index displayed.\n\n### The index owns both states\n\n`spec.html` renders the index plus an empty detail region carrying a `&lt;noscript&gt;` explanation. The script shows one and hides the other; with no script the index stands alone and every row still prints its on-disk path, so the page degrades to a directory rather than to a blank frame. Injection uses `innerHTML` with content generated and escaped at build time, then dispatches the existing `iris:visibilitychange` event so Mermaid figures inside a newly shown record render through the current code path.\n\n### Detail HTML keeps its own anchors\n\nRecord HTML retains the per-artifact id prefixes introduced by the previous change, so requirement anchors keep working inside the injected region and the table of contents resolves within the page.\n\n## Risks / Trade-offs\n\n- [Opening a record requires scripts] → Accepted for this section only, and stated on the page. The index — the part that answers &quot;what exists&quot; — needs no script.\n- [One large data file replaces many small ones] → It is a single deterministic artifact keyed by record; agents can read the index for structure and grep the bundle for one record.\n- [`innerHTML` injection] → Content is generated by Iris from already-escaped parser output; no untrusted string reaches the DOM unescaped, and inserted scripts do not execute by specification.\n- [Bundle drift from the index] → Both are produced from one snapshot in one pass, and a test asserts every index row resolves to a bundle key.\n\n## Migration Plan\n\n1. Add the bundle builder and its escaping with tests, including a record containing `&lt;/script&gt;`.\n2. Emit `spec/data.js`, add the detail region and hash routing, and stop generating per-record pages.\n3. Move tests from per-record files to bundle records; regenerate output and run the release gate.\n\nRollback: revert; stale `iris/spec/**/page.html` files are inert and the next render stops linking them.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-data-bundle\">1. Data bundle\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Add a bundle builder that keys each canonical spec, change, and legacy archive by kind and name and carries its pre-rendered detail HTML, title, and source path.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Encode the bundle so \u003ccode>&lt;\u003c/code>, \u003ccode>U+2028\u003c/code>, and \u003ccode>U+2029\u003c/code> cannot terminate the script element or an HTML comment, and add tests using a record whose content contains \u003ccode>&lt;/script&gt;\u003c/code> and an HTML comment close.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Emit the bundle at \u003ccode>iris/spec/data.js\u003c/code> from the render pipeline and stop generating per-record detail pages.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-spec-page-routing\">2. Spec page routing\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Render the index plus an empty detail region with a \u003ccode>noscript\u003c/code> explanation, and address each index row by its record hash.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Add hash-routed record display to the classic script: show the record, mark the breadcrumb, restore focus, return to the index on an empty or unknown hash, and re-fire the visibility event so Mermaid renders inside a shown record.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Keep the index complete without scripts, with every row still naming its on-disk source path.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-verification-and-documentation\">3. Verification and documentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Move per-record file assertions to bundle records; add tests for index-row-to-bundle-key integrity, unknown-hash behavior, missing artifacts, and nested capability paths.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Regenerate the checked-in output, confirm the generated HTML count drops to the section, project, and content pages, and run \u003ccode>pnpm html-check\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Update \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/design-system.md\u003c/code>, \u003ccode>docs/tech.md\u003c/code>, and \u003ccode>docs/status.md\u003c/code> for the bundle, recording the measured \u003ccode>file://\u003c/code> constraint that forces it.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.4 Run \u003ccode>openspec validate spec-data-bundle --strict\u003c/code> and the full release gate.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Data bundle\n\n- [x] 1.1 Add a bundle builder that keys each canonical spec, change, and legacy archive by kind and name and carries its pre-rendered detail HTML, title, and source path.\n- [x] 1.2 Encode the bundle so `&lt;`, `U+2028`, and `U+2029` cannot terminate the script element or an HTML comment, and add tests using a record whose content contains `&lt;/script&gt;` and an HTML comment close.\n- [x] 1.3 Emit the bundle at `iris/spec/data.js` from the render pipeline and stop generating per-record detail pages.\n\n## 2. Spec page routing\n\n- [x] 2.1 Render the index plus an empty detail region with a `noscript` explanation, and address each index row by its record hash.\n- [x] 2.2 Add hash-routed record display to the classic script: show the record, mark the breadcrumb, restore focus, return to the index on an empty or unknown hash, and re-fire the visibility event so Mermaid renders inside a shown record.\n- [x] 2.3 Keep the index complete without scripts, with every row still naming its on-disk source path.\n\n## 3. Verification and documentation\n\n- [x] 3.1 Move per-record file assertions to bundle records; add tests for index-row-to-bundle-key integrity, unknown-hash behavior, missing artifacts, and nested capability paths.\n- [x] 3.2 Regenerate the checked-in output, confirm the generated HTML count drops to the section, project, and content pages, and run `pnpm html-check`.\n- [x] 3.3 Update `docs/cmds.md`, `docs/design-system.md`, `docs/tech.md`, and `docs/status.md` for the bundle, recording the measured `file://` constraint that forces it.\n- [x] 3.4 Run `openspec validate spec-data-bundle --strict` and the full release gate.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated workspace MUST consist of an Overview page at \u003ccode>iris/index.html\u003c/code>, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page presenting an index of canonical specs, active changes, and the archive with each record addressable in place, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Overview from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-generated-work-pages\">Scenario: repository has generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Work page with one or more page contracts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-spec-detail-page-is-opened\">Scenario: a spec detail page is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens a canonical spec, change, or legacy record from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST keep the navigation shell and the Spec section current, present that record's content, and provide a way back to the index\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: newcomer-first dashboard hierarchy\n\nThe generated workspace MUST consist of an Overview page at `iris/index.html`, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec page presenting an index of canonical specs, active changes, and the archive with each record addressable in place, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\n\n#### Scenario: repository has no generated work pages\n\n- **WHEN** a newcomer opens the Overview from `file://`\n- **THEN** it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n#### Scenario: repository has generated work pages\n\n- **WHEN** a newcomer opens the Work page with one or more page contracts\n- **THEN** the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\n\n#### Scenario: user opens the Spec view\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\n\n#### Scenario: state is communicated in the Spec view\n\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n#### Scenario: a spec detail page is opened\n\n- **WHEN** a user opens a canonical spec, change, or legacy record from the index\n- **THEN** the page MUST keep the navigation shell and the Spec section current, present that record&#039;s content, and provide a way back to the index\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-openspec-spec-browser-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · openspec-spec-browser\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-data-bundle/specs/openspec-spec-browser/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-safe-generated-data-bundle\">Requirement: safe generated data bundle\u003c/h3>\n\u003cp>The Spec section MUST carry its record detail in one generated classic-script data file that assigns a single global, and that file MUST be encoded so no record content can terminate the script element, close an HTML comment, or introduce executable or network-loading markup.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-a-record-contains-a-script-like-sequence\">Scenario: a record contains a script-like sequence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an artifact legitimately contains the characters that would close a script element or an HTML comment\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the generated bundle MUST encode them so the browser still parses one complete script and the characters appear as inert text when the record is displayed\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-the-bundle-is-loaded\">Scenario: the bundle is loaded\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a generated Spec page loads from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the bundle MUST load as a classic script without a network request and MUST expose every listed record\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-spec-index-and-detail-pages\">Requirement: spec index and detail pages\u003c/h3>\n\u003cp>The generated Spec section MUST provide one index page listing every canonical spec, active change, and archived change with its real counts, health, and task progress, and MUST make each record's artifact bodies available from that page through a local hash address backed by the generated data bundle. The index listing MUST remain complete and MUST name each record's on-disk source path whether or not scripts run.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-opens-the-spec-index\">Scenario: user opens the Spec index\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index MUST show overview counts and compact canonical, active-change, and archive listings, each row naming its source path and addressing that record\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-opens-a-canonical-spec-detail-page\">Scenario: user opens a canonical spec detail page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows a canonical spec row from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST show the capability name, source path, requirement and scenario counts, health, the rendered specification, and its exact escaped source without a full page load\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-opens-a-change-detail-page\">Scenario: user opens a change detail page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows a change row from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST show the change lifecycle, completeness, task progress, and every available artifact including delta specs, each with its exact escaped source\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-a-record-has-no-readable-artifacts\">Scenario: a record has no readable artifacts\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a listed change or capability has missing or unreadable artifacts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> its detail view MUST state which artifacts are missing and MUST render the readable ones\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-scripts-are-unavailable\">Scenario: scripts are unavailable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Spec index is opened without executing JavaScript\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index listings, counts, source paths, and warnings MUST remain readable and the page MUST state that opening a record requires scripts rather than presenting an empty region\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-an-unknown-record-is-addressed\">Scenario: an unknown record is addressed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the page is opened with a hash that names no record in the bundle\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index MUST remain displayed unchanged rather than showing an empty or partial record\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-deep-linkable-specification-headings\">Requirement: deep-linkable specification headings\u003c/h3>\n\u003cp>A rendered record MUST give every heading in a rendered document a stable identifier derived from its text, MUST provide a table of contents for a document with two or more section headings, and MUST keep identifiers unique when one record renders several documents.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-a-specification-with-several-requirements-is-render\">Scenario: a specification with several requirements is rendered\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a canonical spec containing multiple requirements is displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each requirement heading MUST carry an identifier derived from its text and the table of contents MUST link to it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-one-page-renders-several-documents\">Scenario: one page renders several documents\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a change record on one page renders a proposal, a design, and a delta spec that share a heading name\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each rendered heading MUST receive a distinct identifier and each table-of-contents entry MUST resolve to its own document's heading\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## ADDED Requirements\n\n### Requirement: safe generated data bundle\n\nThe Spec section MUST carry its record detail in one generated classic-script data file that assigns a single global, and that file MUST be encoded so no record content can terminate the script element, close an HTML comment, or introduce executable or network-loading markup.\n\n#### Scenario: a record contains a script-like sequence\n\n- **WHEN** an artifact legitimately contains the characters that would close a script element or an HTML comment\n- **THEN** the generated bundle MUST encode them so the browser still parses one complete script and the characters appear as inert text when the record is displayed\n\n#### Scenario: the bundle is loaded\n\n- **WHEN** a generated Spec page loads from `file://`\n- **THEN** the bundle MUST load as a classic script without a network request and MUST expose every listed record\n\n## MODIFIED Requirements\n\n### Requirement: spec index and detail pages\n\nThe generated Spec section MUST provide one index page listing every canonical spec, active change, and archived change with its real counts, health, and task progress, and MUST make each record&#039;s artifact bodies available from that page through a local hash address backed by the generated data bundle. The index listing MUST remain complete and MUST name each record&#039;s on-disk source path whether or not scripts run.\n\n#### Scenario: user opens the Spec index\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the index MUST show overview counts and compact canonical, active-change, and archive listings, each row naming its source path and addressing that record\n\n#### Scenario: user opens a canonical spec detail page\n\n- **WHEN** a user follows a canonical spec row from the index\n- **THEN** the page MUST show the capability name, source path, requirement and scenario counts, health, the rendered specification, and its exact escaped source without a full page load\n\n#### Scenario: user opens a change detail page\n\n- **WHEN** a user follows a change row from the index\n- **THEN** the page MUST show the change lifecycle, completeness, task progress, and every available artifact including delta specs, each with its exact escaped source\n\n#### Scenario: a record has no readable artifacts\n\n- **WHEN** a listed change or capability has missing or unreadable artifacts\n- **THEN** its detail view MUST state which artifacts are missing and MUST render the readable ones\n\n#### Scenario: scripts are unavailable\n\n- **WHEN** the Spec index is opened without executing JavaScript\n- **THEN** the index listings, counts, source paths, and warnings MUST remain readable and the page MUST state that opening a record requires scripts rather than presenting an empty region\n\n#### Scenario: an unknown record is addressed\n\n- **WHEN** the page is opened with a hash that names no record in the bundle\n- **THEN** the index MUST remain displayed unchanged rather than showing an empty or partial record\n\n### Requirement: deep-linkable specification headings\n\nA rendered record MUST give every heading in a rendered document a stable identifier derived from its text, MUST provide a table of contents for a document with two or more section headings, and MUST keep identifiers unique when one record renders several documents.\n\n#### Scenario: a specification with several requirements is rendered\n\n- **WHEN** a canonical spec containing multiple requirements is displayed\n- **THEN** each requirement heading MUST carry an identifier derived from its text and the table of contents MUST link to it\n\n#### Scenario: one page renders several documents\n\n- **WHEN** a change record on one page renders a proposal, a design, and a delta spec that share a heading name\n- **THEN** each rendered heading MUST receive a distinct identifier and each table-of-contents entry MUST resolve to its own document&#039;s heading\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-one-global-json-encoded-with-the-unsafe-sequences-escaped\">One global, JSON-encoded, with the unsafe sequences escaped\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-records-are-keyed-by-kind-and-name\">Records are keyed by kind and name\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-the-index-owns-both-states\">The index owns both states\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-detail-html-keeps-its-own-anchors\">Detail HTML keeps its own anchors\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-data-bundle\">1. Data bundle\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-spec-page-routing\">2. Spec page routing\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-verification-and-documentation\">3. Verification and documentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-artifact\">Delta spec · openspec-spec-browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-safe-generated-data-bundle\">Requirement: safe generated data bundle\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-spec-index-and-detail-pages\">Requirement: spec index and detail pages\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-deep-linkable-specification-headings\">Requirement: deep-linkable specification headings\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-spec-detail-pages":{"kind":"change","name":"2026-08-21-spec-detail-pages","title":"2026-08-21-spec-detail-pages","path":"changes/archive/2026-08-21-spec-detail-pages","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-spec-detail-pages\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">13\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">2\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-spec-detail-pages: 13 of 13 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">13/13 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>\u003ccode>iris/spec.html\u003c/code> is 640 KB — 45 times the next-largest generated page — because it renders every canonical spec and every change artifact inline, each one twice: as semantic Markdown and again as its escaped exact source. Escaped source alone is 40% of the page. The whole page carries four \u003ccode>id\u003c/code> attributes, so a capability with 8 requirements and 23 scenarios cannot be linked to at all, while a research page of a fraction the size gets generated heading ids and a table of contents. The previous change relocated the Spec view without restructuring it, so the section that holds most of the workspace's bytes is the one that never had an information-design pass.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Turn \u003ccode>iris/spec.html\u003c/code> into a light index: the existing summary strip plus compact tables for canonical specs, active changes, and the archive, each row carrying its real counts, health, and task progress and linking to a detail page.\u003c/li>\n\u003cli>Generate one detail page per canonical spec at \u003ccode>iris/spec/capabilities/&lt;capability-path&gt;/page.html\u003c/code> with a requirement-level table of contents, deep-linkable requirement and scenario headings, and the exact source retained in a disclosure.\u003c/li>\n\u003cli>Generate one detail page per change at \u003ccode>iris/spec/changes/&lt;change-name&gt;/page.html\u003c/code> covering manifest, proposal, design, tasks with progress, and each delta spec, with per-artifact heading namespaces so ids cannot collide.\u003c/li>\n\u003cli>Give the safe Markdown renderer an optional heading-id prefix so several documents can share one page without colliding anchors.\u003c/li>\n\u003cli>Keep every existing guarantee: bounded parsing, escaped exact source, inert hostile content, literal YAML, path-specific warnings, offline \u003ccode>file://\u003c/code> rendering, and no claim of OpenSpec semantic validation.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>\u003ccode>openspec-spec-browser\u003c/code>: the safe Markdown presentation requirement gains per-document heading identity and detail-page navigation obligations; a new requirement covers the index-plus-detail page structure.\u003c/li>\n\u003cli>\u003ccode>aperture-design-system\u003c/code>: the workspace hierarchy requirement changes because Spec becomes an index over generated detail pages rather than one page holding every artifact body.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>New \u003ccode>src/templates/pages/spec-detail.ts\u003c/code>; \u003ccode>pages/spec.ts\u003c/code> becomes the index; \u003ccode>workspace.ts\u003c/code> and \u003ccode>render.ts\u003c/code> emit the detail pages; \u003ccode>markdown.ts\u003c/code> gains the id-prefix option.\u003c/li>\n\u003cli>\u003ccode>html-check\u003c/code> covers many more generated references; \u003ccode>openspec-browser\u003c/code> tests move their artifact-body assertions to the detail pages.\u003c/li>\n\u003cli>Expected result: \u003ccode>spec.html\u003c/code> drops from ~640 KB to roughly the size of the other section pages, with the artifact bodies distributed across detail pages that are only read when opened.\u003c/li>\n\u003cli>No new dependency, network access, schema change, or CLI command.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\n`iris/spec.html` is 640 KB — 45 times the next-largest generated page — because it renders every canonical spec and every change artifact inline, each one twice: as semantic Markdown and again as its escaped exact source. Escaped source alone is 40% of the page. The whole page carries four `id` attributes, so a capability with 8 requirements and 23 scenarios cannot be linked to at all, while a research page of a fraction the size gets generated heading ids and a table of contents. The previous change relocated the Spec view without restructuring it, so the section that holds most of the workspace&#039;s bytes is the one that never had an information-design pass.\n\n## What Changes\n\n- Turn `iris/spec.html` into a light index: the existing summary strip plus compact tables for canonical specs, active changes, and the archive, each row carrying its real counts, health, and task progress and linking to a detail page.\n- Generate one detail page per canonical spec at `iris/spec/capabilities/&lt;capability-path&gt;/page.html` with a requirement-level table of contents, deep-linkable requirement and scenario headings, and the exact source retained in a disclosure.\n- Generate one detail page per change at `iris/spec/changes/&lt;change-name&gt;/page.html` covering manifest, proposal, design, tasks with progress, and each delta spec, with per-artifact heading namespaces so ids cannot collide.\n- Give the safe Markdown renderer an optional heading-id prefix so several documents can share one page without colliding anchors.\n- Keep every existing guarantee: bounded parsing, escaped exact source, inert hostile content, literal YAML, path-specific warnings, offline `file://` rendering, and no claim of OpenSpec semantic validation.\n\n## Capabilities\n\n### Modified Capabilities\n\n- `openspec-spec-browser`: the safe Markdown presentation requirement gains per-document heading identity and detail-page navigation obligations; a new requirement covers the index-plus-detail page structure.\n- `aperture-design-system`: the workspace hierarchy requirement changes because Spec becomes an index over generated detail pages rather than one page holding every artifact body.\n\n## Impact\n\n- New `src/templates/pages/spec-detail.ts`; `pages/spec.ts` becomes the index; `workspace.ts` and `render.ts` emit the detail pages; `markdown.ts` gains the id-prefix option.\n- `html-check` covers many more generated references; `openspec-browser` tests move their artifact-body assertions to the detail pages.\n- Expected result: `spec.html` drops from ~640 KB to roughly the size of the other section pages, with the artifact bodies distributed across detail pages that are only read when opened.\n- No new dependency, network access, schema change, or CLI command.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>See proposal.md for the measurements. \u003ccode>src/templates/pages/spec.ts\u003c/code> renders every capability and change as a \u003ccode>spec-card\u003c/code> in a grid, and \u003ccode>sourceDetails()\u003c/code> emits both the rendered Markdown and an escaped \u003ccode>&lt;pre&gt;\u003c/code> of the same document inside a \u003ccode>&lt;details&gt;\u003c/code>. Nothing is lazy: a collapsed \u003ccode>&lt;details&gt;\u003c/code> still ships its whole body. \u003ccode>renderDocument()\u003c/code> already exists for research pages and produces heading ids plus a heading list, but it keys uniqueness off a per-call \u003ccode>Set\u003c/code>, so two documents rendered onto one page can collide.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Make the Spec index cheap to open and cheap for an agent to read.\u003c/li>\n\u003cli>Give every requirement a stable anchor so a reviewer can link to one.\u003c/li>\n\u003cli>Keep exact-source fidelity, bounded parsing, and inert hostile content exactly as they are.\u003c/li>\n\u003cli>Reuse the research document template's shape rather than inventing a second one.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Search or filtering across specs, and cross-linking requirements to the code that implements them.\u003c/li>\n\u003cli>Rendering archived changes differently from active ones beyond their listing.\u003c/li>\n\u003cli>Changing the parser, the snapshot format, or \u003ccode>iris/spec.json\u003c/code>.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003ch3 id=\"design-two-detail-namespaces-under-iris-spec\">Two detail namespaces under \u003ccode>iris/spec/\u003c/code>\u003c/h3>\n\u003cp>Capabilities render to \u003ccode>iris/spec/capabilities/&lt;capability-path&gt;/page.html\u003c/code> and changes to \u003ccode>iris/spec/changes/&lt;change-name&gt;/page.html\u003c/code>. Separate namespaces mean a capability named \u003ccode>changes\u003c/code> cannot collide with the change namespace, and nested capability paths (\u003ccode>platform/identity/access\u003c/code>) keep working because the path becomes directories. Page depth is derived from the path segment count, so \u003ccode>renderShell\u003c/code>'s existing \u003ccode>assetPrefix(depth)\u003c/code> resolves assets and navigation at any nesting level without special cases.\u003c/p>\n\u003cp>Alternative: one flat directory with slugified names. Rejected because slugging a nested capability path throws away the structure the parser deliberately preserves.\u003c/p>\n\u003ch3 id=\"design-heading-ids-get-an-optional-prefix\">Heading ids get an optional prefix\u003c/h3>\n\u003cp>\u003ccode>renderDocument(value, { idPrefix })\u003c/code> prefixes every generated id and returns headings carrying the prefixed id. A change page renders proposal, design, tasks, and each delta spec with distinct prefixes, so two artifacts that both contain \u003ccode>## Why\u003c/code> produce \u003ccode>proposal-why\u003c/code> and \u003ccode>design-why\u003c/code>. Without a prefix the behavior is unchanged, so research pages and the existing tests are unaffected.\u003c/p>\n\u003cp>Alternative: render each artifact into a separate document fragment and rewrite ids afterwards. Rejected as string surgery over generated HTML when the renderer can simply be told the namespace.\u003c/p>\n\u003ch3 id=\"design-the-index-becomes-tables-not-cards\">The index becomes tables, not cards\u003c/h3>\n\u003cp>Canonical specs, active changes, and archives are uniform records, so each becomes a table row: capability or change name, source path, counts, health, and for changes a task progress bar. Tables scroll inside their own container and drop the least important columns first at narrow widths, matching the Work table's existing behavior. The index keeps the summary strip, project-context disclosures (both small), and the warnings list, because those are page-level facts rather than per-record bodies.\u003c/p>\n\u003ch3 id=\"design-exact-source-stays-but-only-on-detail-pages\">Exact source stays, but only on detail pages\u003c/h3>\n\u003cp>Every artifact keeps its escaped exact source in a disclosure on its detail page. The index carries none. This preserves the source-fidelity contract while removing the duplicate copy from the page that everyone opens first.\u003c/p>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Many more generated files] → \u003ccode>html-check\u003c/code> already walks the tree and every link is generated from the same model; a test asserts every index row resolves to a file that exists.\u003c/li>\n\u003cli>[Detail pages could drift from the index counts] → Both read one \u003ccode>OpenSpecSnapshot\u003c/code>; counts are computed once and passed to both.\u003c/li>\n\u003cli>[Existing tests assert artifact bodies live in \u003ccode>spec.html\u003c/code>] → Move those assertions to the detail pages; the parser contract they cover is unchanged.\u003c/li>\n\u003cli>[A capability path could contain unsafe segments] → Paths come from the existing bounded, confined walker; page paths are built from the already-validated capability path and rejected if a segment is not a safe slug.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Add the id prefix to \u003ccode>renderDocument\u003c/code> with tests, changing no existing output.\u003c/li>\n\u003cli>Add the detail templates and emit them from the render pipeline.\u003c/li>\n\u003cli>Replace the index body with tables and remove inline artifact bodies.\u003c/li>\n\u003cli>Update tests, regenerate the dogfood output, and run the release gate.\u003c/li>\n\u003c/ol>\n\u003cp>Rollback: revert; stale \u003ccode>iris/spec/\u003c/code> files are inert static HTML and the next render no longer links them.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nSee proposal.md for the measurements. `src/templates/pages/spec.ts` renders every capability and change as a `spec-card` in a grid, and `sourceDetails()` emits both the rendered Markdown and an escaped `&lt;pre&gt;` of the same document inside a `&lt;details&gt;`. Nothing is lazy: a collapsed `&lt;details&gt;` still ships its whole body. `renderDocument()` already exists for research pages and produces heading ids plus a heading list, but it keys uniqueness off a per-call `Set`, so two documents rendered onto one page can collide.\n\n## Goals / Non-Goals\n\n**Goals:**\n\n- Make the Spec index cheap to open and cheap for an agent to read.\n- Give every requirement a stable anchor so a reviewer can link to one.\n- Keep exact-source fidelity, bounded parsing, and inert hostile content exactly as they are.\n- Reuse the research document template&#039;s shape rather than inventing a second one.\n\n**Non-Goals:**\n\n- Search or filtering across specs, and cross-linking requirements to the code that implements them.\n- Rendering archived changes differently from active ones beyond their listing.\n- Changing the parser, the snapshot format, or `iris/spec.json`.\n\n## Decisions\n\n### Two detail namespaces under `iris/spec/`\n\nCapabilities render to `iris/spec/capabilities/&lt;capability-path&gt;/page.html` and changes to `iris/spec/changes/&lt;change-name&gt;/page.html`. Separate namespaces mean a capability named `changes` cannot collide with the change namespace, and nested capability paths (`platform/identity/access`) keep working because the path becomes directories. Page depth is derived from the path segment count, so `renderShell`&#039;s existing `assetPrefix(depth)` resolves assets and navigation at any nesting level without special cases.\n\nAlternative: one flat directory with slugified names. Rejected because slugging a nested capability path throws away the structure the parser deliberately preserves.\n\n### Heading ids get an optional prefix\n\n`renderDocument(value, { idPrefix })` prefixes every generated id and returns headings carrying the prefixed id. A change page renders proposal, design, tasks, and each delta spec with distinct prefixes, so two artifacts that both contain `## Why` produce `proposal-why` and `design-why`. Without a prefix the behavior is unchanged, so research pages and the existing tests are unaffected.\n\nAlternative: render each artifact into a separate document fragment and rewrite ids afterwards. Rejected as string surgery over generated HTML when the renderer can simply be told the namespace.\n\n### The index becomes tables, not cards\n\nCanonical specs, active changes, and archives are uniform records, so each becomes a table row: capability or change name, source path, counts, health, and for changes a task progress bar. Tables scroll inside their own container and drop the least important columns first at narrow widths, matching the Work table&#039;s existing behavior. The index keeps the summary strip, project-context disclosures (both small), and the warnings list, because those are page-level facts rather than per-record bodies.\n\n### Exact source stays, but only on detail pages\n\nEvery artifact keeps its escaped exact source in a disclosure on its detail page. The index carries none. This preserves the source-fidelity contract while removing the duplicate copy from the page that everyone opens first.\n\n## Risks / Trade-offs\n\n- [Many more generated files] → `html-check` already walks the tree and every link is generated from the same model; a test asserts every index row resolves to a file that exists.\n- [Detail pages could drift from the index counts] → Both read one `OpenSpecSnapshot`; counts are computed once and passed to both.\n- [Existing tests assert artifact bodies live in `spec.html`] → Move those assertions to the detail pages; the parser contract they cover is unchanged.\n- [A capability path could contain unsafe segments] → Paths come from the existing bounded, confined walker; page paths are built from the already-validated capability path and rejected if a segment is not a safe slug.\n\n## Migration Plan\n\n1. Add the id prefix to `renderDocument` with tests, changing no existing output.\n2. Add the detail templates and emit them from the render pipeline.\n3. Replace the index body with tables and remove inline artifact bodies.\n4. Update tests, regenerate the dogfood output, and run the release gate.\n\nRollback: revert; stale `iris/spec/` files are inert static HTML and the next render no longer links them.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-renderer-support\">1. Renderer support\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Add an optional heading-id prefix to \u003ccode>renderDocument\u003c/code> so several documents can share one page, returning prefixed ids in the heading list and leaving unprefixed output unchanged.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Add tests for prefixed ids, collision handling across documents, and unchanged OpenSpec index output.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-detail-pages\">2. Detail pages\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add a shared spec document section helper that renders one artifact with its heading namespace, table of contents entries, and exact escaped source disclosure.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Generate a canonical spec detail page per capability at \u003ccode>iris/spec/capabilities/&lt;capability-path&gt;/page.html\u003c/code> with header, counts, health, requirement table of contents, rendered body, and source.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Generate a change detail page per change at \u003ccode>iris/spec/changes/&lt;change-name&gt;/page.html\u003c/code> covering manifest, proposal, design, tasks with progress, and each delta spec, stating any missing artifact.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.4 Derive page depth from the path so shell assets and navigation resolve at any nesting level, and refuse a capability path segment that is not a safe slug.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-index-page\">3. Index page\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Replace the canonical, active-change, and archive card grids with compact tables carrying counts, health, task progress, source path, and a link to each detail page.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Remove inline artifact bodies from the index while keeping the summary strip, project-context disclosures, and the warnings list.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Add responsive column priority and narrow-width behavior for the new tables.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Move artifact-body assertions from the index to the detail pages and add tests for index-to-detail link integrity, per-document id uniqueness, missing-artifact states, and nested capability paths.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Regenerate the checked-in output, confirm \u003ccode>spec.html\u003c/code> is comparable in size to the other section pages, and run \u003ccode>pnpm html-check\u003c/code>.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Update \u003ccode>docs/cmds.md\u003c/code>, \u003ccode>docs/design-system.md\u003c/code>, and \u003ccode>docs/status.md\u003c/code> for the index-plus-detail structure.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.4 Run \u003ccode>openspec validate spec-detail-pages --strict\u003c/code> and the full release gate.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Renderer support\n\n- [x] 1.1 Add an optional heading-id prefix to `renderDocument` so several documents can share one page, returning prefixed ids in the heading list and leaving unprefixed output unchanged.\n- [x] 1.2 Add tests for prefixed ids, collision handling across documents, and unchanged OpenSpec index output.\n\n## 2. Detail pages\n\n- [x] 2.1 Add a shared spec document section helper that renders one artifact with its heading namespace, table of contents entries, and exact escaped source disclosure.\n- [x] 2.2 Generate a canonical spec detail page per capability at `iris/spec/capabilities/&lt;capability-path&gt;/page.html` with header, counts, health, requirement table of contents, rendered body, and source.\n- [x] 2.3 Generate a change detail page per change at `iris/spec/changes/&lt;change-name&gt;/page.html` covering manifest, proposal, design, tasks with progress, and each delta spec, stating any missing artifact.\n- [x] 2.4 Derive page depth from the path so shell assets and navigation resolve at any nesting level, and refuse a capability path segment that is not a safe slug.\n\n## 3. Index page\n\n- [x] 3.1 Replace the canonical, active-change, and archive card grids with compact tables carrying counts, health, task progress, source path, and a link to each detail page.\n- [x] 3.2 Remove inline artifact bodies from the index while keeping the summary strip, project-context disclosures, and the warnings list.\n- [x] 3.3 Add responsive column priority and narrow-width behavior for the new tables.\n\n## 4. Verification and documentation\n\n- [x] 4.1 Move artifact-body assertions from the index to the detail pages and add tests for index-to-detail link integrity, per-document id uniqueness, missing-artifact states, and nested capability paths.\n- [x] 4.2 Regenerate the checked-in output, confirm `spec.html` is comparable in size to the other section pages, and run `pnpm html-check`.\n- [x] 4.3 Update `docs/cmds.md`, `docs/design-system.md`, and `docs/status.md` for the index-plus-detail structure.\n- [x] 4.4 Run `openspec validate spec-detail-pages --strict` and the full release gate.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-21\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-aperture-design-system-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · aperture-design-system\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages/specs/aperture-design-system/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/h3>\n\u003cp>The generated workspace MUST consist of an Overview page at \u003ccode>iris/index.html\u003c/code>, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec index page listing canonical specs, active changes, and the archive with links to one generated detail page per record, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\u003c/p>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-no-generated-work-pages\">Scenario: repository has no generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Overview from \u003ccode>file://\u003c/code>\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-repository-has-generated-work-pages\">Scenario: repository has generated work pages\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a newcomer opens the Work page with one or more page contracts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-user-opens-the-spec-view\">Scenario: user opens the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-state-is-communicated-in-the-spec-view\">Scenario: state is communicated in the Spec view\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> canonical, active, archived, legacy, incomplete, or invalid records are displayed\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each state MUST have a textual or structural indicator in addition to any color encoding\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-aperture-design-system-scenario-a-spec-detail-page-is-opened\">Scenario: a spec detail page is opened\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user opens a canonical spec or change detail page from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> it MUST use the same navigation shell, mark Spec as the current section, and provide a path back to the index\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## MODIFIED Requirements\n\n### Requirement: newcomer-first dashboard hierarchy\n\nThe generated workspace MUST consist of an Overview page at `iris/index.html`, a Work page with the dense List/Table/Kanban browser and detail drawer, a Spec index page listing canonical specs, active changes, and the archive with links to one generated detail page per record, a Research page, a Commands page, and the project docs, all reachable through the shared navigation shell; the Overview MUST preserve the briefing hero, per-section summary, architecture pane, and project-docs strip.\n\n#### Scenario: repository has no generated work pages\n\n- **WHEN** a newcomer opens the Overview from `file://`\n- **THEN** it MUST remain useful by naming the commands that populate the briefing, architecture, and work areas without making a network request\n\n#### Scenario: repository has generated work pages\n\n- **WHEN** a newcomer opens the Work page with one or more page contracts\n- **THEN** the page MUST provide compact List, Table, and Kanban representations plus a detail drawer without requiring navigation away from that page\n\n#### Scenario: user opens the Spec view\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the Spec index MUST show OpenSpec overview counts and navigable canonical, active-change, and archive listings while the shell keeps every other section reachable\n\n#### Scenario: state is communicated in the Spec view\n\n- **WHEN** canonical, active, archived, legacy, incomplete, or invalid records are displayed\n- **THEN** each state MUST have a textual or structural indicator in addition to any color encoding\n\n#### Scenario: a spec detail page is opened\n\n- **WHEN** a user opens a canonical spec or change detail page from the index\n- **THEN** it MUST use the same navigation shell, mark Spec as the current section, and provide a path back to the index\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-openspec-spec-browser-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · openspec-spec-browser\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003cspan class=\"badge b-muted\">MODIFIED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-spec-detail-pages/specs/openspec-spec-browser/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-spec-index-and-detail-pages\">Requirement: spec index and detail pages\u003c/h3>\n\u003cp>The generated Spec section MUST consist of an index page listing every canonical spec, active change, and archived change with its real counts, health, and task progress, and one generated detail page per canonical spec and per change holding that record's artifact bodies. The index MUST link every listed record to its detail page and MUST NOT embed artifact bodies itself.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-opens-the-spec-index\">Scenario: user opens the Spec index\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user activates the \u003ccode>Spec\u003c/code> navigation entry\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the index MUST show overview counts and compact canonical, active-change, and archive listings, each row naming its source path and linking to that record's detail page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-opens-a-canonical-spec-detail-page\">Scenario: user opens a canonical spec detail page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows a canonical spec link from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the detail page MUST show the capability name, source path, requirement and scenario counts, health, the rendered specification, and its exact escaped source\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-opens-a-change-detail-page\">Scenario: user opens a change detail page\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a user follows a change link from the index\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the detail page MUST show the change lifecycle, completeness, task progress, and every available artifact including delta specs, each with its exact escaped source\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-a-record-has-no-readable-artifacts\">Scenario: a record has no readable artifacts\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a listed change or capability has missing or unreadable artifacts\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> its detail page MUST state which artifacts are missing and MUST render the readable ones\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-deep-linkable-specification-headings\">Requirement: deep-linkable specification headings\u003c/h3>\n\u003cp>A generated detail page MUST give every heading in a rendered document a stable identifier derived from its text, MUST provide a table of contents for a document with two or more section headings, and MUST keep identifiers unique when one page renders several documents.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-a-specification-with-several-requirements-is-render\">Scenario: a specification with several requirements is rendered\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a canonical spec containing multiple requirements is rendered to its detail page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each requirement heading MUST carry an identifier derived from its text and the table of contents MUST link to it\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-one-page-renders-several-documents\">Scenario: one page renders several documents\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a change detail page renders a proposal, a design, and a delta spec that share a heading name\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> each rendered heading MUST receive a distinct identifier and each table-of-contents entry MUST resolve to its own document's heading\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/h2>\n\u003ch3 id=\"delta-openspec-spec-browser-requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/h3>\n\u003cp>The system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST progressively render fenced blocks labeled exactly \u003ccode>mermaid\u003c/code> as safe offline diagrams with escaped-source fallback, MUST keep the exact escaped document source available as a secondary disclosure, MUST give each rendered heading on a detail page an identifier derived from its text, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\u003c/p>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-supported-markdown-structures-are-present\">Scenario: supported Markdown structures are present\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, fenced code, or Mermaid fences\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the Spec section MUST render those structures with readable semantic markup, progressively enhance Mermaid fences when the local runtime is available, and preserve escaped fallback source rather than displaying Markdown punctuation as the primary presentation\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-user-needs-exact-source-evidence\">Scenario: user needs exact source evidence\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a rendered Markdown artifact is displayed on its detail page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the user MUST be able to reveal the exact escaped source without leaving that page\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-configuration-artifact-is-displayed\">Scenario: configuration artifact is displayed\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> the Spec section presents an OpenSpec YAML manifest or workspace configuration\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown or submitted to the diagram runtime\u003c/li>\n\u003c/ul>\n\u003ch4 id=\"delta-openspec-spec-browser-scenario-rendered-markdown-is-navigable\">Scenario: rendered Markdown is navigable\u003c/h4>\n\u003cul>\n\u003cli>\u003cstrong>WHEN\u003c/strong> a rendered artifact contains section headings on a detail page\u003c/li>\n\u003cli>\u003cstrong>THEN\u003c/strong> those headings MUST be individually addressable so a reader can link to a requirement or section rather than only to the whole document\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## ADDED Requirements\n\n### Requirement: spec index and detail pages\n\nThe generated Spec section MUST consist of an index page listing every canonical spec, active change, and archived change with its real counts, health, and task progress, and one generated detail page per canonical spec and per change holding that record&#039;s artifact bodies. The index MUST link every listed record to its detail page and MUST NOT embed artifact bodies itself.\n\n#### Scenario: user opens the Spec index\n\n- **WHEN** a user activates the `Spec` navigation entry\n- **THEN** the index MUST show overview counts and compact canonical, active-change, and archive listings, each row naming its source path and linking to that record&#039;s detail page\n\n#### Scenario: user opens a canonical spec detail page\n\n- **WHEN** a user follows a canonical spec link from the index\n- **THEN** the detail page MUST show the capability name, source path, requirement and scenario counts, health, the rendered specification, and its exact escaped source\n\n#### Scenario: user opens a change detail page\n\n- **WHEN** a user follows a change link from the index\n- **THEN** the detail page MUST show the change lifecycle, completeness, task progress, and every available artifact including delta specs, each with its exact escaped source\n\n#### Scenario: a record has no readable artifacts\n\n- **WHEN** a listed change or capability has missing or unreadable artifacts\n- **THEN** its detail page MUST state which artifacts are missing and MUST render the readable ones\n\n### Requirement: deep-linkable specification headings\n\nA generated detail page MUST give every heading in a rendered document a stable identifier derived from its text, MUST provide a table of contents for a document with two or more section headings, and MUST keep identifiers unique when one page renders several documents.\n\n#### Scenario: a specification with several requirements is rendered\n\n- **WHEN** a canonical spec containing multiple requirements is rendered to its detail page\n- **THEN** each requirement heading MUST carry an identifier derived from its text and the table of contents MUST link to it\n\n#### Scenario: one page renders several documents\n\n- **WHEN** a change detail page renders a proposal, a design, and a delta spec that share a heading name\n- **THEN** each rendered heading MUST receive a distinct identifier and each table-of-contents entry MUST resolve to its own document&#039;s heading\n\n## MODIFIED Requirements\n\n### Requirement: safe semantic Markdown presentation\n\nThe system MUST present Markdown OpenSpec documents as semantic HTML for reading, MUST progressively render fenced blocks labeled exactly `mermaid` as safe offline diagrams with escaped-source fallback, MUST keep the exact escaped document source available as a secondary disclosure, MUST give each rendered heading on a detail page an identifier derived from its text, and MUST keep non-Markdown configuration artifacts in a literal code presentation.\n\n#### Scenario: supported Markdown structures are present\n\n- **WHEN** an OpenSpec Markdown artifact contains headings, paragraphs, emphasis, links, lists, task lists, blockquotes, tables, inline code, fenced code, or Mermaid fences\n- **THEN** the Spec section MUST render those structures with readable semantic markup, progressively enhance Mermaid fences when the local runtime is available, and preserve escaped fallback source rather than displaying Markdown punctuation as the primary presentation\n\n#### Scenario: user needs exact source evidence\n\n- **WHEN** a rendered Markdown artifact is displayed on its detail page\n- **THEN** the user MUST be able to reveal the exact escaped source without leaving that page\n\n#### Scenario: configuration artifact is displayed\n\n- **WHEN** the Spec section presents an OpenSpec YAML manifest or workspace configuration\n- **THEN** the artifact MUST remain escaped literal code and MUST NOT be interpreted as Markdown or submitted to the diagram runtime\n\n#### Scenario: rendered Markdown is navigable\n\n- **WHEN** a rendered artifact contains section headings on a detail page\n- **THEN** those headings MUST be individually addressable so a reader can link to a requirement or section rather than only to the whole document\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-two-detail-namespaces-under-iris-spec\">Two detail namespaces under `iris/spec/`\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-heading-ids-get-an-optional-prefix\">Heading ids get an optional prefix\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-the-index-becomes-tables-not-cards\">The index becomes tables, not cards\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#design-exact-source-stays-but-only-on-detail-pages\">Exact source stays, but only on detail pages\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-renderer-support\">1. Renderer support\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-detail-pages\">2. Detail pages\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-index-page\">3. Index page\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-verification-and-documentation\">4. Verification and documentation\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-artifact\">Delta spec · aperture-design-system\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-aperture-design-system-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-aperture-design-system-requirement-newcomer-first-dashboard-hierarchy\">Requirement: newcomer-first dashboard hierarchy\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-artifact\">Delta spec · openspec-spec-browser\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-spec-index-and-detail-pages\">Requirement: spec index and detail pages\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-deep-linkable-specification-headings\">Requirement: deep-linkable specification headings\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-openspec-spec-browser-modified-requirements\">MODIFIED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-openspec-spec-browser-requirement-safe-semantic-markdown-presentation\">Requirement: safe semantic Markdown presentation\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"change:2026-08-21-standalone-publish-and-export":{"kind":"change","name":"2026-08-21-standalone-publish-and-export","title":"2026-08-21-standalone-publish-and-export","path":"changes/archive/2026-08-21-standalone-publish-and-export","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived change\u003c/span>\n        \u003ch1>2026-08-21-standalone-publish-and-export\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-21-standalone-publish-and-export\u003c/span>\n          \u003cspan class=\"badge b-success\">complete\u003c/span>\n          \u003cspan class=\"badge b-success\">valid\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \u003csection class=\"strip\" aria-label=\"change summary\">\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">12\u003c/span>\u003cspan class=\"label\">tasks complete\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">0\u003c/span>\u003cspan class=\"label\">tasks open\u003c/span>\u003c/div>\n      \u003cdiv class=\"card stat\">\u003cspan class=\"value\">1\u003c/span>\u003cspan class=\"label\">delta specs\u003c/span>\u003c/div>\n    \u003c/section>\n\n    \u003cdiv class=\"card\">\u003cdiv class=\"meter m-success\" role=\"img\" aria-label=\"2026-08-21-standalone-publish-and-export: 12 of 12 tasks complete\">\u003cdiv class=\"track\">\u003cdiv class=\"fill\" style=\"width: 100%\">\u003c/div>\u003c/div>\u003c/div>\u003cp class=\"work-meta\" style=\"margin: var(--space-2) 0 0\">12/12 tasks complete · 0 open\u003c/p>\u003c/div>\n\n    \u003cdiv class=\"layout\">\n      \u003cdiv class=\"spec-stack\">\u003csection class=\"card doc-body\" id=\"proposal-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Proposal\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-standalone-publish-and-export/proposal.md\u003c/p>\n        \n        \u003ch2 id=\"proposal-why\">Why\u003c/h2>\n\u003cp>A local-first tool becomes far more useful when it can hand off a page as a portable artifact without depending on the active repo or a remote viewer. The current roadmap already defines \u003ccode>publish\u003c/code> and \u003ccode>export\u003c/code> commands, but they still need a clear path to generate shareable, standalone outputs that remain offline-safe and easy to inspect.\u003c/p>\n\u003ch2 id=\"proposal-what-changes\">What Changes\u003c/h2>\n\u003cul>\n\u003cli>Implement the publish flow for standalone, portable HTML output.\u003c/li>\n\u003cli>Implement the single-file export mode and make every deferred mode fail explicitly instead of emitting a mislabeled artifact.\u003c/li>\n\u003cli>Keep generated artifacts self-contained and stable enough to share outside the repo.\u003c/li>\n\u003cli>Preserve the local-first design by avoiding any required cloud or remote dependency.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-capabilities\">Capabilities\u003c/h2>\n\u003ch3 id=\"proposal-new-capabilities\">New Capabilities\u003c/h3>\n\u003cul>\n\u003cli>standalone-publish-and-export: create portable, standalone artifacts from project pages while keeping them offline-safe and easy to share.\u003c/li>\n\u003c/ul>\n\u003ch3 id=\"proposal-modified-capabilities\">Modified Capabilities\u003c/h3>\n\u003cul>\n\u003cli>publish: formalize the output contract for single-file static artifacts.\u003c/li>\n\u003cli>export: add the proper export modes and file handling logic.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"proposal-impact\">Impact\u003c/h2>\n\u003cul>\n\u003cli>Users can share a rendered artifact without requiring a live local project checkout.\u003c/li>\n\u003cli>Design handoff and review becomes easier because a page can be exported to a portable artifact.\u003c/li>\n\u003cli>The project remains faithful to its local-first positioning while still supporting distribution and offline shareability.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Why\n\nA local-first tool becomes far more useful when it can hand off a page as a portable artifact without depending on the active repo or a remote viewer. The current roadmap already defines `publish` and `export` commands, but they still need a clear path to generate shareable, standalone outputs that remain offline-safe and easy to inspect.\n\n## What Changes\n\n- Implement the publish flow for standalone, portable HTML output.\n- Implement the single-file export mode and make every deferred mode fail explicitly instead of emitting a mislabeled artifact.\n- Keep generated artifacts self-contained and stable enough to share outside the repo.\n- Preserve the local-first design by avoiding any required cloud or remote dependency.\n\n## Capabilities\n\n### New Capabilities\n- standalone-publish-and-export: create portable, standalone artifacts from project pages while keeping them offline-safe and easy to share.\n\n### Modified Capabilities\n- publish: formalize the output contract for single-file static artifacts.\n- export: add the proper export modes and file handling logic.\n\n## Impact\n\n- Users can share a rendered artifact without requiring a live local project checkout.\n- Design handoff and review becomes easier because a page can be exported to a portable artifact.\n- The project remains faithful to its local-first positioning while still supporting distribution and offline shareability.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"design-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Design\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-standalone-publish-and-export/design.md\u003c/p>\n        \n        \u003ch2 id=\"design-context\">Context\u003c/h2>\n\u003cp>The product is designed around local rendering and static HTML output, but it still needs an explicit publish/export layer to support handoff, review, and offline sharing. The command contract already declares \u003ccode>publish\u003c/code> and \u003ccode>export\u003c/code>, so the implementation should focus on reliability, portability, and self-contained asset handling rather than broad feature expansion.\u003c/p>\n\u003ch2 id=\"design-goals-non-goals\">Goals / Non-Goals\u003c/h2>\n\u003cp>\u003cstrong>Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Produce a standalone artifact from a rendered page without requiring a live project checkout.\u003c/li>\n\u003cli>Keep the output portable and safe for offline or external review.\u003c/li>\n\u003cli>Support the export modes already documented in the CLI contract.\u003c/li>\n\u003cli>Maintain local-first operating assumptions while enabling shareable files.\u003c/li>\n\u003c/ul>\n\u003cp>\u003cstrong>Non-Goals:\u003c/strong>\u003c/p>\n\u003cul>\n\u003cli>Full remote publishing or hosted document management.\u003c/li>\n\u003cli>Complex visual transformation beyond the already-supported render pipeline.\u003c/li>\n\u003cli>A broad library ecosystem for export formats outside the documented project scope.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-decisions\">Decisions\u003c/h2>\n\u003cul>\n\u003cli>Use the rendered page as the canonical source of truth and build the artifact from that data rather than from ad hoc DOM scraping.\u003c/li>\n\u003cli>Prefer bundled local assets over remote CDN resources so published outputs remain offline-safe.\u003c/li>\n\u003cli>Keep output paths explicit and deterministic for easier automation and review.\u003c/li>\n\u003cli>Limit the first implementation to the export modes already declared in the project contract.\u003c/li>\n\u003cli>Keep PNG/PDF unavailable in this change after evaluating the current browser-renderer options. The existing explicit exit-code-1 path remains the honest contract; task 2.2 stays unchecked.\u003c/li>\n\u003cli>Prefer \u003ccode>puppeteer-core\u003c/code> over Playwright if a later change relaxes the renderer constraint. \u003ccode>puppeteer-core\u003c/code> 25.8.0 does not download Chrome and can use an explicit system executable or installed channel, so it has the smaller operational surface for a single Chrome screenshot/PDF job. Its documented trade-off is decisive here: compatibility is only guaranteed with Puppeteer's bundled browser, not arbitrary system Chrome, so output cannot be treated as stable across browser updates and machines.\u003c/li>\n\u003cli>Do not adopt Playwright 1.62.1 for this narrow export job. Playwright no longer downloads browsers during package installation, but its supported deterministic path still requires \u003ccode>playwright install\u003c/code>, a version-coupled browser lifecycle, and a browser cache measured in hundreds of megabytes. Its cross-browser features do not offset that operational cost for two export modes.\u003c/li>\n\u003cli>Any future implementation must use a fresh temporary browser profile, abort non-\u003ccode>file:\u003c/code>/\u003ccode>data:\u003c/code>/\u003ccode>blob:\u003c/code> requests, disable background networking, and define whether determinism means byte-identical output or a pinned visual baseline. That policy decision belongs in a new renderer change before adding a dependency.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-risks-trade-offs\">Risks / Trade-offs\u003c/h2>\n\u003cul>\n\u003cli>[Large artifact size] → Mitigation: keep the single-file artifact focused on the page and bundled essentials rather than whole project files.\u003c/li>\n\u003cli>[Asset dependency issues] → Mitigation: vendor critical CSS and font assets locally before export.\u003c/li>\n\u003cli>[Renderer mismatch across formats] → Mitigation: keep export features narrow and validated against the known render pipeline.\u003c/li>\n\u003cli>[System-browser drift] → Mitigation: defer browser export until the project chooses between a pinned downloaded browser and explicitly non-byte-stable system Chrome output.\u003c/li>\n\u003cli>[Hidden browser networking] → Mitigation: require request interception and background-network suppression in any future renderer implementation.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"design-migration-plan\">Migration Plan\u003c/h2>\n\u003col>\n\u003cli>Implement the publish pipeline for a single-file static artifact.\u003c/li>\n\u003cli>Keep PNG/PDF on the explicit unsupported path until the browser pinning and determinism policy is accepted.\u003c/li>\n\u003cli>Integrate asset bundling and vendoring rules to keep the final artifact self-contained.\u003c/li>\n\u003cli>Validate the commands against real pages and regression examples.\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"design-open-questions\">Open Questions\u003c/h2>\n\u003cp>Can a future renderer download and pin Chromium as an explicit optional install, or must it rely on a user-managed browser and accept cross-version output drift? This must be resolved before task 2.2 can be implemented honestly.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Context\n\nThe product is designed around local rendering and static HTML output, but it still needs an explicit publish/export layer to support handoff, review, and offline sharing. The command contract already declares `publish` and `export`, so the implementation should focus on reliability, portability, and self-contained asset handling rather than broad feature expansion.\n\n## Goals / Non-Goals\n\n**Goals:**\n- Produce a standalone artifact from a rendered page without requiring a live project checkout.\n- Keep the output portable and safe for offline or external review.\n- Support the export modes already documented in the CLI contract.\n- Maintain local-first operating assumptions while enabling shareable files.\n\n**Non-Goals:**\n- Full remote publishing or hosted document management.\n- Complex visual transformation beyond the already-supported render pipeline.\n- A broad library ecosystem for export formats outside the documented project scope.\n\n## Decisions\n\n- Use the rendered page as the canonical source of truth and build the artifact from that data rather than from ad hoc DOM scraping.\n- Prefer bundled local assets over remote CDN resources so published outputs remain offline-safe.\n- Keep output paths explicit and deterministic for easier automation and review.\n- Limit the first implementation to the export modes already declared in the project contract.\n- Keep PNG/PDF unavailable in this change after evaluating the current browser-renderer options. The existing explicit exit-code-1 path remains the honest contract; task 2.2 stays unchecked.\n- Prefer `puppeteer-core` over Playwright if a later change relaxes the renderer constraint. `puppeteer-core` 25.8.0 does not download Chrome and can use an explicit system executable or installed channel, so it has the smaller operational surface for a single Chrome screenshot/PDF job. Its documented trade-off is decisive here: compatibility is only guaranteed with Puppeteer&#039;s bundled browser, not arbitrary system Chrome, so output cannot be treated as stable across browser updates and machines.\n- Do not adopt Playwright 1.62.1 for this narrow export job. Playwright no longer downloads browsers during package installation, but its supported deterministic path still requires `playwright install`, a version-coupled browser lifecycle, and a browser cache measured in hundreds of megabytes. Its cross-browser features do not offset that operational cost for two export modes.\n- Any future implementation must use a fresh temporary browser profile, abort non-`file:`/`data:`/`blob:` requests, disable background networking, and define whether determinism means byte-identical output or a pinned visual baseline. That policy decision belongs in a new renderer change before adding a dependency.\n\n## Risks / Trade-offs\n\n- [Large artifact size] → Mitigation: keep the single-file artifact focused on the page and bundled essentials rather than whole project files.\n- [Asset dependency issues] → Mitigation: vendor critical CSS and font assets locally before export.\n- [Renderer mismatch across formats] → Mitigation: keep export features narrow and validated against the known render pipeline.\n- [System-browser drift] → Mitigation: defer browser export until the project chooses between a pinned downloaded browser and explicitly non-byte-stable system Chrome output.\n- [Hidden browser networking] → Mitigation: require request interception and background-network suppression in any future renderer implementation.\n\n## Migration Plan\n\n1. Implement the publish pipeline for a single-file static artifact.\n2. Keep PNG/PDF on the explicit unsupported path until the browser pinning and determinism policy is accepted.\n3. Integrate asset bundling and vendoring rules to keep the final artifact self-contained.\n4. Validate the commands against real pages and regression examples.\n\n## Open Questions\n\nCan a future renderer download and pin Chromium as an explicit optional install, or must it rely on a user-managed browser and accept cross-version output drift? This must be resolved before task 2.2 can be implemented honestly.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"tasks-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Tasks\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-standalone-publish-and-export/tasks.md\u003c/p>\n        \n        \u003ch2 id=\"tasks-1-publish-pipeline\">1. Publish pipeline\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.1 Define the output contract for the standalone publish command.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.2 Implement the render-to-artifact transformation for a portable HTML page.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 1.3 Ensure the generated file can be opened without the project tree present.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-2-export-handling\">2. Export handling\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.1 Add the base export flow and output-path handling.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.2 Implement each supported export mode from the CLI contract.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 2.3 Add clear error handling for unsupported or invalid export requests.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-3-asset-bundling-and-offline-safety\">3. Asset bundling and offline safety\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.1 Identify CSS, fonts, and other required assets that must be bundled or vendored.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.2 Add local asset resolution for publish and export outputs.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 3.3 Validate that the exported artifact works without network access.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"tasks-4-regression-and-rollout\">4. Regression and rollout\u003c/h2>\n\u003cul>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.1 Add smoke tests for publish and export commands against existing generated pages.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.2 Confirm the output path and naming conventions match the documented CLI behavior.\u003c/li>\n\u003cli class=\"task-list-item\">\u003cinput class=\"task-checkbox\" type=\"checkbox\" disabled checked aria-label=\"completed task\" /> 4.3 Document the supported publish/export modes and limitations for users.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## 1. Publish pipeline\n\n- [x] 1.1 Define the output contract for the standalone publish command.\n- [x] 1.2 Implement the render-to-artifact transformation for a portable HTML page.\n- [x] 1.3 Ensure the generated file can be opened without the project tree present.\n\n## 2. Export handling\n\n- [x] 2.1 Add the base export flow and output-path handling.\n- [x] 2.2 Implement each supported export mode from the CLI contract.\n- [x] 2.3 Add clear error handling for unsupported or invalid export requests.\n\n## 3. Asset bundling and offline safety\n\n- [x] 3.1 Identify CSS, fonts, and other required assets that must be bundled or vendored.\n- [x] 3.2 Add local asset resolution for publish and export outputs.\n- [x] 3.3 Validate that the exported artifact works without network access.\n\n## 4. Regression and rollout\n\n- [x] 4.1 Add smoke tests for publish and export commands against existing generated pages.\n- [x] 4.2 Confirm the output path and naming conventions match the documented CLI behavior.\n- [x] 4.3 Document the supported publish/export modes and limitations for users.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003csection class=\"card doc-body\" id=\"manifest-artifact\">\n          \u003ch2>Manifest\u003c/h2>\n          \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-standalone-publish-and-export/.openspec.yaml\u003c/p>\n          \u003cpre class=\"spec-source\">\u003ccode>schema: spec-driven\ncreated: 2026-08-18\n\u003c/code>\u003c/pre>\n        \u003c/section>\u003csection class=\"card doc-body\" id=\"delta-standalone-publish-and-export-artifact\">\n        \u003cdiv class=\"spec-card-header\">\n          \u003ch2>Delta spec · standalone-publish-and-export\u003c/h2>\n          \u003cspan class=\"spec-meta\">\u003cspan class=\"badge b-muted\">ADDED\u003c/span>\u003c/span>\n        \u003c/div>\n        \u003cp class=\"mono spec-path\">changes/archive/2026-08-21-standalone-publish-and-export/specs/standalone-publish-and-export/spec.md\u003c/p>\n        \n        \u003ch2 id=\"delta-standalone-publish-and-export-purpose\">Purpose\u003c/h2>\n\u003cp>Provide a reliable way to turn a generated page into a standalone artifact that can be shared, inspected, or archived without requiring a live \u003ccode>iris\u003c/code> project to be present. The system must preserve the rendered output, embed or vendor any required assets, and work in an offline-safe local workflow.\u003c/p>\n\u003ch2 id=\"delta-standalone-publish-and-export-added-requirements\">ADDED Requirements\u003c/h2>\n\u003ch3 id=\"delta-standalone-publish-and-export-requirement-publishing-of-portable-html\">Requirement: publishing of portable HTML\u003c/h3>\n\u003cp>The system MUST support generating a single-file static HTML artifact from a rendered page while preserving the project’s visual structure and data.\u003c/p>\n\u003ch4 id=\"delta-standalone-publish-and-export-scenario-user-publishes-a-page\">Scenario: user publishes a page\u003c/h4>\n\u003cp>WHEN a user runs the publish command for a valid page\nTHEN the system MUST create a generated artifact that can be opened directly in a browser without needing the rest of the project tree.\u003c/p>\n\u003ch4 id=\"delta-standalone-publish-and-export-scenario-page-is-missing-or-invalid\">Scenario: page is missing or invalid\u003c/h4>\n\u003cp>WHEN the selected page does not exist or cannot be rendered\nTHEN the system MUST fail clearly and MUST explain the missing or invalid page identifier.\u003c/p>\n\u003ch3 id=\"delta-standalone-publish-and-export-requirement-export-variants\">Requirement: export variants\u003c/h3>\n\u003cp>The system MUST support the documented single-file HTML export, and for any export mode the project has defined but not made available it MUST fail with an actionable message rather than emitting an artifact of the wrong type.\u003c/p>\n\u003ch4 id=\"delta-standalone-publish-and-export-scenario-user-requests-the-single-file-export\">Scenario: user requests the single-file export\u003c/h4>\n\u003cp>WHEN a user selects the single-file HTML export mode\nTHEN the system MUST produce a standalone HTML artifact and MUST use the correct output path and naming conventions\u003c/p>\n\u003ch4 id=\"delta-standalone-publish-and-export-scenario-user-requests-an-unavailable-export-mode\">Scenario: user requests an unavailable export mode\u003c/h4>\n\u003cp>WHEN a user selects an export mode that the project has deferred, such as an image or PDF mode awaiting a deterministic renderer decision\nTHEN the system MUST refuse the request and name the reason, and MUST NOT write a file of a different type under the requested name\u003c/p>\n\u003ch3 id=\"delta-standalone-publish-and-export-requirement-offline-safe-asset-handling\">Requirement: offline-safe asset handling\u003c/h3>\n\u003cp>The system MUST ensure generated artifacts do not depend on a network call to render or display correctly.\u003c/p>\n\u003ch4 id=\"delta-standalone-publish-and-export-scenario-offline-export\">Scenario: offline export\u003c/h4>\n\u003cp>WHEN a page is generated in an offline environment\nTHEN the export MUST rely on vendored or bundled assets instead of remote CDN content.\u003c/p>\n\u003ch3 id=\"delta-standalone-publish-and-export-requirement-output-path-control\">Requirement: output path control\u003c/h3>\n\u003cp>The system MUST allow the user to choose the destination path for published and exported artifacts and MUST handle output directories consistently.\u003c/p>\n\u003ch4 id=\"delta-standalone-publish-and-export-scenario-user-specifies-a-target-path\">Scenario: user specifies a target path\u003c/h4>\n\u003cp>WHEN the output directory is valid\nTHEN the CLI MUST write the artifact there and MUST create missing parent directories as needed.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode>## Purpose\n\nProvide a reliable way to turn a generated page into a standalone artifact that can be shared, inspected, or archived without requiring a live `iris` project to be present. The system must preserve the rendered output, embed or vendor any required assets, and work in an offline-safe local workflow.\n\n## ADDED Requirements\n\n### Requirement: publishing of portable HTML\nThe system MUST support generating a single-file static HTML artifact from a rendered page while preserving the project’s visual structure and data.\n\n#### Scenario: user publishes a page\nWHEN a user runs the publish command for a valid page\nTHEN the system MUST create a generated artifact that can be opened directly in a browser without needing the rest of the project tree.\n\n#### Scenario: page is missing or invalid\nWHEN the selected page does not exist or cannot be rendered\nTHEN the system MUST fail clearly and MUST explain the missing or invalid page identifier.\n\n### Requirement: export variants\nThe system MUST support the documented single-file HTML export, and for any export mode the project has defined but not made available it MUST fail with an actionable message rather than emitting an artifact of the wrong type.\n\n#### Scenario: user requests the single-file export\nWHEN a user selects the single-file HTML export mode\nTHEN the system MUST produce a standalone HTML artifact and MUST use the correct output path and naming conventions\n\n#### Scenario: user requests an unavailable export mode\nWHEN a user selects an export mode that the project has deferred, such as an image or PDF mode awaiting a deterministic renderer decision\nTHEN the system MUST refuse the request and name the reason, and MUST NOT write a file of a different type under the requested name\n\n### Requirement: offline-safe asset handling\nThe system MUST ensure generated artifacts do not depend on a network call to render or display correctly.\n\n#### Scenario: offline export\nWHEN a page is generated in an offline environment\nTHEN the export MUST rely on vendored or bundled assets instead of remote CDN content.\n\n### Requirement: output path control\nThe system MUST allow the user to choose the destination path for published and exported artifacts and MUST handle output directories consistently.\n\n#### Scenario: user specifies a target path\nWHEN the output directory is valid\nTHEN the CLI MUST write the artifact there and MUST create missing parent directories as needed.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/section>\u003c/div>\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-artifact\">Proposal\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-why\">Why\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-what-changes\">What Changes\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-capabilities\">Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-new-capabilities\">New Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#proposal-modified-capabilities\">Modified Capabilities\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#proposal-impact\">Impact\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-artifact\">Design\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-context\">Context\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-goals-non-goals\">Goals / Non-Goals\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-decisions\">Decisions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-risks-trade-offs\">Risks / Trade-offs\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-migration-plan\">Migration Plan\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#design-open-questions\">Open Questions\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-artifact\">Tasks\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-1-publish-pipeline\">1. Publish pipeline\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-2-export-handling\">2. Export handling\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-3-asset-bundling-and-offline-safety\">3. Asset bundling and offline safety\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#tasks-4-regression-and-rollout\">4. Regression and rollout\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#manifest-artifact\">Manifest\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-standalone-publish-and-export-artifact\">Delta spec · standalone-publish-and-export\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-standalone-publish-and-export-purpose\">Purpose\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#delta-standalone-publish-and-export-added-requirements\">ADDED Requirements\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-standalone-publish-and-export-requirement-publishing-of-portable-html\">Requirement: publishing of portable HTML\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-standalone-publish-and-export-requirement-export-variants\">Requirement: export variants\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-standalone-publish-and-export-requirement-offline-safe-asset-handling\">Requirement: offline-safe asset handling\u003c/a>\u003c/li>\u003cli class=\"toc-3\">\u003ca href=\"#delta-standalone-publish-and-export-requirement-output-path-control\">Requirement: output path control\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n    \u003c/div>"},"legacy:2026-08-18-m0-scaffold":{"kind":"legacy","name":"2026-08-18-m0-scaffold","title":"M0 scaffold (archived)","path":"changes/archive/2026-08-18-m0-scaffold.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived · legacy\u003c/span>\n        \u003ch1>M0 scaffold (archived)\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-18-m0-scaffold.md\u003c/span>\n          \u003cspan class=\"badge b-archived\">legacy\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \n    \u003cdiv class=\"doc-single\">\n      \n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"m0-scaffold-archived\">M0 scaffold (archived)\u003c/h1>\n\u003cul>\n\u003cli>Proposed: bootstrap Node22.13 ESM TS strict CLI with schema validation, tests, docs, and CI.\u003c/li>\n\u003cli>Applied: package/tooling scaffold, command registry with init/render functional stubs, envelope + 5 type schemas, sad-path tests.\u003c/li>\n\u003cli>Archived: complete for milestone M0 acceptance baseline.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># M0 scaffold (archived)\n\n- Proposed: bootstrap Node22.13 ESM TS strict CLI with schema validation, tests, docs, and CI.\n- Applied: package/tooling scaffold, command registry with init/render functional stubs, envelope + 5 type schemas, sad-path tests.\n- Archived: complete for milestone M0 acceptance baseline.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"legacy:2026-08-18-m1-design-system":{"kind":"legacy","name":"2026-08-18-m1-design-system","title":"M1 design system (archived)","path":"changes/archive/2026-08-18-m1-design-system.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived · legacy\u003c/span>\n        \u003ch1>M1 design system (archived)\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-18-m1-design-system.md\u003c/span>\n          \u003cspan class=\"badge b-archived\">legacy\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \n    \u003cdiv class=\"doc-single\">\n      \n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"m1-design-system-archived\">M1 design system (archived)\u003c/h1>\n\u003cul>\n\u003cli>Proposed: establish tokenized design system and dashboard foundation with reduced-motion behavior.\u003c/li>\n\u003cli>Applied: tokens.css dark/light, base components (tabs/timeline/code-card/metrics/table/chart/mermaid wrappers), dashboard LIST|BOARD + signature canvas, token-lint in CI.\u003c/li>\n\u003cli>Archived: complete for milestone M1 baseline.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># M1 design system (archived)\n\n- Proposed: establish tokenized design system and dashboard foundation with reduced-motion behavior.\n- Applied: tokens.css dark/light, base components (tabs/timeline/code-card/metrics/table/chart/mermaid wrappers), dashboard LIST|BOARD + signature canvas, token-lint in CI.\n- Archived: complete for milestone M1 baseline.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"legacy:2026-08-18-m2-merge-friendly-branching":{"kind":"legacy","name":"2026-08-18-m2-merge-friendly-branching","title":"M2 merge-friendly branching (archived)","path":"changes/archive/2026-08-18-m2-merge-friendly-branching.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived · legacy\u003c/span>\n        \u003ch1>M2 merge-friendly branching (archived)\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-18-m2-merge-friendly-branching.md\u003c/span>\n          \u003cspan class=\"badge b-archived\">legacy\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \n    \u003cdiv class=\"doc-single\">\n      \n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"m2-merge-friendly-branching-archived\">M2 merge-friendly branching (archived)\u003c/h1>\n\u003cul>\n\u003cli>Proposed: establish a merge-friendly milestone workflow for this repo so the work can be merged incrementally and split into parallel branches without creating a large, hard-to-review change set.\u003c/li>\n\u003cli>Applied: captured a milestone plan with explicit acceptance criteria, a narrow foundation branch from main for shared groundwork, a sequence for merging that branch first, and follow-on parallel branches by concern (for example CLI behavior, rendering/templates, tests/docs). The plan also calls for small merge-ready PRs, rebase-on-main discipline, and milestone archival once the work is complete.\u003c/li>\n\u003cli>Archived: complete for milestone M2 branch orchestration baseline.\u003c/li>\n\u003c/ul>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># M2 merge-friendly branching (archived)\n\n- Proposed: establish a merge-friendly milestone workflow for this repo so the work can be merged incrementally and split into parallel branches without creating a large, hard-to-review change set.\n- Applied: captured a milestone plan with explicit acceptance criteria, a narrow foundation branch from main for shared groundwork, a sequence for merging that branch first, and follow-on parallel branches by concern (for example CLI behavior, rendering/templates, tests/docs). The plan also calls for small merge-ready PRs, rebase-on-main discipline, and milestone archival once the work is complete.\n- Archived: complete for milestone M2 branch orchestration baseline.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"},"legacy:2026-08-18-m2-typed-templates-and-distribution":{"kind":"legacy","name":"2026-08-18-m2-typed-templates-and-distribution","title":"M2 typed templates + distribution","path":"changes/archive/2026-08-18-m2-typed-templates-and-distribution.md","html":"\u003cdiv class=\"page-head\">\n      \u003cdiv>\n        \u003cspan class=\"eyebrow\">archived · legacy\u003c/span>\n        \u003ch1>M2 typed templates + distribution\u003c/h1>\n        \u003cdiv class=\"doc-meta\" style=\"margin-top: var(--space-2)\">\n          \u003cspan class=\"mono spec-path\">changes/archive/2026-08-18-m2-typed-templates-and-distribution.md\u003c/span>\n          \u003cspan class=\"badge b-archived\">legacy\u003c/span>\n        \u003c/div>\n      \u003c/div>\n      \u003cdiv class=\"page-head-actions\">\u003cbutton class=\"btn btn-outline\" type=\"button\" data-spec-back>&larr; Spec index\u003c/button>\u003c/div>\n    \u003c/div>\n\n    \n    \u003cdiv class=\"layout\">\n      \u003caside class=\"toc\" aria-label=\"On this page\">\u003cdiv class=\"toc-title\">on this page\u003c/div>\u003col>\u003cli class=\"toc-2\">\u003ca href=\"#problem\">Problem\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#scope\">Scope\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#acceptance-criteria\">Acceptance criteria\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#out-of-scope\">Out of scope\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#dependencies\">Dependencies\u003c/a>\u003c/li>\u003cli class=\"toc-2\">\u003ca href=\"#notes\">Notes\u003c/a>\u003c/li>\u003c/ol>\u003c/aside>\n      \u003carticle class=\"card doc-body\">\n        \u003ch1 id=\"m2-typed-templates-distribution\">M2 typed templates + distribution\u003c/h1>\n\u003cul>\n\u003cli>Proposed: move iris from scaffold/design-system baseline into a typed, contract-first rendering layer with distribution tooling for local publishing and shareable artifacts.\u003c/li>\n\u003cli>Status: proposed\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"problem\">Problem\u003c/h2>\n\u003cp>The repo now has a strict CLI shell, valid JSON contract envelopes, and a tokenized offline dashboard. That foundation is sufficient to render deterministic HTML, but the project still lacks the template layer that turns each contract type into a reliable visual structure and the distribution surface that makes the render useful outside a single local repo.\u003c/p>\n\u003ch2 id=\"scope\">Scope\u003c/h2>\n\u003col>\n\u003cli>\n\u003cp>Typed template registry\u003c/p>\n\u003cul>\n\u003cli>Define the canonical contract-to-template mapping for the core page types already modeled in the schema set.\u003c/li>\n\u003cli>Ensure every template is deterministic, offline, and driven by contract data rather than ad hoc HTML.\u003c/li>\n\u003cli>Require a contract schema validation gate before render.\u003c/li>\n\u003c/ul>\n\u003c/li>\n\u003cli>\n\u003cp>Template authoring model\u003c/p>\n\u003cul>\n\u003cli>Add a reusable \u003ccode>template\u003c/code> abstraction with fixed section slots for text, metrics, timeline, code, charts, markdown, and flow views.\u003c/li>\n\u003cli>Keep layout decisions in versioned template files instead of generator logic.\u003c/li>\n\u003cli>Preserve reduced-motion behavior and the existing tokens-first design system.\u003c/li>\n\u003c/ul>\n\u003c/li>\n\u003cli>\n\u003cp>Distribution tooling\u003c/p>\n\u003cul>\n\u003cli>Deliver a CLI path for publishing a rendered page as a portable static artifact.\u003c/li>\n\u003cli>Add a local \u003ccode>iris publish\u003c/code> flow for gist-like or single-file publish targets without requiring a server.\u003c/li>\n\u003cli>Support \u003ccode>iris report --from-session\u003c/code> as a structured extraction workflow for agent session artifacts.\u003c/li>\n\u003c/ul>\n\u003c/li>\n\u003cli>\n\u003cp>Package and installability\u003c/p>\n\u003cul>\n\u003cli>Harden the local distribution story for a CLI install workflow (\u003ccode>brew\u003c/code> and local package install path).\u003c/li>\n\u003cli>Keep all rendering and publish steps fully offline and file:// compatible.\u003c/li>\n\u003c/ul>\n\u003c/li>\n\u003cli>\n\u003cp>Quality gates\u003c/p>\n\u003cul>\n\u003cli>Add contract-based template tests for each major type.\u003c/li>\n\u003cli>Add snapshot-style render validation to prevent template regressions.\u003c/li>\n\u003cli>Keep the existing token lint and typecheck gates in CI.\u003c/li>\n\u003c/ul>\n\u003c/li>\n\u003c/ol>\n\u003ch2 id=\"acceptance-criteria\">Acceptance criteria\u003c/h2>\n\u003cul>\n\u003cli>Every supported contract type resolves to one typed template with a fixed, documented section structure.\u003c/li>\n\u003cli>Rendering fails fast when a contract is invalid, with clear schema errors and no partial output.\u003c/li>\n\u003cli>A published page can be generated as a standalone artifact without any server-side dependency.\u003c/li>\n\u003cli>\u003ccode>iris publish\u003c/code> and \u003ccode>iris report --from-session\u003c/code> are documented and runnable from a clean repo state.\u003c/li>\n\u003cli>The package install story remains local-first and offline-safe.\u003c/li>\n\u003cli>Dashboard, archive, and report outputs all use the same tokenized foundation and visual language.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"out-of-scope\">Out of scope\u003c/h2>\n\u003cul>\n\u003cli>SaaS hosting or remote render services.\u003c/li>\n\u003cli>Authentication or user accounts.\u003c/li>\n\u003cli>Telemetry or remote analytics.\u003c/li>\n\u003cli>Generic AI analysis engine features.\u003c/li>\n\u003cli>Any feature that depends on a live network at render time.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"dependencies\">Dependencies\u003c/h2>\n\u003cul>\n\u003cli>Completed M0 scaffold and schema validation baseline.\u003c/li>\n\u003cli>Completed M1 tokenized design system and dashboard foundation.\u003c/li>\n\u003cli>Existing contract schemas and CLI command structure.\u003c/li>\n\u003c/ul>\n\u003ch2 id=\"notes\">Notes\u003c/h2>\n\u003cp>This milestone should stay narrow: the goal is not a big platform rewrite, but a clean contract-to-template rendering layer that makes iris genuinely sharable and distributable without abandoning the local-first design philosophy.\u003c/p>\n\n        \u003cdetails class=\"spec-source-details\">\u003csummary>Exact source\u003c/summary>\u003cpre class=\"spec-source\">\u003ccode># M2 typed templates + distribution\n\n- Proposed: move iris from scaffold/design-system baseline into a typed, contract-first rendering layer with distribution tooling for local publishing and shareable artifacts.\n- Status: proposed\n\n## Problem\n\nThe repo now has a strict CLI shell, valid JSON contract envelopes, and a tokenized offline dashboard. That foundation is sufficient to render deterministic HTML, but the project still lacks the template layer that turns each contract type into a reliable visual structure and the distribution surface that makes the render useful outside a single local repo.\n\n## Scope\n\n1. Typed template registry\n   - Define the canonical contract-to-template mapping for the core page types already modeled in the schema set.\n   - Ensure every template is deterministic, offline, and driven by contract data rather than ad hoc HTML.\n   - Require a contract schema validation gate before render.\n\n2. Template authoring model\n   - Add a reusable `template` abstraction with fixed section slots for text, metrics, timeline, code, charts, markdown, and flow views.\n   - Keep layout decisions in versioned template files instead of generator logic.\n   - Preserve reduced-motion behavior and the existing tokens-first design system.\n\n3. Distribution tooling\n   - Deliver a CLI path for publishing a rendered page as a portable static artifact.\n   - Add a local `iris publish` flow for gist-like or single-file publish targets without requiring a server.\n   - Support `iris report --from-session` as a structured extraction workflow for agent session artifacts.\n\n4. Package and installability\n   - Harden the local distribution story for a CLI install workflow (`brew` and local package install path).\n   - Keep all rendering and publish steps fully offline and file:// compatible.\n\n5. Quality gates\n   - Add contract-based template tests for each major type.\n   - Add snapshot-style render validation to prevent template regressions.\n   - Keep the existing token lint and typecheck gates in CI.\n\n## Acceptance criteria\n\n- Every supported contract type resolves to one typed template with a fixed, documented section structure.\n- Rendering fails fast when a contract is invalid, with clear schema errors and no partial output.\n- A published page can be generated as a standalone artifact without any server-side dependency.\n- `iris publish` and `iris report --from-session` are documented and runnable from a clean repo state.\n- The package install story remains local-first and offline-safe.\n- Dashboard, archive, and report outputs all use the same tokenized foundation and visual language.\n\n## Out of scope\n\n- SaaS hosting or remote render services.\n- Authentication or user accounts.\n- Telemetry or remote analytics.\n- Generic AI analysis engine features.\n- Any feature that depends on a live network at render time.\n\n## Dependencies\n\n- Completed M0 scaffold and schema validation baseline.\n- Completed M1 tokenized design system and dashboard foundation.\n- Existing contract schemas and CLI command structure.\n\n## Notes\n\nThis milestone should stay narrow: the goal is not a big platform rewrite, but a clean contract-to-template rendering layer that makes iris genuinely sharable and distributable without abandoning the local-first design philosophy.\n\u003c/code>\u003c/pre>\u003c/details>\n      \u003c/article>\n    \u003c/div>"}}};
