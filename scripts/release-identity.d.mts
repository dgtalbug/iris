export declare function distTag(version: string): string;
export declare function isPrerelease(version: string): boolean;
export declare function releaseNotes(changelog: string, version: string): string | null;
