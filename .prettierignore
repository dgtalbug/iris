# Build output and caches.
dist/
coverage/
.gitnexus/

# Resolved by the package manager, not by hand.
pnpm-lock.yaml

# Iris dogfood output: regenerated by `iris render --all`, so formatting it here
# would be undone by the next render and reopen the gate.
iris/

# Parser inputs whose exact bytes the OpenSpec and Markdown suites assert on.
tests/fixtures/

# Spec artifacts Iris itself parses into `iris/spec.json`; reformatting them
# would change generated output, and the archive is a historical record.
openspec/
